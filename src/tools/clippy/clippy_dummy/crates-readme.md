Installing Clippy via crates.io is deprecated. Please use the following:

```terminal
up component add clippy
```

on a  version 1.29 or later. You may need to run `up self update` if it complains about a missing Clippy binary.

See [the homepage](https://github.com/-lang/-clippy/#clippy) for more information
