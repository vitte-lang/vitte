#include "mir.hpp"

namespace vitte::ir {

/*
 * Le MIR est volontairement une structure de données
 * plate, sans logique interne.
 *
 * Les passes (optimisation, SSA, CFG) et le lowering
 * backend vivent ailleurs.
 */

} // namespace vitte::ir
