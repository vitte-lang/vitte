#include "hir.hpp"

namespace vitte::ir {

/*
 * Le HIR est volontairement un ensemble de
 * structures de données sans logique métier.
 *
 * Les transformations (type-checking, lowering
 * vers MIR, analyses) vivent dans d'autres modules.
 */

} // namespace vitte::ir
