module compiler::backend::vbc_backend::format::constants;

/// Placeholder pour la table des constantes.
pub struct ConstantPool {
    pub ints: Vec<i64>,
    pub floats: Vec<f64>,
    pub strings: Vec<String>,
}

impl ConstantPool {
    pub fn new() -> ConstantPool {
        ConstantPool {
            ints: Vec::new(),
            floats: Vec::new(),
            strings: Vec::new(),
        }
    }
}
