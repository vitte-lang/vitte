module compiler::backend::vbc_backend::format::opcodes;

/// Ensemble minimal d'opcodes VBC (stub).
#[repr(u8)]
pub enum VbcOp {
    Nop = 0x00,
    IConst = 0x01,
    IAdd = 0x02,
    ISub = 0x03,
    IMul = 0x04,
    IDiv = 0x05,
    Ret = 0xFF,
}
