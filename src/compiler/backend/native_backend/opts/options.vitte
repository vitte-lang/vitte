module compiler::backend::native_backend::opts::options;

/// Options spécifiques au backend natif.
#[derive(Clone)]
pub struct NativeBackendOptions {
    pub emit_asm: bool,
    pub emit_obj: bool,
    pub opt_level: u8,
    pub debug_info: bool,
}

impl NativeBackendOptions {
    pub fn default() -> NativeBackendOptions {
        NativeBackendOptions {
            emit_asm: true,
            emit_obj: true,
            opt_level: 2,
            debug_info: false,
        }
    }
}
