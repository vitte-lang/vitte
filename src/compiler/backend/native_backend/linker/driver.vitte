module compiler::backend::native_backend::linker::driver;

use crate::compiler::backend::native_backend::linker::{inputs::LinkInput, output::LinkOutput};

/// Driver de linkage natif; actuellement un stub.
pub fn link_executable(_inputs: &[LinkInput], _output: &str) -> LinkOutput {
    LinkOutput {
        path: _output.to_string(),
        success: true,
    }
}
