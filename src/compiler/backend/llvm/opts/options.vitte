module compiler::backend::llvm_backend::opts::options;

/// Options spécifiques pour le backend LLVM.
#[derive(Clone)]
pub struct LlvmBackendOptions {
    pub emit_text_ir: bool,
    pub verify_module: bool,
}

impl LlvmBackendOptions {
    pub fn default() -> LlvmBackendOptions {
        LlvmBackendOptions {
            emit_text_ir: true,
            verify_module: false,
        }
    }
}
