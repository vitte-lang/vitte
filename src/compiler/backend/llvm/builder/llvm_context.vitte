module compiler::backend::llvm_backend::builder::llvm_context;

/// Stub léger de contexte LLVM pour la génération IR.
/// Dans une implémentation réelle, ceci serait un wrapper autour des bindings LLVM.
pub struct LlvmContext;

impl LlvmContext {
    pub fn new() -> LlvmContext {
        LlvmContext
    }
}
