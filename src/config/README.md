# Configuration Files

This directory contains configuration files for the Vitte build system and development environment.

- **bootstrap/**: Bootstrap compiler configuration and build scripts
- Configuration files for various build stages and targets

See `src/bootstrap/README.md` for more details about the bootstrap process.
