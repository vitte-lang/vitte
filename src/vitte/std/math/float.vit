space std/math/float

proc sqrt(x: f64) -> f64
    foreign
.end

proc sin(x: f64) -> f64
    foreign
.end

proc cos(x: f64) -> f64
    foreign
.end

proc pow(x: f64, y: f64) -> f64
    foreign
.end

share all
