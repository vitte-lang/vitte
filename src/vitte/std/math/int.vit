space std/math/int

proc abs(x: i64) -> i64
    if x < 0
        give -x
    .end
    give x
.end

proc min(a: i64, b: i64) -> i64
    if a < b
        give a
    .end
    give b
.end

proc max(a: i64, b: i64) -> i64
    if a > b
        give a
    .end
    give b
.end

share all
