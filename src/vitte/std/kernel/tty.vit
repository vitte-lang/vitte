<<<
tty.vit
std kernel tty helpers
>>>

space std/kernel/tty

use std/core/types.i32
use std/core/types.u8
use std/core/types.usize

#[extern]
proc ioctl(fd: i32, req: i32, arg: usize) -> i32

#[extern]
proc isatty(fd: i32) -> i32
