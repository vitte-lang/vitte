space std/alloc/compiler_builtins/builtins_shim/link

pull .symbols

proc assert_symbols_present()
    for s in SYMBOLS
        if s.required && !builtin.link_has_symbol(s.name)
            builtin.trap("missing compiler-builtin: " + s.name)
        .end
    .end
.end
