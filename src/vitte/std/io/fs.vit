space std/io/fs

pull .result

proc open(path: str) -> IOResult[i32]
    foreign
.end

proc close(fd: i32) -> IOResult[unit]
    foreign
.end

proc read_fd(fd: i32, buf: ptr[u8], len: usize) -> IOResult[usize]
    foreign
.end

proc write_fd(fd: i32, buf: ptr[u8], len: usize) -> IOResult[usize]
    foreign
.end

share all
