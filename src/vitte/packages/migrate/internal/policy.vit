space vitte/migrate/internal/policy

proc default_max_migrations() -> int {
  give 10000
}

proc default_require_unique_ids() -> bool {
  give true
}

proc normalize_max_migrations(value: int) -> int {
  let out = value
  if out < 1 { out = 1 }
  if out > 100000 { out = 100000 }
  give out
}
