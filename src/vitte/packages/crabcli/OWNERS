# Owners for vitte/crabcli
@vitte/platform
@vitte/packages

# =========================================================
# OWNERS — vitte/crabcli (max max)
# =========================================================

# Primary owners
@vitte/platform
@vitte/packages

# Maintainers
@vitte/cli-runtime
@vitte/crabcli-core

# Reviewers (code review required)
@vitte/reviewers
@vitte/runtime
@vitte/validation
@vitte/diagnostics
@vitte/policy

# Domain experts
@vitte/domain
@vitte/orchestration
@vitte/build-system

# Optional reviewers (fallback)
@vitte/contributors
@vitte/community

# Paths ownership (fine-grained)

# Runtime
/src/vitte/packages/crabcli/internal/runtime.vit @vitte/runtime

# Validation
/src/vitte/packages/crabcli/internal/validate.vit @vitte/validation

# Diagnostics
/src/vitte/packages/crabcli/internal/diagnostics.vit @vitte/diagnostics

# Policy
/src/vitte/packages/crabcli/internal/policy.vit @vitte/policy

# Domain
/src/vitte/packages/crabcli/domain @vitte/domain

# Prelude
/src/vitte/packages/crabcli/prelude @vitte/platform

# Root module
/src/vitte/packages/crabcli/mod.vit @vitte/cli-runtime

# Info descriptor
/src/vitte/packages/crabcli/info.vit @vitte/packages

# Rules

# - At least 1 primary owner approval required
# - Runtime/Validate/Diagnostics/Policy changes require domain expert review
# - Breaking changes require @vitte/platform approval
# - CI-related changes require @vitte/packages approval
# - Experimental modules can be approved by maintainers only

# End of OWNERS
