space vitte/crabmedia/benches/smoke

use vitte/crabmedia as crabmedia_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabmedia_pkg.summary().len
    i += 1
  }
  give acc
}
