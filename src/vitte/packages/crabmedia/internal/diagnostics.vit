space vitte/crabmedia/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABMEDIA"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabmedia diagnostic " + code
}
