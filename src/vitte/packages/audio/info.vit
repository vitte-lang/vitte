<<<
info.vit
package vitte/audio
>>>

space vitte/audio

proc package_name() -> string {
    give "audio"
}

proc package_tag() -> string {
    give "vitte/audio"
}

proc package_ready() -> bool {
    give true
}
