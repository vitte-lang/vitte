<<<
info.vit
package vitte/retry
>>>

space vitte/retry

proc package_name() -> string {
    give "retry"
}

proc package_tag() -> string {
    give "vitte/retry"
}

proc package_ready() -> bool {
    give true
}
