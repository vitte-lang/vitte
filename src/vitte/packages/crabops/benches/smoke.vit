space vitte/crabops/benches/smoke

use vitte/crabops as crabops_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabops_pkg.summary().len
    i += 1
  }
  give acc
}
