space vitte/crabops/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABOPS"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabops diagnostic " + code
}
