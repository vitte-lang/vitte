space vitte/crabflow/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABFLOW"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabflow diagnostic " + code
}
