space vitte/crabflow/benches/smoke

use vitte/crabflow as crabflow_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabflow_pkg.summary().len
    i += 1
  }
  give acc
}
