# Dakar Design

Dakar is the structured logging package for Vitte Perl tooling.

It keeps the original in-memory logger model while adding explicit levels,
entries, filters, formatters, sinks, metrics, spans, and structured errors.
Each submodule has a focused test file, and `t/basic.t` covers the full public
export surface.
