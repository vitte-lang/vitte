# Berlin API

Berlin provides byte buffers, cursor reads and writes, binary views, integer
packing, hex and base64 codecs, checksum helpers, and structured errors.

The public API is exported by `Vitte::City::Berlin` and split internally across
the `Buffer`, `Hex`, `Cursor`, `Pack`, `Base64`, `Checksum`, `View`, and `Error`
submodules.
