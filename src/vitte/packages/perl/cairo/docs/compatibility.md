# Cairo Compatibility

- Minimum Perl version: `5.010`.
- Runtime dependencies are core modules: `Digest::SHA`, `Exporter`, `JSON::PP`.
- The scalar/record/list separator format remains stable for version `0.1.0`.
- Document encoding uses canonical ASCII JSON for deterministic output.
