<<<
info.vit
package vitte/codec
>>>

space vitte/codec

proc package_name() -> string {
    give "codec"
}

proc package_tag() -> string {
    give "vitte/codec"
}

proc package_ready() -> bool {
    give true
}
