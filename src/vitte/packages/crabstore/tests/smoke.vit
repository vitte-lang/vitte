space vitte/crabstore/tests/smoke

use vitte/crabstore as crabstore_pkg

proc run() -> bool {
  if crabstore_pkg.ready() == false { give false }
  if crabstore_pkg.version().len == 0 { give false }
  give true
}
