space vitte/crabstore/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABSTORE"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabstore diagnostic " + code
}
