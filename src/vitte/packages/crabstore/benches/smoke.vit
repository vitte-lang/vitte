space vitte/crabstore/benches/smoke

use vitte/crabstore as crabstore_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabstore_pkg.summary().len
    i += 1
  }
  give acc
}
