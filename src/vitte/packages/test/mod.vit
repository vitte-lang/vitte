<<<
mod.vit
package vitte/test
>>>

space vitte/test

proc assert_true(cond: bool) {
    if !cond {
        builtin.trap("assert_true failed")
    }
}

proc assert_eq_int(a: int, b: int) {
    if a != b {
        builtin.trap("assert_eq_int failed")
    }
}

proc ready() -> bool {
    give true
}


<<< ROLE-CONTRACT
package: vitte/test
role: Outillage tests: assertions runner et snapshots
input_contract: Valeurs metier explicites, deja parsees et typables
output_contract: Sorties stables, observables et compatibles avec les autres packages
boundary: Ne remplace pas les autres couches; respecte la separation de roles
>>>
