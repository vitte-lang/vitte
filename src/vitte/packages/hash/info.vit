<<<
info.vit
package vitte/hash
>>>

space vitte/hash

proc package_name() -> string {
    give "hash"
}

proc package_tag() -> string {
    give "vitte/hash"
}

proc package_ready() -> bool {
    give true
}
