space vitte/crabmeta/tests/smoke

use vitte/crabmeta as crabmeta_pkg

proc run() -> bool {
  if crabmeta_pkg.ready() == false { give false }
  if crabmeta_pkg.version().len == 0 { give false }
  give true
}
