space vitte/crabmeta/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABMETA"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabmeta diagnostic " + code
}
