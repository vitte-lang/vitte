space vitte/crabmeta/benches/smoke

use vitte/crabmeta as crabmeta_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabmeta_pkg.summary().len
    i += 1
  }
  give acc
}
