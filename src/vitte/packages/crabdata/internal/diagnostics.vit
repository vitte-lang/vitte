space vitte/crabdata/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABDATA"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabdata diagnostic " + code
}
