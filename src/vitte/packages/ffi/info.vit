<<<
info.vit
package vitte/ffi
>>>

space vitte/ffi

proc package_name() -> string {
    give "ffi"
}

proc package_tag() -> string {
    give "vitte/ffi"
}

proc package_ready() -> bool {
    give true
}
