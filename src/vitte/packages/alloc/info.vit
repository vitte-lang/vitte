<<<
info.vit
package vitte/alloc
>>>

space vitte/alloc

proc package_name() -> string {
    give "alloc"
}

proc package_tag() -> string {
    give "vitte/alloc"
}

proc package_ready() -> bool {
    give true
}
