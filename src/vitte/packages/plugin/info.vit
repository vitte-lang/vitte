<<<
info.vit
package vitte/plugin
>>>

space vitte/plugin

proc package_name() -> string {
    give "plugin"
}

proc package_tag() -> string {
    give "vitte/plugin"
}

proc package_ready() -> bool {
    give true
}
