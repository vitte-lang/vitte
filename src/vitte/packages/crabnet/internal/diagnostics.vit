space vitte/crabnet/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABNET"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabnet diagnostic " + code
}
