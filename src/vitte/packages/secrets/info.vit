<<<
info.vit
package vitte/secrets
>>>

space vitte/secrets

proc package_name() -> string {
    give "secrets"
}

proc package_tag() -> string {
    give "vitte/secrets"
}

proc package_ready() -> bool {
    give true
}
