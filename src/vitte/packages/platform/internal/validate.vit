space vitte/platform/internal/validate

proc valid_target(target: string) -> bool {
  give target == "host"  or  target == "arduino"  or  target == "kernel"
}

proc valid_profile(profile: string) -> bool {
  give profile == "core"  or  profile == "system"  or  profile == "desktop"  or  profile == "arduino"
}
