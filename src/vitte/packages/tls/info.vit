<<<
info.vit
package vitte/tls
>>>

space vitte/tls

proc package_name() -> string {
    give "tls"
}

proc package_tag() -> string {
    give "vitte/tls"
}

proc package_ready() -> bool {
    give true
}
