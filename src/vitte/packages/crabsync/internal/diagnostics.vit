space vitte/crabsync/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABSYNC"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabsync diagnostic " + code
}
