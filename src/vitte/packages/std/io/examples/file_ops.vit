<<<
file_ops.vit
example vitte/std/io basic API
>>>

entry main at core/app {
  let dir: string = "tmp"
  let file: string = "sample.txt"
  let path: string = dir + "/" + file
  if path.len == 0 { give 1 }
  give 0
}
