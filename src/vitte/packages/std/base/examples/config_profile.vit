<<<
config_profile.vit
example vitte/std/base profile + normalize
>>>

entry main at core/app {
  let timeout_ms: int = 500
  let retry_budget: int = 2
  let strict: bool = true
  if timeout_ms <= 0 { give 1 }
  if retry_budget < 0 { give 2 }
  if strict == false { give 3 }
  give 0
}
