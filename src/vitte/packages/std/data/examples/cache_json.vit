<<<
cache_json.vit
example vitte/std/data cache + json facade
>>>

entry main at core/app {
  let key: string = "key"
  let value: string = "value"
  let payload: string = "{\"ok\":true}"
  if key.len == 0 || value.len == 0 { give 1 }
  if payload.len == 0 { give 2 }
  give 0
}
