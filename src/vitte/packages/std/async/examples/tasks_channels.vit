<<<
tasks_channels.vit
example vitte/std/async tasks + channels
>>>

entry main at core/app {
  let queue_depth: int = 2
  let sent: int = 1
  let recv: int = 1
  if queue_depth <= 0 { give 1 }
  if sent != recv { give 2 }
  give 0
}
