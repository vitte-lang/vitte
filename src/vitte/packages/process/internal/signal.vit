<<<
signal.vit
package vitte/process/internal/signal
>>>

space vitte/process/internal/signal

proc terminate_ok(running: bool) -> bool {
  if running == false { give false }
  give true
}

proc kill_ok(running: bool) -> bool {
  if running == false { give false }
  give true
}
