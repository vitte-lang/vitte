<<<
info.vit
package vitte/saga
>>>

space vitte/saga

proc package_name() -> string {
    give "saga"
}

proc package_tag() -> string {
    give "vitte/saga"
}

proc package_ready() -> bool {
    give true
}
