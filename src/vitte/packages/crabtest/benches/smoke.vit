space vitte/crabtest/benches/smoke

use vitte/crabtest as crabtest_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabtest_pkg.summary().len
    i += 1
  }
  give acc
}
