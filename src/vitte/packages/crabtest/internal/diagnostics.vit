space vitte/crabtest/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABTEST"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabtest diagnostic " + code
}
