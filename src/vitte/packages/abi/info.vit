<<<
info.vit
package vitte/abi
>>>

space vitte/abi

proc package_name() -> string {
    give "abi"
}

proc package_tag() -> string {
    give "vitte/abi"
}

proc package_ready() -> bool {
    give true
}
