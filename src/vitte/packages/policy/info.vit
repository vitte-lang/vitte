<<<
info.vit
package vitte/policy
>>>

space vitte/policy

proc package_name() -> string {
    give "policy"
}

proc package_tag() -> string {
    give "vitte/policy"
}

proc package_ready() -> bool {
    give true
}
