<<<
info.vit
package vitte/yaml
>>>

space vitte/yaml

proc package_name() -> string {
    give "yaml"
}

proc package_tag() -> string {
    give "vitte/yaml"
}

proc package_ready() -> bool {
    give true
}
