space vitte/crabwire/benches/smoke

use vitte/crabwire as crabwire_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabwire_pkg.summary().len
    i += 1
  }
  give acc
}
