space vitte/crabwire/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABWIRE"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabwire diagnostic " + code
}
