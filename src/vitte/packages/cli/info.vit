<<<
info.vit
package vitte/cli
>>>

space vitte/cli

proc package_name() -> string {
    give "cli"
}

proc package_tag() -> string {
    give "vitte/cli"
}

proc package_ready() -> bool {
    give true
}
