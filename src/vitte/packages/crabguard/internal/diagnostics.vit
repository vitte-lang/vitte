space vitte/crabguard/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABGUARD"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabguard diagnostic " + code
}
