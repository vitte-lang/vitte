space vitte/crabguard/benches/smoke

use vitte/crabguard as crabguard_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabguard_pkg.summary().len
    i += 1
  }
  give acc
}
