<<<
info.vit
package vitte/stream
>>>

space vitte/stream

proc package_name() -> string {
    give "stream"
}

proc package_tag() -> string {
    give "vitte/stream"
}

proc package_ready() -> bool {
    give true
}
