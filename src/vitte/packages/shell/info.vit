<<<
info.vit
package vitte/shell
>>>

space vitte/shell

proc package_name() -> string {
    give "shell"
}

proc package_tag() -> string {
    give "vitte/shell"
}

proc package_ready() -> bool {
    give true
}
