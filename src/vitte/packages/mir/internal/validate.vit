space vitte/mir/internal/validate

proc valid_module_name(name: string) -> bool {
  give name.len > 0
}

proc valid_limits(max_blocks: int, max_instrs: int) -> bool {
  if max_blocks < 1 { give false }
  if max_instrs < 1 { give false }
  give true
}
