space vitte/crabbuilder/tests/smoke

use vitte/crabbuilder as crabbuilder_pkg

proc run() -> bool {
  if crabbuilder_pkg.ready() == false { give false }
  if crabbuilder_pkg.version().len == 0 { give false }
  give true
}
