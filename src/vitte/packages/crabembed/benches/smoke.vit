space vitte/crabembed/benches/smoke

use vitte/crabembed as crabembed_pkg

proc bench(iterations: int) -> int {
  let i: int = 0
  let acc: int = 0
  while i < iterations {
    acc = acc + crabembed_pkg.summary().len
    i += 1
  }
  give acc
}
