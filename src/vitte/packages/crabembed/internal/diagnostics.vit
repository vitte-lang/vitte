space vitte/crabembed/internal/diagnostics

proc namespace() -> string {
  give "VITTE-CRABEMBED"
}

proc message(code: string) -> string {
  if code == namespace() + "0000" { give "ok" }
  give "crabembed diagnostic " + code
}
