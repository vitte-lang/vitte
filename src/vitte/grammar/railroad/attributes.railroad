Diagram(
  Choice(
    0,

    Sequence(
      Terminal("#"),
      Terminal("["),
      NonTerminal("attribute-name"),
      Optional(
        Sequence(
          Terminal("("),
          NonTerminal("attribute-arguments"),
          Terminal(")")
        )
      ),
      Terminal("]")
    ),

    Sequence(
      Terminal("#"),
      Terminal("!"),
      Terminal("["),
      NonTerminal("attribute-name"),
      Optional(
        Sequence(
          Terminal("("),
          NonTerminal("attribute-arguments"),
          Terminal(")")
        )
      ),
      Terminal("]")
    )
  )
)

attribute-name =
Choice(
  0,
  Terminal("allow"),
  Terminal("aligned"),
  Terminal("benchmark"),
  Terminal("c"),
  Terminal("cold"),
  Terminal("deprecated"),
  Terminal("deny"),
  Terminal("error"),
  Terminal("export"),
  Terminal("hot"),
  Terminal("import"),
  Terminal("inline"),
  Terminal("intrinsic"),
  Terminal("must_use"),
  Terminal("no_inline"),
  Terminal("no_mangle"),
  Terminal("packed"),
  Terminal("repr"),
  Terminal("test"),
  Terminal("warning")
)

attribute-arguments =
Choice(
  0,
  NonTerminal("expression"),
  OneOrMore(
    NonTerminal("expression"),
    Terminal(",")
  )
)
