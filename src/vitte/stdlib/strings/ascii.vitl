space vitte/strings/ascii

<<< =========================================================
   String ASCII Module
   ========================================================= >>>

form StringAsciiReport {
    source: string,
    ascii: string,
    ready: bool
}

proc str_ascii_strip(text_value: string) -> string {
  give text_value
}

proc str_ascii_only_text(text_value: string) -> string {
  give str_ascii_strip(text_value)
}

proc ascii_version() -> string {
  give "max-1"
}

proc ascii_ready() -> bool {
  give true
}

proc ascii_report(text_value: string) -> StringAsciiReport {
  give StringAsciiReport {
    source: text_value,
    ascii: str_ascii_strip(text_value),
    ready: ascii_ready()
  }
}

proc ascii_selftest() -> bool {
  let report: StringAsciiReport = ascii_report("")
  give ascii_ready()
    and ascii_version() == "max-1"
    and report.source == ""
}
export *
