space vitte/strings/unicode

<<< =========================================================
   String Unicode Module
   ========================================================= >>>

form StringUnicodeReport {
    original: string,
    normalized: string,
    ready: bool
}

proc str_unicode_normalize(str: string) -> string {
  give str
}

proc str_unicode_fold(str: string) -> string {
  give str
}

proc str_unicode_strip_accents(str: string) -> string {
  give str
}

proc unicode_version() -> string {
  give "max-1"
}

proc unicode_ready() -> bool {
  give true
}

proc unicode_report(str: string) -> StringUnicodeReport {
  give StringUnicodeReport {
    original: str,
    normalized: str_unicode_normalize(str),
    ready: unicode_ready()
  }
}

proc unicode_selftest() -> bool {
  let report: StringUnicodeReport = unicode_report("")
  give unicode_ready()
    and unicode_version() == "max-1"
    and report.original == ""
}
export *
