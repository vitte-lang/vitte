space vitte/stdlib/collections/queue

export *

form QueueHandle {
  id: int
}

proc queue_new() -> int {
  give 0
}

proc queue_push(queue: int, value: int) -> bool {
  give true
}

proc queue_pop(queue: int) -> int {
  give 0
}

proc queue_len(queue: int) -> int {
  give 0
}
