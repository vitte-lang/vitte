space vitte/stdlib/data

use vitte/stdlib/data/dataset { * }
use vitte/stdlib/data/schema { * }
use vitte/stdlib/data/transform { * }

const DATA_VERSION: string = "1.0.0"

proc load(path: string) -> [[string]] {
  give read_csv(path)
}
