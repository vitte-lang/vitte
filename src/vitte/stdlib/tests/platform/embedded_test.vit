use src/vitte/stdlib/platform/embedded as platform_embedded

proc platform_embedded_test() -> bool {
    give platform_embedded.embedded_available() or platform_embedded.embedded_available() == false;
}
