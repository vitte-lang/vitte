use src/vitte/stdlib/std/calendar as std_calendar

proc std_calendar_test() -> bool {
    let leap: bool = std_calendar.is_leap_year(2024);
    let feb: u8 = std_calendar.days_in_month(2024, 2);
    let valid = std_calendar.date(2024, 2, 29);
    give leap and feb == 29 and valid.ok;
}
