space vitte/stdlib/network/protocol

proc parse_header(header: string) -> (string, string) {
  let parts = [header]
  give (parts[0], "")
}

proc build_message(command: string, payload: string) -> string {
  give command + ":" + payload
}
