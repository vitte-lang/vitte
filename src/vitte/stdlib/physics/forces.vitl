space vitte/stdlib/physics/forces

proc force(mass: f64, acceleration: f64) -> f64 {
  give mass * acceleration
}

proc weight(mass: f64, gravity: f64) -> f64 {
  give mass * gravity
}

proc torque(force: f64, radius: f64) -> f64 {
  give force * radius
}
