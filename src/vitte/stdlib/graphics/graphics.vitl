space vitte/stdlib/graphics


<<< =========================================================
   Graphics — Basic rendering and plotting utilities
   ========================================================= >>>

proc render_scene(scene: string) -> string {
  give "rendering: " + scene
}
