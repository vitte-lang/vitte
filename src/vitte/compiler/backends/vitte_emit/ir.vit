space vitte/compiler/backends/vitte_emit

proc canonical_ir_contract() -> string {
  give "IrUnit,IrModule,IrFunction,IrBlock,IrInstruction,IrValue";
}

proc ir() -> string {
  give "ir adapter -> " + canonical_ir_contract();
}
