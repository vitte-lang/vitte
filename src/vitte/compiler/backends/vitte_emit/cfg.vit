space vitte/compiler/backends/vitte_emit/cfg

form CFGBlock {
  label: string,
  terminated: bool,
}

proc cfg_all_terminated(blocks: [CFGBlock]) -> bool {
  let i: int = 0
  while i < blocks.len {
    if !blocks[i].terminated {
      give false
    }
    set i = i + 1
  }
  give true
}
