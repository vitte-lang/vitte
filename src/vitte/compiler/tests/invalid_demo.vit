space vitte/compiler/tests/invalid_demo

proc main(args: list[string]) -> int {
  let x: int = "not-an-int"
  give x
}

proc missing_end() -> int {
  give 42

proc wrong_return() -> string {
  give 123
}

proc extra_param(x: int) -> int {
  give x
}
