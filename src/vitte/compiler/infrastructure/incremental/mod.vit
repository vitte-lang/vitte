space vitte/compiler/infrastructure/incremental

use vitte/compiler/infrastructure/incremental/dep_graph.{
  IncrementalGraph,
  IncrementalNode,
  IncrementalNodeKind,
  add_node,
  analysis_node_key,
  build_analysis_graph,
  incremental_graph_empty,
  incremental_node,
  node_index,
  rebuild_dependents,
  set_node_dependencies
}
use vitte/compiler/infrastructure/incremental/invalidation.{ InvalidationResult, invalidate_graph_from_key }
