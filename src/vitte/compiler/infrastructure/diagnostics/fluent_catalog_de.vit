space vitte/compiler/infrastructure/diagnostics/fluent_catalog_de

export *

form FluentDiagnosticFields {
    message: string
    primary_label: string
    secondary_label: string
    cause: string
    help: string
    fix_it: string
    corrected_example: string
    note: string
    lsp_code_action_title: string
    valid: bool
}

proc fluent_diagnostic_fields(message: string, primary_label: string, secondary_label: string, cause: string, help: string, fix_it: string, corrected_example: string, note: string, lsp_code_action_title: string) -> FluentDiagnosticFields {
    give FluentDiagnosticFields {
        message: message,
        primary_label: primary_label,
        secondary_label: secondary_label,
        cause: cause,
        help: help,
        fix_it: fix_it,
        corrected_example: corrected_example,
        note: note,
        lsp_code_action_title: lsp_code_action_title,
        valid: message != "" and primary_label != "" and cause != "" and help != ""
    };
}

proc fluent_diagnostic_fields_empty() -> FluentDiagnosticFields {
    give fluent_diagnostic_fields("", "", "", "", "", "", "", "", "");
}

proc fluent_catalog_fields_de(code: string) -> FluentDiagnosticFields {
    if code == "E0001" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "The Parser expected a Name for something (variable, Typ, Modul, etc.).", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The Parser expected a Name for something (variable, Typ, Modul, etc.).", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0002" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "The Parser expected an Ausdruck at this location.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The Parser expected an Ausdruck at this location.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0003" {
        give fluent_diagnostic_fields("Muster erwartet", "The Parser expected a Muster.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The Parser expected a Muster.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0004" {
        give fluent_diagnostic_fields("Typ erwartet", "The Parser expected a Typ Name.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The Parser expected a Typ Name.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0005" {
        give fluent_diagnostic_fields("'end' erwartet", "A Block was opened but not closed with 'end' or '.end'.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A Block was opened but not closed with 'end' or '.end'.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0006" {
        give fluent_diagnostic_fields("proc after attribute erwartet", "An attribute muss followed by a proc Deklaration sein.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "An attribute muss followed by a proc Deklaration sein.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0007" {
        give fluent_diagnostic_fields("top-level Deklaration erwartet", "The Parser expected a top-level Deklaration.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The Parser expected a top-level Deklaration.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0008" {
        give fluent_diagnostic_fields("extern proc darf keinen Body haben", "A Muster bound the same Name more than once.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A Muster bound the same Name more than once.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0009" {
        give fluent_diagnostic_fields("proc requires a body unless marked #[extern]", "A referenced Typ Name was fehlend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A referenced Typ Name was fehlend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0010" {
        give fluent_diagnostic_fields("Typ alias requires a Ziel Typ", "The base Typ of a generisch was fehlend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The base Typ of a generisch was fehlend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0011" {
        give fluent_diagnostic_fields("select erfordert mindestens ein when branch", "A generisch Typ must include at least one Argument.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A generisch Typ must include at least one Argument.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0012" {
        give fluent_diagnostic_fields("select branch muss a when Anweisung sein", "This Typ form is not supported yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let p: *int = &value", "This Typ form is not supported yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0013" {
        give fluent_diagnostic_fields("Prozedur kann ohne give enden", "proc kann enden, ohne einen Wert zurueckzugeben.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc to_code(ok: bool) -> int {\\n  if ok { give 0 }\\n  give 1\\n}", "proc kann enden, ohne einen Wert zurueckzugeben.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0014" {
        give fluent_diagnostic_fields("verbotene Top-Level-Syntax im strict-core-Modus", "An invocation is fehlend its Aufrufziel.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "print(\\\"hi\\\")", "An invocation is fehlend its Aufrufziel.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0015" {
        give fluent_diagnostic_fields("verbotene Anweisungssyntax im strict-core-Modus", "This Ausdruck is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let x = value", "This Ausdruck is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0016" {
        give fluent_diagnostic_fields("verbotene Ausdruckssyntax im strict-core-Modus", "This Muster is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This Muster is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0017" {
        give fluent_diagnostic_fields("'}' erwartet", "This Anweisung is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This Anweisung is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0018" {
        give fluent_diagnostic_fields("'case' oder 'otherwise' in match erwartet", "An extern procedure cannot define a body.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "#[extern]\\nproc puts(s: string) -> int", "An extern procedure cannot define a body.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E1001" {
        give fluent_diagnostic_fields("Muster Bindung doppelt", "Muster Bindung doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Muster Bindung doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1002" {
        give fluent_diagnostic_fields("Typ (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) unbekannt", "Typ (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Typ (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1003" {
        give fluent_diagnostic_fields("generisch base Typ unbekannt", "generisch base Typ unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generisch base Typ unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1004" {
        give fluent_diagnostic_fields("generisch Typ erfordert mindestens ein Argument", "generisch Typ erfordert mindestens ein Argument.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generisch Typ erfordert mindestens ein Argument.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1005" {
        give fluent_diagnostic_fields("Bezeichner unbekannt", "Bezeichner unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Bezeichner unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1006" {
        give fluent_diagnostic_fields("generisch Typ erfordert mindestens ein Typ Argument", "generisch Typ erfordert mindestens ein Typ Argument.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generisch Typ erfordert mindestens ein Typ Argument.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1007" {
        give fluent_diagnostic_fields("Umwandlung between signed and unsigned values ungueltig", "Umwandlung between signed and unsigned values ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Umwandlung between signed and unsigned values ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1010" {
        give fluent_diagnostic_fields("stdlib Modul von active stdlib Profil abgelehnt", "stdlib Modul von active stdlib Profil abgelehnt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib Modul von active stdlib Profil abgelehnt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1011" {
        give fluent_diagnostic_fields("strict-imports requires explicit alias", "strict-imports requires explicit alias.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports requires explicit alias.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1012" {
        give fluent_diagnostic_fields("strict-imports verbietet unused Import aliases", "strict-imports verbietet unused Import aliases.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports verbietet unused Import aliases.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1013" {
        give fluent_diagnostic_fields("strict-imports verbietet non-canonical Import paths", "strict-imports verbietet non-canonical Import paths.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports verbietet non-canonical Import paths.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1014" {
        give fluent_diagnostic_fields("stdlib Modul fehlend", "stdlib Modul fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib Modul fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1015" {
        give fluent_diagnostic_fields("experimental Modul Import denied", "experimental Modul Import denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "experimental Modul Import denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1016" {
        give fluent_diagnostic_fields("intern Modul Import denied", "intern Modul Import denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "intern Modul Import denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1017" {
        give fluent_diagnostic_fields("re-export Symbol Konflikt", "re-export Symbol Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "re-export Symbol Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1018" {
        give fluent_diagnostic_fields("ambiguous Import Pfad", "ambiguous Import Pfad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ambiguous Import Pfad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1019" {
        give fluent_diagnostic_fields("strict-modules verbietet glob imports", "strict-modules verbietet glob imports.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-modules verbietet glob imports.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1020" {
        give fluent_diagnostic_fields("legacy Import Pfad ist veraltet", "legacy Import Pfad ist veraltet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "legacy Import Pfad ist veraltet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1021" {
        give fluent_diagnostic_fields("entry Modul Pfad muss canonical sein", "entry Modul Pfad muss canonical sein.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry Modul Pfad muss canonical sein.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1022" {
        give fluent_diagnostic_fields("entry Name doppelt", "entry Name doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry Name doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1023" {
        give fluent_diagnostic_fields("share referenziert unbekanntes Symbol", "share referenziert unbekanntes Symbol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share referenziert unbekanntes Symbol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1024" {
        give fluent_diagnostic_fields("Symbol in share list doppelt", "Symbol in share list doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Symbol in share list doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1025" {
        give fluent_diagnostic_fields("Symbol nicht von Modul exportiert", "Symbol nicht von Modul exportiert.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Symbol nicht von Modul exportiert.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1026" {
        give fluent_diagnostic_fields("share Deklaration doppelt", "share Deklaration doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share Deklaration doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1027" {
        give fluent_diagnostic_fields("Import Bindung doppelt", "Import Bindung doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Import Bindung doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1028" {
        give fluent_diagnostic_fields("Import Bindung kollidiert mit lokal Deklaration", "Import Bindung kollidiert mit lokal Deklaration.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Import Bindung kollidiert mit lokal Deklaration.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1029" {
        give fluent_diagnostic_fields("lokal Deklaration Name doppelt", "lokal Deklaration Name doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "lokal Deklaration Name doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1030" {
        give fluent_diagnostic_fields("Modul alias Member not exported", "Modul alias Member not exported.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Modul alias Member not exported.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1031" {
        give fluent_diagnostic_fields("qualified Typ Member fehlend", "qualified Typ Member fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "qualified Typ Member fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1032" {
        give fluent_diagnostic_fields("Ausdruck ist nicht aufrufbar", "Ausdruck ist nicht aufrufbar.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "Ausdruck ist nicht aufrufbar.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E2001" {
        give fluent_diagnostic_fields("Typ nicht unterstuetzt", "Typ nicht unterstuetzt.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2002" {
        give fluent_diagnostic_fields("invoke has no Aufrufziel", "invoke has no Aufrufziel.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "invoke has no Aufrufziel.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2003" {
        give fluent_diagnostic_fields("Ausdruck in HIR nicht unterstuetzt", "Ausdruck in HIR nicht unterstuetzt.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck in HIR nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2004" {
        give fluent_diagnostic_fields("Muster in HIR nicht unterstuetzt", "Muster in HIR nicht unterstuetzt.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster in HIR nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2005" {
        give fluent_diagnostic_fields("Anweisung in HIR nicht unterstuetzt", "Anweisung in HIR nicht unterstuetzt.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Anweisung in HIR nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2006" {
        give fluent_diagnostic_fields("HIR Typ kind unerwartet", "HIR Typ kind unerwartet.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR Typ kind unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2007" {
        give fluent_diagnostic_fields("HIR expr kind unerwartet", "HIR expr kind unerwartet.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR expr kind unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2008" {
        give fluent_diagnostic_fields("HIR stmt kind unerwartet", "HIR stmt kind unerwartet.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR stmt kind unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2009" {
        give fluent_diagnostic_fields("HIR Muster kind unerwartet", "HIR Muster kind unerwartet.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR Muster kind unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2010" {
        give fluent_diagnostic_fields("HIR decl kind unerwartet", "HIR decl kind unerwartet.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR decl kind unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "LEX_E_INVALID_CHAR" {
        give fluent_diagnostic_fields("Zeichen ungueltig", "Zeichen ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Zeichen ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_NUMBER" {
        give fluent_diagnostic_fields("numeric Literal ungueltig", "numeric Literal ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "numeric Literal ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_ESCAPE" {
        give fluent_diagnostic_fields("Escape sequence ungueltig", "Escape sequence ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Escape sequence ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE_ESCAPE" {
        give fluent_diagnostic_fields("unicode Escape sequence ungueltig", "unicode Escape sequence ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode Escape sequence ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UTF8" {
        give fluent_diagnostic_fields("UTF-8 byte sequence ungueltig", "UTF-8 byte sequence ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "UTF-8 byte sequence ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_CHAR_LITERAL" {
        give fluent_diagnostic_fields("Zeichen Literal ungueltig", "Zeichen Literal ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Zeichen Literal ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner ungueltig", "Bezeichner ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Bezeichner ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_STRING" {
        give fluent_diagnostic_fields("string Literal nicht beendet", "string Literal nicht beendet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add the closing `\"` on the same line; do not close a string with a single quote", "print(\"message\")", "string Literal nicht beendet.", "add the closing `\"` on the same line; do not close a string with a single quote");
    }

    if code == "LEX_E_UNTERMINATED_CHAR" {
        give fluent_diagnostic_fields("Zeichen Literal nicht beendet", "Zeichen Literal nicht beendet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Zeichen Literal nicht beendet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_BLOCK_COMMENT" {
        give fluent_diagnostic_fields("Block comment nicht beendet", "Block comment nicht beendet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Block comment nicht beendet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_REGION_COMMENT" {
        give fluent_diagnostic_fields("region comment nicht beendet", "region comment nicht beendet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "region comment nicht beendet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNEXPECTED_EOF" {
        give fluent_diagnostic_fields("end of Datei unerwartet", "end of Datei unerwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "end of Datei unerwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_TOKEN_TOO_LARGE" {
        give fluent_diagnostic_fields("Token zu", "Token zu.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Token zu.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PLOOP" {
        give fluent_diagnostic_fields("Parser hat keinen Fortschritt gemacht", "Parser hat keinen Fortschritt gemacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Parser hat keinen Fortschritt gemacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PPRIMARY999" {
        give fluent_diagnostic_fields("Ausdruck Token unerwartet", "Ausdruck Token unerwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ausdruck Token unerwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PSTMT007" {
        give fluent_diagnostic_fields("Zuweisung Operator erwartet", "Zuweisung Operator erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Zuweisung Operator erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P0001" {
        give fluent_diagnostic_fields("top-level Token unerwartet", "top-level Token unerwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level Token unerwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P000_UNBALANCED" {
        give fluent_diagnostic_fields("Block nicht geschlossen", "Block nicht geschlossen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Block nicht geschlossen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E_CLI_IO" {
        give fluent_diagnostic_fields("cannot read Eingabe Datei", "The compiler cannot read the requested Eingabe Datei.", "The requested file does not exist, is not a regular file, or is not readable.", "The requested file does not exist, is not a regular file, or is not readable.", "Verify the path passed to `vitte build` and the file permissions.", "use an existing readable source path", "vitte build src/main.vit -o build/main", "The compiler cannot read the requested Eingabe Datei.", "use an existing readable source path");
    }

    if code == "E_CLI_MISSING_ARG" {
        give fluent_diagnostic_fields("command Argument fehlt", "command Argument fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "command Argument fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_COMMAND" {
        give fluent_diagnostic_fields("compiler command unbekannt", "compiler command unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_OPTION" {
        give fluent_diagnostic_fields("compiler option unbekannt", "compiler option unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("compiler Argument ungueltig", "compiler Argument ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler Argument ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_IO_FILE_NOT_FOUND" {
        give fluent_diagnostic_fields("Eingabe Datei fehlend", "Eingabe Datei fehlend.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "Eingabe Datei fehlend.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_FILE_UNREADABLE" {
        give fluent_diagnostic_fields("Eingabe Datei is not readable", "Eingabe Datei is not readable.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "Eingabe Datei is not readable.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OUTPUT_UNWRITABLE" {
        give fluent_diagnostic_fields("cannot Schreibzugriff Ausgabe Datei", "cannot Schreibzugriff Ausgabe Datei.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "cannot Schreibzugriff Ausgabe Datei.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "permission denied.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_DIRECTORY_NOT_FOUND" {
        give fluent_diagnostic_fields("Ausgabe directory fehlend", "Ausgabe directory fehlend.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "Ausgabe directory fehlend.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OVERWRITE_FORBIDDEN" {
        give fluent_diagnostic_fields("refusing to overwrite existing Ausgabe", "refusing to overwrite existing Ausgabe.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "refusing to overwrite existing Ausgabe.", "use an existing readable input path and a writable output destination");
    }

    if code == "DRIVER_E_PIPELINE_FAILURE" {
        give fluent_diagnostic_fields("compiler pipeline fehlgeschlagen", "compiler pipeline fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler pipeline fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_FRONTEND_INCOMPLETE" {
        give fluent_diagnostic_fields("analysis frontend incomplete", "analysis frontend incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis frontend incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("analysis ungueltig", "analysis ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROWCK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("borrowck not executed", "borrowck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "borrowck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CODEGEN_IR_INCOMPLETE" {
        give fluent_diagnostic_fields("Codeerzeugung ir incomplete", "Codeerzeugung ir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Codeerzeugung ir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INPUT_NOT_LOADED" {
        give fluent_diagnostic_fields("frontend Eingabe not loaded", "frontend Eingabe not loaded.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend Eingabe not loaded.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INVALID" {
        give fluent_diagnostic_fields("frontend ungueltig", "frontend ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_PROFILE" {
        give fluent_diagnostic_fields("link Profil ungueltig", "link Profil ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link Profil ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_INVALID" {
        give fluent_diagnostic_fields("ir ungueltig", "ir ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_MIR_INCOMPLETE" {
        give fluent_diagnostic_fields("ir mir incomplete", "ir mir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir mir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_CODEGEN_INCOMPLETE" {
        give fluent_diagnostic_fields("link Codeerzeugung incomplete", "link Codeerzeugung incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link Codeerzeugung incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INCOMPLETE" {
        give fluent_diagnostic_fields("mir analysis incomplete", "mir analysis incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("mir analysis ungueltig", "mir analysis ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_INVALID" {
        give fluent_diagnostic_fields("mir ungueltig", "mir ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NO_INPUT" {
        give fluent_diagnostic_fields("no Eingabe", "no Eingabe.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "no Eingabe.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_AST" {
        give fluent_diagnostic_fields("parse leer ast", "parse leer ast.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse leer ast.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_SOURCE" {
        give fluent_diagnostic_fields("parse leer Quelle", "parse leer Quelle.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse leer Quelle.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_LINK_INCOMPLETE" {
        give fluent_diagnostic_fields("run link incomplete", "run link incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run link incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_PROCESS_FAILED" {
        give fluent_diagnostic_fields("run process fehlgeschlagen", "run process fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run process fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SEMA_NOT_EXECUTED" {
        give fluent_diagnostic_fields("sema not executed", "sema not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "sema not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TYPECK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("typeck not executed", "typeck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "typeck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LINK_E_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "permission denied.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OUTPUT_NOT_MATERIALIZED" {
        give fluent_diagnostic_fields("linker did not create the requested executable", "linker did not create the requested executable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker did not create the requested executable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_C_COMPILER" {
        give fluent_diagnostic_fields("required C compiler was fehlend", "required C compiler was fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "required C compiler was fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_COMMAND_FAILED" {
        give fluent_diagnostic_fields("linker command fehlgeschlagen", "linker command fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker command fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LEX_E_INVALID_FLOAT" {
        give fluent_diagnostic_fields("Fliesszahl ungueltig", "Fliesszahl ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Fliesszahl ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_BINARY" {
        give fluent_diagnostic_fields("binaer ungueltig", "binaer ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "binaer ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_OCTAL" {
        give fluent_diagnostic_fields("octal ungueltig", "octal ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "octal ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_HEX" {
        give fluent_diagnostic_fields("hex ungueltig", "hex ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "hex ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_INDENTATION" {
        give fluent_diagnostic_fields("Einrueckung ungueltig", "Einrueckung ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Einrueckung ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_TOKEN" {
        give fluent_diagnostic_fields("Token ungueltig", "Token ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Token ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE" {
        give fluent_diagnostic_fields("unicode ungueltig", "unicode ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_COMMENT" {
        give fluent_diagnostic_fields("comment nicht beendet", "comment nicht beendet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comment nicht beendet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PATTR003" {
        give fluent_diagnostic_fields("pattr003", "pattr003.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "pattr003.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TOPLEVEL_DECL_EXPECTED" {
        give fluent_diagnostic_fields("top-level Deklaration expected", "top-level Deklaration expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level Deklaration expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_INCOMPLETE_EXPR" {
        give fluent_diagnostic_fields("incomplete Ausdruck", "incomplete Ausdruck.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "incomplete Ausdruck.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_RPAREN" {
        give fluent_diagnostic_fields("closing parenthesis fehlt", "closing parenthesis fehlt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "closing parenthesis fehlt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_COMMA" {
        give fluent_diagnostic_fields("comma fehlt", "comma fehlt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comma fehlt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TYPE_EXPECTED" {
        give fluent_diagnostic_fields("Typ expected", "Typ expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Typ expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PATTERN_EXPECTED" {
        give fluent_diagnostic_fields("Muster expected", "Muster expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Muster expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_BLOCK_EXPECTED" {
        give fluent_diagnostic_fields("Block expected", "Block expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Block expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_UNCLOSED_BLOCK" {
        give fluent_diagnostic_fields("Block nicht geschlossen", "Block nicht geschlossen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add `}` to close the highlighted block, then run the checker again for its parent block", "while running { set running = false }", "Block nicht geschlossen.", "add `}` to close the highlighted block, then run the checker again for its parent block");
    }

    if code == "PARSE_E_EXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token erwartet", "Token erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Token erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PARAMETER_COLON_EXPECTED" {
        give fluent_diagnostic_fields("colon in procedure parameter fehlt", "parameter colon expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "insert `:` between the parameter name and its type, for example `right: f64`", "proc calculate(right: f64) -> f64 { give right }", "parameter colon expected.", "insert `:` between the parameter name and its type, for example `right: f64`");
    }

    if code == "PARSE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Token unerwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_OLD_VITTE_SYNTAX" {
        give fluent_diagnostic_fields("old Vitte Syntax is not canonical", "old Vitte Syntax is not canonical.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "Identify the intended construct, then compare it with the current Vitte keywords.", "replace the old token with the current Vitte keyword only when the construct is equivalent", "proc main() -> int { give 0 }", "old Vitte Syntax is not canonical.", "replace the old token with the current Vitte keyword only when the construct is equivalent");
    }

    if code == "PARSE_EXPECTED_EXPR" {
        give fluent_diagnostic_fields("parse expected expr", "parse expected expr.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected expr.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("parse expected Typ", "parse expected Typ.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected Typ.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("parse expected Muster", "parse expected Muster.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected Muster.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("parse expected Block", "parse expected Block.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected Block.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("parse expected Bezeichner", "parse expected Bezeichner.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected Bezeichner.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FAST0001" {
        give fluent_diagnostic_fields("fast0001", "fast0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "fast0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FLEX0001" {
        give fluent_diagnostic_fields("flex0001", "flex0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "flex0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "AST_E_INVALID_NODE" {
        give fluent_diagnostic_fields("Knoten ungueltig", "Knoten ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Knoten ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_EMPTY_MODULE" {
        give fluent_diagnostic_fields("leer Modul", "leer Modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "leer Modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_DECL" {
        give fluent_diagnostic_fields("decl ungueltig", "decl ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "decl ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ungueltig", "expr ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_DUPLICATE_FIELD" {
        give fluent_diagnostic_fields("Feld doppelt", "Feld doppelt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Find the earlier field label and the highlighted duplicate.", "keep one field entry and remove or rename the duplicate", "vitte check path/to/file.vit", "Feld doppelt.", "keep one field entry and remove or rename the duplicate");
    }

    if code == "AST_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("form Feld unbekannt", "form Feld unbekannt.", "A `form` construction or field access names a field that the form declaration does not contain.", "A `form` construction or field access names a field that the form declaration does not contain.", "Compare the highlighted field name with the fields declared in the `form`.", "rename the field to one declared by the form, or add the missing field to the form declaration", "form Point { x: int, y: int }", "form Feld unbekannt.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "AST_E_MISSING_FIELD" {
        give fluent_diagnostic_fields("form Feld fehlt", "form Feld fehlt.", "A `form` construction omits a required field that has no default value.", "A `form` construction omits a required field that has no default value.", "Compare the construction with every required field in the `form` declaration.", "add the missing `field: value` entry to the construction", "form Point { x: int, y: int }", "form Feld fehlt.", "add the missing `field: value` entry to the construction");
    }

    if code == "AST_E_FIELD_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("form Feld Typ Nichtuebereinstimmung", "form Feld Typ Nichtuebereinstimmung.", "A value assigned to a `form` field does not match that field's declared type.", "A value assigned to a `form` field does not match that field's declared type.", "Compare the field declaration type with the highlighted field initializer.", "change the field initializer to the declared type, or change the field type in the `form`", "form User { name: string, age: int }", "form Feld Typ Nichtuebereinstimmung.", "change the field initializer to the declared type, or change the field type in the `form`");
    }

    if code == "AST_E_FIELD_ORDER" {
        give fluent_diagnostic_fields("form fields are out of order", "form fields are out of order.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "Read the field order from the `form` declaration.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them", "form Pair { left: int, right: int }", "form fields are out of order.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them");
    }

    if code == "AST_E_INCOMPLETE_CONSTRUCTION" {
        give fluent_diagnostic_fields("form construction is incomplete", "form construction is incomplete.", "A `form` construction ended before all required fields were supplied.", "A `form` construction ended before all required fields were supplied.", "Check whether the construction is missing fields or a closing brace.", "complete the construction with every required `field: value` entry and the closing `}`", "form Point { x: int, y: int }", "form construction is incomplete.", "complete the construction with every required `field: value` entry and the closing `}`");
    }

    if code == "AST_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("Sichtbarkeit ungueltig", "Sichtbarkeit ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Sichtbarkeit ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ENTRY" {
        give fluent_diagnostic_fields("entry ungueltig", "entry ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "entry ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "SEMA_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("Symbol doppelt", "Symbol doppelt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "Symbol doppelt.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_UNKNOWN_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner unbekannt", "Bezeichner unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "Bezeichner unbekannt.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_AMBIGUOUS_SYMBOL" {
        give fluent_diagnostic_fields("ambiguous Symbol", "ambiguous Symbol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous Symbol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_SHADOWING_FORBIDDEN" {
        give fluent_diagnostic_fields("shadowing is forbidden", "shadowing is forbidden.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "shadowing is forbidden.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("Feld does not exist", "Feld does not exist.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename the field to one declared by the form, or add the missing field to the form declaration", "use vitte/core", "Feld does not exist.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "SEMA_E_UNKNOWN_VARIANT" {
        give fluent_diagnostic_fields("variant does not exist", "variant does not exist.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "Compare the highlighted variant with the `case` names declared in the `pick`.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration", "pick Status { Ready, Failed }", "variant does not exist.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration");
    }

    if code == "SEMA_E_INACCESSIBLE_VARIANT" {
        give fluent_diagnostic_fields("pick variant is inaccessible", "pick variant is inaccessible.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "Read the branches in order and find the earlier branch that already covers this variant.", "remove the unreachable branch or move the more specific `case` before the broader branch", "match status {", "pick variant is inaccessible.", "remove the unreachable branch or move the more specific `case` before the broader branch");
    }

    if code == "SEMA_E_DUPLICATE_PICK_BRANCH" {
        give fluent_diagnostic_fields("pick branch is duplicated", "pick branch is duplicated.", "The same `pick` variant is matched by more than one branch in the same `match`.", "The same `pick` variant is matched by more than one branch in the same `match`.", "Find the earlier branch for the same `case` and decide which body should remain.", "merge the duplicate branch bodies or remove the later duplicate branch", "match status {", "pick branch is duplicated.", "merge the duplicate branch bodies or remove the later duplicate branch");
    }

    if code == "TYPECK_E_PICK_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("pick Muster is not exhaustive", "pick Muster is not exhaustive.", "A `match` over a `pick` value does not cover every reachable variant.", "A `match` over a `pick` value does not cover every reachable variant.", "List the declared `case` variants and compare them with the branches already present.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended", "match status {", "pick Muster is not exhaustive.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended");
    }

    if code == "TYPECK_E_PICK_PAYLOAD_MISMATCH" {
        give fluent_diagnostic_fields("pick payload does not match variant", "pick payload does not match variant.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "Compare each payload position or name with the selected variant declaration.", "use the expected payload shape for that `case`, including the correct field count and types", "pick Event { Data(value: int) }", "pick payload does not match variant.", "use the expected payload shape for that `case`, including the correct field count and types");
    }

    if code == "TYPECK_E_IMPOSSIBLE_PATTERN" {
        give fluent_diagnostic_fields("pick Muster can never match", "pick Muster can never match.", "The pattern cannot match the scrutinee type produced by the expression.", "The pattern cannot match the scrutinee type produced by the expression.", "Compare the matched expression type with the variant or literal named by the pattern.", "replace the branch pattern with one that belongs to the matched `pick` or value type", "match status { case Ready { give 0 } }", "pick Muster can never match.", "replace the branch pattern with one that belongs to the matched `pick` or value type");
    }

    if code == "SEMA_E_UNKNOWN_FUNCTION" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "SEMA_E_UNKNOWN_SYMBOL" {
        give fluent_diagnostic_fields("Symbol unbekannt", "Symbol unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "Symbol unbekannt.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_INVALID_IMPORT" {
        give fluent_diagnostic_fields("Import ungueltig", "Import ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Import ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_EXPORT" {
        give fluent_diagnostic_fields("export ungueltig", "export ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("Sichtbarkeit ungueltig", "Sichtbarkeit ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Sichtbarkeit ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control Kontrollfluss ungueltig", "control Kontrollfluss ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "control Kontrollfluss ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("Modul ungueltig", "Modul ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Modul ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ASSIGN_TARGET" {
        give fluent_diagnostic_fields("assign Ziel ungueltig", "assign Ziel ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "assign Ziel ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNDECLARED_TARGET" {
        give fluent_diagnostic_fields("undeclared Ziel", "undeclared Ziel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "undeclared Ziel.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_MISSING_BINDING" {
        give fluent_diagnostic_fields("Bindung fehlt", "Bindung fehlt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Bindung fehlt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_DUPLICATE_BINDING" {
        give fluent_diagnostic_fields("Bindung doppelt", "Bindung doppelt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "Bindung doppelt.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_DUPLICATE_ITEM" {
        give fluent_diagnostic_fields("Element doppelt", "Element doppelt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "Element doppelt.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir ungueltig", "hir ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "hir ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "intern.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPECK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_ITEM" {
        give fluent_diagnostic_fields("Element unbekannt", "Element unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Element unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_W_UNRESOLVED_NAME" {
        give fluent_diagnostic_fields("unresolved Name", "unresolved Name.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unresolved Name.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ungueltig", "expr ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expr ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_BINARY_MISMATCH" {
        give fluent_diagnostic_fields("binaer Nichtuebereinstimmung", "binaer Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make both operands valid for the operator before MIR lowering", "let count: int = 1", "binaer Nichtuebereinstimmung.", "make both operands valid for the operator before MIR lowering");
    }

    if code == "TYPECK_E_ASSIGN_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Typ Nichtuebereinstimmung", "Zuweisung Typ Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "assign a value of the declared binding type, or change the binding annotation at its declaration", "let count: int = 1", "Zuweisung Typ Nichtuebereinstimmung.", "assign a value of the declared binding type, or change the binding annotation at its declaration");
    }

    if code == "TYPECK_E_RETURN_MISMATCH" {
        give fluent_diagnostic_fields("give Typ Nichtuebereinstimmung", "Rueckgabe Typ Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "change the `give` expression or the procedure result type so both name the same Vitte type", "let count: int = 1", "Rueckgabe Typ Nichtuebereinstimmung.", "change the `give` expression or the procedure result type so both name the same Vitte type");
    }

    if code == "TYPECK_E_MISSING_GIVE" {
        give fluent_diagnostic_fields("give in Wert procedure fehlt", "give in Wert procedure fehlt.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "Inspect the procedure body and decide which value should be produced.", "add `give expression` on every successful path, or remove the `-> Type` result contract", "proc answer() -> int { give 42 }", "give in Wert procedure fehlt.", "add `give expression` on every successful path, or remove the `-> Type` result contract");
    }

    if code == "TYPECK_E_GIVE_IN_VOID_PROC" {
        give fluent_diagnostic_fields("give Wert in procedure without result Typ", "give Wert in procedure without Rueckgabe Typ.", "A procedure without a `-> Type` contract uses `give` with a value.", "A procedure without a `-> Type` contract uses `give` with a value.", "Check whether the procedure should produce a value or only perform effects.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement", "proc answer() -> int { give 42 }", "give Wert in procedure without Rueckgabe Typ.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement");
    }

    if code == "TYPECK_E_CONTROL_PATH_MISSING_GIVE" {
        give fluent_diagnostic_fields("control Pfad reaches end without give", "control Pfad reaches end without give.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "Follow each `if`, `match`, `loop`, and early-exit path in the procedure.", "add a final `give` or make every branch produce a value before control reaches the closing brace", "proc code(ok: bool) -> int {", "control Pfad reaches end without give.", "add a final `give` or make every branch produce a value before control reaches the closing brace");
    }

    if code == "TYPECK_W_UNREACHABLE_AFTER_GIVE" {
        give fluent_diagnostic_fields("code after give is unerreichbar", "code after give is unerreichbar.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "Inspect the statement immediately after the highlighted `give`.", "move the statement before `give`, put it in another branch, or remove it", "proc main() -> int {", "code after give is unerreichbar.", "move the statement before `give`, put it in another branch, or remove it");
    }

    if code == "TYPECK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("cannot assign to immutable Bindung", "cannot assign to immutable Bindung.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "Find the original `let`, parameter, or field declaration for the highlighted target.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`", "let count: int = 0", "cannot assign to immutable Bindung.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`");
    }

    if code == "TYPECK_W_MUTABLE_NEVER_MODIFIED" {
        give fluent_diagnostic_fields("veraenderbar Bindung is never changed", "veraenderbar Bindung is never changed.", "A binding was declared mutable, but no reachable `set` changes it.", "A binding was declared mutable, but no reachable `set` changes it.", "Search the binding scope for `set name = ...` or field updates through that binding.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended", "let count: int = 0", "veraenderbar Bindung is never changed.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended");
    }

    if code == "TYPECK_E_REASSIGNMENT_FORBIDDEN" {
        give fluent_diagnostic_fields("Ziel cannot be reassigned", "Ziel cannot be reassigned.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "Check that the left side of `set` is a binding, field, or index place that Vitte allows to be assigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`", "set user.name = name", "Ziel cannot be reassigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`");
    }

    if code == "TYPECK_E_MISSING_INITIALIZER" {
        give fluent_diagnostic_fields("let Bindung requires an initializer or Typ", "let Bindung requires an initializer or Typ.", "`let` introduced a binding without enough information to create a value.", "`let` introduced a binding without enough information to create a value.", "Inspect the binding and check whether it has either an initializer or an explicit type plus a supported delayed-initialization path.", "add `= expression`, or add the missing type annotation required by the active Vitte rule", "let count: int = 0", "let Bindung requires an initializer or Typ.", "add `= expression`, or add the missing type annotation required by the active Vitte rule");
    }

    if code == "TYPECK_E_INFERENCE_FAILED" {
        give fluent_diagnostic_fields("Bindung Typ could not be inferred", "Bindung Typ could not be inferred.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "Look at the highlighted `let` and the first use of the binding.", "add an explicit Vitte type annotation after the binding name", "let items: [int] = []", "Bindung Typ could not be inferred.", "add an explicit Vitte type annotation after the binding name");
    }

    if code == "TYPECK_E_CONDITION_TYPE" {
        give fluent_diagnostic_fields("Bedingung Typ", "Bedingung Typ.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the condition produce bool, for example by adding an explicit comparison", "let count: int = 1", "Bedingung Typ.", "make the condition produce bool, for example by adding an explicit comparison");
    }

    if code == "TYPECK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "rename the field to one declared by the form, or add the missing field to the form declaration", "let count: int = 1", "Member unbekannt.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "TYPECK_E_INDEX_TYPE" {
        give fluent_diagnostic_fields("Index Typ", "Index Typ.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Index Typ.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_INDEX_TARGET" {
        give fluent_diagnostic_fields("Index Ziel ungueltig", "Index Ziel ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Index Ziel ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IF_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("if branch Nichtuebereinstimmung", "if branch Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "if branch Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_COMPARE_MISMATCH" {
        give fluent_diagnostic_fields("compare Nichtuebereinstimmung", "compare Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "compare Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Umwandlung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MATCH_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("match non exhaustive", "match non exhaustive.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "match non exhaustive.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CALL_TARGET" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "Aufruf Ziel is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_UNKNOWN_PROCEDURE" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "TYPECK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Aufruf Argument Typ Nichtuebereinstimmung", "Aufruf Argument Typ Nichtuebereinstimmung.", "An argument expression does not match the type of the parameter it is passed to.", "An argument expression does not match the type of the parameter it is passed to.", "Link the highlighted argument to its parameter and compare the expected and found Vitte types.", "change that argument expression or the parameter type so the single argument matches its parameter", "scale(value: 4, factor: 2)", "Aufruf Argument Typ Nichtuebereinstimmung.", "change that argument expression or the parameter type so the single argument matches its parameter");
    }

    if code == "TYPECK_E_CALL_ARITY" {
        give fluent_diagnostic_fields("wrong Zahl of Aufruf Argumente", "wrong Zahl of Aufruf Argumente.", "The call supplies a different number of arguments than the procedure parameter list.", "The call supplies a different number of arguments than the procedure parameter list.", "Show the expected parameters in order, then attach each extra or missing argument to its nearest parameter.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list", "add(1, 2)", "wrong Zahl of Aufruf Argumente.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list");
    }

    if code == "TYPECK_E_UNKNOWN_NAMED_ARGUMENT" {
        give fluent_diagnostic_fields("named Argument has no matching parameter", "named Argument has no matching parameter.", "A named argument does not correspond to any parameter in the called procedure.", "A named argument does not correspond to any parameter in the called procedure.", "Display the expected parameter names and highlight the unknown argument name.", "rename the argument to an expected parameter name or remove it", "connect(host: \"localhost\", port: 8080)", "named Argument has no matching parameter.", "rename the argument to an expected parameter name or remove it");
    }

    if code == "TYPECK_E_ARGUMENT_ORDER" {
        give fluent_diagnostic_fields("Aufruf Argumente are in the wrong order", "Aufruf Argumente are in the wrong order.", "A positional or named argument appears after an argument form that makes the order invalid.", "A positional or named argument appears after an argument form that makes the order invalid.", "Compare the call argument order with the procedure parameter order shown in the diagnostic.", "reorder the arguments to match the parameter list, or use named arguments consistently", "draw(x: 1, y: 2, color: \"blue\")", "Aufruf Argumente are in the wrong order.", "reorder the arguments to match the parameter list, or use named arguments consistently");
    }

    if code == "TYPECK_E_AMBIGUOUS_CALL" {
        give fluent_diagnostic_fields("procedure Aufruf is ambiguous", "procedure Aufruf is ambiguous.", "More than one visible procedure can accept the same call shape.", "More than one visible procedure can accept the same call shape.", "Show the candidate procedure signatures and identify the arguments that do not disambiguate them.", "add a type annotation to one argument or call a more specific procedure name", "let result: int = parse(\"42\")", "procedure Aufruf is ambiguous.", "add a type annotation to one argument or call a more specific procedure name");
    }

    if code == "TYPECK_W_IGNORED_RESULT" {
        give fluent_diagnostic_fields("procedure result is ignored", "procedure result is ignored.", "A procedure result is produced but the surrounding statement does not use it.", "A procedure result is produced but the surrounding statement does not use it.", "Check whether the result should be bound with `let`, passed onward, or intentionally discarded.", "bind the result with `let name: Type = call(...)` when the value is needed", "let total: int = add(1, 2)", "procedure result is ignored.", "bind the result with `let name: Type = call(...)` when the value is needed");
    }

    if code == "TYPECK_E_NOT_CALLABLE" {
        give fluent_diagnostic_fields("Wert is not callable", "Wert is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "Wert is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_IMPL_MISSING_MEMBER" {
        give fluent_diagnostic_fields("implementation is fehlend a required Member", "implementation is fehlend a required Member.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation is fehlend a required Member.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_SIGNATURE_MISMATCH" {
        give fluent_diagnostic_fields("implementation Member signature does not match", "implementation Member signature does not match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation Member signature does not match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_UNKNOWN_TRAIT" {
        give fluent_diagnostic_fields("trait does not exist", "trait does not exist.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait does not exist.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_GENERIC_INFERENCE" {
        give fluent_diagnostic_fields("generisch Typ could not be inferred", "generisch Typ could not be inferred.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch Typ could not be inferred.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_TRAIT_BOUND" {
        give fluent_diagnostic_fields("trait constraint is not satisfied", "trait constraint is not satisfied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait constraint is not satisfied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CAUSE_CHAIN_MISSING" {
        give fluent_diagnostic_fields("Typ diagnostic is fehlend a cause chain", "Typ diagnostic is fehlend a cause chain.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ diagnostic is fehlend a cause chain.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONFLICTING_IMPL" {
        give fluent_diagnostic_fields("conflicting trait implementation", "conflicting trait implementation.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conflicting trait implementation.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONSTRAINT_CYCLE" {
        give fluent_diagnostic_fields("cyclic generisch constraint", "cyclic generisch constraint.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cyclic generisch constraint.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_AMBIGUOUS_METHOD" {
        give fluent_diagnostic_fields("ambiguous method resolution", "ambiguous method resolution.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous method resolution.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Verschiebung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_UNARY" {
        give fluent_diagnostic_fields("malformed unary", "malformed unary.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed unary.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNARY_MISMATCH" {
        give fluent_diagnostic_fields("unary Nichtuebereinstimmung", "unary Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unary Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BORROW" {
        give fluent_diagnostic_fields("malformed Ausleihe", "malformed Ausleihe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed Ausleihe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_MEMBER" {
        give fluent_diagnostic_fields("malformed Member", "malformed Member.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed Member.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_INDEX" {
        give fluent_diagnostic_fields("malformed Index", "malformed Index.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed Index.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BINARY" {
        give fluent_diagnostic_fields("malformed binaer", "malformed binaer.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed binaer.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_CAST" {
        give fluent_diagnostic_fields("malformed Umwandlung", "malformed Umwandlung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed Umwandlung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_IF" {
        give fluent_diagnostic_fields("malformed if", "malformed if.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed if.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir ungueltig", "hir ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "hir ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "intern.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0001" {
        give fluent_diagnostic_fields("type0001", "type0001.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0001.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0002" {
        give fluent_diagnostic_fields("type0002", "type0002.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0002.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0003" {
        give fluent_diagnostic_fields("type0003", "type0003.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0003.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0004" {
        give fluent_diagnostic_fields("type0004", "type0004.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0004.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0005" {
        give fluent_diagnostic_fields("type0005", "type0005.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0005.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0006" {
        give fluent_diagnostic_fields("type0006", "type0006.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0006.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0007" {
        give fluent_diagnostic_fields("type0007", "type0007.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0007.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0008" {
        give fluent_diagnostic_fields("type0008", "type0008.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0008.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0009" {
        give fluent_diagnostic_fields("type0009", "type0009.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0009.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0010" {
        give fluent_diagnostic_fields("type0010", "type0010.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0010.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE9999" {
        give fluent_diagnostic_fields("type9999", "type9999.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type9999.", "make the expression type match the type contract named by the type checker");
    }

    if code == "BORROWCK_E_MOVE_AFTER_MOVE" {
        give fluent_diagnostic_fields("Verschiebung after Verschiebung", "Verschiebung after Verschiebung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Verschiebung after Verschiebung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("Wert verwendet after Verschiebung", "Wert verwendet after Verschiebung.", "A value is used after ownership has moved away from its binding.", "A value is used after ownership has moved away from its binding.", "Show where the value is created, where it is moved, and where it is reused after the move.", "use the value before the move, borrow it instead of moving it, or create a new value", "let value: string = \"vitte\"", "Wert verwendet after Verschiebung.", "use the value before the move, borrow it instead of moving it, or create a new value");
    }

    if code == "BORROWCK_E_PARTIAL_MOVE" {
        give fluent_diagnostic_fields("Wert partially verschoben", "Wert partially verschoben.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Show the original construction, the field or variant payload that moved, and the later whole-value use.", "avoid using the whole value after moving one part, or rebuild the missing part before use", "form User { name: string, age: int }", "Wert partially verschoben.", "avoid using the whole value after moving one part, or rebuild the missing part before use");
    }

    if code == "BORROWCK_E_BORROW_OF_MOVED_VALUE" {
        give fluent_diagnostic_fields("Ausleihe of verschoben Wert", "Ausleihe of verschoben Wert.", "A borrow starts after ownership has already moved away from the original binding.", "A borrow starts after ownership has already moved away from the original binding.", "Show the value creation, the move, and the later borrow attempt in that order.", "create the borrow before the move, or move a different value", "let value: string = \"vitte\"", "Ausleihe of verschoben Wert.", "create the borrow before the move, or move a different value");
    }

    if code == "BORROWCK_E_MUTABLE_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("veraenderbar Ausleihe Konflikt", "veraenderbar Ausleihe Konflikt.", "A value is already mutably borrowed when another borrow of the same value starts.", "A value is already mutably borrowed when another borrow of the same value starts.", "Report the value creation first, then the first mutable borrow, then the later conflicting borrow, then where the first borrow ends.", "end the first mutable borrow before starting the next borrow", "let value: int = 1", "veraenderbar Ausleihe Konflikt.", "end the first mutable borrow before starting the next borrow");
    }

    if code == "BORROWCK_E_MUTABLE_SHARED_CONFLICT" {
        give fluent_diagnostic_fields("veraenderbar and geteilt borrows overlap", "veraenderbar and geteilt borrows overlap.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "veraenderbar and geteilt borrows overlap.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_SHARED_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("geteilt Ausleihe Konflikt", "geteilt Ausleihe Konflikt.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "geteilt Ausleihe Konflikt.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_WRITE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("Schreibzugriff waehrend ausgeliehen", "Schreibzugriff waehrend ausgeliehen.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "Schreibzugriff waehrend ausgeliehen.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_MOVE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("Verschiebung waehrend ausgeliehen", "Verschiebung waehrend ausgeliehen.", "Ownership moves out of a value while an active borrow can still use it.", "Ownership moves out of a value while an active borrow can still use it.", "Show the value creation, the borrow start, the move, the later borrow use, and where the borrow ends.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership", "let value: string = \"vitte\"", "Verschiebung waehrend ausgeliehen.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership");
    }

    if code == "BORROWCK_E_DROP_WHILE_BORROWED" {
        give fluent_diagnostic_fields("Freigabe waehrend ausgeliehen", "Freigabe waehrend ausgeliehen.", "A value is destroyed while an active borrow still points to it.", "A value is destroyed while an active borrow still points to it.", "Show the value creation, the borrow start, the destruction point, and the last borrow use.", "destroy the value only after the borrow is no longer used", "let value: string = \"vitte\"", "Freigabe waehrend ausgeliehen.", "destroy the value only after the borrow is no longer used");
    }

    if code == "BORROWCK_E_ASSIGN_WHILE_BORROWED" {
        give fluent_diagnostic_fields("assign waehrend ausgeliehen", "assign waehrend ausgeliehen.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "assign waehrend ausgeliehen.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_RETURN_REF_TO_LOCAL" {
        give fluent_diagnostic_fields("Rueckgabe ref to lokal", "Rueckgabe ref to lokal.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "Rueckgabe ref to lokal.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_RETURN_BORROW_OF_LOCAL" {
        give fluent_diagnostic_fields("Rueckgabe Ausleihe of lokal", "Rueckgabe Ausleihe of lokal.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "Rueckgabe Ausleihe of lokal.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "dangling Referenz.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_REFERENCE_OUTLIVES_VALUE" {
        give fluent_diagnostic_fields("Referenz outlives the Wert it points to", "Referenz outlives the Wert it points to.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "Referenz outlives the Wert it points to.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "Lebensdauer zu short.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("immutable assign", "immutable assign.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "immutable assign.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNINITIALIZED_USE" {
        give fluent_diagnostic_fields("nicht initialisiert use", "nicht initialisiert use.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "nicht initialisiert use.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MOVE_AFTER_BORROW" {
        give fluent_diagnostic_fields("Verschiebung after Ausleihe", "Verschiebung after Ausleihe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Verschiebung after Ausleihe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MUTABLE_ALIAS" {
        give fluent_diagnostic_fields("veraenderbar alias", "veraenderbar alias.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "veraenderbar alias.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "intern.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNKNOWN" {
        give fluent_diagnostic_fields("unbekannt", "unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_EVAL_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("Division by Null in Konstante evaluation", "Division by Null in Konstante evaluation.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "change the const divisor to a non-zero value or guard the expression before const evaluation", "const size: int = 4", "Division by Null in Konstante evaluation.", "change the const divisor to a non-zero value or guard the expression before const evaluation");
    }

    if code == "CONST_EVAL_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Name unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_NON_CONST_CALL" {
        give fluent_diagnostic_fields("non const Aufruf", "non const Aufruf.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non const Aufruf.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_MUTATION_IN_CONST" {
        give fluent_diagnostic_fields("mutation in const", "mutation in const.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutation in const.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNSUPPORTED_EXPR" {
        give fluent_diagnostic_fields("expr nicht unterstuetzt", "expr nicht unterstuetzt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "replace the const expression with literals, supported arithmetic, or another compile-time value", "const size: int = 4", "expr nicht unterstuetzt.", "replace the const expression with literals, supported arithmetic, or another compile-time value");
    }

    if code == "CONST_EVAL_E_OVERFLOW" {
        give fluent_diagnostic_fields("Ueberlauf", "Ueberlauf.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "use a wider const type or reduce the arithmetic result before overflow", "const size: int = 4", "Ueberlauf.", "use a wider const type or reduce the arithmetic result before overflow");
    }

    if code == "CONST_EVAL_E_STATIC_ASSERT_FAILED" {
        give fluent_diagnostic_fields("static Assertion fehlgeschlagen", "static Assertion fehlgeschlagen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "make the asserted const condition true or remove the invalid compile-time assumption", "const size: int = 4", "static Assertion fehlgeschlagen.", "make the asserted const condition true or remove the invalid compile-time assumption");
    }

    if code == "CONST_EVAL_E_CYCLE" {
        give fluent_diagnostic_fields("Zyklus", "Zyklus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Zyklus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_PARSE" {
        give fluent_diagnostic_fields("parse", "parse.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "parse.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNKNOWN" {
        give fluent_diagnostic_fields("unbekannt", "unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MOD_E_MODULE_NOT_FOUND" {
        give fluent_diagnostic_fields("Modul fehlend", "Modul fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "Modul fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus detected", "Import Zyklus detected.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "Import Zyklus detected.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_SYMBOL_NOT_EXPORTED" {
        give fluent_diagnostic_fields("Symbol is nicht von Modul exportiert", "Symbol is nicht von Modul exportiert.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "Symbol is nicht von Modul exportiert.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "Import fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_PACKAGE_MISSING" {
        give fluent_diagnostic_fields("package fehlend", "package fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "package fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_STDLIB_MISSING" {
        give fluent_diagnostic_fields("stdlib fehlend", "stdlib fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "stdlib fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_AMBIGUOUS_MODULE" {
        give fluent_diagnostic_fields("ambiguous Modul Pfad", "ambiguous Modul Pfad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ambiguous Modul Pfad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "HIR_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ungueltig", "expr ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STMT" {
        give fluent_diagnostic_fields("stmt ungueltig", "stmt ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "stmt ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_TYPE" {
        give fluent_diagnostic_fields("Typ ungueltig", "Typ ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_SYMBOL" {
        give fluent_diagnostic_fields("Symbol fehlt", "Symbol fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Symbol fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control Kontrollfluss ungueltig", "control Kontrollfluss ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "control Kontrollfluss ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LOWERING_FAILED" {
        give fluent_diagnostic_fields("lowering fehlgeschlagen", "lowering fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lowering fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("Block ungueltig", "Block ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Block ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_TERMINATOR" {
        give fluent_diagnostic_fields("Terminator ungueltig", "Terminator ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Terminator ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_BLOCK" {
        give fluent_diagnostic_fields("unerreichbar Block", "unerreichbar Block.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unerreichbar Block.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERAND" {
        give fluent_diagnostic_fields("Operand ungueltig", "Operand ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Operand ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PLACE" {
        give fluent_diagnostic_fields("place ungueltig", "place ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "place ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DATAFLOW_CONFLICT" {
        give fluent_diagnostic_fields("dataflow Konflikt", "dataflow Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dataflow Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_VERIFICATION_FAILED" {
        give fluent_diagnostic_fields("verification fehlgeschlagen", "verification fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verification fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("Modul ungueltig", "Modul ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Modul ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_FUNCTION" {
        give fluent_diagnostic_fields("procedure ungueltig", "procedure ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "procedure ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("Block ungueltig", "Block ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Block ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INSTRUCTION" {
        give fluent_diagnostic_fields("Instruktion ungueltig", "Instruktion ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Instruktion ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("Typ Nichtuebereinstimmung", "Typ Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_VERIFY_FAILED" {
        give fluent_diagnostic_fields("verify fehlgeschlagen", "verify fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verify fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ziel nicht unterstuetzt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNSUPPORTED_FEATURE" {
        give fluent_diagnostic_fields("Funktion nicht unterstuetzt", "Funktion nicht unterstuetzt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Funktion nicht unterstuetzt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CODEGEN_FAILED" {
        give fluent_diagnostic_fields("Codeerzeugung fehlgeschlagen", "Codeerzeugung fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Codeerzeugung fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_OBJECT_WRITE_FAILED" {
        give fluent_diagnostic_fields("Objekt Schreibzugriff fehlgeschlagen", "Objekt Schreibzugriff fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Objekt Schreibzugriff fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSEMBLER_FAILED" {
        give fluent_diagnostic_fields("assembler fehlgeschlagen", "assembler fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "assembler fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NATIVE_TOOL_MISSING" {
        give fluent_diagnostic_fields("native Werkzeug fehlend", "native Werkzeug fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "native Werkzeug fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNDEFINED_SYMBOL" {
        give fluent_diagnostic_fields("undefiniert Symbol", "undefiniert Symbol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "undefiniert Symbol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("Symbol doppelt", "Symbol doppelt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Symbol doppelt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIBRARY_NOT_FOUND" {
        give fluent_diagnostic_fields("library fehlend", "library fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "library fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OBJECT_NOT_FOUND" {
        give fluent_diagnostic_fields("Objekt fehlend", "Objekt fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Objekt fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_FORMAT" {
        give fluent_diagnostic_fields("Format nicht unterstuetzt", "Format nicht unterstuetzt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Format nicht unterstuetzt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_SYSTEM_LINKER_FAILED" {
        give fluent_diagnostic_fields("system linker fehlgeschlagen", "system linker fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "system linker fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_PANIC" {
        give fluent_diagnostic_fields("Panik", "Panik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Panik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSERT_FAILED" {
        give fluent_diagnostic_fields("Assertion fehlgeschlagen", "Assertion fehlgeschlagen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Assertion fehlgeschlagen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_STACK_OVERFLOW" {
        give fluent_diagnostic_fields("Stack Ueberlauf", "Stack Ueberlauf.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Stack Ueberlauf.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_OUT_OF_MEMORY" {
        give fluent_diagnostic_fields("out of memory", "out of memory.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "out of memory.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("Division by Null", "Division by Null.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Division by Null.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BOUNDS_CHECK" {
        give fluent_diagnostic_fields("bounds check", "bounds check.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bounds check.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NULL_DEREF" {
        give fluent_diagnostic_fields("Null deref", "Null deref.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Null deref.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("Argument ungueltig", "Argument ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Argument ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_INPUT" {
        give fluent_diagnostic_fields("Eingabe fehlt", "Eingabe fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Eingabe fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_COMMAND" {
        give fluent_diagnostic_fields("compiler command fehlt", "compiler command fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OPTION_VALUE" {
        give fluent_diagnostic_fields("compiler option Wert fehlt", "compiler option Wert fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option Wert fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SOURCE_UNREADABLE" {
        give fluent_diagnostic_fields("or unreadable Eingabe Quelle fehlt", "or unreadable Eingabe Quelle fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "or unreadable Eingabe Quelle fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CONFIGURATION" {
        give fluent_diagnostic_fields("compiler configuration ungueltig", "compiler configuration ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler configuration ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_TARGET" {
        give fluent_diagnostic_fields("compiler Ziel fehlt", "compiler Ziel fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler Ziel fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_DIR" {
        give fluent_diagnostic_fields("compiler Ausgabe directory fehlt", "compiler Ausgabe directory fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler Ausgabe directory fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_PATH" {
        give fluent_diagnostic_fields("compiler Ausgabe Pfad fehlt", "compiler Ausgabe Pfad fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler Ausgabe Pfad fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_TARGET" {
        give fluent_diagnostic_fields("compiler link Ziel ungueltig", "compiler link Ziel ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler link Ziel ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INPUT_NOT_FOUND" {
        give fluent_diagnostic_fields("Eingabe fehlend", "Eingabe fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Eingabe fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_OUTPUT_WRITE_FAILED" {
        give fluent_diagnostic_fields("Ausgabe Schreibzugriff fehlgeschlagen", "Ausgabe Schreibzugriff fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ausgabe Schreibzugriff fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_READ_FAILED" {
        give fluent_diagnostic_fields("cache read fehlgeschlagen", "cache read fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache read fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_WRITE_FAILED" {
        give fluent_diagnostic_fields("cache Schreibzugriff fehlgeschlagen", "cache Schreibzugriff fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache Schreibzugriff fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PROFILE_NOT_FOUND" {
        give fluent_diagnostic_fields("Profil fehlend", "Profil fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Profil fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TARGET_NOT_FOUND" {
        give fluent_diagnostic_fields("Ziel fehlend", "Ziel fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ziel fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "BOOTSTRAP_E_STAGE_FAILURE" {
        give fluent_diagnostic_fields("stage failure", "stage failure.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "stage failure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SEED_MISSING" {
        give fluent_diagnostic_fields("seed fehlend", "seed fehlend.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "seed fehlend.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_COMPILER_MISSING" {
        give fluent_diagnostic_fields("compiler fehlend", "compiler fehlend.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "compiler fehlend.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SELF_CHECK_FAILED" {
        give fluent_diagnostic_fields("self check fehlgeschlagen", "self check fehlgeschlagen.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "self check fehlgeschlagen.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_ARTIFACT_INVALID" {
        give fluent_diagnostic_fields("artifact ungueltig", "artifact ungueltig.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "artifact ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_BANNER" {
        give fluent_diagnostic_fields("bootstrap banner is ungueltig", "bootstrap banner is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap banner is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap Konstante signature is ungueltig", "bootstrap Konstante signature is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap Konstante signature is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_TYPE" {
        give fluent_diagnostic_fields("bootstrap Konstante has wrong Typ", "bootstrap Konstante has wrong Typ.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap Konstante has wrong Typ.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_DUP_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure doppelt", "bootstrap procedure doppelt.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure doppelt.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_EXPORT" {
        give fluent_diagnostic_fields("bootstrap export is ungueltig", "bootstrap export is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap export is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_BODY" {
        give fluent_diagnostic_fields("bootstrap main body is ungueltig", "bootstrap main body is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main body is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap main signature is ungueltig", "bootstrap main signature is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main signature is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_BODY" {
        give fluent_diagnostic_fields("bootstrap procedure body is ungueltig", "bootstrap procedure body is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap procedure signature is ungueltig", "bootstrap procedure signature is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure signature is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_SPACE" {
        give fluent_diagnostic_fields("bootstrap space Deklaration is ungueltig", "bootstrap space Deklaration is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap space Deklaration is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_TOP_LEVEL" {
        give fluent_diagnostic_fields("bootstrap top-level Deklaration is ungueltig", "bootstrap top-level Deklaration is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap top-level Deklaration is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNCLOSED_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure body is not closed", "bootstrap procedure body is not closed.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is not closed.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_CONST" {
        give fluent_diagnostic_fields("bootstrap Konstante unbekannt", "bootstrap Konstante unbekannt.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap Konstante unbekannt.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure nicht unterstuetzt", "bootstrap procedure nicht unterstuetzt.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure nicht unterstuetzt.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_VERSION" {
        give fluent_diagnostic_fields("bootstrap version is ungueltig", "bootstrap version is ungueltig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap version is ungueltig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "INSTALL_E_VITTE_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vitte help command fehlgeschlagen", "installed vitte help command fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vitte help command fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_VITTEC_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vittec help command fehlgeschlagen", "installed vittec help command fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vittec help command fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("installed package Import test fehlgeschlagen", "installed package Import test fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed package Import test fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("installed shell completion is fehlend", "installed shell completion is fehlend.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed shell completion is fehlend.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_CC_MISSING" {
        give fluent_diagnostic_fields("macOS package build requires cc", "macOS package build requires cc.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires cc.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_LIPO_MISSING" {
        give fluent_diagnostic_fields("universal macOS package build requires lipo", "universal macOS package build requires lipo.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "universal macOS package build requires lipo.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_MACOS_REQUIRED" {
        give fluent_diagnostic_fields("macOS package build requires macOS", "macOS package build requires macOS.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires macOS.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_SOURCE_MISSING" {
        give fluent_diagnostic_fields("package Quelle Datei is fehlend", "package Quelle Datei is fehlend.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "package Quelle Datei is fehlend.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_HELP_FAILED" {
        give fluent_diagnostic_fields("postinstall vitte help check fehlgeschlagen", "postinstall vitte help check fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall vitte help check fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("postinstall package Import check fehlgeschlagen", "postinstall package Import check fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall package Import check fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("postinstall shell completion check fehlgeschlagen", "postinstall shell completion check fehlgeschlagen.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall shell completion check fehlgeschlagen.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "LIMIT_FILE_SIZE_MAX" {
        give fluent_diagnostic_fields("Datei size max", "Datei size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Datei size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_TOKEN_SIZE_MAX" {
        give fluent_diagnostic_fields("Token size max", "Token size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Token size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_AST_DEPTH_MAX" {
        give fluent_diagnostic_fields("ast depth max", "ast depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ast depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_EXPR_DEPTH_MAX" {
        give fluent_diagnostic_fields("expr depth max", "expr depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expr depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_IMPORT_DEPTH_MAX" {
        give fluent_diagnostic_fields("Import depth max", "Import depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Import depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MODULE_COUNT_MAX" {
        give fluent_diagnostic_fields("Modul count max", "Modul count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Modul count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_DIAGNOSTICS_MAX" {
        give fluent_diagnostic_fields("zu many Diagnosen emitted", "zu many Diagnosen emitted.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "zu many Diagnosen emitted.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_SYMBOL_COUNT_MAX" {
        give fluent_diagnostic_fields("Symbol count max", "Symbol count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Symbol count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_PARSER_RECURSION_MAX" {
        give fluent_diagnostic_fields("Parser recursion max", "Parser recursion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Parser recursion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MACRO_EXPANSION_MAX" {
        give fluent_diagnostic_fields("macro expansion max", "macro expansion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "MACRO_E_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("expansion fehlgeschlagen", "expansion fehlgeschlagen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expansion fehlgeschlagen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RECURSION_LIMIT" {
        give fluent_diagnostic_fields("recursion limit", "recursion limit.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "recursion limit.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("Argument ungueltig", "Argument ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Argument ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MACRO" {
        give fluent_diagnostic_fields("macro unbekannt", "macro unbekannt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro unbekannt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSTABLE_FEATURE" {
        give fluent_diagnostic_fields("unstable Funktion", "unstable Funktion.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unstable Funktion.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "SYNTAX_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Bezeichner erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ausdruck erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Typ erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Muster erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Block erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Trennzeichen erwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Token unerwartet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unbalanced Trennzeichen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "attribute ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Deklaration ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Anweisung ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ausdruck ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Muster ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Literal ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Operator ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "modifier ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "body fehlt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Rueckgabe fehlt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Name doppelt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Name unbekannt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Typ unbekannt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Modul unbekannt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Member unbekannt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ambiguous Name.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "private Symbol.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Import fehlend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Import Zyklus.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "export Konflikt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "arity Nichtuebereinstimmung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Argument Nichtuebereinstimmung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Zuweisung Nichtuebereinstimmung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "branch Nichtuebereinstimmung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Aufruf Ziel is not callable.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Umwandlung ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Index ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "deref ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ausleihe ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Verschiebung ungueltig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after Verschiebung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after Freigabe.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use before init.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "double Freigabe.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ausleihe Konflikt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "mutability Konflikt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Lebensdauer zu short.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "dangling Referenz.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "non exhaustive match.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unerreichbar Muster.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const required.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const Ueberlauf.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const Division by Null.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const Zyklus.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro fehlend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro recursion.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro expansion fehlgeschlagen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait not implemented.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait ambiguous.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "generisch Argument fehlend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "generisch bound fehlgeschlagen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "Ziel nicht unterstuetzt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "abi Nichtuebereinstimmung.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "link fehlgeschlagen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "runtime Panik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "NAME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Bezeichner erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausdruck erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Typ erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Muster erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Block erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Trennzeichen erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Token unerwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced Trennzeichen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Deklaration ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Anweisung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausdruck ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Muster ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Literal ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Operator ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body fehlt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Rueckgabe fehlt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Name doppelt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Name unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Typ unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Modul unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Member unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous Name.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private Symbol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Import fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Import Zyklus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Argument Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Zuweisung Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Aufruf Ziel is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Umwandlung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Index ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausleihe ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Verschiebung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after Verschiebung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after Freigabe.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double Freigabe.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausleihe Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Lebensdauer zu short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling Referenz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unerreichbar Muster.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Ueberlauf.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Division by Null.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Zyklus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generisch Argument fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generisch bound fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ziel nicht unterstuetzt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime Panik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Bezeichner erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausdruck erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Typ erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Muster erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Block erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Trennzeichen erwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Token unerwartet.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced Trennzeichen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Deklaration ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Anweisung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausdruck ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Muster ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Literal ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Operator ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body fehlt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Rueckgabe fehlt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Name doppelt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Name unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Typ unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Modul unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Member unbekannt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous Name.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private Symbol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Import fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Import Zyklus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Argument Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Zuweisung Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Aufruf Ziel is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Umwandlung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Index ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausleihe ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Verschiebung ungueltig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after Verschiebung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after Freigabe.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double Freigabe.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ausleihe Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability Konflikt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Lebensdauer zu short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling Referenz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unerreichbar Muster.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Ueberlauf.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Division by Null.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const Zyklus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generisch Argument fehlend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generisch bound fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "Ziel nicht unterstuetzt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi Nichtuebereinstimmung.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link fehlgeschlagen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime Panik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Bezeichner erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Block erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Trennzeichen erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Token unerwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced Trennzeichen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Deklaration ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Anweisung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Literal ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Operator ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Rueckgabe fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Modul unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Member unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous Name.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private Symbol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Argument Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Zuweisung Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Aufruf Ziel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Umwandlung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Index ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Verschiebung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Verschiebung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Lebensdauer zu short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling Referenz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unerreichbar Muster.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Ueberlauf.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Division by Null.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch Argument fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch bound fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ziel nicht unterstuetzt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime Panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Bezeichner erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Block erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Trennzeichen erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Token unerwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced Trennzeichen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Deklaration ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Anweisung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Literal ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Operator ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Rueckgabe fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Modul unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Member unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous Name.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private Symbol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Argument Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Zuweisung Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Aufruf Ziel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Umwandlung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Index ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Verschiebung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Verschiebung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Lebensdauer zu short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling Referenz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unerreichbar Muster.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Ueberlauf.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Division by Null.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch Argument fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch bound fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ziel nicht unterstuetzt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime Panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Bezeichner erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Block erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Trennzeichen erwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Token unerwartet.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced Trennzeichen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Deklaration ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Anweisung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausdruck ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Muster ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Literal ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Operator ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Rueckgabe fehlt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name doppelt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Name unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Typ unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Modul unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Member unbekannt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous Name.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private Symbol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Import Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Argument Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Zuweisung Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Aufruf Ziel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Umwandlung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Index ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Verschiebung ungueltig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Verschiebung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double Freigabe.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ausleihe Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability Konflikt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Lebensdauer zu short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling Referenz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unerreichbar Muster.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Ueberlauf.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Division by Null.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const Zyklus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch Argument fehlend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generisch bound fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "Ziel nicht unterstuetzt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi Nichtuebereinstimmung.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link fehlgeschlagen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime Panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "OWNERSHIP_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Bezeichner erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausdruck erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Typ erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Muster erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Block erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Trennzeichen erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Token unerwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced Trennzeichen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Deklaration ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Anweisung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausdruck ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Muster ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Literal ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Operator ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body fehlt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Rueckgabe fehlt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Name doppelt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Name unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Typ unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Modul unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Member unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous Name.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private Symbol.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Import fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Import Zyklus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Argument Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Zuweisung Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Aufruf Ziel is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Umwandlung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Index ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausleihe ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Verschiebung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after Verschiebung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausleihe Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Lebensdauer zu short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling Referenz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unerreichbar Muster.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Ueberlauf.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Division by Null.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Zyklus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generisch Argument fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generisch bound fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ziel nicht unterstuetzt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime Panik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Bezeichner erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausdruck erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Typ erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Muster erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Block erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Trennzeichen erwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Token unerwartet.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced Trennzeichen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Deklaration ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Anweisung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausdruck ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Muster ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Literal ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Operator ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body fehlt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Rueckgabe fehlt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Name doppelt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Name unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Typ unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Modul unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Member unbekannt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous Name.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private Symbol.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Import fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Import Zyklus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Argument Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Zuweisung Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Aufruf Ziel is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Umwandlung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Index ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausleihe ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Verschiebung ungueltig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after Verschiebung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double Freigabe.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ausleihe Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability Konflikt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Lebensdauer zu short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling Referenz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unerreichbar Muster.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Ueberlauf.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Division by Null.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const Zyklus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generisch Argument fehlend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generisch bound fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "Ziel nicht unterstuetzt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi Nichtuebereinstimmung.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link fehlgeschlagen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime Panik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Bezeichner erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Ausdruck erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Typ erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Muster erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Block erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Trennzeichen erwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Token unerwartet.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unbalanced Trennzeichen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "attribute ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Deklaration ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Anweisung ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Ausdruck ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Muster ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Literal ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Operator ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "modifier ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "body fehlt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Rueckgabe fehlt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Name doppelt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Name unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Typ unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Modul unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Member unbekannt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ambiguous Name.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "private Symbol.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Import fehlend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Import Zyklus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "export Konflikt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "arity Nichtuebereinstimmung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Argument Nichtuebereinstimmung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Zuweisung Nichtuebereinstimmung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "branch Nichtuebereinstimmung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Aufruf Ziel is not callable.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Umwandlung ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Index ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "deref ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Ausleihe ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Verschiebung ungueltig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after Verschiebung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after Freigabe.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use before init.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "double Freigabe.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Ausleihe Konflikt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutability Konflikt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Lebensdauer zu short.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "dangling Referenz.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non exhaustive match.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unerreichbar Muster.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const required.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const Ueberlauf.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const Division by Null.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const Zyklus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro fehlend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro recursion.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro expansion fehlgeschlagen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait not implemented.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait ambiguous.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "generisch Argument fehlend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "generisch bound fehlgeschlagen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "Ziel nicht unterstuetzt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "abi Nichtuebereinstimmung.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "link fehlgeschlagen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "runtime Panik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MACRO_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Bezeichner erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Ausdruck erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Typ erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Muster erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Block erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Trennzeichen erwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Token unerwartet.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unbalanced Trennzeichen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "attribute ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Deklaration ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Anweisung ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Ausdruck ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Muster ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Literal ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Operator ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "modifier ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "body fehlt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Rueckgabe fehlt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Name doppelt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Name unbekannt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Typ unbekannt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Modul unbekannt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Member unbekannt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ambiguous Name.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "private Symbol.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Import fehlend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Import Zyklus.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "export Konflikt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "arity Nichtuebereinstimmung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Argument Nichtuebereinstimmung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Zuweisung Nichtuebereinstimmung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "branch Nichtuebereinstimmung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "my_macro(value)", "Aufruf Ziel is not callable.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Umwandlung ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Index ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "deref ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Ausleihe ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Verschiebung ungueltig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after Verschiebung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after Freigabe.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use before init.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "double Freigabe.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Ausleihe Konflikt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "mutability Konflikt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Lebensdauer zu short.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "dangling Referenz.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "non exhaustive match.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unerreichbar Muster.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const required.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const Ueberlauf.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const Division by Null.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const Zyklus.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro fehlend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro recursion.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro expansion fehlgeschlagen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait not implemented.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait ambiguous.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "generisch Argument fehlend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "generisch bound fehlgeschlagen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "Ziel nicht unterstuetzt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "abi Nichtuebereinstimmung.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "link fehlgeschlagen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "runtime Panik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "HIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Bezeichner erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Block erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Trennzeichen erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Token unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced Trennzeichen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Deklaration ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Anweisung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Literal ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Operator ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Rueckgabe fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name doppelt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Modul unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Member unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous Name.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private Symbol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Argument Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Zuweisung Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "Aufruf Ziel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Umwandlung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Index ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Verschiebung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Verschiebung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Lebensdauer zu short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling Referenz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unerreichbar Muster.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Ueberlauf.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Division by Null.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch Argument fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch bound fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ziel nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime Panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Bezeichner erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Block erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Trennzeichen erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Token unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced Trennzeichen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Deklaration ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Anweisung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Literal ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Operator ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Rueckgabe fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name doppelt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Modul unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Member unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous Name.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private Symbol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Argument Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Zuweisung Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "Aufruf Ziel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Umwandlung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Index ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Verschiebung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Verschiebung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Lebensdauer zu short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling Referenz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unerreichbar Muster.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Ueberlauf.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Division by Null.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch Argument fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch bound fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ziel nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime Panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Bezeichner erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Block erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Trennzeichen erwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Token unerwartet.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced Trennzeichen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Deklaration ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Anweisung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausdruck ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Muster ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Literal ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Operator ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Rueckgabe fehlt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name doppelt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Name unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Typ unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Modul unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Member unbekannt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous Name.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private Symbol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Import Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Argument Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Zuweisung Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "Aufruf Ziel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Umwandlung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Index ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Verschiebung ungueltig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Verschiebung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double Freigabe.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ausleihe Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability Konflikt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Lebensdauer zu short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling Referenz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unerreichbar Muster.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Ueberlauf.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Division by Null.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const Zyklus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch Argument fehlend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generisch bound fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "Ziel nicht unterstuetzt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi Nichtuebereinstimmung.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link fehlgeschlagen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime Panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Bezeichner erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausdruck erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Typ erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Muster erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Block erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Trennzeichen erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Token unerwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced Trennzeichen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Deklaration ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Anweisung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausdruck ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Muster ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Literal ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Operator ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body fehlt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Rueckgabe fehlt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Name doppelt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Name unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Typ unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Modul unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Member unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous Name.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private Symbol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Import fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Import Zyklus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Argument Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Zuweisung Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Aufruf Ziel is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Umwandlung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Index ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausleihe ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Verschiebung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after Verschiebung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after Freigabe.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double Freigabe.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausleihe Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Lebensdauer zu short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling Referenz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unerreichbar Muster.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Ueberlauf.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Division by Null.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Zyklus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generisch Argument fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generisch bound fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime Panik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Bezeichner erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausdruck erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Typ erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Muster erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Block erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Trennzeichen erwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Token unerwartet.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced Trennzeichen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Deklaration ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Anweisung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausdruck ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Muster ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Literal ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Operator ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body fehlt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Rueckgabe fehlt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Name doppelt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Name unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Typ unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Modul unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Member unbekannt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous Name.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private Symbol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Import fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Import Zyklus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Argument Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Zuweisung Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Aufruf Ziel is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Umwandlung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Index ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausleihe ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Verschiebung ungueltig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after Verschiebung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after Freigabe.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double Freigabe.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ausleihe Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability Konflikt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Lebensdauer zu short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling Referenz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unerreichbar Muster.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Ueberlauf.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Division by Null.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const Zyklus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generisch Argument fehlend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generisch bound fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "Ziel nicht unterstuetzt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi Nichtuebereinstimmung.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link fehlgeschlagen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime Panik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Bezeichner erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Ausdruck erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Typ erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Muster erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Block erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Trennzeichen erwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Token unerwartet.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "unbalanced Trennzeichen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "attribute ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Deklaration ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Anweisung ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Ausdruck ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Muster ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Literal ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Operator ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "modifier ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "body fehlt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Rueckgabe fehlt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Name doppelt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Name unbekannt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Typ unbekannt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Modul unbekannt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Member unbekannt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ambiguous Name.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "private Symbol.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Import fehlend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Import Zyklus.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "export Konflikt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "arity Nichtuebereinstimmung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Argument Nichtuebereinstimmung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Zuweisung Nichtuebereinstimmung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "branch Nichtuebereinstimmung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "if index < len(items) { give items[index] }", "Aufruf Ziel is not callable.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Umwandlung ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Index ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "deref ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Ausleihe ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Verschiebung ungueltig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after Verschiebung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after Freigabe.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use before init.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "double Freigabe.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Ausleihe Konflikt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "mutability Konflikt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Lebensdauer zu short.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "dangling Referenz.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "non exhaustive match.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "unerreichbar Muster.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const required.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const Ueberlauf.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const Division by Null.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const Zyklus.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro fehlend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro recursion.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro expansion fehlgeschlagen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait not implemented.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait ambiguous.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "generisch Argument fehlend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "generisch bound fehlgeschlagen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "Ziel nicht unterstuetzt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "abi Nichtuebereinstimmung.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "link fehlgeschlagen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "runtime Panik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Bezeichner erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ausdruck erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Typ erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Muster erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Block erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Trennzeichen erwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Token unerwartet.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "unbalanced Trennzeichen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "attribute ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Deklaration ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Anweisung ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ausdruck ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Muster ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Literal ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Operator ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "modifier ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "body fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Rueckgabe fehlt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Name doppelt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Name unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Typ unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Modul unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Member unbekannt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ambiguous Name.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "private Symbol.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Import fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Import Zyklus.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "export Konflikt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "arity Nichtuebereinstimmung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Argument Nichtuebereinstimmung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Zuweisung Nichtuebereinstimmung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "branch Nichtuebereinstimmung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Aufruf Ziel is not callable.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Umwandlung ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Index ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "deref ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ausleihe ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Verschiebung ungueltig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after Verschiebung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after Freigabe.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use before init.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "double Freigabe.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ausleihe Konflikt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mutability Konflikt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Lebensdauer zu short.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "dangling Referenz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "non exhaustive match.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "unerreichbar Muster.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const required.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const Ueberlauf.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const Division by Null.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const Zyklus.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro recursion.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro expansion fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait not implemented.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait ambiguous.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "generisch Argument fehlend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "generisch bound fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "Ziel nicht unterstuetzt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "abi Nichtuebereinstimmung.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link fehlgeschlagen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "runtime Panik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LIMIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("Bezeichner erwartet", "Bezeichner erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Bezeichner erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck erwartet", "Ausdruck erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Ausdruck erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("Typ erwartet", "Typ erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Typ erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("Muster erwartet", "Muster erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Muster erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("Block erwartet", "Block erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Block erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("Trennzeichen erwartet", "Trennzeichen erwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Trennzeichen erwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("Token unerwartet", "Token unerwartet.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Token unerwartet.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced Trennzeichen", "unbalanced Trennzeichen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "unbalanced Trennzeichen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ungueltig", "attribute ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "attribute ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("Deklaration ungueltig", "Deklaration ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Deklaration ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("Anweisung ungueltig", "Anweisung ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Anweisung ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("Ausdruck ungueltig", "Ausdruck ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Ausdruck ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("Muster ungueltig", "Muster ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Muster ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("Literal ungueltig", "Literal ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Literal ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("Operator ungueltig", "Operator ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Operator ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ungueltig", "modifier ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "modifier ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body fehlt", "body fehlt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "body fehlt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give fehlt", "Rueckgabe fehlt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Rueckgabe fehlt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("Name doppelt", "Name doppelt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Name doppelt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("Name unbekannt", "Name unbekannt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Name unbekannt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("Typ unbekannt", "Typ unbekannt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Typ unbekannt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("Modul unbekannt", "Modul unbekannt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Modul unbekannt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("Member unbekannt", "Member unbekannt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Member unbekannt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous Name", "ambiguous Name.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ambiguous Name.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private Symbol", "private Symbol.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "private Symbol.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("Import fehlend", "Import fehlend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Import fehlend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("Import Zyklus", "Import Zyklus.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Import Zyklus.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export Konflikt", "export Konflikt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "export Konflikt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity Nichtuebereinstimmung", "arity Nichtuebereinstimmung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "arity Nichtuebereinstimmung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("Argument Nichtuebereinstimmung", "Argument Nichtuebereinstimmung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Argument Nichtuebereinstimmung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("Zuweisung Nichtuebereinstimmung", "Zuweisung Nichtuebereinstimmung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Zuweisung Nichtuebereinstimmung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch Nichtuebereinstimmung", "branch Nichtuebereinstimmung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "branch Nichtuebereinstimmung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("Aufruf Ziel is not callable", "Aufruf Ziel is not callable.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Aufruf Ziel is not callable.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("Umwandlung ungueltig", "Umwandlung ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Umwandlung ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("Index ungueltig", "Index ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Index ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ungueltig", "deref ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "deref ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("Ausleihe ungueltig", "Ausleihe ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Ausleihe ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("Verschiebung ungueltig", "Verschiebung ungueltig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Verschiebung ungueltig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after Verschiebung", "use after Verschiebung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after Verschiebung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after Freigabe", "use after Freigabe.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after Freigabe.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use before init.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double Freigabe", "double Freigabe.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "double Freigabe.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("Ausleihe Konflikt", "Ausleihe Konflikt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Ausleihe Konflikt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability Konflikt", "mutability Konflikt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "mutability Konflikt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("Lebensdauer zu short", "Lebensdauer zu short.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Lebensdauer zu short.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling Referenz", "dangling Referenz.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "dangling Referenz.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "non exhaustive match.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("unerreichbar Muster", "unerreichbar Muster.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "unerreichbar Muster.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const required.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const Ueberlauf", "const Ueberlauf.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const Ueberlauf.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const Division by Null", "const Division by Null.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const Division by Null.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const Zyklus", "const Zyklus.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const Zyklus.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro fehlend", "macro fehlend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro fehlend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro recursion.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion fehlgeschlagen", "macro expansion fehlgeschlagen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion fehlgeschlagen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait not implemented.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait ambiguous.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generisch Argument fehlend", "generisch Argument fehlend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "generisch Argument fehlend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generisch bound fehlgeschlagen", "generisch bound fehlgeschlagen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "generisch bound fehlgeschlagen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("Ziel nicht unterstuetzt", "Ziel nicht unterstuetzt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "Ziel nicht unterstuetzt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi Nichtuebereinstimmung", "abi Nichtuebereinstimmung.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "abi Nichtuebereinstimmung.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link fehlgeschlagen", "link fehlgeschlagen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "link fehlgeschlagen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime Panik", "runtime Panik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "runtime Panik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    give fluent_diagnostic_fields_empty();
}
