space vitte/compiler/infrastructure/diagnostics/fluent_catalog_tr

export *

form FluentDiagnosticFields {
    message: string
    primary_label: string
    secondary_label: string
    cause: string
    help: string
    fix_it: string
    corrected_example: string
    note: string
    lsp_code_action_title: string
    valid: bool
}

proc fluent_diagnostic_fields(message: string, primary_label: string, secondary_label: string, cause: string, help: string, fix_it: string, corrected_example: string, note: string, lsp_code_action_title: string) -> FluentDiagnosticFields {
    give FluentDiagnosticFields {
        message: message,
        primary_label: primary_label,
        secondary_label: secondary_label,
        cause: cause,
        help: help,
        fix_it: fix_it,
        corrected_example: corrected_example,
        note: note,
        lsp_code_action_title: lsp_code_action_title,
        valid: message != "" and primary_label != "" and cause != "" and help != ""
    };
}

proc fluent_diagnostic_fields_empty() -> FluentDiagnosticFields {
    give fluent_diagnostic_fields("", "", "", "", "", "", "", "", "");
}

proc fluent_catalog_fields_tr(code: string) -> FluentDiagnosticFields {
    if code == "E0001" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "The ayristirici expected a ad for something (variable, tur, modul, etc.).", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The ayristirici expected a ad for something (variable, tur, modul, etc.).", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0002" {
        give fluent_diagnostic_fields("ifade bekleniyor", "The ayristirici expected an ifade at this location.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The ayristirici expected an ifade at this location.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0003" {
        give fluent_diagnostic_fields("desen bekleniyor", "The ayristirici expected a desen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The ayristirici expected a desen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0004" {
        give fluent_diagnostic_fields("tur bekleniyor", "The ayristirici expected a tur ad.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The ayristirici expected a tur ad.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0005" {
        give fluent_diagnostic_fields("'end' bekleniyor", "A blok was opened but not closed with 'end' or '.end'.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A blok was opened but not closed with 'end' or '.end'.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0006" {
        give fluent_diagnostic_fields("proc after attribute bekleniyor", "An attribute followed by a proc bildirim olmalidir.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "An attribute followed by a proc bildirim olmalidir.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0007" {
        give fluent_diagnostic_fields("top-level bildirim bekleniyor", "The ayristirici expected a top-level bildirim.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The ayristirici expected a top-level bildirim.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0008" {
        give fluent_diagnostic_fields("extern proc govde iceremez", "A desen bound the same ad more than once.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A desen bound the same ad more than once.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0009" {
        give fluent_diagnostic_fields("proc requires a body unless marked #[extern]", "A referenced tur ad was eksik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A referenced tur ad was eksik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0010" {
        give fluent_diagnostic_fields("tur alias requires a hedef tur", "The base tur of a jenerik was eksik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The base tur of a jenerik was eksik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0011" {
        give fluent_diagnostic_fields("select en az bir when branch gerektirir", "A jenerik tur must include at least one arguman.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A jenerik tur must include at least one arguman.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0012" {
        give fluent_diagnostic_fields("select branch a when deyim olmalidir", "This tur form is not supported yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let p: *int = &value", "This tur form is not supported yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0013" {
        give fluent_diagnostic_fields("procedure give olmadan bitebilir", "proc deger dondurmeden cikabilir.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc to_code(ok: bool) -> int {\\n  if ok { give 0 }\\n  give 1\\n}", "proc deger dondurmeden cikabilir.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0014" {
        give fluent_diagnostic_fields("strict-core modunda yasak ust duzey soz dizimi", "An invocation is eksik its cagri hedefi.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "print(\\\"hi\\\")", "An invocation is eksik its cagri hedefi.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0015" {
        give fluent_diagnostic_fields("strict-core modunda yasak deyim soz dizimi", "This ifade is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let x = value", "This ifade is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0016" {
        give fluent_diagnostic_fields("strict-core modunda yasak ifade soz dizimi", "This desen is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This desen is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0017" {
        give fluent_diagnostic_fields("'}' bekleniyor", "This deyim is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This deyim is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0018" {
        give fluent_diagnostic_fields("match icinde 'case' veya 'otherwise' bekleniyor", "An extern procedure cannot define a body.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "#[extern]\\nproc puts(s: string) -> int", "An extern procedure cannot define a body.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E1001" {
        give fluent_diagnostic_fields("yinelenen desen baglama", "yinelenen desen baglama.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen desen baglama.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1002" {
        give fluent_diagnostic_fields("bilinmeyen tur (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?)", "bilinmeyen tur (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?).", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "bilinmeyen tur (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?).", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1003" {
        give fluent_diagnostic_fields("bilinmeyen jenerik base tur", "bilinmeyen jenerik base tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "bilinmeyen jenerik base tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1004" {
        give fluent_diagnostic_fields("jenerik tur en az bir arguman gerektirir", "jenerik tur en az bir arguman gerektirir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "jenerik tur en az bir arguman gerektirir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1005" {
        give fluent_diagnostic_fields("bilinmeyen tanimlayici", "bilinmeyen tanimlayici.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "bilinmeyen tanimlayici.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1006" {
        give fluent_diagnostic_fields("jenerik tur en az bir tur arguman gerektirir", "jenerik tur en az bir tur arguman gerektirir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "jenerik tur en az bir tur arguman gerektirir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1007" {
        give fluent_diagnostic_fields("gecersiz donusum between signed and unsigned values", "gecersiz donusum between signed and unsigned values.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "gecersiz donusum between signed and unsigned values.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1010" {
        give fluent_diagnostic_fields("stdlib modul active stdlib profil tarafindan reddedildi", "stdlib modul active stdlib profil tarafindan reddedildi.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib modul active stdlib profil tarafindan reddedildi.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1011" {
        give fluent_diagnostic_fields("strict-imports requires explicit alias", "strict-imports requires explicit alias.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports requires explicit alias.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1012" {
        give fluent_diagnostic_fields("strict-imports unused ice aktarma aliases yasaklar", "strict-imports unused ice aktarma aliases yasaklar.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports unused ice aktarma aliases yasaklar.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1013" {
        give fluent_diagnostic_fields("strict-imports non-canonical ice aktarma paths yasaklar", "strict-imports non-canonical ice aktarma paths yasaklar.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports non-canonical ice aktarma paths yasaklar.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1014" {
        give fluent_diagnostic_fields("stdlib modul eksik", "stdlib modul eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib modul eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1015" {
        give fluent_diagnostic_fields("experimental modul ice aktarma denied", "experimental modul ice aktarma denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "experimental modul ice aktarma denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1016" {
        give fluent_diagnostic_fields("ic modul ice aktarma denied", "ic modul ice aktarma denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ic modul ice aktarma denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1017" {
        give fluent_diagnostic_fields("re-export sembol cakismazlik", "re-export sembol cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "re-export sembol cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1018" {
        give fluent_diagnostic_fields("ambiguous ice aktarma yol", "ambiguous ice aktarma yol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ambiguous ice aktarma yol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1019" {
        give fluent_diagnostic_fields("strict-modules glob imports yasaklar", "strict-modules glob imports yasaklar.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-modules glob imports yasaklar.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1020" {
        give fluent_diagnostic_fields("legacy ice aktarma yol kullanımdan kaldirildi", "legacy ice aktarma yol kullanımdan kaldirildi.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "legacy ice aktarma yol kullanımdan kaldirildi.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1021" {
        give fluent_diagnostic_fields("entry modul yol canonical olmalidir", "entry modul yol canonical olmalidir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry modul yol canonical olmalidir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1022" {
        give fluent_diagnostic_fields("yinelenen entry ad", "yinelenen entry ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen entry ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1023" {
        give fluent_diagnostic_fields("share bilinmeyen sembol basvuruyor", "share bilinmeyen sembol basvuruyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share bilinmeyen sembol basvuruyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1024" {
        give fluent_diagnostic_fields("yinelenen sembol in share list", "yinelenen sembol in share list.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen sembol in share list.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1025" {
        give fluent_diagnostic_fields("sembol modul tarafindan disa aktarilmadi", "sembol modul tarafindan disa aktarilmadi.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "sembol modul tarafindan disa aktarilmadi.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1026" {
        give fluent_diagnostic_fields("yinelenen share bildirim", "yinelenen share bildirim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen share bildirim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1027" {
        give fluent_diagnostic_fields("yinelenen ice aktarma baglama", "yinelenen ice aktarma baglama.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen ice aktarma baglama.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1028" {
        give fluent_diagnostic_fields("ice aktarma baglama yerel bildirim ile cakisir", "ice aktarma baglama yerel bildirim ile cakisir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ice aktarma baglama yerel bildirim ile cakisir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1029" {
        give fluent_diagnostic_fields("yinelenen yerel bildirim ad", "yinelenen yerel bildirim ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "yinelenen yerel bildirim ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1030" {
        give fluent_diagnostic_fields("modul alias uye not exported", "modul alias uye not exported.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "modul alias uye not exported.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1031" {
        give fluent_diagnostic_fields("qualified tur uye eksik", "qualified tur uye eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "qualified tur uye eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1032" {
        give fluent_diagnostic_fields("ifade cagrilabilir degil", "ifade cagrilabilir degil.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ifade cagrilabilir degil.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E2001" {
        give fluent_diagnostic_fields("desteklenmeyen tur", "desteklenmeyen tur.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen tur.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2002" {
        give fluent_diagnostic_fields("invoke has no cagri hedefi", "invoke has no cagri hedefi.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "invoke has no cagri hedefi.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2003" {
        give fluent_diagnostic_fields("desteklenmeyen ifade in HIR", "desteklenmeyen ifade in HIR.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen ifade in HIR.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2004" {
        give fluent_diagnostic_fields("desteklenmeyen desen in HIR", "desteklenmeyen desen in HIR.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen desen in HIR.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2005" {
        give fluent_diagnostic_fields("desteklenmeyen deyim in HIR", "desteklenmeyen deyim in HIR.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen deyim in HIR.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2006" {
        give fluent_diagnostic_fields("beklenmeyen HIR tur kind", "beklenmeyen HIR tur kind.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen HIR tur kind.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2007" {
        give fluent_diagnostic_fields("beklenmeyen HIR expr kind", "beklenmeyen HIR expr kind.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen HIR expr kind.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2008" {
        give fluent_diagnostic_fields("beklenmeyen HIR stmt kind", "beklenmeyen HIR stmt kind.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen HIR stmt kind.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2009" {
        give fluent_diagnostic_fields("beklenmeyen HIR desen kind", "beklenmeyen HIR desen kind.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen HIR desen kind.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2010" {
        give fluent_diagnostic_fields("beklenmeyen HIR decl kind", "beklenmeyen HIR decl kind.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen HIR decl kind.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "LEX_E_INVALID_CHAR" {
        give fluent_diagnostic_fields("gecersiz karakter", "gecersiz karakter.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz karakter.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_NUMBER" {
        give fluent_diagnostic_fields("gecersiz numeric literal", "gecersiz numeric literal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz numeric literal.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_ESCAPE" {
        give fluent_diagnostic_fields("gecersiz kacis sequence", "gecersiz kacis sequence.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz kacis sequence.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE_ESCAPE" {
        give fluent_diagnostic_fields("gecersiz unicode kacis sequence", "gecersiz unicode kacis sequence.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz unicode kacis sequence.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UTF8" {
        give fluent_diagnostic_fields("gecersiz UTF-8 byte sequence", "gecersiz UTF-8 byte sequence.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz UTF-8 byte sequence.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_CHAR_LITERAL" {
        give fluent_diagnostic_fields("gecersiz karakter literal", "gecersiz karakter literal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz karakter literal.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_IDENTIFIER" {
        give fluent_diagnostic_fields("gecersiz tanimlayici", "gecersiz tanimlayici.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz tanimlayici.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_STRING" {
        give fluent_diagnostic_fields("sonlandirilmamis string literal", "sonlandirilmamis string literal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add the closing `\"` on the same line; do not close a string with a single quote", "print(\"message\")", "sonlandirilmamis string literal.", "add the closing `\"` on the same line; do not close a string with a single quote");
    }

    if code == "LEX_E_UNTERMINATED_CHAR" {
        give fluent_diagnostic_fields("sonlandirilmamis karakter literal", "sonlandirilmamis karakter literal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "sonlandirilmamis karakter literal.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_BLOCK_COMMENT" {
        give fluent_diagnostic_fields("sonlandirilmamis blok comment", "sonlandirilmamis blok comment.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "sonlandirilmamis blok comment.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_REGION_COMMENT" {
        give fluent_diagnostic_fields("sonlandirilmamis region comment", "sonlandirilmamis region comment.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "sonlandirilmamis region comment.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNEXPECTED_EOF" {
        give fluent_diagnostic_fields("beklenmeyen end of dosya", "beklenmeyen end of dosya.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "beklenmeyen end of dosya.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_TOKEN_TOO_LARGE" {
        give fluent_diagnostic_fields("token cok", "token cok.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token cok.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PLOOP" {
        give fluent_diagnostic_fields("ayristirici ilerlemedi", "ayristirici ilerlemedi.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ayristirici ilerlemedi.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PPRIMARY999" {
        give fluent_diagnostic_fields("beklenmeyen ifade token", "beklenmeyen ifade token.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "beklenmeyen ifade token.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PSTMT007" {
        give fluent_diagnostic_fields("atama operator bekleniyor", "atama operator bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "atama operator bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P0001" {
        give fluent_diagnostic_fields("beklenmeyen top-level token", "beklenmeyen top-level token.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "beklenmeyen top-level token.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P000_UNBALANCED" {
        give fluent_diagnostic_fields("kapatilmamis blok", "kapatilmamis blok.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "kapatilmamis blok.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E_CLI_IO" {
        give fluent_diagnostic_fields("cannot read girdi dosya", "The compiler cannot read the requested girdi dosya.", "The requested file does not exist, is not a regular file, or is not readable.", "The requested file does not exist, is not a regular file, or is not readable.", "Verify the path passed to `vitte build` and the file permissions.", "use an existing readable source path", "vitte build src/main.vit -o build/main", "The compiler cannot read the requested girdi dosya.", "use an existing readable source path");
    }

    if code == "E_CLI_MISSING_ARG" {
        give fluent_diagnostic_fields("eksik command arguman", "eksik command arguman.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik command arguman.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_COMMAND" {
        give fluent_diagnostic_fields("bilinmeyen compiler command", "bilinmeyen compiler command.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen compiler command.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_OPTION" {
        give fluent_diagnostic_fields("bilinmeyen compiler option", "bilinmeyen compiler option.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen compiler option.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("gecersiz compiler arguman", "gecersiz compiler arguman.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz compiler arguman.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_IO_FILE_NOT_FOUND" {
        give fluent_diagnostic_fields("girdi dosya eksik", "girdi dosya eksik.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "girdi dosya eksik.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_FILE_UNREADABLE" {
        give fluent_diagnostic_fields("girdi dosya is not readable", "girdi dosya is not readable.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "girdi dosya is not readable.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OUTPUT_UNWRITABLE" {
        give fluent_diagnostic_fields("cannot yazma cikti dosya", "cannot yazma cikti dosya.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "cannot yazma cikti dosya.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "permission denied.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_DIRECTORY_NOT_FOUND" {
        give fluent_diagnostic_fields("cikti directory eksik", "cikti directory eksik.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "cikti directory eksik.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OVERWRITE_FORBIDDEN" {
        give fluent_diagnostic_fields("refusing to overwrite existing cikti", "refusing to overwrite existing cikti.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "refusing to overwrite existing cikti.", "use an existing readable input path and a writable output destination");
    }

    if code == "DRIVER_E_PIPELINE_FAILURE" {
        give fluent_diagnostic_fields("compiler pipeline basarisiz", "compiler pipeline basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler pipeline basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_FRONTEND_INCOMPLETE" {
        give fluent_diagnostic_fields("analysis frontend incomplete", "analysis frontend incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis frontend incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("analysis gecersiz", "analysis gecersiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis gecersiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROWCK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("borrowck not executed", "borrowck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "borrowck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CODEGEN_IR_INCOMPLETE" {
        give fluent_diagnostic_fields("kod uretimi ir incomplete", "kod uretimi ir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "kod uretimi ir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INPUT_NOT_LOADED" {
        give fluent_diagnostic_fields("frontend girdi not loaded", "frontend girdi not loaded.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend girdi not loaded.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INVALID" {
        give fluent_diagnostic_fields("frontend gecersiz", "frontend gecersiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend gecersiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_PROFILE" {
        give fluent_diagnostic_fields("gecersiz link profil", "gecersiz link profil.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz link profil.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_INVALID" {
        give fluent_diagnostic_fields("ir gecersiz", "ir gecersiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir gecersiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_MIR_INCOMPLETE" {
        give fluent_diagnostic_fields("ir mir incomplete", "ir mir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir mir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_CODEGEN_INCOMPLETE" {
        give fluent_diagnostic_fields("link kod uretimi incomplete", "link kod uretimi incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link kod uretimi incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INCOMPLETE" {
        give fluent_diagnostic_fields("mir analysis incomplete", "mir analysis incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("mir analysis gecersiz", "mir analysis gecersiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis gecersiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_INVALID" {
        give fluent_diagnostic_fields("mir gecersiz", "mir gecersiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir gecersiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NO_INPUT" {
        give fluent_diagnostic_fields("no girdi", "no girdi.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "no girdi.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_AST" {
        give fluent_diagnostic_fields("parse bos ast", "parse bos ast.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse bos ast.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_SOURCE" {
        give fluent_diagnostic_fields("parse bos kaynak", "parse bos kaynak.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse bos kaynak.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_LINK_INCOMPLETE" {
        give fluent_diagnostic_fields("run link incomplete", "run link incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run link incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_PROCESS_FAILED" {
        give fluent_diagnostic_fields("run process basarisiz", "run process basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run process basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SEMA_NOT_EXECUTED" {
        give fluent_diagnostic_fields("sema not executed", "sema not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "sema not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TYPECK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("typeck not executed", "typeck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "typeck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LINK_E_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "permission denied.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OUTPUT_NOT_MATERIALIZED" {
        give fluent_diagnostic_fields("linker did not create the requested executable", "linker did not create the requested executable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker did not create the requested executable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_C_COMPILER" {
        give fluent_diagnostic_fields("required C compiler was eksik", "required C compiler was eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "required C compiler was eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_COMMAND_FAILED" {
        give fluent_diagnostic_fields("linker command basarisiz", "linker command basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker command basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LEX_E_INVALID_FLOAT" {
        give fluent_diagnostic_fields("gecersiz ondalik", "gecersiz ondalik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz ondalik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_BINARY" {
        give fluent_diagnostic_fields("gecersiz ikili", "gecersiz ikili.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz ikili.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_OCTAL" {
        give fluent_diagnostic_fields("gecersiz octal", "gecersiz octal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz octal.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_HEX" {
        give fluent_diagnostic_fields("gecersiz hex", "gecersiz hex.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz hex.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_INDENTATION" {
        give fluent_diagnostic_fields("gecersiz girinti", "gecersiz girinti.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz girinti.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_TOKEN" {
        give fluent_diagnostic_fields("gecersiz token", "gecersiz token.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz token.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE" {
        give fluent_diagnostic_fields("gecersiz unicode", "gecersiz unicode.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz unicode.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_COMMENT" {
        give fluent_diagnostic_fields("sonlandirilmamis comment", "sonlandirilmamis comment.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "sonlandirilmamis comment.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PATTR003" {
        give fluent_diagnostic_fields("pattr003", "pattr003.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "pattr003.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TOPLEVEL_DECL_EXPECTED" {
        give fluent_diagnostic_fields("top-level bildirim expected", "top-level bildirim expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level bildirim expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_INCOMPLETE_EXPR" {
        give fluent_diagnostic_fields("incomplete ifade", "incomplete ifade.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "incomplete ifade.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_RPAREN" {
        give fluent_diagnostic_fields("eksik closing parenthesis", "eksik closing parenthesis.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "eksik closing parenthesis.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_COMMA" {
        give fluent_diagnostic_fields("eksik comma", "eksik comma.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "eksik comma.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TYPE_EXPECTED" {
        give fluent_diagnostic_fields("tur expected", "tur expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "tur expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PATTERN_EXPECTED" {
        give fluent_diagnostic_fields("desen expected", "desen expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "desen expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_BLOCK_EXPECTED" {
        give fluent_diagnostic_fields("blok expected", "blok expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_UNCLOSED_BLOCK" {
        give fluent_diagnostic_fields("kapatilmamis blok", "kapatilmamis blok.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add `}` to close the highlighted block, then run the checker again for its parent block", "while running { set running = false }", "kapatilmamis blok.", "add `}` to close the highlighted block, then run the checker again for its parent block");
    }

    if code == "PARSE_E_EXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token bekleniyor", "token bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PARAMETER_COLON_EXPECTED" {
        give fluent_diagnostic_fields("eksik colon in procedure parameter", "parameter colon expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "insert `:` between the parameter name and its type, for example `right: f64`", "proc calculate(right: f64) -> f64 { give right }", "parameter colon expected.", "insert `:` between the parameter name and its type, for example `right: f64`");
    }

    if code == "PARSE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "beklenmeyen token.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_OLD_VITTE_SYNTAX" {
        give fluent_diagnostic_fields("old Vitte soz dizimi is not canonical", "old Vitte soz dizimi is not canonical.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "Identify the intended construct, then compare it with the current Vitte keywords.", "replace the old token with the current Vitte keyword only when the construct is equivalent", "proc main() -> int { give 0 }", "old Vitte soz dizimi is not canonical.", "replace the old token with the current Vitte keyword only when the construct is equivalent");
    }

    if code == "PARSE_EXPECTED_EXPR" {
        give fluent_diagnostic_fields("parse expected expr", "parse expected expr.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected expr.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("parse expected tur", "parse expected tur.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected tur.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("parse expected desen", "parse expected desen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected desen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("parse expected blok", "parse expected blok.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected blok.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("parse expected tanimlayici", "parse expected tanimlayici.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected tanimlayici.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FAST0001" {
        give fluent_diagnostic_fields("fast0001", "fast0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "fast0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FLEX0001" {
        give fluent_diagnostic_fields("flex0001", "flex0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "flex0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "AST_E_INVALID_NODE" {
        give fluent_diagnostic_fields("gecersiz dugum", "gecersiz dugum.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz dugum.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_EMPTY_MODULE" {
        give fluent_diagnostic_fields("bos modul", "bos modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bos modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_DECL" {
        give fluent_diagnostic_fields("gecersiz decl", "gecersiz decl.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz decl.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("gecersiz expr", "gecersiz expr.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz expr.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz attribute.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_DUPLICATE_FIELD" {
        give fluent_diagnostic_fields("yinelenen alan", "yinelenen alan.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Find the earlier field label and the highlighted duplicate.", "keep one field entry and remove or rename the duplicate", "vitte check path/to/file.vit", "yinelenen alan.", "keep one field entry and remove or rename the duplicate");
    }

    if code == "AST_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("bilinmeyen form alan", "bilinmeyen form alan.", "A `form` construction or field access names a field that the form declaration does not contain.", "A `form` construction or field access names a field that the form declaration does not contain.", "Compare the highlighted field name with the fields declared in the `form`.", "rename the field to one declared by the form, or add the missing field to the form declaration", "form Point { x: int, y: int }", "bilinmeyen form alan.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "AST_E_MISSING_FIELD" {
        give fluent_diagnostic_fields("eksik form alan", "eksik form alan.", "A `form` construction omits a required field that has no default value.", "A `form` construction omits a required field that has no default value.", "Compare the construction with every required field in the `form` declaration.", "add the missing `field: value` entry to the construction", "form Point { x: int, y: int }", "eksik form alan.", "add the missing `field: value` entry to the construction");
    }

    if code == "AST_E_FIELD_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("form alan tur uyusmazlik", "form alan tur uyusmazlik.", "A value assigned to a `form` field does not match that field's declared type.", "A value assigned to a `form` field does not match that field's declared type.", "Compare the field declaration type with the highlighted field initializer.", "change the field initializer to the declared type, or change the field type in the `form`", "form User { name: string, age: int }", "form alan tur uyusmazlik.", "change the field initializer to the declared type, or change the field type in the `form`");
    }

    if code == "AST_E_FIELD_ORDER" {
        give fluent_diagnostic_fields("form fields are out of order", "form fields are out of order.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "Read the field order from the `form` declaration.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them", "form Pair { left: int, right: int }", "form fields are out of order.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them");
    }

    if code == "AST_E_INCOMPLETE_CONSTRUCTION" {
        give fluent_diagnostic_fields("form construction is incomplete", "form construction is incomplete.", "A `form` construction ended before all required fields were supplied.", "A `form` construction ended before all required fields were supplied.", "Check whether the construction is missing fields or a closing brace.", "complete the construction with every required `field: value` entry and the closing `}`", "form Point { x: int, y: int }", "form construction is incomplete.", "complete the construction with every required `field: value` entry and the closing `}`");
    }

    if code == "AST_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("gecersiz gorunurluk", "gecersiz gorunurluk.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz gorunurluk.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ENTRY" {
        give fluent_diagnostic_fields("gecersiz entry", "gecersiz entry.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz entry.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "SEMA_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("yinelenen sembol", "yinelenen sembol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "yinelenen sembol.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_UNKNOWN_IDENTIFIER" {
        give fluent_diagnostic_fields("bilinmeyen tanimlayici", "bilinmeyen tanimlayici.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "bilinmeyen tanimlayici.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_AMBIGUOUS_SYMBOL" {
        give fluent_diagnostic_fields("ambiguous sembol", "ambiguous sembol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous sembol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_SHADOWING_FORBIDDEN" {
        give fluent_diagnostic_fields("shadowing is forbidden", "shadowing is forbidden.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "shadowing is forbidden.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("alan does not exist", "alan does not exist.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename the field to one declared by the form, or add the missing field to the form declaration", "use vitte/core", "alan does not exist.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "SEMA_E_UNKNOWN_VARIANT" {
        give fluent_diagnostic_fields("variant does not exist", "variant does not exist.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "Compare the highlighted variant with the `case` names declared in the `pick`.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration", "pick Status { Ready, Failed }", "variant does not exist.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration");
    }

    if code == "SEMA_E_INACCESSIBLE_VARIANT" {
        give fluent_diagnostic_fields("pick variant is inaccessible", "pick variant is inaccessible.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "Read the branches in order and find the earlier branch that already covers this variant.", "remove the unreachable branch or move the more specific `case` before the broader branch", "match status {", "pick variant is inaccessible.", "remove the unreachable branch or move the more specific `case` before the broader branch");
    }

    if code == "SEMA_E_DUPLICATE_PICK_BRANCH" {
        give fluent_diagnostic_fields("pick branch is duplicated", "pick branch is duplicated.", "The same `pick` variant is matched by more than one branch in the same `match`.", "The same `pick` variant is matched by more than one branch in the same `match`.", "Find the earlier branch for the same `case` and decide which body should remain.", "merge the duplicate branch bodies or remove the later duplicate branch", "match status {", "pick branch is duplicated.", "merge the duplicate branch bodies or remove the later duplicate branch");
    }

    if code == "TYPECK_E_PICK_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("pick desen is not exhaustive", "pick desen is not exhaustive.", "A `match` over a `pick` value does not cover every reachable variant.", "A `match` over a `pick` value does not cover every reachable variant.", "List the declared `case` variants and compare them with the branches already present.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended", "match status {", "pick desen is not exhaustive.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended");
    }

    if code == "TYPECK_E_PICK_PAYLOAD_MISMATCH" {
        give fluent_diagnostic_fields("pick payload does not match variant", "pick payload does not match variant.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "Compare each payload position or name with the selected variant declaration.", "use the expected payload shape for that `case`, including the correct field count and types", "pick Event { Data(value: int) }", "pick payload does not match variant.", "use the expected payload shape for that `case`, including the correct field count and types");
    }

    if code == "TYPECK_E_IMPOSSIBLE_PATTERN" {
        give fluent_diagnostic_fields("pick desen can never match", "pick desen can never match.", "The pattern cannot match the scrutinee type produced by the expression.", "The pattern cannot match the scrutinee type produced by the expression.", "Compare the matched expression type with the variant or literal named by the pattern.", "replace the branch pattern with one that belongs to the matched `pick` or value type", "match status { case Ready { give 0 } }", "pick desen can never match.", "replace the branch pattern with one that belongs to the matched `pick` or value type");
    }

    if code == "SEMA_E_UNKNOWN_FUNCTION" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "SEMA_E_UNKNOWN_SYMBOL" {
        give fluent_diagnostic_fields("bilinmeyen sembol", "bilinmeyen sembol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "bilinmeyen sembol.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_INVALID_IMPORT" {
        give fluent_diagnostic_fields("gecersiz ice aktarma", "gecersiz ice aktarma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz ice aktarma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_EXPORT" {
        give fluent_diagnostic_fields("gecersiz export", "gecersiz export.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz export.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz attribute.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("gecersiz gorunurluk", "gecersiz gorunurluk.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz gorunurluk.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("gecersiz control akis", "gecersiz control akis.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz control akis.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("gecersiz modul", "gecersiz modul.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz modul.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ASSIGN_TARGET" {
        give fluent_diagnostic_fields("gecersiz assign hedef", "gecersiz assign hedef.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz assign hedef.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNDECLARED_TARGET" {
        give fluent_diagnostic_fields("undeclared hedef", "undeclared hedef.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "undeclared hedef.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_MISSING_BINDING" {
        give fluent_diagnostic_fields("eksik baglama", "eksik baglama.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "eksik baglama.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_DUPLICATE_BINDING" {
        give fluent_diagnostic_fields("yinelenen baglama", "yinelenen baglama.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "yinelenen baglama.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_DUPLICATE_ITEM" {
        give fluent_diagnostic_fields("yinelenen oge", "yinelenen oge.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "yinelenen oge.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_INVALID_HIR" {
        give fluent_diagnostic_fields("gecersiz hir", "gecersiz hir.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz hir.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INTERNAL" {
        give fluent_diagnostic_fields("ic", "ic.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ic.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPECK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_ITEM" {
        give fluent_diagnostic_fields("bilinmeyen oge", "bilinmeyen oge.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen oge.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_W_UNRESOLVED_NAME" {
        give fluent_diagnostic_fields("unresolved ad", "unresolved ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unresolved ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("gecersiz expr", "gecersiz expr.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz expr.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deref.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_BINARY_MISMATCH" {
        give fluent_diagnostic_fields("ikili uyusmazlik", "ikili uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make both operands valid for the operator before MIR lowering", "let count: int = 1", "ikili uyusmazlik.", "make both operands valid for the operator before MIR lowering");
    }

    if code == "TYPECK_E_ASSIGN_MISMATCH" {
        give fluent_diagnostic_fields("atama tur uyusmazlik", "atama tur uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "assign a value of the declared binding type, or change the binding annotation at its declaration", "let count: int = 1", "atama tur uyusmazlik.", "assign a value of the declared binding type, or change the binding annotation at its declaration");
    }

    if code == "TYPECK_E_RETURN_MISMATCH" {
        give fluent_diagnostic_fields("give tur uyusmazlik", "donus tur uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "change the `give` expression or the procedure result type so both name the same Vitte type", "let count: int = 1", "donus tur uyusmazlik.", "change the `give` expression or the procedure result type so both name the same Vitte type");
    }

    if code == "TYPECK_E_MISSING_GIVE" {
        give fluent_diagnostic_fields("eksik give in deger procedure", "eksik give in deger procedure.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "Inspect the procedure body and decide which value should be produced.", "add `give expression` on every successful path, or remove the `-> Type` result contract", "proc answer() -> int { give 42 }", "eksik give in deger procedure.", "add `give expression` on every successful path, or remove the `-> Type` result contract");
    }

    if code == "TYPECK_E_GIVE_IN_VOID_PROC" {
        give fluent_diagnostic_fields("give deger in procedure without result tur", "give deger in procedure without donus tur.", "A procedure without a `-> Type` contract uses `give` with a value.", "A procedure without a `-> Type` contract uses `give` with a value.", "Check whether the procedure should produce a value or only perform effects.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement", "proc answer() -> int { give 42 }", "give deger in procedure without donus tur.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement");
    }

    if code == "TYPECK_E_CONTROL_PATH_MISSING_GIVE" {
        give fluent_diagnostic_fields("control yol reaches end without give", "control yol reaches end without give.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "Follow each `if`, `match`, `loop`, and early-exit path in the procedure.", "add a final `give` or make every branch produce a value before control reaches the closing brace", "proc code(ok: bool) -> int {", "control yol reaches end without give.", "add a final `give` or make every branch produce a value before control reaches the closing brace");
    }

    if code == "TYPECK_W_UNREACHABLE_AFTER_GIVE" {
        give fluent_diagnostic_fields("code after give is ulasilamaz", "code after give is ulasilamaz.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "Inspect the statement immediately after the highlighted `give`.", "move the statement before `give`, put it in another branch, or remove it", "proc main() -> int {", "code after give is ulasilamaz.", "move the statement before `give`, put it in another branch, or remove it");
    }

    if code == "TYPECK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("cannot assign to immutable baglama", "cannot assign to immutable baglama.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "Find the original `let`, parameter, or field declaration for the highlighted target.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`", "let count: int = 0", "cannot assign to immutable baglama.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`");
    }

    if code == "TYPECK_W_MUTABLE_NEVER_MODIFIED" {
        give fluent_diagnostic_fields("degistirilebilir baglama is never changed", "degistirilebilir baglama is never changed.", "A binding was declared mutable, but no reachable `set` changes it.", "A binding was declared mutable, but no reachable `set` changes it.", "Search the binding scope for `set name = ...` or field updates through that binding.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended", "let count: int = 0", "degistirilebilir baglama is never changed.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended");
    }

    if code == "TYPECK_E_REASSIGNMENT_FORBIDDEN" {
        give fluent_diagnostic_fields("hedef cannot be reassigned", "hedef cannot be reassigned.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "Check that the left side of `set` is a binding, field, or index place that Vitte allows to be assigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`", "set user.name = name", "hedef cannot be reassigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`");
    }

    if code == "TYPECK_E_MISSING_INITIALIZER" {
        give fluent_diagnostic_fields("let baglama requires an initializer or tur", "let baglama requires an initializer or tur.", "`let` introduced a binding without enough information to create a value.", "`let` introduced a binding without enough information to create a value.", "Inspect the binding and check whether it has either an initializer or an explicit type plus a supported delayed-initialization path.", "add `= expression`, or add the missing type annotation required by the active Vitte rule", "let count: int = 0", "let baglama requires an initializer or tur.", "add `= expression`, or add the missing type annotation required by the active Vitte rule");
    }

    if code == "TYPECK_E_INFERENCE_FAILED" {
        give fluent_diagnostic_fields("baglama tur could not be inferred", "baglama tur could not be inferred.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "Look at the highlighted `let` and the first use of the binding.", "add an explicit Vitte type annotation after the binding name", "let items: [int] = []", "baglama tur could not be inferred.", "add an explicit Vitte type annotation after the binding name");
    }

    if code == "TYPECK_E_CONDITION_TYPE" {
        give fluent_diagnostic_fields("kosul tur", "kosul tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the condition produce bool, for example by adding an explicit comparison", "let count: int = 1", "kosul tur.", "make the condition produce bool, for example by adding an explicit comparison");
    }

    if code == "TYPECK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "rename the field to one declared by the form, or add the missing field to the form declaration", "let count: int = 1", "bilinmeyen uye.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "TYPECK_E_INDEX_TYPE" {
        give fluent_diagnostic_fields("indeks tur", "indeks tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "indeks tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_INDEX_TARGET" {
        give fluent_diagnostic_fields("gecersiz indeks hedef", "gecersiz indeks hedef.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz indeks hedef.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IF_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("if branch uyusmazlik", "if branch uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "if branch uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_COMPARE_MISMATCH" {
        give fluent_diagnostic_fields("compare uyusmazlik", "compare uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "compare uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz donusum.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MATCH_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("match non exhaustive", "match non exhaustive.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "match non exhaustive.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CALL_TARGET" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "cagri hedef is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_UNKNOWN_PROCEDURE" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "TYPECK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("cagri arguman tur uyusmazlik", "cagri arguman tur uyusmazlik.", "An argument expression does not match the type of the parameter it is passed to.", "An argument expression does not match the type of the parameter it is passed to.", "Link the highlighted argument to its parameter and compare the expected and found Vitte types.", "change that argument expression or the parameter type so the single argument matches its parameter", "scale(value: 4, factor: 2)", "cagri arguman tur uyusmazlik.", "change that argument expression or the parameter type so the single argument matches its parameter");
    }

    if code == "TYPECK_E_CALL_ARITY" {
        give fluent_diagnostic_fields("wrong sayi of cagri argumanlar", "wrong sayi of cagri argumanlar.", "The call supplies a different number of arguments than the procedure parameter list.", "The call supplies a different number of arguments than the procedure parameter list.", "Show the expected parameters in order, then attach each extra or missing argument to its nearest parameter.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list", "add(1, 2)", "wrong sayi of cagri argumanlar.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list");
    }

    if code == "TYPECK_E_UNKNOWN_NAMED_ARGUMENT" {
        give fluent_diagnostic_fields("named arguman has no matching parameter", "named arguman has no matching parameter.", "A named argument does not correspond to any parameter in the called procedure.", "A named argument does not correspond to any parameter in the called procedure.", "Display the expected parameter names and highlight the unknown argument name.", "rename the argument to an expected parameter name or remove it", "connect(host: \"localhost\", port: 8080)", "named arguman has no matching parameter.", "rename the argument to an expected parameter name or remove it");
    }

    if code == "TYPECK_E_ARGUMENT_ORDER" {
        give fluent_diagnostic_fields("cagri argumanlar are in the wrong order", "cagri argumanlar are in the wrong order.", "A positional or named argument appears after an argument form that makes the order invalid.", "A positional or named argument appears after an argument form that makes the order invalid.", "Compare the call argument order with the procedure parameter order shown in the diagnostic.", "reorder the arguments to match the parameter list, or use named arguments consistently", "draw(x: 1, y: 2, color: \"blue\")", "cagri argumanlar are in the wrong order.", "reorder the arguments to match the parameter list, or use named arguments consistently");
    }

    if code == "TYPECK_E_AMBIGUOUS_CALL" {
        give fluent_diagnostic_fields("procedure cagri is ambiguous", "procedure cagri is ambiguous.", "More than one visible procedure can accept the same call shape.", "More than one visible procedure can accept the same call shape.", "Show the candidate procedure signatures and identify the arguments that do not disambiguate them.", "add a type annotation to one argument or call a more specific procedure name", "let result: int = parse(\"42\")", "procedure cagri is ambiguous.", "add a type annotation to one argument or call a more specific procedure name");
    }

    if code == "TYPECK_W_IGNORED_RESULT" {
        give fluent_diagnostic_fields("procedure result is ignored", "procedure result is ignored.", "A procedure result is produced but the surrounding statement does not use it.", "A procedure result is produced but the surrounding statement does not use it.", "Check whether the result should be bound with `let`, passed onward, or intentionally discarded.", "bind the result with `let name: Type = call(...)` when the value is needed", "let total: int = add(1, 2)", "procedure result is ignored.", "bind the result with `let name: Type = call(...)` when the value is needed");
    }

    if code == "TYPECK_E_NOT_CALLABLE" {
        give fluent_diagnostic_fields("deger is not callable", "deger is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "deger is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_IMPL_MISSING_MEMBER" {
        give fluent_diagnostic_fields("implementation is eksik a required uye", "implementation is eksik a required uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation is eksik a required uye.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_SIGNATURE_MISMATCH" {
        give fluent_diagnostic_fields("implementation uye signature does not match", "implementation uye signature does not match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation uye signature does not match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_UNKNOWN_TRAIT" {
        give fluent_diagnostic_fields("trait does not exist", "trait does not exist.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait does not exist.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_GENERIC_INFERENCE" {
        give fluent_diagnostic_fields("jenerik tur could not be inferred", "jenerik tur could not be inferred.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik tur could not be inferred.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_TRAIT_BOUND" {
        give fluent_diagnostic_fields("trait constraint is not satisfied", "trait constraint is not satisfied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait constraint is not satisfied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CAUSE_CHAIN_MISSING" {
        give fluent_diagnostic_fields("tur diagnostic is eksik a cause chain", "tur diagnostic is eksik a cause chain.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tur diagnostic is eksik a cause chain.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONFLICTING_IMPL" {
        give fluent_diagnostic_fields("conflicting trait implementation", "conflicting trait implementation.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conflicting trait implementation.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONSTRAINT_CYCLE" {
        give fluent_diagnostic_fields("cyclic jenerik constraint", "cyclic jenerik constraint.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cyclic jenerik constraint.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_AMBIGUOUS_METHOD" {
        give fluent_diagnostic_fields("ambiguous method resolution", "ambiguous method resolution.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous method resolution.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_UNARY" {
        give fluent_diagnostic_fields("malformed unary", "malformed unary.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed unary.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNARY_MISMATCH" {
        give fluent_diagnostic_fields("unary uyusmazlik", "unary uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unary uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BORROW" {
        give fluent_diagnostic_fields("malformed odunc", "malformed odunc.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed odunc.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_MEMBER" {
        give fluent_diagnostic_fields("malformed uye", "malformed uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed uye.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_INDEX" {
        give fluent_diagnostic_fields("malformed indeks", "malformed indeks.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed indeks.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BINARY" {
        give fluent_diagnostic_fields("malformed ikili", "malformed ikili.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed ikili.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_CAST" {
        give fluent_diagnostic_fields("malformed donusum", "malformed donusum.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed donusum.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_IF" {
        give fluent_diagnostic_fields("malformed if", "malformed if.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed if.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_HIR" {
        give fluent_diagnostic_fields("gecersiz hir", "gecersiz hir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz hir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INTERNAL" {
        give fluent_diagnostic_fields("ic", "ic.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ic.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0001" {
        give fluent_diagnostic_fields("type0001", "type0001.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0001.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0002" {
        give fluent_diagnostic_fields("type0002", "type0002.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0002.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0003" {
        give fluent_diagnostic_fields("type0003", "type0003.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0003.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0004" {
        give fluent_diagnostic_fields("type0004", "type0004.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0004.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0005" {
        give fluent_diagnostic_fields("type0005", "type0005.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0005.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0006" {
        give fluent_diagnostic_fields("type0006", "type0006.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0006.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0007" {
        give fluent_diagnostic_fields("type0007", "type0007.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0007.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0008" {
        give fluent_diagnostic_fields("type0008", "type0008.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0008.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0009" {
        give fluent_diagnostic_fields("type0009", "type0009.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0009.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0010" {
        give fluent_diagnostic_fields("type0010", "type0010.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0010.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE9999" {
        give fluent_diagnostic_fields("type9999", "type9999.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type9999.", "make the expression type match the type contract named by the type checker");
    }

    if code == "BORROWCK_E_MOVE_AFTER_MOVE" {
        give fluent_diagnostic_fields("tasima after tasima", "tasima after tasima.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tasima after tasima.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("deger kullanildi after tasima", "deger kullanildi after tasima.", "A value is used after ownership has moved away from its binding.", "A value is used after ownership has moved away from its binding.", "Show where the value is created, where it is moved, and where it is reused after the move.", "use the value before the move, borrow it instead of moving it, or create a new value", "let value: string = \"vitte\"", "deger kullanildi after tasima.", "use the value before the move, borrow it instead of moving it, or create a new value");
    }

    if code == "BORROWCK_E_PARTIAL_MOVE" {
        give fluent_diagnostic_fields("deger partially tasinmis", "deger partially tasinmis.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Show the original construction, the field or variant payload that moved, and the later whole-value use.", "avoid using the whole value after moving one part, or rebuild the missing part before use", "form User { name: string, age: int }", "deger partially tasinmis.", "avoid using the whole value after moving one part, or rebuild the missing part before use");
    }

    if code == "BORROWCK_E_BORROW_OF_MOVED_VALUE" {
        give fluent_diagnostic_fields("odunc of tasinmis deger", "odunc of tasinmis deger.", "A borrow starts after ownership has already moved away from the original binding.", "A borrow starts after ownership has already moved away from the original binding.", "Show the value creation, the move, and the later borrow attempt in that order.", "create the borrow before the move, or move a different value", "let value: string = \"vitte\"", "odunc of tasinmis deger.", "create the borrow before the move, or move a different value");
    }

    if code == "BORROWCK_E_MUTABLE_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("degistirilebilir odunc cakismazlik", "degistirilebilir odunc cakismazlik.", "A value is already mutably borrowed when another borrow of the same value starts.", "A value is already mutably borrowed when another borrow of the same value starts.", "Report the value creation first, then the first mutable borrow, then the later conflicting borrow, then where the first borrow ends.", "end the first mutable borrow before starting the next borrow", "let value: int = 1", "degistirilebilir odunc cakismazlik.", "end the first mutable borrow before starting the next borrow");
    }

    if code == "BORROWCK_E_MUTABLE_SHARED_CONFLICT" {
        give fluent_diagnostic_fields("degistirilebilir and paylasilan borrows overlap", "degistirilebilir and paylasilan borrows overlap.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "degistirilebilir and paylasilan borrows overlap.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_SHARED_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("paylasilan odunc cakismazlik", "paylasilan odunc cakismazlik.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "paylasilan odunc cakismazlik.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_WRITE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("yazma sirasinda odunc alinmis", "yazma sirasinda odunc alinmis.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "yazma sirasinda odunc alinmis.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_MOVE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("tasima sirasinda odunc alinmis", "tasima sirasinda odunc alinmis.", "Ownership moves out of a value while an active borrow can still use it.", "Ownership moves out of a value while an active borrow can still use it.", "Show the value creation, the borrow start, the move, the later borrow use, and where the borrow ends.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership", "let value: string = \"vitte\"", "tasima sirasinda odunc alinmis.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership");
    }

    if code == "BORROWCK_E_DROP_WHILE_BORROWED" {
        give fluent_diagnostic_fields("birakma sirasinda odunc alinmis", "birakma sirasinda odunc alinmis.", "A value is destroyed while an active borrow still points to it.", "A value is destroyed while an active borrow still points to it.", "Show the value creation, the borrow start, the destruction point, and the last borrow use.", "destroy the value only after the borrow is no longer used", "let value: string = \"vitte\"", "birakma sirasinda odunc alinmis.", "destroy the value only after the borrow is no longer used");
    }

    if code == "BORROWCK_E_ASSIGN_WHILE_BORROWED" {
        give fluent_diagnostic_fields("assign sirasinda odunc alinmis", "assign sirasinda odunc alinmis.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "assign sirasinda odunc alinmis.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_RETURN_REF_TO_LOCAL" {
        give fluent_diagnostic_fields("donus ref to yerel", "donus ref to yerel.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "donus ref to yerel.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_RETURN_BORROW_OF_LOCAL" {
        give fluent_diagnostic_fields("donus odunc of yerel", "donus odunc of yerel.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "donus odunc of yerel.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "dangling basvuru.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_REFERENCE_OUTLIVES_VALUE" {
        give fluent_diagnostic_fields("basvuru outlives the deger it points to", "basvuru outlives the deger it points to.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "basvuru outlives the deger it points to.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "yasam suresi cok short.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("immutable assign", "immutable assign.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "immutable assign.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNINITIALIZED_USE" {
        give fluent_diagnostic_fields("baslatilmamis use", "baslatilmamis use.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "baslatilmamis use.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MOVE_AFTER_BORROW" {
        give fluent_diagnostic_fields("tasima after odunc", "tasima after odunc.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tasima after odunc.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MUTABLE_ALIAS" {
        give fluent_diagnostic_fields("degistirilebilir alias", "degistirilebilir alias.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "degistirilebilir alias.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_INTERNAL" {
        give fluent_diagnostic_fields("ic", "ic.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ic.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNKNOWN" {
        give fluent_diagnostic_fields("bilinmeyen", "bilinmeyen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_EVAL_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("bolme by sifir in sabit evaluation", "bolme by sifir in sabit evaluation.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "change the const divisor to a non-zero value or guard the expression before const evaluation", "const size: int = 4", "bolme by sifir in sabit evaluation.", "change the const divisor to a non-zero value or guard the expression before const evaluation");
    }

    if code == "CONST_EVAL_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen ad.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_NON_CONST_CALL" {
        give fluent_diagnostic_fields("non const cagri", "non const cagri.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non const cagri.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_MUTATION_IN_CONST" {
        give fluent_diagnostic_fields("mutation in const", "mutation in const.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutation in const.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNSUPPORTED_EXPR" {
        give fluent_diagnostic_fields("desteklenmeyen expr", "desteklenmeyen expr.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "replace the const expression with literals, supported arithmetic, or another compile-time value", "const size: int = 4", "desteklenmeyen expr.", "replace the const expression with literals, supported arithmetic, or another compile-time value");
    }

    if code == "CONST_EVAL_E_OVERFLOW" {
        give fluent_diagnostic_fields("tasma", "tasma.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "use a wider const type or reduce the arithmetic result before overflow", "const size: int = 4", "tasma.", "use a wider const type or reduce the arithmetic result before overflow");
    }

    if code == "CONST_EVAL_E_STATIC_ASSERT_FAILED" {
        give fluent_diagnostic_fields("static dogrulama basarisiz", "static dogrulama basarisiz.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "make the asserted const condition true or remove the invalid compile-time assumption", "const size: int = 4", "static dogrulama basarisiz.", "make the asserted const condition true or remove the invalid compile-time assumption");
    }

    if code == "CONST_EVAL_E_CYCLE" {
        give fluent_diagnostic_fields("dongu", "dongu.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "dongu.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_PARSE" {
        give fluent_diagnostic_fields("parse", "parse.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "parse.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNKNOWN" {
        give fluent_diagnostic_fields("bilinmeyen", "bilinmeyen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MOD_E_MODULE_NOT_FOUND" {
        give fluent_diagnostic_fields("modul eksik", "modul eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "modul eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu detected", "ice aktarma dongu detected.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ice aktarma dongu detected.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_SYMBOL_NOT_EXPORTED" {
        give fluent_diagnostic_fields("sembol is modul tarafindan disa aktarilmadi", "sembol is modul tarafindan disa aktarilmadi.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "sembol is modul tarafindan disa aktarilmadi.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ice aktarma eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_PACKAGE_MISSING" {
        give fluent_diagnostic_fields("package eksik", "package eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "package eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_STDLIB_MISSING" {
        give fluent_diagnostic_fields("stdlib eksik", "stdlib eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "stdlib eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_AMBIGUOUS_MODULE" {
        give fluent_diagnostic_fields("ambiguous modul yol", "ambiguous modul yol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ambiguous modul yol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "HIR_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("gecersiz expr", "gecersiz expr.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz expr.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STMT" {
        give fluent_diagnostic_fields("gecersiz stmt", "gecersiz stmt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz stmt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_TYPE" {
        give fluent_diagnostic_fields("gecersiz tur", "gecersiz tur.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz tur.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_SYMBOL" {
        give fluent_diagnostic_fields("eksik sembol", "eksik sembol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik sembol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("gecersiz control akis", "gecersiz control akis.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz control akis.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LOWERING_FAILED" {
        give fluent_diagnostic_fields("lowering basarisiz", "lowering basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lowering basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("gecersiz blok", "gecersiz blok.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz blok.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_TERMINATOR" {
        give fluent_diagnostic_fields("gecersiz sonlandirici", "gecersiz sonlandirici.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz sonlandirici.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_BLOCK" {
        give fluent_diagnostic_fields("ulasilamaz blok", "ulasilamaz blok.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ulasilamaz blok.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERAND" {
        give fluent_diagnostic_fields("gecersiz operand", "gecersiz operand.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz operand.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PLACE" {
        give fluent_diagnostic_fields("gecersiz place", "gecersiz place.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz place.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DATAFLOW_CONFLICT" {
        give fluent_diagnostic_fields("dataflow cakismazlik", "dataflow cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dataflow cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_VERIFICATION_FAILED" {
        give fluent_diagnostic_fields("verification basarisiz", "verification basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verification basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("gecersiz modul", "gecersiz modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_FUNCTION" {
        give fluent_diagnostic_fields("gecersiz procedure", "gecersiz procedure.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz procedure.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("gecersiz blok", "gecersiz blok.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz blok.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INSTRUCTION" {
        give fluent_diagnostic_fields("gecersiz komut", "gecersiz komut.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz komut.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("tur uyusmazlik", "tur uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tur uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_VERIFY_FAILED" {
        give fluent_diagnostic_fields("verify basarisiz", "verify basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verify basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desteklenmeyen hedef.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNSUPPORTED_FEATURE" {
        give fluent_diagnostic_fields("desteklenmeyen ozellik", "desteklenmeyen ozellik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desteklenmeyen ozellik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CODEGEN_FAILED" {
        give fluent_diagnostic_fields("kod uretimi basarisiz", "kod uretimi basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "kod uretimi basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_OBJECT_WRITE_FAILED" {
        give fluent_diagnostic_fields("nesne yazma basarisiz", "nesne yazma basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "nesne yazma basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSEMBLER_FAILED" {
        give fluent_diagnostic_fields("assembler basarisiz", "assembler basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "assembler basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NATIVE_TOOL_MISSING" {
        give fluent_diagnostic_fields("native arac eksik", "native arac eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "native arac eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNDEFINED_SYMBOL" {
        give fluent_diagnostic_fields("tanimlanmamis sembol", "tanimlanmamis sembol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "tanimlanmamis sembol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("yinelenen sembol", "yinelenen sembol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "yinelenen sembol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIBRARY_NOT_FOUND" {
        give fluent_diagnostic_fields("library eksik", "library eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "library eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OBJECT_NOT_FOUND" {
        give fluent_diagnostic_fields("nesne eksik", "nesne eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "nesne eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_FORMAT" {
        give fluent_diagnostic_fields("desteklenmeyen bicim", "desteklenmeyen bicim.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desteklenmeyen bicim.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_SYSTEM_LINKER_FAILED" {
        give fluent_diagnostic_fields("system linker basarisiz", "system linker basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "system linker basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_PANIC" {
        give fluent_diagnostic_fields("panik", "panik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "panik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSERT_FAILED" {
        give fluent_diagnostic_fields("dogrulama basarisiz", "dogrulama basarisiz.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "dogrulama basarisiz.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_STACK_OVERFLOW" {
        give fluent_diagnostic_fields("yigin tasma", "yigin tasma.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "yigin tasma.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_OUT_OF_MEMORY" {
        give fluent_diagnostic_fields("out of memory", "out of memory.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "out of memory.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("bolme by sifir", "bolme by sifir.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bolme by sifir.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BOUNDS_CHECK" {
        give fluent_diagnostic_fields("bounds check", "bounds check.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bounds check.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NULL_DEREF" {
        give fluent_diagnostic_fields("null deref", "null deref.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "null deref.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("gecersiz arguman", "gecersiz arguman.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz arguman.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_INPUT" {
        give fluent_diagnostic_fields("eksik girdi", "eksik girdi.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik girdi.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_COMMAND" {
        give fluent_diagnostic_fields("eksik compiler command", "eksik compiler command.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik compiler command.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OPTION_VALUE" {
        give fluent_diagnostic_fields("eksik compiler option deger", "eksik compiler option deger.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik compiler option deger.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SOURCE_UNREADABLE" {
        give fluent_diagnostic_fields("eksik or unreadable girdi kaynak", "eksik or unreadable girdi kaynak.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik or unreadable girdi kaynak.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CONFIGURATION" {
        give fluent_diagnostic_fields("gecersiz compiler configuration", "gecersiz compiler configuration.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz compiler configuration.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_TARGET" {
        give fluent_diagnostic_fields("eksik compiler hedef", "eksik compiler hedef.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik compiler hedef.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_DIR" {
        give fluent_diagnostic_fields("eksik compiler cikti directory", "eksik compiler cikti directory.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik compiler cikti directory.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_PATH" {
        give fluent_diagnostic_fields("eksik compiler cikti yol", "eksik compiler cikti yol.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik compiler cikti yol.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_TARGET" {
        give fluent_diagnostic_fields("gecersiz compiler link hedef", "gecersiz compiler link hedef.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz compiler link hedef.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INPUT_NOT_FOUND" {
        give fluent_diagnostic_fields("girdi eksik", "girdi eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "girdi eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_OUTPUT_WRITE_FAILED" {
        give fluent_diagnostic_fields("cikti yazma basarisiz", "cikti yazma basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cikti yazma basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_READ_FAILED" {
        give fluent_diagnostic_fields("cache read basarisiz", "cache read basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache read basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_WRITE_FAILED" {
        give fluent_diagnostic_fields("cache yazma basarisiz", "cache yazma basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache yazma basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PROFILE_NOT_FOUND" {
        give fluent_diagnostic_fields("profil eksik", "profil eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "profil eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TARGET_NOT_FOUND" {
        give fluent_diagnostic_fields("hedef eksik", "hedef eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "hedef eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "BOOTSTRAP_E_STAGE_FAILURE" {
        give fluent_diagnostic_fields("stage failure", "stage failure.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "stage failure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SEED_MISSING" {
        give fluent_diagnostic_fields("seed eksik", "seed eksik.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "seed eksik.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_COMPILER_MISSING" {
        give fluent_diagnostic_fields("compiler eksik", "compiler eksik.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "compiler eksik.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SELF_CHECK_FAILED" {
        give fluent_diagnostic_fields("self check basarisiz", "self check basarisiz.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "self check basarisiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_ARTIFACT_INVALID" {
        give fluent_diagnostic_fields("artifact gecersiz", "artifact gecersiz.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "artifact gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_BANNER" {
        give fluent_diagnostic_fields("bootstrap banner is gecersiz", "bootstrap banner is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap banner is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap sabit signature is gecersiz", "bootstrap sabit signature is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap sabit signature is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_TYPE" {
        give fluent_diagnostic_fields("bootstrap sabit has wrong tur", "bootstrap sabit has wrong tur.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap sabit has wrong tur.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_DUP_PROC" {
        give fluent_diagnostic_fields("yinelenen bootstrap procedure", "yinelenen bootstrap procedure.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "yinelenen bootstrap procedure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_EXPORT" {
        give fluent_diagnostic_fields("bootstrap export is gecersiz", "bootstrap export is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap export is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_BODY" {
        give fluent_diagnostic_fields("bootstrap main body is gecersiz", "bootstrap main body is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main body is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap main signature is gecersiz", "bootstrap main signature is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main signature is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_BODY" {
        give fluent_diagnostic_fields("bootstrap procedure body is gecersiz", "bootstrap procedure body is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap procedure signature is gecersiz", "bootstrap procedure signature is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure signature is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_SPACE" {
        give fluent_diagnostic_fields("bootstrap space bildirim is gecersiz", "bootstrap space bildirim is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap space bildirim is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_TOP_LEVEL" {
        give fluent_diagnostic_fields("bootstrap top-level bildirim is gecersiz", "bootstrap top-level bildirim is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap top-level bildirim is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNCLOSED_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure body is not closed", "bootstrap procedure body is not closed.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is not closed.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_CONST" {
        give fluent_diagnostic_fields("bilinmeyen bootstrap sabit", "bilinmeyen bootstrap sabit.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bilinmeyen bootstrap sabit.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_PROC" {
        give fluent_diagnostic_fields("desteklenmeyen bootstrap procedure", "desteklenmeyen bootstrap procedure.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "desteklenmeyen bootstrap procedure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_VERSION" {
        give fluent_diagnostic_fields("bootstrap version is gecersiz", "bootstrap version is gecersiz.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap version is gecersiz.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "INSTALL_E_VITTE_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vitte help command basarisiz", "installed vitte help command basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vitte help command basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_VITTEC_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vittec help command basarisiz", "installed vittec help command basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vittec help command basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("installed package ice aktarma test basarisiz", "installed package ice aktarma test basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed package ice aktarma test basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("installed shell completion is eksik", "installed shell completion is eksik.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed shell completion is eksik.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_CC_MISSING" {
        give fluent_diagnostic_fields("macOS package build requires cc", "macOS package build requires cc.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires cc.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_LIPO_MISSING" {
        give fluent_diagnostic_fields("universal macOS package build requires lipo", "universal macOS package build requires lipo.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "universal macOS package build requires lipo.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_MACOS_REQUIRED" {
        give fluent_diagnostic_fields("macOS package build requires macOS", "macOS package build requires macOS.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires macOS.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_SOURCE_MISSING" {
        give fluent_diagnostic_fields("package kaynak dosya is eksik", "package kaynak dosya is eksik.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "package kaynak dosya is eksik.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_HELP_FAILED" {
        give fluent_diagnostic_fields("postinstall vitte help check basarisiz", "postinstall vitte help check basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall vitte help check basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("postinstall package ice aktarma check basarisiz", "postinstall package ice aktarma check basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall package ice aktarma check basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("postinstall shell completion check basarisiz", "postinstall shell completion check basarisiz.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall shell completion check basarisiz.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "LIMIT_FILE_SIZE_MAX" {
        give fluent_diagnostic_fields("dosya size max", "dosya size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "dosya size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_TOKEN_SIZE_MAX" {
        give fluent_diagnostic_fields("token size max", "token size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "token size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_AST_DEPTH_MAX" {
        give fluent_diagnostic_fields("ast depth max", "ast depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ast depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_EXPR_DEPTH_MAX" {
        give fluent_diagnostic_fields("expr depth max", "expr depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expr depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_IMPORT_DEPTH_MAX" {
        give fluent_diagnostic_fields("ice aktarma depth max", "ice aktarma depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ice aktarma depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MODULE_COUNT_MAX" {
        give fluent_diagnostic_fields("modul count max", "modul count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "modul count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_DIAGNOSTICS_MAX" {
        give fluent_diagnostic_fields("cok many tanilar emitted", "cok many tanilar emitted.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "cok many tanilar emitted.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_SYMBOL_COUNT_MAX" {
        give fluent_diagnostic_fields("sembol count max", "sembol count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "sembol count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_PARSER_RECURSION_MAX" {
        give fluent_diagnostic_fields("ayristirici recursion max", "ayristirici recursion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ayristirici recursion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MACRO_EXPANSION_MAX" {
        give fluent_diagnostic_fields("macro expansion max", "macro expansion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "MACRO_E_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("expansion basarisiz", "expansion basarisiz.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expansion basarisiz.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RECURSION_LIMIT" {
        give fluent_diagnostic_fields("recursion limit", "recursion limit.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "recursion limit.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("gecersiz arguman", "gecersiz arguman.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz arguman.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MACRO" {
        give fluent_diagnostic_fields("bilinmeyen macro", "bilinmeyen macro.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "bilinmeyen macro.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSTABLE_FEATURE" {
        give fluent_diagnostic_fields("unstable ozellik", "unstable ozellik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unstable ozellik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "SYNTAX_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "tanimlayici bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ifade bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "tur bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "desen bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ayirici bekleniyor.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "beklenmeyen token.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unbalanced ayirici.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz attribute.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz bildirim.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz deyim.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz ifade.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz desen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz literal.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz operator.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz modifier.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "eksik body.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "eksik donus.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "yinelenen ad.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "bilinmeyen ad.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "bilinmeyen tur.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "bilinmeyen modul.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "bilinmeyen uye.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ambiguous ad.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "private sembol.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ice aktarma eksik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ice aktarma dongu.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "export cakismazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "arity uyusmazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "arguman uyusmazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "atama uyusmazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "branch uyusmazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "cagri hedef is not callable.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz donusum.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz indeks.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz deref.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz odunc.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "gecersiz tasima.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after tasima.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after birakma.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use before init.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "double birakma.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "odunc cakismazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "mutability cakismazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "yasam suresi cok short.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "dangling basvuru.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "non exhaustive match.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ulasilamaz desen.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const required.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const tasma.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const bolme by sifir.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const dongu.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro eksik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro recursion.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro expansion basarisiz.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait not implemented.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait ambiguous.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "jenerik arguman eksik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "jenerik bound basarisiz.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "desteklenmeyen hedef.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "abi uyusmazlik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "link basarisiz.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "runtime panik.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "NAME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "tanimlayici bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ifade bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "tur bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "desen bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "blok bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ayirici bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "beklenmeyen token.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced ayirici.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz attribute.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz bildirim.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz deyim.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz ifade.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz desen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz literal.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz operator.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz modifier.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "eksik body.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "eksik donus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "yinelenen ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen tur.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen modul.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen uye.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private sembol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ice aktarma eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ice aktarma dongu.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arguman uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "atama uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "cagri hedef is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz donusum.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz indeks.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz deref.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz odunc.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz tasima.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after tasima.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after birakma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double birakma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "odunc cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "yasam suresi cok short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling basvuru.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ulasilamaz desen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const tasma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const bolme by sifir.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const dongu.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "jenerik arguman eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "jenerik bound basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "desteklenmeyen hedef.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime panik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "tanimlayici bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ifade bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "tur bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "desen bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "blok bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ayirici bekleniyor.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "beklenmeyen token.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced ayirici.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz attribute.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz bildirim.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz deyim.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz ifade.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz desen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz literal.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz operator.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz modifier.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "eksik body.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "eksik donus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "yinelenen ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen tur.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen modul.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "bilinmeyen uye.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous ad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private sembol.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ice aktarma eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ice aktarma dongu.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arguman uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "atama uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "cagri hedef is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz donusum.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz indeks.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz deref.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz odunc.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "gecersiz tasima.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after tasima.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after birakma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double birakma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "odunc cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability cakismazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "yasam suresi cok short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling basvuru.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ulasilamaz desen.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const tasma.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const bolme by sifir.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const dongu.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "jenerik arguman eksik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "jenerik bound basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "desteklenmeyen hedef.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi uyusmazlik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link basarisiz.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime panik.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tanimlayici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ifade bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tur bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desen bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ayirici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "beklenmeyen token.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced ayirici.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz attribute.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz bildirim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deyim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz ifade.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz literal.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz operator.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz modifier.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik body.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik donus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yinelenen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen modul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen uye.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private sembol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arguman uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "atama uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cagri hedef is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz donusum.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz indeks.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deref.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz odunc.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "odunc cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yasam suresi cok short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling basvuru.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ulasilamaz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const tasma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const bolme by sifir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik arguman eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik bound basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desteklenmeyen hedef.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tanimlayici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ifade bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tur bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desen bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ayirici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "beklenmeyen token.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced ayirici.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz attribute.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz bildirim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deyim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz ifade.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz literal.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz operator.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz modifier.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik body.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik donus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yinelenen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen modul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen uye.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private sembol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arguman uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "atama uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cagri hedef is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz donusum.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz indeks.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deref.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz odunc.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "odunc cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yasam suresi cok short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling basvuru.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ulasilamaz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const tasma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const bolme by sifir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik arguman eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik bound basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desteklenmeyen hedef.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tanimlayici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ifade bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "tur bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desen bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ayirici bekleniyor.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "beklenmeyen token.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced ayirici.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz attribute.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz bildirim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deyim.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz ifade.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz literal.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz operator.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz modifier.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik body.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "eksik donus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yinelenen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen tur.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen modul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "bilinmeyen uye.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous ad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private sembol.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ice aktarma dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arguman uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "atama uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cagri hedef is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz donusum.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz indeks.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz deref.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz odunc.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "gecersiz tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after tasima.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double birakma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "odunc cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability cakismazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "yasam suresi cok short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling basvuru.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ulasilamaz desen.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const tasma.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const bolme by sifir.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const dongu.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik arguman eksik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "jenerik bound basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "desteklenmeyen hedef.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi uyusmazlik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link basarisiz.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panik.", "make the expression type match the type contract named by the type checker");
    }

    if code == "OWNERSHIP_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tanimlayici bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ifade bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tur bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "desen bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "blok bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ayirici bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "beklenmeyen token.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced ayirici.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz attribute.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz bildirim.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz deyim.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz ifade.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz desen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz literal.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz operator.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz modifier.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "eksik body.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "eksik donus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "yinelenen ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen tur.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen modul.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen uye.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private sembol.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ice aktarma eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ice aktarma dongu.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arguman uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "atama uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "cagri hedef is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz donusum.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz indeks.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz deref.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz odunc.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz tasima.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after tasima.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "odunc cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "yasam suresi cok short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling basvuru.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ulasilamaz desen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const tasma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const bolme by sifir.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const dongu.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "jenerik arguman eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "jenerik bound basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "desteklenmeyen hedef.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime panik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tanimlayici bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ifade bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "tur bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "desen bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "blok bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ayirici bekleniyor.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "beklenmeyen token.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced ayirici.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz attribute.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz bildirim.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz deyim.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz ifade.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz desen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz literal.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz operator.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz modifier.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "eksik body.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "eksik donus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "yinelenen ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen tur.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen modul.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "bilinmeyen uye.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous ad.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private sembol.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ice aktarma eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ice aktarma dongu.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arguman uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "atama uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "cagri hedef is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz donusum.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz indeks.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz deref.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz odunc.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "gecersiz tasima.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after tasima.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double birakma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "odunc cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability cakismazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "yasam suresi cok short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling basvuru.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ulasilamaz desen.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const tasma.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const bolme by sifir.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const dongu.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "jenerik arguman eksik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "jenerik bound basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "desteklenmeyen hedef.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi uyusmazlik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link basarisiz.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime panik.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "tanimlayici bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ifade bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "tur bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "desen bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "blok bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ayirici bekleniyor.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "beklenmeyen token.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unbalanced ayirici.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz attribute.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz bildirim.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz deyim.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz ifade.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz desen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz literal.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz operator.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz modifier.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "eksik body.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "eksik donus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "yinelenen ad.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen ad.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen tur.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen modul.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "bilinmeyen uye.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ambiguous ad.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "private sembol.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ice aktarma eksik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ice aktarma dongu.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "export cakismazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "arity uyusmazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "arguman uyusmazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "atama uyusmazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "branch uyusmazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "cagri hedef is not callable.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz donusum.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz indeks.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz deref.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz odunc.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "gecersiz tasima.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after tasima.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after birakma.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use before init.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "double birakma.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "odunc cakismazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutability cakismazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "yasam suresi cok short.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "dangling basvuru.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non exhaustive match.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ulasilamaz desen.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const required.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const tasma.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const bolme by sifir.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const dongu.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro eksik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro recursion.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro expansion basarisiz.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait not implemented.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait ambiguous.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "jenerik arguman eksik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "jenerik bound basarisiz.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "desteklenmeyen hedef.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "abi uyusmazlik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "link basarisiz.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "runtime panik.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MACRO_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "tanimlayici bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ifade bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "tur bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "desen bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "blok bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ayirici bekleniyor.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "beklenmeyen token.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unbalanced ayirici.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz attribute.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz bildirim.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz deyim.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz ifade.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz desen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz literal.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz operator.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz modifier.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "eksik body.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "eksik donus.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "yinelenen ad.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "bilinmeyen ad.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "bilinmeyen tur.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "bilinmeyen modul.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "bilinmeyen uye.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ambiguous ad.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "private sembol.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ice aktarma eksik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ice aktarma dongu.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "export cakismazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "arity uyusmazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "arguman uyusmazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "atama uyusmazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "branch uyusmazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "my_macro(value)", "cagri hedef is not callable.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz donusum.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz indeks.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz deref.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz odunc.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "gecersiz tasima.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after tasima.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after birakma.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use before init.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "double birakma.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "odunc cakismazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "mutability cakismazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "yasam suresi cok short.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "dangling basvuru.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "non exhaustive match.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ulasilamaz desen.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const required.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const tasma.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const bolme by sifir.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const dongu.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro eksik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro recursion.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro expansion basarisiz.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait not implemented.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait ambiguous.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "jenerik arguman eksik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "jenerik bound basarisiz.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "desteklenmeyen hedef.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "abi uyusmazlik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "link basarisiz.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "runtime panik.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "HIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tanimlayici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ifade bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tur bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desen bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ayirici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen token.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced ayirici.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz attribute.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz bildirim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deyim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz ifade.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz literal.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz operator.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz modifier.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik body.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik donus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yinelenen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen tur.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen uye.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private sembol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arguman uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "atama uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "cagri hedef is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz donusum.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz indeks.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deref.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz odunc.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "odunc cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yasam suresi cok short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling basvuru.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ulasilamaz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const tasma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const bolme by sifir.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik arguman eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik bound basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen hedef.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tanimlayici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ifade bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tur bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desen bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ayirici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen token.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced ayirici.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz attribute.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz bildirim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deyim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz ifade.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz literal.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz operator.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz modifier.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik body.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik donus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yinelenen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen tur.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen uye.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private sembol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arguman uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "atama uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "cagri hedef is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz donusum.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz indeks.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deref.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz odunc.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "odunc cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yasam suresi cok short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling basvuru.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ulasilamaz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const tasma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const bolme by sifir.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik arguman eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik bound basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen hedef.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tanimlayici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ifade bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "tur bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desen bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ayirici bekleniyor.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "beklenmeyen token.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced ayirici.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz attribute.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz bildirim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deyim.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz ifade.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz literal.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz operator.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz modifier.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik body.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "eksik donus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yinelenen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen tur.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen modul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "bilinmeyen uye.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous ad.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private sembol.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ice aktarma dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arguman uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "atama uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "cagri hedef is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz donusum.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz indeks.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz deref.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz odunc.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "gecersiz tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after tasima.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double birakma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "odunc cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability cakismazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "yasam suresi cok short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling basvuru.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ulasilamaz desen.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const tasma.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const bolme by sifir.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const dongu.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik arguman eksik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "jenerik bound basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "desteklenmeyen hedef.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi uyusmazlik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link basarisiz.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panik.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "tanimlayici bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ifade bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "tur bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desen bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "blok bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ayirici bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "beklenmeyen token.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced ayirici.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz attribute.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz bildirim.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz deyim.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz ifade.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz desen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz literal.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz operator.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz modifier.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "eksik body.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "eksik donus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "yinelenen ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen tur.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen modul.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen uye.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private sembol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ice aktarma eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ice aktarma dongu.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arguman uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "atama uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "cagri hedef is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz donusum.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz indeks.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz deref.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz odunc.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz tasima.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after tasima.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after birakma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double birakma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "odunc cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "yasam suresi cok short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling basvuru.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ulasilamaz desen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const tasma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const bolme by sifir.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const dongu.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "jenerik arguman eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "jenerik bound basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime panik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "tanimlayici bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ifade bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "tur bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desen bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "blok bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ayirici bekleniyor.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "beklenmeyen token.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced ayirici.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz attribute.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz bildirim.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz deyim.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz ifade.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz desen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz literal.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz operator.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz modifier.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "eksik body.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "eksik donus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "yinelenen ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen tur.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen modul.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "bilinmeyen uye.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous ad.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private sembol.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ice aktarma eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ice aktarma dongu.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arguman uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "atama uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "cagri hedef is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz donusum.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz indeks.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz deref.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz odunc.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "gecersiz tasima.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after tasima.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after birakma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double birakma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "odunc cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability cakismazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "yasam suresi cok short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling basvuru.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ulasilamaz desen.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const tasma.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const bolme by sifir.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const dongu.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "jenerik arguman eksik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "jenerik bound basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "desteklenmeyen hedef.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi uyusmazlik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link basarisiz.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime panik.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "tanimlayici bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ifade bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "tur bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "desen bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "blok bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ayirici bekleniyor.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "beklenmeyen token.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "unbalanced ayirici.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz attribute.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz bildirim.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz deyim.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz ifade.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz desen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz literal.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz operator.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz modifier.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "eksik body.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "eksik donus.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "yinelenen ad.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bilinmeyen ad.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bilinmeyen tur.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bilinmeyen modul.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bilinmeyen uye.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ambiguous ad.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "private sembol.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ice aktarma eksik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ice aktarma dongu.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "export cakismazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "arity uyusmazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "arguman uyusmazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "atama uyusmazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "branch uyusmazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "if index < len(items) { give items[index] }", "cagri hedef is not callable.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz donusum.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz indeks.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz deref.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz odunc.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "gecersiz tasima.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after tasima.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after birakma.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use before init.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "double birakma.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "odunc cakismazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "mutability cakismazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "yasam suresi cok short.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "dangling basvuru.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "non exhaustive match.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ulasilamaz desen.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const required.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const tasma.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const bolme by sifir.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const dongu.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro eksik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro recursion.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro expansion basarisiz.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait not implemented.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait ambiguous.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "jenerik arguman eksik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "jenerik bound basarisiz.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "desteklenmeyen hedef.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "abi uyusmazlik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "link basarisiz.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "runtime panik.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "tanimlayici bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ifade bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "tur bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "desen bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "blok bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ayirici bekleniyor.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "beklenmeyen token.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "unbalanced ayirici.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz attribute.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz bildirim.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz deyim.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz ifade.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz desen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz literal.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz operator.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz modifier.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik body.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "eksik donus.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "yinelenen ad.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen ad.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen tur.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen modul.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "bilinmeyen uye.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ambiguous ad.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "private sembol.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ice aktarma eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ice aktarma dongu.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "export cakismazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "arity uyusmazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "arguman uyusmazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "atama uyusmazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "branch uyusmazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cagri hedef is not callable.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz donusum.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz indeks.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz deref.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz odunc.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "gecersiz tasima.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after tasima.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after birakma.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use before init.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "double birakma.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "odunc cakismazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mutability cakismazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "yasam suresi cok short.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "dangling basvuru.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "non exhaustive match.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ulasilamaz desen.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const required.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const tasma.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const bolme by sifir.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const dongu.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro recursion.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro expansion basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait not implemented.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait ambiguous.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "jenerik arguman eksik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "jenerik bound basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "desteklenmeyen hedef.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "abi uyusmazlik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link basarisiz.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "runtime panik.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LIMIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("tanimlayici bekleniyor", "tanimlayici bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "tanimlayici bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("ifade bekleniyor", "ifade bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ifade bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("tur bekleniyor", "tur bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "tur bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("desen bekleniyor", "desen bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "desen bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok bekleniyor", "blok bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "blok bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("ayirici bekleniyor", "ayirici bekleniyor.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ayirici bekleniyor.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("beklenmeyen token", "beklenmeyen token.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "beklenmeyen token.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced ayirici", "unbalanced ayirici.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "unbalanced ayirici.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("gecersiz attribute", "gecersiz attribute.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz attribute.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("gecersiz bildirim", "gecersiz bildirim.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz bildirim.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("gecersiz deyim", "gecersiz deyim.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz deyim.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("gecersiz ifade", "gecersiz ifade.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz ifade.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("gecersiz desen", "gecersiz desen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz desen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("gecersiz literal", "gecersiz literal.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz literal.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("gecersiz operator", "gecersiz operator.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz operator.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("gecersiz modifier", "gecersiz modifier.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz modifier.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("eksik body", "eksik body.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "eksik body.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("eksik give", "eksik donus.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "eksik donus.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("yinelenen ad", "yinelenen ad.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "yinelenen ad.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("bilinmeyen ad", "bilinmeyen ad.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "bilinmeyen ad.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("bilinmeyen tur", "bilinmeyen tur.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "bilinmeyen tur.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("bilinmeyen modul", "bilinmeyen modul.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "bilinmeyen modul.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("bilinmeyen uye", "bilinmeyen uye.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "bilinmeyen uye.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous ad", "ambiguous ad.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ambiguous ad.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private sembol", "private sembol.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "private sembol.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("ice aktarma eksik", "ice aktarma eksik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ice aktarma eksik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("ice aktarma dongu", "ice aktarma dongu.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ice aktarma dongu.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export cakismazlik", "export cakismazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "export cakismazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity uyusmazlik", "arity uyusmazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "arity uyusmazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("arguman uyusmazlik", "arguman uyusmazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "arguman uyusmazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("atama uyusmazlik", "atama uyusmazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "atama uyusmazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch uyusmazlik", "branch uyusmazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "branch uyusmazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("cagri hedef is not callable", "cagri hedef is not callable.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "cagri hedef is not callable.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("gecersiz donusum", "gecersiz donusum.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz donusum.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("gecersiz indeks", "gecersiz indeks.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz indeks.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("gecersiz deref", "gecersiz deref.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz deref.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("gecersiz odunc", "gecersiz odunc.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz odunc.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("gecersiz tasima", "gecersiz tasima.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "gecersiz tasima.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after tasima", "use after tasima.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after tasima.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after birakma", "use after birakma.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after birakma.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use before init.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double birakma", "double birakma.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "double birakma.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("odunc cakismazlik", "odunc cakismazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "odunc cakismazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability cakismazlik", "mutability cakismazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "mutability cakismazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("yasam suresi cok short", "yasam suresi cok short.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "yasam suresi cok short.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling basvuru", "dangling basvuru.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "dangling basvuru.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "non exhaustive match.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("ulasilamaz desen", "ulasilamaz desen.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ulasilamaz desen.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const required.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const tasma", "const tasma.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const tasma.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const bolme by sifir", "const bolme by sifir.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const bolme by sifir.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const dongu", "const dongu.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const dongu.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro eksik", "macro eksik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro eksik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro recursion.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion basarisiz", "macro expansion basarisiz.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion basarisiz.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait not implemented.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait ambiguous.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("jenerik arguman eksik", "jenerik arguman eksik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "jenerik arguman eksik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("jenerik bound basarisiz", "jenerik bound basarisiz.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "jenerik bound basarisiz.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("desteklenmeyen hedef", "desteklenmeyen hedef.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "desteklenmeyen hedef.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi uyusmazlik", "abi uyusmazlik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "abi uyusmazlik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link basarisiz", "link basarisiz.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "link basarisiz.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panik", "runtime panik.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "runtime panik.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    give fluent_diagnostic_fields_empty();
}
