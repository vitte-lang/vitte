space vitte/compiler/infrastructure/diagnostics/fluent_catalog_nl

export *

form FluentDiagnosticFields {
    message: string
    primary_label: string
    secondary_label: string
    cause: string
    help: string
    fix_it: string
    corrected_example: string
    note: string
    lsp_code_action_title: string
    valid: bool
}

proc fluent_diagnostic_fields(message: string, primary_label: string, secondary_label: string, cause: string, help: string, fix_it: string, corrected_example: string, note: string, lsp_code_action_title: string) -> FluentDiagnosticFields {
    give FluentDiagnosticFields {
        message: message,
        primary_label: primary_label,
        secondary_label: secondary_label,
        cause: cause,
        help: help,
        fix_it: fix_it,
        corrected_example: corrected_example,
        note: note,
        lsp_code_action_title: lsp_code_action_title,
        valid: message != "" and primary_label != "" and cause != "" and help != ""
    };
}

proc fluent_diagnostic_fields_empty() -> FluentDiagnosticFields {
    give fluent_diagnostic_fields("", "", "", "", "", "", "", "", "");
}

proc fluent_catalog_fields_nl(code: string) -> FluentDiagnosticFields {
    if code == "E0001" {
        give fluent_diagnostic_fields("identifier verwacht", "The parser expected a naam for something (variable, type, module, etc.).", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The parser expected a naam for something (variable, type, module, etc.).", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0002" {
        give fluent_diagnostic_fields("expressie verwacht", "The parser expected an expressie at this location.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The parser expected an expressie at this location.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0003" {
        give fluent_diagnostic_fields("patroon verwacht", "The parser expected a patroon.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The parser expected a patroon.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0004" {
        give fluent_diagnostic_fields("type verwacht", "The parser expected a type naam.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The parser expected a type naam.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0005" {
        give fluent_diagnostic_fields("'end' verwacht", "A blok was opened but not closed with 'end' or '.end'.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A blok was opened but not closed with 'end' or '.end'.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0006" {
        give fluent_diagnostic_fields("proc after attribute verwacht", "An attribute moet followed by a proc declaratie zijn.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "An attribute moet followed by a proc declaratie zijn.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0007" {
        give fluent_diagnostic_fields("top-level declaratie verwacht", "The parser expected a top-level declaratie.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The parser expected a top-level declaratie.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0008" {
        give fluent_diagnostic_fields("extern proc mag geen body hebben", "A patroon bound the same naam more than once.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A patroon bound the same naam more than once.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0009" {
        give fluent_diagnostic_fields("proc requires a body unless marked #[extern]", "A referenced type naam was ontbrekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A referenced type naam was ontbrekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0010" {
        give fluent_diagnostic_fields("type alias requires a doel type", "The base type of a generiek was ontbrekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The base type of a generiek was ontbrekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0011" {
        give fluent_diagnostic_fields("select vereist ten minste een when branch", "A generiek type must include at least one argument.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A generiek type must include at least one argument.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0012" {
        give fluent_diagnostic_fields("select branch moet a when statement zijn", "This type form is not supported yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let p: *int = &value", "This type form is not supported yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0013" {
        give fluent_diagnostic_fields("procedure may exit without give", "procedure may exit without give.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc to_code(ok: bool) -> int {\\n  if ok { give 0 }\\n  give 1\\n}", "procedure may exit without give.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0014" {
        give fluent_diagnostic_fields("forbidden top-level syntaxis in strict-core mode", "An invocation is ontbrekend its aanroepdoel.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "print(\\\"hi\\\")", "An invocation is ontbrekend its aanroepdoel.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0015" {
        give fluent_diagnostic_fields("forbidden statement syntaxis in strict-core mode", "This expressie is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let x = value", "This expressie is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0016" {
        give fluent_diagnostic_fields("forbidden expressie syntaxis in strict-core mode", "This patroon is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This patroon is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0017" {
        give fluent_diagnostic_fields("'}' verwacht", "This statement is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This statement is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0018" {
        give fluent_diagnostic_fields("'case' or 'otherwise' in match verwacht", "An extern procedure cannot define a body.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "#[extern]\\nproc puts(s: string) -> int", "An extern procedure cannot define a body.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E1001" {
        give fluent_diagnostic_fields("patroon binding dubbel", "patroon binding dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "patroon binding dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1002" {
        give fluent_diagnostic_fields("type (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) onbekend", "type (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "type (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1003" {
        give fluent_diagnostic_fields("generiek base type onbekend", "generiek base type onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generiek base type onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1004" {
        give fluent_diagnostic_fields("generiek type vereist ten minste een argument", "generiek type vereist ten minste een argument.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generiek type vereist ten minste een argument.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1005" {
        give fluent_diagnostic_fields("identifier onbekend", "identifier onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "identifier onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1006" {
        give fluent_diagnostic_fields("generiek type vereist ten minste een type argument", "generiek type vereist ten minste een type argument.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "generiek type vereist ten minste een type argument.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1007" {
        give fluent_diagnostic_fields("conversie between signed and unsigned values ongeldig", "conversie between signed and unsigned values ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "conversie between signed and unsigned values ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1010" {
        give fluent_diagnostic_fields("stdlib module geweigerd door active stdlib profiel", "stdlib module geweigerd door active stdlib profiel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib module geweigerd door active stdlib profiel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1011" {
        give fluent_diagnostic_fields("strict-imports requires explicit alias", "strict-imports requires explicit alias.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports requires explicit alias.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1012" {
        give fluent_diagnostic_fields("strict-imports verbiedt unused import aliases", "strict-imports verbiedt unused import aliases.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports verbiedt unused import aliases.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1013" {
        give fluent_diagnostic_fields("strict-imports verbiedt non-canonical import paths", "strict-imports verbiedt non-canonical import paths.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports verbiedt non-canonical import paths.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1014" {
        give fluent_diagnostic_fields("stdlib module ontbrekend", "stdlib module ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib module ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1015" {
        give fluent_diagnostic_fields("experimental module import denied", "experimental module import denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "experimental module import denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1016" {
        give fluent_diagnostic_fields("intern module import denied", "intern module import denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "intern module import denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1017" {
        give fluent_diagnostic_fields("re-export symbool conflict", "re-export symbool conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "re-export symbool conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1018" {
        give fluent_diagnostic_fields("ambiguous import pad", "ambiguous import pad.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ambiguous import pad.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1019" {
        give fluent_diagnostic_fields("strict-modules verbiedt glob imports", "strict-modules verbiedt glob imports.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-modules verbiedt glob imports.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1020" {
        give fluent_diagnostic_fields("legacy import pad is verouderd", "legacy import pad is verouderd.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "legacy import pad is verouderd.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1021" {
        give fluent_diagnostic_fields("entry module pad moet canonical zijn", "entry module pad moet canonical zijn.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry module pad moet canonical zijn.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1022" {
        give fluent_diagnostic_fields("entry naam dubbel", "entry naam dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry naam dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1023" {
        give fluent_diagnostic_fields("share verwijst naar onbekend symbool", "share verwijst naar onbekend symbool.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share verwijst naar onbekend symbool.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1024" {
        give fluent_diagnostic_fields("symbool in share list dubbel", "symbool in share list dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "symbool in share list dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1025" {
        give fluent_diagnostic_fields("symbool niet geexporteerd door module", "symbool niet geexporteerd door module.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "symbool niet geexporteerd door module.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1026" {
        give fluent_diagnostic_fields("share declaratie dubbel", "share declaratie dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share declaratie dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1027" {
        give fluent_diagnostic_fields("import binding dubbel", "import binding dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "import binding dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1028" {
        give fluent_diagnostic_fields("import binding conflicteert met lokaal declaratie", "import binding conflicteert met lokaal declaratie.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "import binding conflicteert met lokaal declaratie.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1029" {
        give fluent_diagnostic_fields("lokaal declaratie naam dubbel", "lokaal declaratie naam dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "lokaal declaratie naam dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1030" {
        give fluent_diagnostic_fields("module alias lid not exported", "module alias lid not exported.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "module alias lid not exported.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1031" {
        give fluent_diagnostic_fields("qualified type lid ontbrekend", "qualified type lid ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "qualified type lid ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1032" {
        give fluent_diagnostic_fields("expressie is not callable", "expressie is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "expressie is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E2001" {
        give fluent_diagnostic_fields("type niet ondersteund", "type niet ondersteund.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2002" {
        give fluent_diagnostic_fields("invoke has no aanroepdoel", "invoke has no aanroepdoel.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "invoke has no aanroepdoel.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2003" {
        give fluent_diagnostic_fields("expressie in HIR niet ondersteund", "expressie in HIR niet ondersteund.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie in HIR niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2004" {
        give fluent_diagnostic_fields("patroon in HIR niet ondersteund", "patroon in HIR niet ondersteund.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon in HIR niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2005" {
        give fluent_diagnostic_fields("statement in HIR niet ondersteund", "statement in HIR niet ondersteund.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "statement in HIR niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2006" {
        give fluent_diagnostic_fields("HIR type kind onverwacht", "HIR type kind onverwacht.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR type kind onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2007" {
        give fluent_diagnostic_fields("HIR expr kind onverwacht", "HIR expr kind onverwacht.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR expr kind onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2008" {
        give fluent_diagnostic_fields("HIR stmt kind onverwacht", "HIR stmt kind onverwacht.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR stmt kind onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2009" {
        give fluent_diagnostic_fields("HIR patroon kind onverwacht", "HIR patroon kind onverwacht.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR patroon kind onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2010" {
        give fluent_diagnostic_fields("HIR decl kind onverwacht", "HIR decl kind onverwacht.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR decl kind onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "LEX_E_INVALID_CHAR" {
        give fluent_diagnostic_fields("teken ongeldig", "teken ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "teken ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_NUMBER" {
        give fluent_diagnostic_fields("numeric literal ongeldig", "numeric literal ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "numeric literal ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_ESCAPE" {
        give fluent_diagnostic_fields("escape sequence ongeldig", "escape sequence ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "escape sequence ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE_ESCAPE" {
        give fluent_diagnostic_fields("unicode escape sequence ongeldig", "unicode escape sequence ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode escape sequence ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UTF8" {
        give fluent_diagnostic_fields("UTF-8 byte sequence ongeldig", "UTF-8 byte sequence ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "UTF-8 byte sequence ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_CHAR_LITERAL" {
        give fluent_diagnostic_fields("teken literal ongeldig", "teken literal ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "teken literal ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier ongeldig", "identifier ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "identifier ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_STRING" {
        give fluent_diagnostic_fields("string literal niet beeindigd", "string literal niet beeindigd.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add the closing `\"` on the same line; do not close a string with a single quote", "print(\"message\")", "string literal niet beeindigd.", "add the closing `\"` on the same line; do not close a string with a single quote");
    }

    if code == "LEX_E_UNTERMINATED_CHAR" {
        give fluent_diagnostic_fields("teken literal niet beeindigd", "teken literal niet beeindigd.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "teken literal niet beeindigd.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_BLOCK_COMMENT" {
        give fluent_diagnostic_fields("blok comment niet beeindigd", "blok comment niet beeindigd.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok comment niet beeindigd.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_REGION_COMMENT" {
        give fluent_diagnostic_fields("region comment niet beeindigd", "region comment niet beeindigd.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "region comment niet beeindigd.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNEXPECTED_EOF" {
        give fluent_diagnostic_fields("end of bestand onverwacht", "end of bestand onverwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "end of bestand onverwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_TOKEN_TOO_LARGE" {
        give fluent_diagnostic_fields("token te", "token te.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token te.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PLOOP" {
        give fluent_diagnostic_fields("parser made no progress", "parser made no progress.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parser made no progress.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PPRIMARY999" {
        give fluent_diagnostic_fields("expressie token onverwacht", "expressie token onverwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "expressie token onverwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PSTMT007" {
        give fluent_diagnostic_fields("toewijzing operator verwacht", "toewijzing operator verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "toewijzing operator verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P0001" {
        give fluent_diagnostic_fields("top-level token onverwacht", "top-level token onverwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level token onverwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P000_UNBALANCED" {
        give fluent_diagnostic_fields("blok niet gesloten", "blok niet gesloten.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok niet gesloten.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E_CLI_IO" {
        give fluent_diagnostic_fields("cannot read invoer bestand", "The compiler cannot read the requested invoer bestand.", "The requested file does not exist, is not a regular file, or is not readable.", "The requested file does not exist, is not a regular file, or is not readable.", "Verify the path passed to `vitte build` and the file permissions.", "use an existing readable source path", "vitte build src/main.vit -o build/main", "The compiler cannot read the requested invoer bestand.", "use an existing readable source path");
    }

    if code == "E_CLI_MISSING_ARG" {
        give fluent_diagnostic_fields("command argument ontbreekt", "command argument ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "command argument ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_COMMAND" {
        give fluent_diagnostic_fields("compiler command onbekend", "compiler command onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_OPTION" {
        give fluent_diagnostic_fields("compiler option onbekend", "compiler option onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("compiler argument ongeldig", "compiler argument ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler argument ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_IO_FILE_NOT_FOUND" {
        give fluent_diagnostic_fields("invoer bestand ontbrekend", "invoer bestand ontbrekend.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "invoer bestand ontbrekend.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_FILE_UNREADABLE" {
        give fluent_diagnostic_fields("invoer bestand is not readable", "invoer bestand is not readable.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "invoer bestand is not readable.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OUTPUT_UNWRITABLE" {
        give fluent_diagnostic_fields("cannot schrijven uitvoer bestand", "cannot schrijven uitvoer bestand.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "cannot schrijven uitvoer bestand.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "permission denied.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_DIRECTORY_NOT_FOUND" {
        give fluent_diagnostic_fields("uitvoer directory ontbrekend", "uitvoer directory ontbrekend.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "uitvoer directory ontbrekend.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OVERWRITE_FORBIDDEN" {
        give fluent_diagnostic_fields("refusing to overwrite existing uitvoer", "refusing to overwrite existing uitvoer.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "refusing to overwrite existing uitvoer.", "use an existing readable input path and a writable output destination");
    }

    if code == "DRIVER_E_PIPELINE_FAILURE" {
        give fluent_diagnostic_fields("compiler pipeline mislukt", "compiler pipeline mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler pipeline mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_FRONTEND_INCOMPLETE" {
        give fluent_diagnostic_fields("analysis frontend incomplete", "analysis frontend incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis frontend incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("analysis ongeldig", "analysis ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROWCK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("borrowck not executed", "borrowck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "borrowck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CODEGEN_IR_INCOMPLETE" {
        give fluent_diagnostic_fields("codegeneratie ir incomplete", "codegeneratie ir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "codegeneratie ir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INPUT_NOT_LOADED" {
        give fluent_diagnostic_fields("frontend invoer not loaded", "frontend invoer not loaded.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend invoer not loaded.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INVALID" {
        give fluent_diagnostic_fields("frontend ongeldig", "frontend ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_PROFILE" {
        give fluent_diagnostic_fields("link profiel ongeldig", "link profiel ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link profiel ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_INVALID" {
        give fluent_diagnostic_fields("ir ongeldig", "ir ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_MIR_INCOMPLETE" {
        give fluent_diagnostic_fields("ir mir incomplete", "ir mir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir mir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_CODEGEN_INCOMPLETE" {
        give fluent_diagnostic_fields("link codegeneratie incomplete", "link codegeneratie incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link codegeneratie incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INCOMPLETE" {
        give fluent_diagnostic_fields("mir analysis incomplete", "mir analysis incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("mir analysis ongeldig", "mir analysis ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_INVALID" {
        give fluent_diagnostic_fields("mir ongeldig", "mir ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NO_INPUT" {
        give fluent_diagnostic_fields("no invoer", "no invoer.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "no invoer.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_AST" {
        give fluent_diagnostic_fields("parse leeg ast", "parse leeg ast.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse leeg ast.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_SOURCE" {
        give fluent_diagnostic_fields("parse leeg bron", "parse leeg bron.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse leeg bron.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_LINK_INCOMPLETE" {
        give fluent_diagnostic_fields("run link incomplete", "run link incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run link incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_PROCESS_FAILED" {
        give fluent_diagnostic_fields("run process mislukt", "run process mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run process mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SEMA_NOT_EXECUTED" {
        give fluent_diagnostic_fields("sema not executed", "sema not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "sema not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TYPECK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("typeck not executed", "typeck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "typeck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LINK_E_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "permission denied.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OUTPUT_NOT_MATERIALIZED" {
        give fluent_diagnostic_fields("linker did not create the requested executable", "linker did not create the requested executable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker did not create the requested executable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_C_COMPILER" {
        give fluent_diagnostic_fields("required C compiler was ontbrekend", "required C compiler was ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "required C compiler was ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_COMMAND_FAILED" {
        give fluent_diagnostic_fields("linker command mislukt", "linker command mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker command mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LEX_E_INVALID_FLOAT" {
        give fluent_diagnostic_fields("kommagetal ongeldig", "kommagetal ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "kommagetal ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_BINARY" {
        give fluent_diagnostic_fields("binair ongeldig", "binair ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "binair ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_OCTAL" {
        give fluent_diagnostic_fields("octal ongeldig", "octal ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "octal ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_HEX" {
        give fluent_diagnostic_fields("hex ongeldig", "hex ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "hex ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_INDENTATION" {
        give fluent_diagnostic_fields("inspringing ongeldig", "inspringing ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "inspringing ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_TOKEN" {
        give fluent_diagnostic_fields("token ongeldig", "token ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE" {
        give fluent_diagnostic_fields("unicode ongeldig", "unicode ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_COMMENT" {
        give fluent_diagnostic_fields("comment niet beeindigd", "comment niet beeindigd.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comment niet beeindigd.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PATTR003" {
        give fluent_diagnostic_fields("pattr003", "pattr003.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "pattr003.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TOPLEVEL_DECL_EXPECTED" {
        give fluent_diagnostic_fields("top-level declaratie expected", "top-level declaratie expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level declaratie expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_INCOMPLETE_EXPR" {
        give fluent_diagnostic_fields("incomplete expressie", "incomplete expressie.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "incomplete expressie.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_RPAREN" {
        give fluent_diagnostic_fields("closing parenthesis ontbreekt", "closing parenthesis ontbreekt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "closing parenthesis ontbreekt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_COMMA" {
        give fluent_diagnostic_fields("comma ontbreekt", "comma ontbreekt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comma ontbreekt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TYPE_EXPECTED" {
        give fluent_diagnostic_fields("type expected", "type expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "type expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PATTERN_EXPECTED" {
        give fluent_diagnostic_fields("patroon expected", "patroon expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "patroon expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_BLOCK_EXPECTED" {
        give fluent_diagnostic_fields("blok expected", "blok expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_UNCLOSED_BLOCK" {
        give fluent_diagnostic_fields("blok niet gesloten", "blok niet gesloten.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add `}` to close the highlighted block, then run the checker again for its parent block", "while running { set running = false }", "blok niet gesloten.", "add `}` to close the highlighted block, then run the checker again for its parent block");
    }

    if code == "PARSE_E_EXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token verwacht", "token verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PARAMETER_COLON_EXPECTED" {
        give fluent_diagnostic_fields("colon in procedure parameter ontbreekt", "parameter colon expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "insert `:` between the parameter name and its type, for example `right: f64`", "proc calculate(right: f64) -> f64 { give right }", "parameter colon expected.", "insert `:` between the parameter name and its type, for example `right: f64`");
    }

    if code == "PARSE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token onverwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_OLD_VITTE_SYNTAX" {
        give fluent_diagnostic_fields("old Vitte syntaxis is not canonical", "old Vitte syntaxis is not canonical.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "Identify the intended construct, then compare it with the current Vitte keywords.", "replace the old token with the current Vitte keyword only when the construct is equivalent", "proc main() -> int { give 0 }", "old Vitte syntaxis is not canonical.", "replace the old token with the current Vitte keyword only when the construct is equivalent");
    }

    if code == "PARSE_EXPECTED_EXPR" {
        give fluent_diagnostic_fields("parse expected expr", "parse expected expr.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected expr.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("parse expected type", "parse expected type.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected type.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("parse expected patroon", "parse expected patroon.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected patroon.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("parse expected blok", "parse expected blok.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected blok.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("parse expected identifier", "parse expected identifier.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected identifier.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FAST0001" {
        give fluent_diagnostic_fields("fast0001", "fast0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "fast0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FLEX0001" {
        give fluent_diagnostic_fields("flex0001", "flex0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "flex0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "AST_E_INVALID_NODE" {
        give fluent_diagnostic_fields("knoop ongeldig", "knoop ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "knoop ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_EMPTY_MODULE" {
        give fluent_diagnostic_fields("leeg module", "leeg module.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "leeg module.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_DECL" {
        give fluent_diagnostic_fields("decl ongeldig", "decl ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "decl ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ongeldig", "expr ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_DUPLICATE_FIELD" {
        give fluent_diagnostic_fields("veld dubbel", "veld dubbel.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Find the earlier field label and the highlighted duplicate.", "keep one field entry and remove or rename the duplicate", "vitte check path/to/file.vit", "veld dubbel.", "keep one field entry and remove or rename the duplicate");
    }

    if code == "AST_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("form veld onbekend", "form veld onbekend.", "A `form` construction or field access names a field that the form declaration does not contain.", "A `form` construction or field access names a field that the form declaration does not contain.", "Compare the highlighted field name with the fields declared in the `form`.", "rename the field to one declared by the form, or add the missing field to the form declaration", "form Point { x: int, y: int }", "form veld onbekend.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "AST_E_MISSING_FIELD" {
        give fluent_diagnostic_fields("form veld ontbreekt", "form veld ontbreekt.", "A `form` construction omits a required field that has no default value.", "A `form` construction omits a required field that has no default value.", "Compare the construction with every required field in the `form` declaration.", "add the missing `field: value` entry to the construction", "form Point { x: int, y: int }", "form veld ontbreekt.", "add the missing `field: value` entry to the construction");
    }

    if code == "AST_E_FIELD_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("form veld type mismatch", "form veld type mismatch.", "A value assigned to a `form` field does not match that field's declared type.", "A value assigned to a `form` field does not match that field's declared type.", "Compare the field declaration type with the highlighted field initializer.", "change the field initializer to the declared type, or change the field type in the `form`", "form User { name: string, age: int }", "form veld type mismatch.", "change the field initializer to the declared type, or change the field type in the `form`");
    }

    if code == "AST_E_FIELD_ORDER" {
        give fluent_diagnostic_fields("form fields are out of order", "form fields are out of order.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "Read the field order from the `form` declaration.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them", "form Pair { left: int, right: int }", "form fields are out of order.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them");
    }

    if code == "AST_E_INCOMPLETE_CONSTRUCTION" {
        give fluent_diagnostic_fields("form construction is incomplete", "form construction is incomplete.", "A `form` construction ended before all required fields were supplied.", "A `form` construction ended before all required fields were supplied.", "Check whether the construction is missing fields or a closing brace.", "complete the construction with every required `field: value` entry and the closing `}`", "form Point { x: int, y: int }", "form construction is incomplete.", "complete the construction with every required `field: value` entry and the closing `}`");
    }

    if code == "AST_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("zichtbaarheid ongeldig", "zichtbaarheid ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "zichtbaarheid ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ENTRY" {
        give fluent_diagnostic_fields("entry ongeldig", "entry ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "entry ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "SEMA_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("symbool dubbel", "symbool dubbel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "symbool dubbel.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_UNKNOWN_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier onbekend", "identifier onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "identifier onbekend.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_AMBIGUOUS_SYMBOL" {
        give fluent_diagnostic_fields("ambiguous symbool", "ambiguous symbool.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous symbool.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_SHADOWING_FORBIDDEN" {
        give fluent_diagnostic_fields("shadowing is forbidden", "shadowing is forbidden.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "shadowing is forbidden.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("veld does not exist", "veld does not exist.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename the field to one declared by the form, or add the missing field to the form declaration", "use vitte/core", "veld does not exist.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "SEMA_E_UNKNOWN_VARIANT" {
        give fluent_diagnostic_fields("variant does not exist", "variant does not exist.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "Compare the highlighted variant with the `case` names declared in the `pick`.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration", "pick Status { Ready, Failed }", "variant does not exist.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration");
    }

    if code == "SEMA_E_INACCESSIBLE_VARIANT" {
        give fluent_diagnostic_fields("pick variant is inaccessible", "pick variant is inaccessible.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "Read the branches in order and find the earlier branch that already covers this variant.", "remove the unreachable branch or move the more specific `case` before the broader branch", "match status {", "pick variant is inaccessible.", "remove the unreachable branch or move the more specific `case` before the broader branch");
    }

    if code == "SEMA_E_DUPLICATE_PICK_BRANCH" {
        give fluent_diagnostic_fields("pick branch is duplicated", "pick branch is duplicated.", "The same `pick` variant is matched by more than one branch in the same `match`.", "The same `pick` variant is matched by more than one branch in the same `match`.", "Find the earlier branch for the same `case` and decide which body should remain.", "merge the duplicate branch bodies or remove the later duplicate branch", "match status {", "pick branch is duplicated.", "merge the duplicate branch bodies or remove the later duplicate branch");
    }

    if code == "TYPECK_E_PICK_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("pick patroon is not exhaustive", "pick patroon is not exhaustive.", "A `match` over a `pick` value does not cover every reachable variant.", "A `match` over a `pick` value does not cover every reachable variant.", "List the declared `case` variants and compare them with the branches already present.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended", "match status {", "pick patroon is not exhaustive.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended");
    }

    if code == "TYPECK_E_PICK_PAYLOAD_MISMATCH" {
        give fluent_diagnostic_fields("pick payload does not match variant", "pick payload does not match variant.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "Compare each payload position or name with the selected variant declaration.", "use the expected payload shape for that `case`, including the correct field count and types", "pick Event { Data(value: int) }", "pick payload does not match variant.", "use the expected payload shape for that `case`, including the correct field count and types");
    }

    if code == "TYPECK_E_IMPOSSIBLE_PATTERN" {
        give fluent_diagnostic_fields("pick patroon can never match", "pick patroon can never match.", "The pattern cannot match the scrutinee type produced by the expression.", "The pattern cannot match the scrutinee type produced by the expression.", "Compare the matched expression type with the variant or literal named by the pattern.", "replace the branch pattern with one that belongs to the matched `pick` or value type", "match status { case Ready { give 0 } }", "pick patroon can never match.", "replace the branch pattern with one that belongs to the matched `pick` or value type");
    }

    if code == "SEMA_E_UNKNOWN_FUNCTION" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "SEMA_E_UNKNOWN_SYMBOL" {
        give fluent_diagnostic_fields("symbool onbekend", "symbool onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "symbool onbekend.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_INVALID_IMPORT" {
        give fluent_diagnostic_fields("import ongeldig", "import ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "import ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_EXPORT" {
        give fluent_diagnostic_fields("export ongeldig", "export ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("zichtbaarheid ongeldig", "zichtbaarheid ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "zichtbaarheid ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control stroom ongeldig", "control stroom ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "control stroom ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("module ongeldig", "module ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "module ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ASSIGN_TARGET" {
        give fluent_diagnostic_fields("assign doel ongeldig", "assign doel ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "assign doel ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNDECLARED_TARGET" {
        give fluent_diagnostic_fields("undeclared doel", "undeclared doel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "undeclared doel.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_MISSING_BINDING" {
        give fluent_diagnostic_fields("binding ontbreekt", "binding ontbreekt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "binding ontbreekt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_DUPLICATE_BINDING" {
        give fluent_diagnostic_fields("binding dubbel", "binding dubbel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "binding dubbel.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_DUPLICATE_ITEM" {
        give fluent_diagnostic_fields("item dubbel", "item dubbel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "item dubbel.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir ongeldig", "hir ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "hir ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "intern.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPECK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_ITEM" {
        give fluent_diagnostic_fields("item onbekend", "item onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "item onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_W_UNRESOLVED_NAME" {
        give fluent_diagnostic_fields("unresolved naam", "unresolved naam.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unresolved naam.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ongeldig", "expr ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expr ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_BINARY_MISMATCH" {
        give fluent_diagnostic_fields("binair mismatch", "binair mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make both operands valid for the operator before MIR lowering", "let count: int = 1", "binair mismatch.", "make both operands valid for the operator before MIR lowering");
    }

    if code == "TYPECK_E_ASSIGN_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing type mismatch", "toewijzing type mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "assign a value of the declared binding type, or change the binding annotation at its declaration", "let count: int = 1", "toewijzing type mismatch.", "assign a value of the declared binding type, or change the binding annotation at its declaration");
    }

    if code == "TYPECK_E_RETURN_MISMATCH" {
        give fluent_diagnostic_fields("give type mismatch", "give type mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "change the `give` expression or the procedure result type so both name the same Vitte type", "let count: int = 1", "give type mismatch.", "change the `give` expression or the procedure result type so both name the same Vitte type");
    }

    if code == "TYPECK_E_MISSING_GIVE" {
        give fluent_diagnostic_fields("give in waarde procedure ontbreekt", "give in waarde procedure ontbreekt.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "Inspect the procedure body and decide which value should be produced.", "add `give expression` on every successful path, or remove the `-> Type` result contract", "proc answer() -> int { give 42 }", "give in waarde procedure ontbreekt.", "add `give expression` on every successful path, or remove the `-> Type` result contract");
    }

    if code == "TYPECK_E_GIVE_IN_VOID_PROC" {
        give fluent_diagnostic_fields("give waarde in procedure without result type", "give waarde in procedure without result type.", "A procedure without a `-> Type` contract uses `give` with a value.", "A procedure without a `-> Type` contract uses `give` with a value.", "Check whether the procedure should produce a value or only perform effects.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement", "proc answer() -> int { give 42 }", "give waarde in procedure without result type.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement");
    }

    if code == "TYPECK_E_CONTROL_PATH_MISSING_GIVE" {
        give fluent_diagnostic_fields("control pad reaches end without give", "control pad reaches end without give.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "Follow each `if`, `match`, `loop`, and early-exit path in the procedure.", "add a final `give` or make every branch produce a value before control reaches the closing brace", "proc code(ok: bool) -> int {", "control pad reaches end without give.", "add a final `give` or make every branch produce a value before control reaches the closing brace");
    }

    if code == "TYPECK_W_UNREACHABLE_AFTER_GIVE" {
        give fluent_diagnostic_fields("code after give is onbereikbaar", "code after give is onbereikbaar.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "Inspect the statement immediately after the highlighted `give`.", "move the statement before `give`, put it in another branch, or remove it", "proc main() -> int {", "code after give is onbereikbaar.", "move the statement before `give`, put it in another branch, or remove it");
    }

    if code == "TYPECK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("cannot assign to immutable binding", "cannot assign to immutable binding.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "Find the original `let`, parameter, or field declaration for the highlighted target.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`", "let count: int = 0", "cannot assign to immutable binding.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`");
    }

    if code == "TYPECK_W_MUTABLE_NEVER_MODIFIED" {
        give fluent_diagnostic_fields("muteerbaar binding is never changed", "muteerbaar binding is never changed.", "A binding was declared mutable, but no reachable `set` changes it.", "A binding was declared mutable, but no reachable `set` changes it.", "Search the binding scope for `set name = ...` or field updates through that binding.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended", "let count: int = 0", "muteerbaar binding is never changed.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended");
    }

    if code == "TYPECK_E_REASSIGNMENT_FORBIDDEN" {
        give fluent_diagnostic_fields("doel cannot be reassigned", "doel cannot be reassigned.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "Check that the left side of `set` is a binding, field, or index place that Vitte allows to be assigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`", "set user.name = name", "doel cannot be reassigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`");
    }

    if code == "TYPECK_E_MISSING_INITIALIZER" {
        give fluent_diagnostic_fields("let binding requires an initializer or type", "let binding requires an initializer or type.", "`let` introduced a binding without enough information to create a value.", "`let` introduced a binding without enough information to create a value.", "Inspect the binding and check whether it has either an initializer or an explicit type plus a supported delayed-initialization path.", "add `= expression`, or add the missing type annotation required by the active Vitte rule", "let count: int = 0", "let binding requires an initializer or type.", "add `= expression`, or add the missing type annotation required by the active Vitte rule");
    }

    if code == "TYPECK_E_INFERENCE_FAILED" {
        give fluent_diagnostic_fields("binding type could not be inferred", "binding type could not be inferred.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "Look at the highlighted `let` and the first use of the binding.", "add an explicit Vitte type annotation after the binding name", "let items: [int] = []", "binding type could not be inferred.", "add an explicit Vitte type annotation after the binding name");
    }

    if code == "TYPECK_E_CONDITION_TYPE" {
        give fluent_diagnostic_fields("voorwaarde type", "voorwaarde type.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the condition produce bool, for example by adding an explicit comparison", "let count: int = 1", "voorwaarde type.", "make the condition produce bool, for example by adding an explicit comparison");
    }

    if code == "TYPECK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "rename the field to one declared by the form, or add the missing field to the form declaration", "let count: int = 1", "lid onbekend.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "TYPECK_E_INDEX_TYPE" {
        give fluent_diagnostic_fields("index type", "index type.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "index type.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_INDEX_TARGET" {
        give fluent_diagnostic_fields("index doel ongeldig", "index doel ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "index doel ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IF_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("if branch mismatch", "if branch mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "if branch mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_COMPARE_MISMATCH" {
        give fluent_diagnostic_fields("compare mismatch", "compare mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "compare mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conversie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MATCH_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("match non exhaustive", "match non exhaustive.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "match non exhaustive.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CALL_TARGET" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "aanroep doel is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_UNKNOWN_PROCEDURE" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "TYPECK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("aanroep argument type mismatch", "aanroep argument type mismatch.", "An argument expression does not match the type of the parameter it is passed to.", "An argument expression does not match the type of the parameter it is passed to.", "Link the highlighted argument to its parameter and compare the expected and found Vitte types.", "change that argument expression or the parameter type so the single argument matches its parameter", "scale(value: 4, factor: 2)", "aanroep argument type mismatch.", "change that argument expression or the parameter type so the single argument matches its parameter");
    }

    if code == "TYPECK_E_CALL_ARITY" {
        give fluent_diagnostic_fields("wrong nummer of aanroep argumenten", "wrong nummer of aanroep argumenten.", "The call supplies a different number of arguments than the procedure parameter list.", "The call supplies a different number of arguments than the procedure parameter list.", "Show the expected parameters in order, then attach each extra or missing argument to its nearest parameter.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list", "add(1, 2)", "wrong nummer of aanroep argumenten.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list");
    }

    if code == "TYPECK_E_UNKNOWN_NAMED_ARGUMENT" {
        give fluent_diagnostic_fields("named argument has no matching parameter", "named argument has no matching parameter.", "A named argument does not correspond to any parameter in the called procedure.", "A named argument does not correspond to any parameter in the called procedure.", "Display the expected parameter names and highlight the unknown argument name.", "rename the argument to an expected parameter name or remove it", "connect(host: \"localhost\", port: 8080)", "named argument has no matching parameter.", "rename the argument to an expected parameter name or remove it");
    }

    if code == "TYPECK_E_ARGUMENT_ORDER" {
        give fluent_diagnostic_fields("aanroep argumenten are in the wrong order", "aanroep argumenten are in the wrong order.", "A positional or named argument appears after an argument form that makes the order invalid.", "A positional or named argument appears after an argument form that makes the order invalid.", "Compare the call argument order with the procedure parameter order shown in the diagnostic.", "reorder the arguments to match the parameter list, or use named arguments consistently", "draw(x: 1, y: 2, color: \"blue\")", "aanroep argumenten are in the wrong order.", "reorder the arguments to match the parameter list, or use named arguments consistently");
    }

    if code == "TYPECK_E_AMBIGUOUS_CALL" {
        give fluent_diagnostic_fields("procedure aanroep is ambiguous", "procedure aanroep is ambiguous.", "More than one visible procedure can accept the same call shape.", "More than one visible procedure can accept the same call shape.", "Show the candidate procedure signatures and identify the arguments that do not disambiguate them.", "add a type annotation to one argument or call a more specific procedure name", "let result: int = parse(\"42\")", "procedure aanroep is ambiguous.", "add a type annotation to one argument or call a more specific procedure name");
    }

    if code == "TYPECK_W_IGNORED_RESULT" {
        give fluent_diagnostic_fields("procedure result is ignored", "procedure result is ignored.", "A procedure result is produced but the surrounding statement does not use it.", "A procedure result is produced but the surrounding statement does not use it.", "Check whether the result should be bound with `let`, passed onward, or intentionally discarded.", "bind the result with `let name: Type = call(...)` when the value is needed", "let total: int = add(1, 2)", "procedure result is ignored.", "bind the result with `let name: Type = call(...)` when the value is needed");
    }

    if code == "TYPECK_E_NOT_CALLABLE" {
        give fluent_diagnostic_fields("waarde is not callable", "waarde is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "waarde is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_IMPL_MISSING_MEMBER" {
        give fluent_diagnostic_fields("implementation is ontbrekend a required lid", "implementation is ontbrekend a required lid.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation is ontbrekend a required lid.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_SIGNATURE_MISMATCH" {
        give fluent_diagnostic_fields("implementation lid signature does not match", "implementation lid signature does not match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation lid signature does not match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_UNKNOWN_TRAIT" {
        give fluent_diagnostic_fields("trait does not exist", "trait does not exist.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait does not exist.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_GENERIC_INFERENCE" {
        give fluent_diagnostic_fields("generiek type could not be inferred", "generiek type could not be inferred.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek type could not be inferred.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_TRAIT_BOUND" {
        give fluent_diagnostic_fields("trait constraint is not satisfied", "trait constraint is not satisfied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait constraint is not satisfied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CAUSE_CHAIN_MISSING" {
        give fluent_diagnostic_fields("type diagnostic is ontbrekend a cause chain", "type diagnostic is ontbrekend a cause chain.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type diagnostic is ontbrekend a cause chain.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONFLICTING_IMPL" {
        give fluent_diagnostic_fields("conflicting trait implementation", "conflicting trait implementation.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conflicting trait implementation.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONSTRAINT_CYCLE" {
        give fluent_diagnostic_fields("cyclic generiek constraint", "cyclic generiek constraint.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cyclic generiek constraint.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_AMBIGUOUS_METHOD" {
        give fluent_diagnostic_fields("ambiguous method resolution", "ambiguous method resolution.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous method resolution.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after verplaatsing.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_UNARY" {
        give fluent_diagnostic_fields("malformed unary", "malformed unary.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed unary.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNARY_MISMATCH" {
        give fluent_diagnostic_fields("unary mismatch", "unary mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unary mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BORROW" {
        give fluent_diagnostic_fields("malformed lening", "malformed lening.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed lening.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_MEMBER" {
        give fluent_diagnostic_fields("malformed lid", "malformed lid.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed lid.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_INDEX" {
        give fluent_diagnostic_fields("malformed index", "malformed index.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed index.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BINARY" {
        give fluent_diagnostic_fields("malformed binair", "malformed binair.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed binair.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_CAST" {
        give fluent_diagnostic_fields("malformed conversie", "malformed conversie.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed conversie.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_IF" {
        give fluent_diagnostic_fields("malformed if", "malformed if.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed if.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir ongeldig", "hir ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "hir ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "intern.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0001" {
        give fluent_diagnostic_fields("type0001", "type0001.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0001.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0002" {
        give fluent_diagnostic_fields("type0002", "type0002.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0002.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0003" {
        give fluent_diagnostic_fields("type0003", "type0003.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0003.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0004" {
        give fluent_diagnostic_fields("type0004", "type0004.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0004.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0005" {
        give fluent_diagnostic_fields("type0005", "type0005.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0005.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0006" {
        give fluent_diagnostic_fields("type0006", "type0006.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0006.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0007" {
        give fluent_diagnostic_fields("type0007", "type0007.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0007.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0008" {
        give fluent_diagnostic_fields("type0008", "type0008.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0008.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0009" {
        give fluent_diagnostic_fields("type0009", "type0009.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0009.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0010" {
        give fluent_diagnostic_fields("type0010", "type0010.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0010.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE9999" {
        give fluent_diagnostic_fields("type9999", "type9999.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type9999.", "make the expression type match the type contract named by the type checker");
    }

    if code == "BORROWCK_E_MOVE_AFTER_MOVE" {
        give fluent_diagnostic_fields("verplaatsing after verplaatsing", "verplaatsing after verplaatsing.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "verplaatsing after verplaatsing.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("waarde gebruikt after verplaatsing", "waarde gebruikt after verplaatsing.", "A value is used after ownership has moved away from its binding.", "A value is used after ownership has moved away from its binding.", "Show where the value is created, where it is moved, and where it is reused after the move.", "use the value before the move, borrow it instead of moving it, or create a new value", "let value: string = \"vitte\"", "waarde gebruikt after verplaatsing.", "use the value before the move, borrow it instead of moving it, or create a new value");
    }

    if code == "BORROWCK_E_PARTIAL_MOVE" {
        give fluent_diagnostic_fields("waarde partially verplaatst", "waarde partially verplaatst.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Show the original construction, the field or variant payload that moved, and the later whole-value use.", "avoid using the whole value after moving one part, or rebuild the missing part before use", "form User { name: string, age: int }", "waarde partially verplaatst.", "avoid using the whole value after moving one part, or rebuild the missing part before use");
    }

    if code == "BORROWCK_E_BORROW_OF_MOVED_VALUE" {
        give fluent_diagnostic_fields("lening of verplaatst waarde", "lening of verplaatst waarde.", "A borrow starts after ownership has already moved away from the original binding.", "A borrow starts after ownership has already moved away from the original binding.", "Show the value creation, the move, and the later borrow attempt in that order.", "create the borrow before the move, or move a different value", "let value: string = \"vitte\"", "lening of verplaatst waarde.", "create the borrow before the move, or move a different value");
    }

    if code == "BORROWCK_E_MUTABLE_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("muteerbaar lening conflict", "muteerbaar lening conflict.", "A value is already mutably borrowed when another borrow of the same value starts.", "A value is already mutably borrowed when another borrow of the same value starts.", "Report the value creation first, then the first mutable borrow, then the later conflicting borrow, then where the first borrow ends.", "end the first mutable borrow before starting the next borrow", "let value: int = 1", "muteerbaar lening conflict.", "end the first mutable borrow before starting the next borrow");
    }

    if code == "BORROWCK_E_MUTABLE_SHARED_CONFLICT" {
        give fluent_diagnostic_fields("muteerbaar and gedeeld borrows overlap", "muteerbaar and gedeeld borrows overlap.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "muteerbaar and gedeeld borrows overlap.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_SHARED_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("gedeeld lening conflict", "gedeeld lening conflict.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "gedeeld lening conflict.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_WRITE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("schrijven terwijl geleend", "schrijven terwijl geleend.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "schrijven terwijl geleend.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_MOVE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("verplaatsing terwijl geleend", "verplaatsing terwijl geleend.", "Ownership moves out of a value while an active borrow can still use it.", "Ownership moves out of a value while an active borrow can still use it.", "Show the value creation, the borrow start, the move, the later borrow use, and where the borrow ends.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership", "let value: string = \"vitte\"", "verplaatsing terwijl geleend.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership");
    }

    if code == "BORROWCK_E_DROP_WHILE_BORROWED" {
        give fluent_diagnostic_fields("vrijgave terwijl geleend", "vrijgave terwijl geleend.", "A value is destroyed while an active borrow still points to it.", "A value is destroyed while an active borrow still points to it.", "Show the value creation, the borrow start, the destruction point, and the last borrow use.", "destroy the value only after the borrow is no longer used", "let value: string = \"vitte\"", "vrijgave terwijl geleend.", "destroy the value only after the borrow is no longer used");
    }

    if code == "BORROWCK_E_ASSIGN_WHILE_BORROWED" {
        give fluent_diagnostic_fields("assign terwijl geleend", "assign terwijl geleend.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "assign terwijl geleend.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_RETURN_REF_TO_LOCAL" {
        give fluent_diagnostic_fields("return ref to lokaal", "return ref to lokaal.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "return ref to lokaal.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_RETURN_BORROW_OF_LOCAL" {
        give fluent_diagnostic_fields("return lening of lokaal", "return lening of lokaal.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "return lening of lokaal.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "dangling referentie.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_REFERENCE_OUTLIVES_VALUE" {
        give fluent_diagnostic_fields("referentie outlives the waarde it points to", "referentie outlives the waarde it points to.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "referentie outlives the waarde it points to.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "levensduur te short.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("immutable assign", "immutable assign.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "immutable assign.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNINITIALIZED_USE" {
        give fluent_diagnostic_fields("niet geinitialiseerd use", "niet geinitialiseerd use.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "niet geinitialiseerd use.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MOVE_AFTER_BORROW" {
        give fluent_diagnostic_fields("verplaatsing after lening", "verplaatsing after lening.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "verplaatsing after lening.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MUTABLE_ALIAS" {
        give fluent_diagnostic_fields("muteerbaar alias", "muteerbaar alias.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "muteerbaar alias.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_INTERNAL" {
        give fluent_diagnostic_fields("intern", "intern.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "intern.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNKNOWN" {
        give fluent_diagnostic_fields("onbekend", "onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_EVAL_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("deling by nul in constante evaluation", "deling by nul in constante evaluation.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "change the const divisor to a non-zero value or guard the expression before const evaluation", "const size: int = 4", "deling by nul in constante evaluation.", "change the const divisor to a non-zero value or guard the expression before const evaluation");
    }

    if code == "CONST_EVAL_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "naam onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_NON_CONST_CALL" {
        give fluent_diagnostic_fields("non const aanroep", "non const aanroep.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non const aanroep.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_MUTATION_IN_CONST" {
        give fluent_diagnostic_fields("mutation in const", "mutation in const.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutation in const.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNSUPPORTED_EXPR" {
        give fluent_diagnostic_fields("expr niet ondersteund", "expr niet ondersteund.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "replace the const expression with literals, supported arithmetic, or another compile-time value", "const size: int = 4", "expr niet ondersteund.", "replace the const expression with literals, supported arithmetic, or another compile-time value");
    }

    if code == "CONST_EVAL_E_OVERFLOW" {
        give fluent_diagnostic_fields("overloop", "overloop.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "use a wider const type or reduce the arithmetic result before overflow", "const size: int = 4", "overloop.", "use a wider const type or reduce the arithmetic result before overflow");
    }

    if code == "CONST_EVAL_E_STATIC_ASSERT_FAILED" {
        give fluent_diagnostic_fields("static assertie mislukt", "static assertie mislukt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "make the asserted const condition true or remove the invalid compile-time assumption", "const size: int = 4", "static assertie mislukt.", "make the asserted const condition true or remove the invalid compile-time assumption");
    }

    if code == "CONST_EVAL_E_CYCLE" {
        give fluent_diagnostic_fields("cyclus", "cyclus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "cyclus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_PARSE" {
        give fluent_diagnostic_fields("parse", "parse.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "parse.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNKNOWN" {
        give fluent_diagnostic_fields("onbekend", "onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MOD_E_MODULE_NOT_FOUND" {
        give fluent_diagnostic_fields("module ontbrekend", "module ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "module ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus detected", "import cyclus detected.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "import cyclus detected.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_SYMBOL_NOT_EXPORTED" {
        give fluent_diagnostic_fields("symbool is niet geexporteerd door module", "symbool is niet geexporteerd door module.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "symbool is niet geexporteerd door module.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "import ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_PACKAGE_MISSING" {
        give fluent_diagnostic_fields("package ontbrekend", "package ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "package ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_STDLIB_MISSING" {
        give fluent_diagnostic_fields("stdlib ontbrekend", "stdlib ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "stdlib ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_AMBIGUOUS_MODULE" {
        give fluent_diagnostic_fields("ambiguous module pad", "ambiguous module pad.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ambiguous module pad.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "HIR_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr ongeldig", "expr ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STMT" {
        give fluent_diagnostic_fields("stmt ongeldig", "stmt ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "stmt ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_TYPE" {
        give fluent_diagnostic_fields("type ongeldig", "type ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_SYMBOL" {
        give fluent_diagnostic_fields("symbool ontbreekt", "symbool ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "symbool ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control stroom ongeldig", "control stroom ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "control stroom ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LOWERING_FAILED" {
        give fluent_diagnostic_fields("lowering mislukt", "lowering mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lowering mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("blok ongeldig", "blok ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_TERMINATOR" {
        give fluent_diagnostic_fields("terminator ongeldig", "terminator ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "terminator ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_BLOCK" {
        give fluent_diagnostic_fields("onbereikbaar blok", "onbereikbaar blok.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "onbereikbaar blok.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERAND" {
        give fluent_diagnostic_fields("operand ongeldig", "operand ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "operand ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PLACE" {
        give fluent_diagnostic_fields("place ongeldig", "place ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "place ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DATAFLOW_CONFLICT" {
        give fluent_diagnostic_fields("dataflow conflict", "dataflow conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dataflow conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_VERIFICATION_FAILED" {
        give fluent_diagnostic_fields("verification mislukt", "verification mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verification mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("module ongeldig", "module ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "module ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_FUNCTION" {
        give fluent_diagnostic_fields("procedure ongeldig", "procedure ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "procedure ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("blok ongeldig", "blok ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INSTRUCTION" {
        give fluent_diagnostic_fields("instructie ongeldig", "instructie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "instructie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("type mismatch", "type mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_VERIFY_FAILED" {
        give fluent_diagnostic_fields("verify mislukt", "verify mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verify mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "doel niet ondersteund.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNSUPPORTED_FEATURE" {
        give fluent_diagnostic_fields("functie niet ondersteund", "functie niet ondersteund.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "functie niet ondersteund.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CODEGEN_FAILED" {
        give fluent_diagnostic_fields("codegeneratie mislukt", "codegeneratie mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "codegeneratie mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_OBJECT_WRITE_FAILED" {
        give fluent_diagnostic_fields("object schrijven mislukt", "object schrijven mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "object schrijven mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSEMBLER_FAILED" {
        give fluent_diagnostic_fields("assembler mislukt", "assembler mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "assembler mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NATIVE_TOOL_MISSING" {
        give fluent_diagnostic_fields("native hulpmiddel ontbrekend", "native hulpmiddel ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "native hulpmiddel ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNDEFINED_SYMBOL" {
        give fluent_diagnostic_fields("ongedefinieerd symbool", "ongedefinieerd symbool.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ongedefinieerd symbool.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("symbool dubbel", "symbool dubbel.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "symbool dubbel.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIBRARY_NOT_FOUND" {
        give fluent_diagnostic_fields("library ontbrekend", "library ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "library ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OBJECT_NOT_FOUND" {
        give fluent_diagnostic_fields("object ontbrekend", "object ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "object ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_FORMAT" {
        give fluent_diagnostic_fields("formaat niet ondersteund", "formaat niet ondersteund.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "formaat niet ondersteund.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_SYSTEM_LINKER_FAILED" {
        give fluent_diagnostic_fields("system linker mislukt", "system linker mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "system linker mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_PANIC" {
        give fluent_diagnostic_fields("panic", "panic.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "panic.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSERT_FAILED" {
        give fluent_diagnostic_fields("assertie mislukt", "assertie mislukt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "assertie mislukt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_STACK_OVERFLOW" {
        give fluent_diagnostic_fields("stack overloop", "stack overloop.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "stack overloop.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_OUT_OF_MEMORY" {
        give fluent_diagnostic_fields("out of memory", "out of memory.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "out of memory.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("deling by nul", "deling by nul.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "deling by nul.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BOUNDS_CHECK" {
        give fluent_diagnostic_fields("bounds check", "bounds check.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bounds check.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NULL_DEREF" {
        give fluent_diagnostic_fields("nul deref", "nul deref.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "nul deref.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("argument ongeldig", "argument ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "argument ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_INPUT" {
        give fluent_diagnostic_fields("invoer ontbreekt", "invoer ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "invoer ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_COMMAND" {
        give fluent_diagnostic_fields("compiler command ontbreekt", "compiler command ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OPTION_VALUE" {
        give fluent_diagnostic_fields("compiler option waarde ontbreekt", "compiler option waarde ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option waarde ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SOURCE_UNREADABLE" {
        give fluent_diagnostic_fields("or unreadable invoer bron ontbreekt", "or unreadable invoer bron ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "or unreadable invoer bron ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CONFIGURATION" {
        give fluent_diagnostic_fields("compiler configuration ongeldig", "compiler configuration ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler configuration ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_TARGET" {
        give fluent_diagnostic_fields("compiler doel ontbreekt", "compiler doel ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler doel ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_DIR" {
        give fluent_diagnostic_fields("compiler uitvoer directory ontbreekt", "compiler uitvoer directory ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler uitvoer directory ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_PATH" {
        give fluent_diagnostic_fields("compiler uitvoer pad ontbreekt", "compiler uitvoer pad ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler uitvoer pad ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_TARGET" {
        give fluent_diagnostic_fields("compiler link doel ongeldig", "compiler link doel ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler link doel ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INPUT_NOT_FOUND" {
        give fluent_diagnostic_fields("invoer ontbrekend", "invoer ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "invoer ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_OUTPUT_WRITE_FAILED" {
        give fluent_diagnostic_fields("uitvoer schrijven mislukt", "uitvoer schrijven mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "uitvoer schrijven mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_READ_FAILED" {
        give fluent_diagnostic_fields("cache read mislukt", "cache read mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache read mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_WRITE_FAILED" {
        give fluent_diagnostic_fields("cache schrijven mislukt", "cache schrijven mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache schrijven mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PROFILE_NOT_FOUND" {
        give fluent_diagnostic_fields("profiel ontbrekend", "profiel ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "profiel ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TARGET_NOT_FOUND" {
        give fluent_diagnostic_fields("doel ontbrekend", "doel ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "doel ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "BOOTSTRAP_E_STAGE_FAILURE" {
        give fluent_diagnostic_fields("stage failure", "stage failure.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "stage failure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SEED_MISSING" {
        give fluent_diagnostic_fields("seed ontbrekend", "seed ontbrekend.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "seed ontbrekend.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_COMPILER_MISSING" {
        give fluent_diagnostic_fields("compiler ontbrekend", "compiler ontbrekend.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "compiler ontbrekend.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SELF_CHECK_FAILED" {
        give fluent_diagnostic_fields("self check mislukt", "self check mislukt.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "self check mislukt.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_ARTIFACT_INVALID" {
        give fluent_diagnostic_fields("artifact ongeldig", "artifact ongeldig.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "artifact ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_BANNER" {
        give fluent_diagnostic_fields("bootstrap banner is ongeldig", "bootstrap banner is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap banner is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap constante signature is ongeldig", "bootstrap constante signature is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap constante signature is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_TYPE" {
        give fluent_diagnostic_fields("bootstrap constante has wrong type", "bootstrap constante has wrong type.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap constante has wrong type.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_DUP_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure dubbel", "bootstrap procedure dubbel.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure dubbel.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_EXPORT" {
        give fluent_diagnostic_fields("bootstrap export is ongeldig", "bootstrap export is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap export is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_BODY" {
        give fluent_diagnostic_fields("bootstrap main body is ongeldig", "bootstrap main body is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main body is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap main signature is ongeldig", "bootstrap main signature is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main signature is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_BODY" {
        give fluent_diagnostic_fields("bootstrap procedure body is ongeldig", "bootstrap procedure body is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap procedure signature is ongeldig", "bootstrap procedure signature is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure signature is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_SPACE" {
        give fluent_diagnostic_fields("bootstrap space declaratie is ongeldig", "bootstrap space declaratie is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap space declaratie is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_TOP_LEVEL" {
        give fluent_diagnostic_fields("bootstrap top-level declaratie is ongeldig", "bootstrap top-level declaratie is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap top-level declaratie is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNCLOSED_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure body is not closed", "bootstrap procedure body is not closed.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is not closed.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_CONST" {
        give fluent_diagnostic_fields("bootstrap constante onbekend", "bootstrap constante onbekend.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap constante onbekend.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure niet ondersteund", "bootstrap procedure niet ondersteund.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure niet ondersteund.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_VERSION" {
        give fluent_diagnostic_fields("bootstrap version is ongeldig", "bootstrap version is ongeldig.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap version is ongeldig.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "INSTALL_E_VITTE_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vitte help command mislukt", "installed vitte help command mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vitte help command mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_VITTEC_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vittec help command mislukt", "installed vittec help command mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vittec help command mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("installed package import test mislukt", "installed package import test mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed package import test mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("installed shell completion is ontbrekend", "installed shell completion is ontbrekend.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed shell completion is ontbrekend.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_CC_MISSING" {
        give fluent_diagnostic_fields("macOS package build requires cc", "macOS package build requires cc.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires cc.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_LIPO_MISSING" {
        give fluent_diagnostic_fields("universal macOS package build requires lipo", "universal macOS package build requires lipo.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "universal macOS package build requires lipo.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_MACOS_REQUIRED" {
        give fluent_diagnostic_fields("macOS package build requires macOS", "macOS package build requires macOS.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires macOS.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_SOURCE_MISSING" {
        give fluent_diagnostic_fields("package bron bestand is ontbrekend", "package bron bestand is ontbrekend.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "package bron bestand is ontbrekend.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_HELP_FAILED" {
        give fluent_diagnostic_fields("postinstall vitte help check mislukt", "postinstall vitte help check mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall vitte help check mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("postinstall package import check mislukt", "postinstall package import check mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall package import check mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("postinstall shell completion check mislukt", "postinstall shell completion check mislukt.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall shell completion check mislukt.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "LIMIT_FILE_SIZE_MAX" {
        give fluent_diagnostic_fields("bestand size max", "bestand size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "bestand size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_TOKEN_SIZE_MAX" {
        give fluent_diagnostic_fields("token size max", "token size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "token size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_AST_DEPTH_MAX" {
        give fluent_diagnostic_fields("ast depth max", "ast depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ast depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_EXPR_DEPTH_MAX" {
        give fluent_diagnostic_fields("expr depth max", "expr depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expr depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_IMPORT_DEPTH_MAX" {
        give fluent_diagnostic_fields("import depth max", "import depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "import depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MODULE_COUNT_MAX" {
        give fluent_diagnostic_fields("module count max", "module count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "module count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_DIAGNOSTICS_MAX" {
        give fluent_diagnostic_fields("te many diagnostiek emitted", "te many diagnostiek emitted.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "te many diagnostiek emitted.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_SYMBOL_COUNT_MAX" {
        give fluent_diagnostic_fields("symbool count max", "symbool count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "symbool count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_PARSER_RECURSION_MAX" {
        give fluent_diagnostic_fields("parser recursion max", "parser recursion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "parser recursion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MACRO_EXPANSION_MAX" {
        give fluent_diagnostic_fields("macro expansion max", "macro expansion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "MACRO_E_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("expansion mislukt", "expansion mislukt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expansion mislukt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RECURSION_LIMIT" {
        give fluent_diagnostic_fields("recursion limit", "recursion limit.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "recursion limit.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("argument ongeldig", "argument ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "argument ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MACRO" {
        give fluent_diagnostic_fields("macro onbekend", "macro onbekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro onbekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSTABLE_FEATURE" {
        give fluent_diagnostic_fields("unstable functie", "unstable functie.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unstable functie.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "SYNTAX_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "identifier verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "expressie verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "type verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "patroon verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "blok verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "scheidingsteken verwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "token onverwacht.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unbalanced scheidingsteken.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "attribute ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "declaratie ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "statement ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "expressie ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "patroon ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "literal ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "operator ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "modifier ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "body ontbreekt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "return ontbreekt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "naam dubbel.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "naam onbekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "type onbekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "module onbekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "lid onbekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ambiguous naam.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "private symbool.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "import ontbrekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "import cyclus.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "export conflict.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "arity mismatch.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "argument mismatch.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "toewijzing mismatch.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "branch mismatch.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "aanroep doel is not callable.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "conversie ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "index ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "deref ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "lening ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "verplaatsing ongeldig.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after verplaatsing.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after vrijgave.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use before init.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "double vrijgave.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "lening conflict.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "mutability conflict.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "levensduur te short.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "dangling referentie.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "non exhaustive match.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "onbereikbaar patroon.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const required.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const overloop.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const deling by nul.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const cyclus.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro ontbrekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro recursion.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro expansion mislukt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait not implemented.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait ambiguous.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "generiek argument ontbrekend.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "generiek bound mislukt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "doel niet ondersteund.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "abi mismatch.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "link mislukt.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "runtime panic.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "NAME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "identifier verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "expressie verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "type verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "patroon verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "blok verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "scheidingsteken verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "token onverwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced scheidingsteken.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "declaratie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "statement ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "expressie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "patroon ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "literal ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "operator ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body ontbreekt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "return ontbreekt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "naam dubbel.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "naam onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "type onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "module onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lid onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous naam.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private symbool.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "import ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "import cyclus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "argument mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "toewijzing mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "aanroep doel is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "conversie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "index ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lening ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "verplaatsing ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after verplaatsing.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after vrijgave.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double vrijgave.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lening conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "levensduur te short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling referentie.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "onbereikbaar patroon.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const overloop.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const deling by nul.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const cyclus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generiek argument ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generiek bound mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "doel niet ondersteund.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime panic.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "identifier verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "expressie verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "type verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "patroon verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "blok verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "scheidingsteken verwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "token onverwacht.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced scheidingsteken.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "declaratie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "statement ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "expressie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "patroon ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "literal ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "operator ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body ontbreekt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "return ontbreekt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "naam dubbel.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "naam onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "type onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "module onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lid onbekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous naam.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private symbool.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "import ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "import cyclus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "argument mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "toewijzing mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "aanroep doel is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "conversie ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "index ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lening ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "verplaatsing ongeldig.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after verplaatsing.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after vrijgave.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double vrijgave.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "lening conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability conflict.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "levensduur te short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling referentie.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "onbereikbaar patroon.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const overloop.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const deling by nul.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const cyclus.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generiek argument ontbrekend.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "generiek bound mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "doel niet ondersteund.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi mismatch.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link mislukt.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime panic.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "identifier verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "scheidingsteken verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "token onverwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced scheidingsteken.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "declaratie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "statement ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "literal ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "operator ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "return ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "module onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lid onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous naam.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private symbool.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "argument mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "toewijzing mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "aanroep doel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conversie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "index ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "verplaatsing ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after verplaatsing.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "levensduur te short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling referentie.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "onbereikbaar patroon.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const overloop.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const deling by nul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek argument ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek bound mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "doel niet ondersteund.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panic.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "identifier verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "scheidingsteken verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "token onverwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced scheidingsteken.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "declaratie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "statement ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "literal ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "operator ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "return ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "module onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lid onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous naam.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private symbool.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "argument mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "toewijzing mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "aanroep doel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conversie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "index ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "verplaatsing ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after verplaatsing.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "levensduur te short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling referentie.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "onbereikbaar patroon.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const overloop.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const deling by nul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek argument ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek bound mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "doel niet ondersteund.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panic.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "identifier verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "blok verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "scheidingsteken verwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "token onverwacht.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced scheidingsteken.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "declaratie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "statement ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expressie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "patroon ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "literal ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "operator ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "return ontbreekt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam dubbel.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "naam onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "module onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lid onbekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous naam.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private symbool.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "import cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "argument mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "toewijzing mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "aanroep doel is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conversie ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "index ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "verplaatsing ongeldig.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after verplaatsing.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double vrijgave.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "lening conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability conflict.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "levensduur te short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling referentie.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "onbereikbaar patroon.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const overloop.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const deling by nul.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const cyclus.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek argument ontbrekend.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "generiek bound mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "doel niet ondersteund.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi mismatch.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link mislukt.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime panic.", "make the expression type match the type contract named by the type checker");
    }

    if code == "OWNERSHIP_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "identifier verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "expressie verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "type verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "patroon verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "blok verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "scheidingsteken verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "token onverwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced scheidingsteken.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "declaratie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "statement ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "expressie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "patroon ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "literal ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "operator ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body ontbreekt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "return ontbreekt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "naam dubbel.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "naam onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "type onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "module onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lid onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous naam.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private symbool.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "import ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "import cyclus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "argument mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "toewijzing mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "aanroep doel is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "conversie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "index ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lening ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "verplaatsing ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after verplaatsing.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lening conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "levensduur te short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling referentie.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "onbereikbaar patroon.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const overloop.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const deling by nul.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const cyclus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generiek argument ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generiek bound mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "doel niet ondersteund.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime panic.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "identifier verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "expressie verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "type verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "patroon verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "blok verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "scheidingsteken verwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "token onverwacht.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced scheidingsteken.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "declaratie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "statement ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "expressie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "patroon ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "literal ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "operator ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body ontbreekt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "return ontbreekt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "naam dubbel.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "naam onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "type onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "module onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lid onbekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous naam.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private symbool.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "import ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "import cyclus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "argument mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "toewijzing mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "aanroep doel is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "conversie ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "index ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lening ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "verplaatsing ongeldig.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after verplaatsing.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double vrijgave.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "lening conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability conflict.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "levensduur te short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling referentie.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "onbereikbaar patroon.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const overloop.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const deling by nul.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const cyclus.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generiek argument ontbrekend.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "generiek bound mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "doel niet ondersteund.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi mismatch.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link mislukt.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime panic.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "identifier verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "expressie verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "type verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "patroon verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "blok verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "scheidingsteken verwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "token onverwacht.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unbalanced scheidingsteken.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "attribute ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "declaratie ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "statement ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "expressie ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "patroon ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "literal ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "operator ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "modifier ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "body ontbreekt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "return ontbreekt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "naam dubbel.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "naam onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "type onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "module onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "lid onbekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ambiguous naam.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "private symbool.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "import ontbrekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "import cyclus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "export conflict.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "arity mismatch.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "argument mismatch.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "toewijzing mismatch.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "branch mismatch.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "aanroep doel is not callable.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "conversie ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "index ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "deref ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "lening ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "verplaatsing ongeldig.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after verplaatsing.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after vrijgave.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use before init.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "double vrijgave.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "lening conflict.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutability conflict.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "levensduur te short.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "dangling referentie.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non exhaustive match.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "onbereikbaar patroon.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const required.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const overloop.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const deling by nul.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const cyclus.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro ontbrekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro recursion.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro expansion mislukt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait not implemented.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait ambiguous.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "generiek argument ontbrekend.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "generiek bound mislukt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "doel niet ondersteund.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "abi mismatch.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "link mislukt.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "runtime panic.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MACRO_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "identifier verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expressie verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "type verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "patroon verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "blok verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "scheidingsteken verwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "token onverwacht.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unbalanced scheidingsteken.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "attribute ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "declaratie ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "statement ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expressie ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "patroon ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "literal ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "operator ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "modifier ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "body ontbreekt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "return ontbreekt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "naam dubbel.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "naam onbekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "type onbekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "module onbekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "lid onbekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ambiguous naam.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "private symbool.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "import ontbrekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "import cyclus.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "export conflict.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "arity mismatch.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "argument mismatch.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "toewijzing mismatch.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "branch mismatch.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "my_macro(value)", "aanroep doel is not callable.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "conversie ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "index ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "deref ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "lening ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "verplaatsing ongeldig.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after verplaatsing.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after vrijgave.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use before init.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "double vrijgave.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "lening conflict.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "mutability conflict.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "levensduur te short.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "dangling referentie.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "non exhaustive match.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "onbereikbaar patroon.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const required.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const overloop.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const deling by nul.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const cyclus.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro ontbrekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro recursion.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro expansion mislukt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait not implemented.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait ambiguous.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "generiek argument ontbrekend.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "generiek bound mislukt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "doel niet ondersteund.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "abi mismatch.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "link mislukt.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "runtime panic.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "HIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "identifier verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "scheidingsteken verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "token onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced scheidingsteken.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "declaratie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "statement ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "literal ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "operator ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "return ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam dubbel.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "module onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lid onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous naam.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private symbool.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "argument mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "toewijzing mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "aanroep doel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "conversie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "index ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verplaatsing ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after verplaatsing.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "levensduur te short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling referentie.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "onbereikbaar patroon.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const overloop.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const deling by nul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek argument ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek bound mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "doel niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panic.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "identifier verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "scheidingsteken verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "token onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced scheidingsteken.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "declaratie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "statement ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "literal ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "operator ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "return ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam dubbel.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "module onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lid onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous naam.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private symbool.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "argument mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "toewijzing mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "aanroep doel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "conversie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "index ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verplaatsing ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after verplaatsing.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "levensduur te short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling referentie.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "onbereikbaar patroon.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const overloop.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const deling by nul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek argument ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek bound mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "doel niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panic.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "identifier verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "blok verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "scheidingsteken verwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "token onverwacht.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced scheidingsteken.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "declaratie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "statement ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expressie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "patroon ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "literal ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "operator ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "return ontbreekt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam dubbel.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "naam onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "type onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "module onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lid onbekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous naam.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private symbool.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "import cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "argument mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "toewijzing mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "aanroep doel is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "conversie ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "index ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verplaatsing ongeldig.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after verplaatsing.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double vrijgave.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lening conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability conflict.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "levensduur te short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling referentie.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "onbereikbaar patroon.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const overloop.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const deling by nul.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const cyclus.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek argument ontbrekend.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "generiek bound mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "doel niet ondersteund.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi mismatch.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link mislukt.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime panic.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "identifier verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "expressie verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "type verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "patroon verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "blok verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "scheidingsteken verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "token onverwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced scheidingsteken.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "declaratie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "statement ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "expressie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "patroon ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "literal ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "operator ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body ontbreekt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "return ontbreekt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "naam dubbel.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "naam onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "type onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "module onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lid onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous naam.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private symbool.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "import ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "import cyclus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "argument mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "toewijzing mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "aanroep doel is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "conversie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "index ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lening ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "verplaatsing ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after verplaatsing.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after vrijgave.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double vrijgave.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lening conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "levensduur te short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling referentie.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "onbereikbaar patroon.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const overloop.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const deling by nul.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const cyclus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generiek argument ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generiek bound mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime panic.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "identifier verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "expressie verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "type verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "patroon verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "blok verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "scheidingsteken verwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "token onverwacht.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced scheidingsteken.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "declaratie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "statement ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "expressie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "patroon ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "literal ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "operator ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body ontbreekt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "return ontbreekt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "naam dubbel.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "naam onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "type onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "module onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lid onbekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous naam.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private symbool.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "import ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "import cyclus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "argument mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "toewijzing mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "aanroep doel is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "conversie ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "index ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lening ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "verplaatsing ongeldig.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after verplaatsing.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after vrijgave.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double vrijgave.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "lening conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability conflict.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "levensduur te short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling referentie.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "onbereikbaar patroon.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const overloop.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const deling by nul.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const cyclus.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generiek argument ontbrekend.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "generiek bound mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "doel niet ondersteund.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi mismatch.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link mislukt.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime panic.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "identifier verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "expressie verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "type verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "patroon verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "blok verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "scheidingsteken verwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "token onverwacht.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "unbalanced scheidingsteken.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "attribute ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "declaratie ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "statement ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "expressie ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "patroon ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "literal ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "operator ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "modifier ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "body ontbreekt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "return ontbreekt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "naam dubbel.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "naam onbekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "type onbekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "module onbekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "lid onbekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ambiguous naam.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "private symbool.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "import ontbrekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "import cyclus.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "export conflict.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "arity mismatch.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "argument mismatch.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "toewijzing mismatch.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "branch mismatch.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "if index < len(items) { give items[index] }", "aanroep doel is not callable.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "conversie ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "index ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "deref ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "lening ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "verplaatsing ongeldig.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after verplaatsing.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after vrijgave.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use before init.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "double vrijgave.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "lening conflict.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "mutability conflict.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "levensduur te short.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "dangling referentie.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "non exhaustive match.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "onbereikbaar patroon.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const required.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const overloop.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const deling by nul.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const cyclus.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro ontbrekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro recursion.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro expansion mislukt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait not implemented.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait ambiguous.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "generiek argument ontbrekend.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "generiek bound mislukt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "doel niet ondersteund.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "abi mismatch.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "link mislukt.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "runtime panic.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "identifier verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "expressie verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "type verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "patroon verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "blok verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "scheidingsteken verwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "token onverwacht.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "unbalanced scheidingsteken.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "attribute ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "declaratie ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "statement ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "expressie ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "patroon ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "literal ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "operator ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "modifier ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "body ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "return ontbreekt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "naam dubbel.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "naam onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "type onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "module onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "lid onbekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ambiguous naam.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "private symbool.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "import ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "import cyclus.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "export conflict.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "arity mismatch.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "argument mismatch.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "toewijzing mismatch.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "branch mismatch.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "aanroep doel is not callable.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "conversie ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "index ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "deref ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "lening ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "verplaatsing ongeldig.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after verplaatsing.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after vrijgave.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use before init.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "double vrijgave.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "lening conflict.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mutability conflict.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "levensduur te short.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "dangling referentie.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "non exhaustive match.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "onbereikbaar patroon.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const required.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const overloop.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const deling by nul.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const cyclus.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro recursion.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro expansion mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait not implemented.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait ambiguous.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "generiek argument ontbrekend.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "generiek bound mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "doel niet ondersteund.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "abi mismatch.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link mislukt.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "runtime panic.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LIMIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("identifier verwacht", "identifier verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "identifier verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("expressie verwacht", "expressie verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expressie verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("type verwacht", "type verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "type verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("patroon verwacht", "patroon verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "patroon verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("blok verwacht", "blok verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "blok verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("scheidingsteken verwacht", "scheidingsteken verwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "scheidingsteken verwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("token onverwacht", "token onverwacht.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "token onverwacht.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced scheidingsteken", "unbalanced scheidingsteken.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "unbalanced scheidingsteken.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute ongeldig", "attribute ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "attribute ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("declaratie ongeldig", "declaratie ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "declaratie ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("statement ongeldig", "statement ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "statement ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("expressie ongeldig", "expressie ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expressie ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("patroon ongeldig", "patroon ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "patroon ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("literal ongeldig", "literal ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "literal ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("operator ongeldig", "operator ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "operator ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier ongeldig", "modifier ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "modifier ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body ontbreekt", "body ontbreekt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "body ontbreekt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give ontbreekt", "return ontbreekt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "return ontbreekt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("naam dubbel", "naam dubbel.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "naam dubbel.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("naam onbekend", "naam onbekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "naam onbekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("type onbekend", "type onbekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "type onbekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("module onbekend", "module onbekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "module onbekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("lid onbekend", "lid onbekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "lid onbekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous naam", "ambiguous naam.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ambiguous naam.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private symbool", "private symbool.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "private symbool.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("import ontbrekend", "import ontbrekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "import ontbrekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("import cyclus", "import cyclus.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "import cyclus.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export conflict", "export conflict.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "export conflict.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity mismatch", "arity mismatch.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "arity mismatch.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("argument mismatch", "argument mismatch.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "argument mismatch.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("toewijzing mismatch", "toewijzing mismatch.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "toewijzing mismatch.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch mismatch", "branch mismatch.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "branch mismatch.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("aanroep doel is not callable", "aanroep doel is not callable.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "aanroep doel is not callable.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("conversie ongeldig", "conversie ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "conversie ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("index ongeldig", "index ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "index ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref ongeldig", "deref ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "deref ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("lening ongeldig", "lening ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "lening ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("verplaatsing ongeldig", "verplaatsing ongeldig.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "verplaatsing ongeldig.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after verplaatsing", "use after verplaatsing.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after verplaatsing.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after vrijgave", "use after vrijgave.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after vrijgave.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use before init.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double vrijgave", "double vrijgave.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "double vrijgave.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("lening conflict", "lening conflict.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "lening conflict.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability conflict", "mutability conflict.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "mutability conflict.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("levensduur te short", "levensduur te short.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "levensduur te short.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling referentie", "dangling referentie.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "dangling referentie.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "non exhaustive match.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("onbereikbaar patroon", "onbereikbaar patroon.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "onbereikbaar patroon.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const required.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const overloop", "const overloop.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const overloop.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const deling by nul", "const deling by nul.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const deling by nul.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const cyclus", "const cyclus.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const cyclus.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro ontbrekend", "macro ontbrekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro ontbrekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro recursion.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion mislukt", "macro expansion mislukt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion mislukt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait not implemented.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait ambiguous.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("generiek argument ontbrekend", "generiek argument ontbrekend.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "generiek argument ontbrekend.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("generiek bound mislukt", "generiek bound mislukt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "generiek bound mislukt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("doel niet ondersteund", "doel niet ondersteund.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "doel niet ondersteund.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi mismatch", "abi mismatch.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "abi mismatch.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link mislukt", "link mislukt.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "link mislukt.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime panic", "runtime panic.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "runtime panic.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    give fluent_diagnostic_fields_empty();
}
