space vitte/compiler/infrastructure/diagnostics/fluent

use vitte/compiler/infrastructure/diagnostics/fluent/types { FluentMessage }

form FluentCatalog {
  locale: string,
  messages: [FluentMessage],
}

proc empty_catalog(locale: string) -> FluentCatalog {
  give FluentCatalog { locale: locale, messages: [] }
}
