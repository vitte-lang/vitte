space vitte/compiler/infrastructure/diagnostics/fluent_catalog_ar

export *

form FluentDiagnosticFields {
    message: string
    primary_label: string
    secondary_label: string
    cause: string
    help: string
    fix_it: string
    corrected_example: string
    note: string
    lsp_code_action_title: string
    valid: bool
}

proc fluent_diagnostic_fields(message: string, primary_label: string, secondary_label: string, cause: string, help: string, fix_it: string, corrected_example: string, note: string, lsp_code_action_title: string) -> FluentDiagnosticFields {
    give FluentDiagnosticFields {
        message: message,
        primary_label: primary_label,
        secondary_label: secondary_label,
        cause: cause,
        help: help,
        fix_it: fix_it,
        corrected_example: corrected_example,
        note: note,
        lsp_code_action_title: lsp_code_action_title,
        valid: message != "" and primary_label != "" and cause != "" and help != ""
    };
}

proc fluent_diagnostic_fields_empty() -> FluentDiagnosticFields {
    give fluent_diagnostic_fields("", "", "", "", "", "", "", "", "");
}

proc fluent_catalog_fields_ar(code: string) -> FluentDiagnosticFields {
    if code == "E0001" {
        give fluent_diagnostic_fields("معرف متوقع", "The محلل expected a اسم for something (variable, نوع, وحدة, etc.).", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The محلل expected a اسم for something (variable, نوع, وحدة, etc.).", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0002" {
        give fluent_diagnostic_fields("تعبير متوقع", "The محلل expected an تعبير at this location.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The محلل expected an تعبير at this location.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0003" {
        give fluent_diagnostic_fields("نمط متوقع", "The محلل expected a نمط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The محلل expected a نمط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0004" {
        give fluent_diagnostic_fields("نوع متوقع", "The محلل expected a نوع اسم.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The محلل expected a نوع اسم.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0005" {
        give fluent_diagnostic_fields("'end' متوقع", "A كتلة was opened but not closed with 'end' or '.end'.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A كتلة was opened but not closed with 'end' or '.end'.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0006" {
        give fluent_diagnostic_fields("proc after attribute متوقع", "An attribute يجب ان يكون followed by a proc تصريح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "An attribute يجب ان يكون followed by a proc تصريح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0007" {
        give fluent_diagnostic_fields("top-level تصريح متوقع", "The محلل expected a top-level تصريح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The محلل expected a top-level تصريح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0008" {
        give fluent_diagnostic_fields("extern proc لا يمكن ان يملك جسما", "A نمط bound the same اسم more than once.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A نمط bound the same اسم more than once.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0009" {
        give fluent_diagnostic_fields("proc requires a body unless marked #[extern]", "A referenced نوع اسم was مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A referenced نوع اسم was مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0010" {
        give fluent_diagnostic_fields("نوع alias requires a هدف نوع", "The base نوع of a عام was مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "The base نوع of a عام was مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0011" {
        give fluent_diagnostic_fields("select يتطلب when branch واحدا على الاقل", "A عام نوع must include at least one وسيط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "A عام نوع must include at least one وسيط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0012" {
        give fluent_diagnostic_fields("select branch يجب ان يكون a when عبارة", "This نوع form is not supported yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let p: *int = &value", "This نوع form is not supported yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0013" {
        give fluent_diagnostic_fields("قد تنتهي procedure دون give", "قد يخرج proc دون ارجاع قيمة.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc to_code(ok: bool) -> int {\\n  if ok { give 0 }\\n  give 1\\n}", "قد يخرج proc دون ارجاع قيمة.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0014" {
        give fluent_diagnostic_fields("بناء مستوى علوي محظور في وضع strict-core", "An invocation is مفقود its هدف الاستدعاء.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "print(\\\"hi\\\")", "An invocation is مفقود its هدف الاستدعاء.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0015" {
        give fluent_diagnostic_fields("بناء عبارة محظور في وضع strict-core", "This تعبير is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "let x = value", "This تعبير is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0016" {
        give fluent_diagnostic_fields("بناء تعبير محظور في وضع strict-core", "This نمط is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This نمط is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0017" {
        give fluent_diagnostic_fields("'}' متوقع", "This عبارة is not supported by the HIR lowering yet.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "This عبارة is not supported by the HIR lowering yet.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E0018" {
        give fluent_diagnostic_fields("متوقع 'case' او 'otherwise' في match", "An extern procedure cannot define a body.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "#[extern]\\nproc puts(s: string) -> int", "An extern procedure cannot define a body.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E1001" {
        give fluent_diagnostic_fields("نمط ربط مكرر", "نمط ربط مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "نمط ربط مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1002" {
        give fluent_diagnostic_fields("نوع (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) مجهول", "نوع (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "نوع (did you mean a built-in like int/i32/i64/i128/u32/u64/u128/bool/string?) مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1003" {
        give fluent_diagnostic_fields("عام base نوع مجهول", "عام base نوع مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "عام base نوع مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1004" {
        give fluent_diagnostic_fields("عام نوع يتطلب وسيط واحدا على الاقل", "عام نوع يتطلب وسيط واحدا على الاقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "عام نوع يتطلب وسيط واحدا على الاقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1005" {
        give fluent_diagnostic_fields("معرف مجهول", "معرف مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "معرف مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1006" {
        give fluent_diagnostic_fields("عام نوع يتطلب نوع وسيط واحدا على الاقل", "عام نوع يتطلب نوع وسيط واحدا على الاقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "عام نوع يتطلب نوع وسيط واحدا على الاقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1007" {
        give fluent_diagnostic_fields("تحويل between signed and unsigned values غير صالح", "تحويل between signed and unsigned values غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "تحويل between signed and unsigned values غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1010" {
        give fluent_diagnostic_fields("stdlib وحدة مرفوض بواسطة active stdlib ملف تعريف", "stdlib وحدة مرفوض بواسطة active stdlib ملف تعريف.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib وحدة مرفوض بواسطة active stdlib ملف تعريف.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1011" {
        give fluent_diagnostic_fields("strict-imports requires explicit alias", "strict-imports requires explicit alias.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports requires explicit alias.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1012" {
        give fluent_diagnostic_fields("strict-imports يمنع unused استيراد aliases", "strict-imports يمنع unused استيراد aliases.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports يمنع unused استيراد aliases.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1013" {
        give fluent_diagnostic_fields("strict-imports يمنع non-canonical استيراد paths", "strict-imports يمنع non-canonical استيراد paths.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-imports يمنع non-canonical استيراد paths.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1014" {
        give fluent_diagnostic_fields("stdlib وحدة مفقود", "stdlib وحدة مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "stdlib وحدة مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1015" {
        give fluent_diagnostic_fields("experimental وحدة استيراد denied", "experimental وحدة استيراد denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "experimental وحدة استيراد denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1016" {
        give fluent_diagnostic_fields("داخلي وحدة استيراد denied", "داخلي وحدة استيراد denied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "داخلي وحدة استيراد denied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1017" {
        give fluent_diagnostic_fields("re-export رمز تعارض", "re-export رمز تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "re-export رمز تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1018" {
        give fluent_diagnostic_fields("ambiguous استيراد مسار", "ambiguous استيراد مسار.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "ambiguous استيراد مسار.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1019" {
        give fluent_diagnostic_fields("strict-modules يمنع glob imports", "strict-modules يمنع glob imports.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "strict-modules يمنع glob imports.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1020" {
        give fluent_diagnostic_fields("legacy استيراد مسار مهمل", "legacy استيراد مسار مهمل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "legacy استيراد مسار مهمل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1021" {
        give fluent_diagnostic_fields("entry وحدة مسار يجب ان يكون canonical", "entry وحدة مسار يجب ان يكون canonical.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry وحدة مسار يجب ان يكون canonical.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1022" {
        give fluent_diagnostic_fields("entry اسم مكرر", "entry اسم مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "entry اسم مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1023" {
        give fluent_diagnostic_fields("share يشير الى رمز مجهول", "share يشير الى رمز مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share يشير الى رمز مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1024" {
        give fluent_diagnostic_fields("رمز in share list مكرر", "رمز in share list مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "رمز in share list مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1025" {
        give fluent_diagnostic_fields("رمز غير مصدر بواسطة وحدة", "رمز غير مصدر بواسطة وحدة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "رمز غير مصدر بواسطة وحدة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1026" {
        give fluent_diagnostic_fields("share تصريح مكرر", "share تصريح مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "share تصريح مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1027" {
        give fluent_diagnostic_fields("استيراد ربط مكرر", "استيراد ربط مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "استيراد ربط مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1028" {
        give fluent_diagnostic_fields("استيراد ربط يتعارض مع محلي تصريح", "استيراد ربط يتعارض مع محلي تصريح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "استيراد ربط يتعارض مع محلي تصريح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1029" {
        give fluent_diagnostic_fields("محلي تصريح اسم مكرر", "محلي تصريح اسم مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "محلي تصريح اسم مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1030" {
        give fluent_diagnostic_fields("وحدة alias عضو not exported", "وحدة alias عضو not exported.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "وحدة alias عضو not exported.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1031" {
        give fluent_diagnostic_fields("qualified نوع عضو مفقود", "qualified نوع عضو مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "qualified نوع عضو مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E1032" {
        give fluent_diagnostic_fields("التعبير غير قابل للاستدعاء", "التعبير غير قابل للاستدعاء.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "vitte check path/to/file.vit", "التعبير غير قابل للاستدعاء.", "make the expression type match the type contract named by the type checker");
    }

    if code == "E2001" {
        give fluent_diagnostic_fields("نوع غير مدعوم", "نوع غير مدعوم.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2002" {
        give fluent_diagnostic_fields("invoke has no هدف الاستدعاء", "invoke has no هدف الاستدعاء.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "invoke has no هدف الاستدعاء.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2003" {
        give fluent_diagnostic_fields("تعبير in HIR غير مدعوم", "تعبير in HIR غير مدعوم.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير in HIR غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2004" {
        give fluent_diagnostic_fields("نمط in HIR غير مدعوم", "نمط in HIR غير مدعوم.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط in HIR غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2005" {
        give fluent_diagnostic_fields("عبارة in HIR غير مدعوم", "عبارة in HIR غير مدعوم.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عبارة in HIR غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2006" {
        give fluent_diagnostic_fields("HIR نوع kind غير متوقع", "HIR نوع kind غير متوقع.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR نوع kind غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2007" {
        give fluent_diagnostic_fields("HIR expr kind غير متوقع", "HIR expr kind غير متوقع.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR expr kind غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2008" {
        give fluent_diagnostic_fields("HIR stmt kind غير متوقع", "HIR stmt kind غير متوقع.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR stmt kind غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2009" {
        give fluent_diagnostic_fields("HIR نمط kind غير متوقع", "HIR نمط kind غير متوقع.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR نمط kind غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "E2010" {
        give fluent_diagnostic_fields("HIR decl kind غير متوقع", "HIR decl kind غير متوقع.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "HIR decl kind غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "LEX_E_INVALID_CHAR" {
        give fluent_diagnostic_fields("حرف غير صالح", "حرف غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "حرف غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_NUMBER" {
        give fluent_diagnostic_fields("numeric قيمة حرفية غير صالح", "numeric قيمة حرفية غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "numeric قيمة حرفية غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_ESCAPE" {
        give fluent_diagnostic_fields("هروب sequence غير صالح", "هروب sequence غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "هروب sequence غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE_ESCAPE" {
        give fluent_diagnostic_fields("unicode هروب sequence غير صالح", "unicode هروب sequence غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode هروب sequence غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UTF8" {
        give fluent_diagnostic_fields("UTF-8 byte sequence غير صالح", "UTF-8 byte sequence غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "UTF-8 byte sequence غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_CHAR_LITERAL" {
        give fluent_diagnostic_fields("حرف قيمة حرفية غير صالح", "حرف قيمة حرفية غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "حرف قيمة حرفية غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف غير صالح", "معرف غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "معرف غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_STRING" {
        give fluent_diagnostic_fields("string قيمة حرفية غير منته", "string قيمة حرفية غير منته.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add the closing `\"` on the same line; do not close a string with a single quote", "print(\"message\")", "string قيمة حرفية غير منته.", "add the closing `\"` on the same line; do not close a string with a single quote");
    }

    if code == "LEX_E_UNTERMINATED_CHAR" {
        give fluent_diagnostic_fields("حرف قيمة حرفية غير منته", "حرف قيمة حرفية غير منته.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "حرف قيمة حرفية غير منته.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_BLOCK_COMMENT" {
        give fluent_diagnostic_fields("كتلة comment غير منته", "كتلة comment غير منته.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "كتلة comment غير منته.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_REGION_COMMENT" {
        give fluent_diagnostic_fields("region comment غير منته", "region comment غير منته.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "region comment غير منته.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNEXPECTED_EOF" {
        give fluent_diagnostic_fields("end of ملف غير متوقع", "end of ملف غير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "end of ملف غير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_TOKEN_TOO_LARGE" {
        give fluent_diagnostic_fields("رمز لغوي زائد", "رمز لغوي زائد.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "رمز لغوي زائد.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PLOOP" {
        give fluent_diagnostic_fields("المحلل لم يتقدم", "المحلل لم يتقدم.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "المحلل لم يتقدم.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PPRIMARY999" {
        give fluent_diagnostic_fields("تعبير رمز لغوي غير متوقع", "تعبير رمز لغوي غير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تعبير رمز لغوي غير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PSTMT007" {
        give fluent_diagnostic_fields("تعيين عامل متوقع", "تعيين عامل متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تعيين عامل متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P0001" {
        give fluent_diagnostic_fields("top-level رمز لغوي غير متوقع", "top-level رمز لغوي غير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level رمز لغوي غير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "P000_UNBALANCED" {
        give fluent_diagnostic_fields("كتلة غير مغلق", "كتلة غير مغلق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "كتلة غير مغلق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "E_CLI_IO" {
        give fluent_diagnostic_fields("cannot read ادخال ملف", "The compiler cannot read the requested ادخال ملف.", "The requested file does not exist, is not a regular file, or is not readable.", "The requested file does not exist, is not a regular file, or is not readable.", "Verify the path passed to `vitte build` and the file permissions.", "use an existing readable source path", "vitte build src/main.vit -o build/main", "The compiler cannot read the requested ادخال ملف.", "use an existing readable source path");
    }

    if code == "E_CLI_MISSING_ARG" {
        give fluent_diagnostic_fields("command وسيط مفقود", "command وسيط مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "command وسيط مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_COMMAND" {
        give fluent_diagnostic_fields("compiler command مجهول", "compiler command مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_UNKNOWN_OPTION" {
        give fluent_diagnostic_fields("compiler option مجهول", "compiler option مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_CLI_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("compiler وسيط غير صالح", "compiler وسيط غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler وسيط غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "E_IO_FILE_NOT_FOUND" {
        give fluent_diagnostic_fields("ادخال ملف مفقود", "ادخال ملف مفقود.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "ادخال ملف مفقود.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_FILE_UNREADABLE" {
        give fluent_diagnostic_fields("ادخال ملف is not readable", "ادخال ملف is not readable.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "ادخال ملف is not readable.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OUTPUT_UNWRITABLE" {
        give fluent_diagnostic_fields("cannot كتابة اخراج ملف", "cannot كتابة اخراج ملف.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "cannot كتابة اخراج ملف.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "permission denied.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_DIRECTORY_NOT_FOUND" {
        give fluent_diagnostic_fields("اخراج directory مفقود", "اخراج directory مفقود.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "اخراج directory مفقود.", "use an existing readable input path and a writable output destination");
    }

    if code == "E_IO_OVERWRITE_FORBIDDEN" {
        give fluent_diagnostic_fields("refusing to overwrite existing اخراج", "refusing to overwrite existing اخراج.", "The compiler could not read an input resource or create the requested output resource.", "The compiler could not read an input resource or create the requested output resource.", "Verify that the path exists, is accessible, and that its parent directory is writable.", "use an existing readable input path and a writable output destination", "vitte build src/main.vit -o build/main", "refusing to overwrite existing اخراج.", "use an existing readable input path and a writable output destination");
    }

    if code == "DRIVER_E_PIPELINE_FAILURE" {
        give fluent_diagnostic_fields("compiler pipeline فشل", "compiler pipeline فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler pipeline فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_FRONTEND_INCOMPLETE" {
        give fluent_diagnostic_fields("analysis frontend incomplete", "analysis frontend incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis frontend incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("analysis غير صالح", "analysis غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "analysis غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROWCK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("borrowck not executed", "borrowck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "borrowck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CODEGEN_IR_INCOMPLETE" {
        give fluent_diagnostic_fields("توليد الكود ir incomplete", "توليد الكود ir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "توليد الكود ir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INPUT_NOT_LOADED" {
        give fluent_diagnostic_fields("frontend ادخال not loaded", "frontend ادخال not loaded.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend ادخال not loaded.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_FRONTEND_INVALID" {
        give fluent_diagnostic_fields("frontend غير صالح", "frontend غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "frontend غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_PROFILE" {
        give fluent_diagnostic_fields("link ملف تعريف غير صالح", "link ملف تعريف غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link ملف تعريف غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_INVALID" {
        give fluent_diagnostic_fields("ir غير صالح", "ir غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IR_MIR_INCOMPLETE" {
        give fluent_diagnostic_fields("ir mir incomplete", "ir mir incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ir mir incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_CODEGEN_INCOMPLETE" {
        give fluent_diagnostic_fields("link توليد الكود incomplete", "link توليد الكود incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link توليد الكود incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INCOMPLETE" {
        give fluent_diagnostic_fields("mir analysis incomplete", "mir analysis incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_ANALYSIS_INVALID" {
        give fluent_diagnostic_fields("mir analysis غير صالح", "mir analysis غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir analysis غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MIR_INVALID" {
        give fluent_diagnostic_fields("mir غير صالح", "mir غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mir غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NO_INPUT" {
        give fluent_diagnostic_fields("no ادخال", "no ادخال.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "no ادخال.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_AST" {
        give fluent_diagnostic_fields("parse فارغ ast", "parse فارغ ast.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse فارغ ast.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PARSE_EMPTY_SOURCE" {
        give fluent_diagnostic_fields("parse فارغ مصدر", "parse فارغ مصدر.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "parse فارغ مصدر.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_LINK_INCOMPLETE" {
        give fluent_diagnostic_fields("run link incomplete", "run link incomplete.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run link incomplete.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUN_PROCESS_FAILED" {
        give fluent_diagnostic_fields("run process فشل", "run process فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "run process فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SEMA_NOT_EXECUTED" {
        give fluent_diagnostic_fields("sema not executed", "sema not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "sema not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TYPECK_NOT_EXECUTED" {
        give fluent_diagnostic_fields("typeck not executed", "typeck not executed.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "typeck not executed.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LINK_E_PERMISSION_DENIED" {
        give fluent_diagnostic_fields("permission denied", "permission denied.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "permission denied.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OUTPUT_NOT_MATERIALIZED" {
        give fluent_diagnostic_fields("linker did not create the requested executable", "linker did not create the requested executable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker did not create the requested executable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_C_COMPILER" {
        give fluent_diagnostic_fields("required C compiler was مفقود", "required C compiler was مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "required C compiler was مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_COMMAND_FAILED" {
        give fluent_diagnostic_fields("linker command فشل", "linker command فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "linker command فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LEX_E_INVALID_FLOAT" {
        give fluent_diagnostic_fields("عدد عشري غير صالح", "عدد عشري غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عدد عشري غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_BINARY" {
        give fluent_diagnostic_fields("ثنائي غير صالح", "ثنائي غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ثنائي غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_OCTAL" {
        give fluent_diagnostic_fields("octal غير صالح", "octal غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "octal غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_HEX" {
        give fluent_diagnostic_fields("hex غير صالح", "hex غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "hex غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_INDENTATION" {
        give fluent_diagnostic_fields("ازاحة غير صالح", "ازاحة غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ازاحة غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير صالح", "رمز لغوي غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "رمز لغوي غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_INVALID_UNICODE" {
        give fluent_diagnostic_fields("unicode غير صالح", "unicode غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unicode غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "LEX_E_UNTERMINATED_COMMENT" {
        give fluent_diagnostic_fields("comment غير منته", "comment غير منته.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comment غير منته.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PATTR003" {
        give fluent_diagnostic_fields("pattr003", "pattr003.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "pattr003.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TOPLEVEL_DECL_EXPECTED" {
        give fluent_diagnostic_fields("top-level تصريح expected", "top-level تصريح expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "top-level تصريح expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_INCOMPLETE_EXPR" {
        give fluent_diagnostic_fields("incomplete تعبير", "incomplete تعبير.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "incomplete تعبير.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_RPAREN" {
        give fluent_diagnostic_fields("closing parenthesis مفقود", "closing parenthesis مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "closing parenthesis مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_MISSING_COMMA" {
        give fluent_diagnostic_fields("comma مفقود", "comma مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "comma مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_TYPE_EXPECTED" {
        give fluent_diagnostic_fields("نوع expected", "نوع expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نوع expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PATTERN_EXPECTED" {
        give fluent_diagnostic_fields("نمط expected", "نمط expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نمط expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_BLOCK_EXPECTED" {
        give fluent_diagnostic_fields("كتلة expected", "كتلة expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "كتلة expected.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_UNCLOSED_BLOCK" {
        give fluent_diagnostic_fields("كتلة غير مغلق", "كتلة غير مغلق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "add `}` to close the highlighted block, then run the checker again for its parent block", "while running { set running = false }", "كتلة غير مغلق.", "add `}` to close the highlighted block, then run the checker again for its parent block");
    }

    if code == "PARSE_E_EXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي متوقع", "رمز لغوي متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "رمز لغوي متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_PARAMETER_COLON_EXPECTED" {
        give fluent_diagnostic_fields("colon in procedure parameter مفقود", "parameter colon expected.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "insert `:` between the parameter name and its type, for example `right: f64`", "proc calculate(right: f64) -> f64 { give right }", "parameter colon expected.", "insert `:` between the parameter name and its type, for example `right: f64`");
    }

    if code == "PARSE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "رمز لغوي غير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_E_OLD_VITTE_SYNTAX" {
        give fluent_diagnostic_fields("old Vitte بناء is not canonical", "old Vitte بناء is not canonical.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "The highlighted token belongs to an older Vitte prototype or to another language surface.", "Identify the intended construct, then compare it with the current Vitte keywords.", "replace the old token with the current Vitte keyword only when the construct is equivalent", "proc main() -> int { give 0 }", "old Vitte بناء is not canonical.", "replace the old token with the current Vitte keyword only when the construct is equivalent");
    }

    if code == "PARSE_EXPECTED_EXPR" {
        give fluent_diagnostic_fields("parse expected expr", "parse expected expr.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected expr.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("parse expected نوع", "parse expected نوع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected نوع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("parse expected نمط", "parse expected نمط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected نمط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("parse expected كتلة", "parse expected كتلة.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected كتلة.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "PARSE_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("parse expected معرف", "parse expected معرف.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "parse expected معرف.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FAST0001" {
        give fluent_diagnostic_fields("fast0001", "fast0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "fast0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "FLEX0001" {
        give fluent_diagnostic_fields("flex0001", "flex0001.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "flex0001.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "AST_E_INVALID_NODE" {
        give fluent_diagnostic_fields("عقدة غير صالح", "عقدة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عقدة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_EMPTY_MODULE" {
        give fluent_diagnostic_fields("فارغ وحدة", "فارغ وحدة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فارغ وحدة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_DECL" {
        give fluent_diagnostic_fields("decl غير صالح", "decl غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "decl غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr غير صالح", "expr غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_DUPLICATE_FIELD" {
        give fluent_diagnostic_fields("حقل مكرر", "حقل مكرر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Find the earlier field label and the highlighted duplicate.", "keep one field entry and remove or rename the duplicate", "vitte check path/to/file.vit", "حقل مكرر.", "keep one field entry and remove or rename the duplicate");
    }

    if code == "AST_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("form حقل مجهول", "form حقل مجهول.", "A `form` construction or field access names a field that the form declaration does not contain.", "A `form` construction or field access names a field that the form declaration does not contain.", "Compare the highlighted field name with the fields declared in the `form`.", "rename the field to one declared by the form, or add the missing field to the form declaration", "form Point { x: int, y: int }", "form حقل مجهول.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "AST_E_MISSING_FIELD" {
        give fluent_diagnostic_fields("form حقل مفقود", "form حقل مفقود.", "A `form` construction omits a required field that has no default value.", "A `form` construction omits a required field that has no default value.", "Compare the construction with every required field in the `form` declaration.", "add the missing `field: value` entry to the construction", "form Point { x: int, y: int }", "form حقل مفقود.", "add the missing `field: value` entry to the construction");
    }

    if code == "AST_E_FIELD_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("form حقل نوع عدم تطابق", "form حقل نوع عدم تطابق.", "A value assigned to a `form` field does not match that field's declared type.", "A value assigned to a `form` field does not match that field's declared type.", "Compare the field declaration type with the highlighted field initializer.", "change the field initializer to the declared type, or change the field type in the `form`", "form User { name: string, age: int }", "form حقل نوع عدم تطابق.", "change the field initializer to the declared type, or change the field type in the `form`");
    }

    if code == "AST_E_FIELD_ORDER" {
        give fluent_diagnostic_fields("form fields are out of order", "form fields are out of order.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "This `form` construction uses positional or order-sensitive fields in a different order than the declaration.", "Read the field order from the `form` declaration.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them", "form Pair { left: int, right: int }", "form fields are out of order.", "reorder the construction fields to match the `form` declaration, or use named fields when the rule allows them");
    }

    if code == "AST_E_INCOMPLETE_CONSTRUCTION" {
        give fluent_diagnostic_fields("form construction is incomplete", "form construction is incomplete.", "A `form` construction ended before all required fields were supplied.", "A `form` construction ended before all required fields were supplied.", "Check whether the construction is missing fields or a closing brace.", "complete the construction with every required `field: value` entry and the closing `}`", "form Point { x: int, y: int }", "form construction is incomplete.", "complete the construction with every required `field: value` entry and the closing `}`");
    }

    if code == "AST_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("مرئية غير صالح", "مرئية غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "مرئية غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "AST_E_INVALID_ENTRY" {
        give fluent_diagnostic_fields("entry غير صالح", "entry غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "entry غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "SEMA_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("رمز مكرر", "رمز مكرر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "رمز مكرر.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_UNKNOWN_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف مجهول", "معرف مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "معرف مجهول.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_AMBIGUOUS_SYMBOL" {
        give fluent_diagnostic_fields("ambiguous رمز", "ambiguous رمز.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous رمز.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_SHADOWING_FORBIDDEN" {
        give fluent_diagnostic_fields("shadowing is forbidden", "shadowing is forbidden.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "shadowing is forbidden.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNKNOWN_FIELD" {
        give fluent_diagnostic_fields("حقل does not exist", "حقل does not exist.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename the field to one declared by the form, or add the missing field to the form declaration", "use vitte/core", "حقل does not exist.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "SEMA_E_UNKNOWN_VARIANT" {
        give fluent_diagnostic_fields("variant does not exist", "variant does not exist.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "A `pick` pattern or construction names a variant that is not declared by the selected `pick` type.", "Compare the highlighted variant with the `case` names declared in the `pick`.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration", "pick Status { Ready, Failed }", "variant does not exist.", "rename the variant to a declared `case`, or add the missing `case` to the `pick` declaration");
    }

    if code == "SEMA_E_INACCESSIBLE_VARIANT" {
        give fluent_diagnostic_fields("pick variant is inaccessible", "pick variant is inaccessible.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "A branch names a `pick` variant that cannot be reached after earlier branches.", "Read the branches in order and find the earlier branch that already covers this variant.", "remove the unreachable branch or move the more specific `case` before the broader branch", "match status {", "pick variant is inaccessible.", "remove the unreachable branch or move the more specific `case` before the broader branch");
    }

    if code == "SEMA_E_DUPLICATE_PICK_BRANCH" {
        give fluent_diagnostic_fields("pick branch is duplicated", "pick branch is duplicated.", "The same `pick` variant is matched by more than one branch in the same `match`.", "The same `pick` variant is matched by more than one branch in the same `match`.", "Find the earlier branch for the same `case` and decide which body should remain.", "merge the duplicate branch bodies or remove the later duplicate branch", "match status {", "pick branch is duplicated.", "merge the duplicate branch bodies or remove the later duplicate branch");
    }

    if code == "TYPECK_E_PICK_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("pick نمط is not exhaustive", "pick نمط is not exhaustive.", "A `match` over a `pick` value does not cover every reachable variant.", "A `match` over a `pick` value does not cover every reachable variant.", "List the declared `case` variants and compare them with the branches already present.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended", "match status {", "pick نمط is not exhaustive.", "add the missing `case` branch, or add an `otherwise` branch when a catch-all is intended");
    }

    if code == "TYPECK_E_PICK_PAYLOAD_MISMATCH" {
        give fluent_diagnostic_fields("pick payload does not match variant", "pick payload does not match variant.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "A `pick` variant payload pattern does not match the fields declared for that `case`.", "Compare each payload position or name with the selected variant declaration.", "use the expected payload shape for that `case`, including the correct field count and types", "pick Event { Data(value: int) }", "pick payload does not match variant.", "use the expected payload shape for that `case`, including the correct field count and types");
    }

    if code == "TYPECK_E_IMPOSSIBLE_PATTERN" {
        give fluent_diagnostic_fields("pick نمط can never match", "pick نمط can never match.", "The pattern cannot match the scrutinee type produced by the expression.", "The pattern cannot match the scrutinee type produced by the expression.", "Compare the matched expression type with the variant or literal named by the pattern.", "replace the branch pattern with one that belongs to the matched `pick` or value type", "match status { case Ready { give 0 } }", "pick نمط can never match.", "replace the branch pattern with one that belongs to the matched `pick` or value type");
    }

    if code == "SEMA_E_UNKNOWN_FUNCTION" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "SEMA_E_UNKNOWN_SYMBOL" {
        give fluent_diagnostic_fields("رمز مجهول", "رمز مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "declare the missing identifier in scope or import the module that exports it", "use vitte/core", "رمز مجهول.", "declare the missing identifier in scope or import the module that exports it");
    }

    if code == "SEMA_E_INVALID_IMPORT" {
        give fluent_diagnostic_fields("استيراد غير صالح", "استيراد غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استيراد غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_EXPORT" {
        give fluent_diagnostic_fields("export غير صالح", "export غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_VISIBILITY" {
        give fluent_diagnostic_fields("مرئية غير صالح", "مرئية غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "مرئية غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control تدفق غير صالح", "control تدفق غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "control تدفق غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("وحدة غير صالح", "وحدة غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "وحدة غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INVALID_ASSIGN_TARGET" {
        give fluent_diagnostic_fields("assign هدف غير صالح", "assign هدف غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "assign هدف غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_UNDECLARED_TARGET" {
        give fluent_diagnostic_fields("undeclared هدف", "undeclared هدف.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "undeclared هدف.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_MISSING_BINDING" {
        give fluent_diagnostic_fields("ربط مفقود", "ربط مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ربط مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_DUPLICATE_BINDING" {
        give fluent_diagnostic_fields("ربط مكرر", "ربط مكرر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "ربط مكرر.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_DUPLICATE_ITEM" {
        give fluent_diagnostic_fields("عنصر مكرر", "عنصر مكرر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "rename one declaration or remove the duplicate from the same scope", "use vitte/core", "عنصر مكرر.", "rename one declaration or remove the duplicate from the same scope");
    }

    if code == "SEMA_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir غير صالح", "hir غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "hir غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "SEMA_E_INTERNAL" {
        give fluent_diagnostic_fields("داخلي", "داخلي.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "داخلي.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPECK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNKNOWN_ITEM" {
        give fluent_diagnostic_fields("عنصر مجهول", "عنصر مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عنصر مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_W_UNRESOLVED_NAME" {
        give fluent_diagnostic_fields("unresolved اسم", "unresolved اسم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unresolved اسم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr غير صالح", "expr غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "expr غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_BINARY_MISMATCH" {
        give fluent_diagnostic_fields("ثنائي عدم تطابق", "ثنائي عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make both operands valid for the operator before MIR lowering", "let count: int = 1", "ثنائي عدم تطابق.", "make both operands valid for the operator before MIR lowering");
    }

    if code == "TYPECK_E_ASSIGN_MISMATCH" {
        give fluent_diagnostic_fields("تعيين نوع عدم تطابق", "تعيين نوع عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "assign a value of the declared binding type, or change the binding annotation at its declaration", "let count: int = 1", "تعيين نوع عدم تطابق.", "assign a value of the declared binding type, or change the binding annotation at its declaration");
    }

    if code == "TYPECK_E_RETURN_MISMATCH" {
        give fluent_diagnostic_fields("give نوع عدم تطابق", "ارجاع نوع عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "change the `give` expression or the procedure result type so both name the same Vitte type", "let count: int = 1", "ارجاع نوع عدم تطابق.", "change the `give` expression or the procedure result type so both name the same Vitte type");
    }

    if code == "TYPECK_E_MISSING_GIVE" {
        give fluent_diagnostic_fields("give in قيمة procedure مفقود", "give in قيمة procedure مفقود.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "A procedure declares a result type with `->`, but its body has no `give` for the value it promises.", "Inspect the procedure body and decide which value should be produced.", "add `give expression` on every successful path, or remove the `-> Type` result contract", "proc answer() -> int { give 42 }", "give in قيمة procedure مفقود.", "add `give expression` on every successful path, or remove the `-> Type` result contract");
    }

    if code == "TYPECK_E_GIVE_IN_VOID_PROC" {
        give fluent_diagnostic_fields("give قيمة in procedure without result نوع", "give قيمة in procedure without ارجاع نوع.", "A procedure without a `-> Type` contract uses `give` with a value.", "A procedure without a `-> Type` contract uses `give` with a value.", "Check whether the procedure should produce a value or only perform effects.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement", "proc answer() -> int { give 42 }", "give قيمة in procedure without ارجاع نوع.", "add the correct `-> Type` to the procedure, or remove the value from the `give` statement");
    }

    if code == "TYPECK_E_CONTROL_PATH_MISSING_GIVE" {
        give fluent_diagnostic_fields("control مسار reaches end without give", "control مسار reaches end without give.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "At least one branch can reach the end of a value-returning procedure without executing `give`.", "Follow each `if`, `match`, `loop`, and early-exit path in the procedure.", "add a final `give` or make every branch produce a value before control reaches the closing brace", "proc code(ok: bool) -> int {", "control مسار reaches end without give.", "add a final `give` or make every branch produce a value before control reaches the closing brace");
    }

    if code == "TYPECK_W_UNREACHABLE_AFTER_GIVE" {
        give fluent_diagnostic_fields("code after give is غير قابل للوصول", "code after give is غير قابل للوصول.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "`give` ends the current procedure path, so later statements in the same block cannot run.", "Inspect the statement immediately after the highlighted `give`.", "move the statement before `give`, put it in another branch, or remove it", "proc main() -> int {", "code after give is غير قابل للوصول.", "move the statement before `give`, put it in another branch, or remove it");
    }

    if code == "TYPECK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("cannot assign to immutable ربط", "cannot assign to immutable ربط.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "`set` can only change a binding or field that the current scope is allowed to mutate.", "Find the original `let`, parameter, or field declaration for the highlighted target.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`", "let count: int = 0", "cannot assign to immutable ربط.", "declare the binding with the current mutable form accepted by Vitte, or replace the later `set` with a new `let`");
    }

    if code == "TYPECK_W_MUTABLE_NEVER_MODIFIED" {
        give fluent_diagnostic_fields("قابل للتغيير ربط is never changed", "قابل للتغيير ربط is never changed.", "A binding was declared mutable, but no reachable `set` changes it.", "A binding was declared mutable, but no reachable `set` changes it.", "Search the binding scope for `set name = ...` or field updates through that binding.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended", "let count: int = 0", "قابل للتغيير ربط is never changed.", "remove the mutable marker from the `let`, or keep it only when a later `set` is intended");
    }

    if code == "TYPECK_E_REASSIGNMENT_FORBIDDEN" {
        give fluent_diagnostic_fields("هدف cannot be reassigned", "هدف cannot be reassigned.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "The target of `set` is not an assignable place, such as a computed expression, temporary value, or non-settable projection.", "Check that the left side of `set` is a binding, field, or index place that Vitte allows to be assigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`", "set user.name = name", "هدف cannot be reassigned.", "assign to a valid place with `set target = value`, or bind the computed value with `let`");
    }

    if code == "TYPECK_E_MISSING_INITIALIZER" {
        give fluent_diagnostic_fields("let ربط requires an initializer or نوع", "let ربط requires an initializer or نوع.", "`let` introduced a binding without enough information to create a value.", "`let` introduced a binding without enough information to create a value.", "Inspect the binding and check whether it has either an initializer or an explicit type plus a supported delayed-initialization path.", "add `= expression`, or add the missing type annotation required by the active Vitte rule", "let count: int = 0", "let ربط requires an initializer or نوع.", "add `= expression`, or add the missing type annotation required by the active Vitte rule");
    }

    if code == "TYPECK_E_INFERENCE_FAILED" {
        give fluent_diagnostic_fields("ربط نوع could not be inferred", "ربط نوع could not be inferred.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "The initializer or later uses do not provide enough constraints to infer the binding type.", "Look at the highlighted `let` and the first use of the binding.", "add an explicit Vitte type annotation after the binding name", "let items: [int] = []", "ربط نوع could not be inferred.", "add an explicit Vitte type annotation after the binding name");
    }

    if code == "TYPECK_E_CONDITION_TYPE" {
        give fluent_diagnostic_fields("شرط نوع", "شرط نوع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the condition produce bool, for example by adding an explicit comparison", "let count: int = 1", "شرط نوع.", "make the condition produce bool, for example by adding an explicit comparison");
    }

    if code == "TYPECK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "rename the field to one declared by the form, or add the missing field to the form declaration", "let count: int = 1", "عضو مجهول.", "rename the field to one declared by the form, or add the missing field to the form declaration");
    }

    if code == "TYPECK_E_INDEX_TYPE" {
        give fluent_diagnostic_fields("فهرس نوع", "فهرس نوع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فهرس نوع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_INDEX_TARGET" {
        give fluent_diagnostic_fields("فهرس هدف غير صالح", "فهرس هدف غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فهرس هدف غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IF_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("if branch عدم تطابق", "if branch عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "if branch عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_COMPARE_MISMATCH" {
        give fluent_diagnostic_fields("compare عدم تطابق", "compare عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "compare عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تحويل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MATCH_NON_EXHAUSTIVE" {
        give fluent_diagnostic_fields("match non exhaustive", "match non exhaustive.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "match non exhaustive.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_CALL_TARGET" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "استدعاء هدف is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_UNKNOWN_PROCEDURE" {
        give fluent_diagnostic_fields("procedure does not exist", "procedure does not exist.", "Name resolution could not find a visible `proc` with the highlighted name.", "Name resolution could not find a visible `proc` with the highlighted name.", "Check the active `use` declarations and the procedure name at the call site.", "import or declare the missing `proc`, or rename the call to a visible procedure", "proc add(left: int, right: int) -> int { give left + right }", "procedure does not exist.", "import or declare the missing `proc`, or rename the call to a visible procedure");
    }

    if code == "TYPECK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("استدعاء وسيط نوع عدم تطابق", "استدعاء وسيط نوع عدم تطابق.", "An argument expression does not match the type of the parameter it is passed to.", "An argument expression does not match the type of the parameter it is passed to.", "Link the highlighted argument to its parameter and compare the expected and found Vitte types.", "change that argument expression or the parameter type so the single argument matches its parameter", "scale(value: 4, factor: 2)", "استدعاء وسيط نوع عدم تطابق.", "change that argument expression or the parameter type so the single argument matches its parameter");
    }

    if code == "TYPECK_E_CALL_ARITY" {
        give fluent_diagnostic_fields("wrong رقم of استدعاء وسائط", "wrong رقم of استدعاء وسائط.", "The call supplies a different number of arguments than the procedure parameter list.", "The call supplies a different number of arguments than the procedure parameter list.", "Show the expected parameters in order, then attach each extra or missing argument to its nearest parameter.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list", "add(1, 2)", "wrong رقم of استدعاء وسائط.", "add missing arguments or remove extra arguments so the call matches the `proc` parameter list");
    }

    if code == "TYPECK_E_UNKNOWN_NAMED_ARGUMENT" {
        give fluent_diagnostic_fields("named وسيط has no matching parameter", "named وسيط has no matching parameter.", "A named argument does not correspond to any parameter in the called procedure.", "A named argument does not correspond to any parameter in the called procedure.", "Display the expected parameter names and highlight the unknown argument name.", "rename the argument to an expected parameter name or remove it", "connect(host: \"localhost\", port: 8080)", "named وسيط has no matching parameter.", "rename the argument to an expected parameter name or remove it");
    }

    if code == "TYPECK_E_ARGUMENT_ORDER" {
        give fluent_diagnostic_fields("استدعاء وسائط are in the wrong order", "استدعاء وسائط are in the wrong order.", "A positional or named argument appears after an argument form that makes the order invalid.", "A positional or named argument appears after an argument form that makes the order invalid.", "Compare the call argument order with the procedure parameter order shown in the diagnostic.", "reorder the arguments to match the parameter list, or use named arguments consistently", "draw(x: 1, y: 2, color: \"blue\")", "استدعاء وسائط are in the wrong order.", "reorder the arguments to match the parameter list, or use named arguments consistently");
    }

    if code == "TYPECK_E_AMBIGUOUS_CALL" {
        give fluent_diagnostic_fields("procedure استدعاء is ambiguous", "procedure استدعاء is ambiguous.", "More than one visible procedure can accept the same call shape.", "More than one visible procedure can accept the same call shape.", "Show the candidate procedure signatures and identify the arguments that do not disambiguate them.", "add a type annotation to one argument or call a more specific procedure name", "let result: int = parse(\"42\")", "procedure استدعاء is ambiguous.", "add a type annotation to one argument or call a more specific procedure name");
    }

    if code == "TYPECK_W_IGNORED_RESULT" {
        give fluent_diagnostic_fields("procedure result is ignored", "procedure result is ignored.", "A procedure result is produced but the surrounding statement does not use it.", "A procedure result is produced but the surrounding statement does not use it.", "Check whether the result should be bound with `let`, passed onward, or intentionally discarded.", "bind the result with `let name: Type = call(...)` when the value is needed", "let total: int = add(1, 2)", "procedure result is ignored.", "bind the result with `let name: Type = call(...)` when the value is needed");
    }

    if code == "TYPECK_E_NOT_CALLABLE" {
        give fluent_diagnostic_fields("قيمة is not callable", "قيمة is not callable.", "The expression before `(` is not a procedure or callable value.", "The expression before `(` is not a procedure or callable value.", "Check the type of the highlighted expression and the declaration it resolves to.", "call a `proc` value, or remove `(...)` when the expression is just a value", "add(1, 2)", "قيمة is not callable.", "call a `proc` value, or remove `(...)` when the expression is just a value");
    }

    if code == "TYPECK_E_IMPL_MISSING_MEMBER" {
        give fluent_diagnostic_fields("implementation is مفقود a required عضو", "implementation is مفقود a required عضو.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation is مفقود a required عضو.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_SIGNATURE_MISMATCH" {
        give fluent_diagnostic_fields("implementation عضو signature does not match", "implementation عضو signature does not match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "implementation عضو signature does not match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_IMPL_UNKNOWN_TRAIT" {
        give fluent_diagnostic_fields("trait does not exist", "trait does not exist.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait does not exist.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_GENERIC_INFERENCE" {
        give fluent_diagnostic_fields("عام نوع could not be inferred", "عام نوع could not be inferred.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام نوع could not be inferred.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_TRAIT_BOUND" {
        give fluent_diagnostic_fields("trait constraint is not satisfied", "trait constraint is not satisfied.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait constraint is not satisfied.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CAUSE_CHAIN_MISSING" {
        give fluent_diagnostic_fields("نوع diagnostic is مفقود a cause chain", "نوع diagnostic is مفقود a cause chain.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع diagnostic is مفقود a cause chain.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONFLICTING_IMPL" {
        give fluent_diagnostic_fields("conflicting trait implementation", "conflicting trait implementation.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "conflicting trait implementation.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_CONSTRAINT_CYCLE" {
        give fluent_diagnostic_fields("cyclic عام constraint", "cyclic عام constraint.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "cyclic عام constraint.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_AMBIGUOUS_METHOD" {
        give fluent_diagnostic_fields("ambiguous method resolution", "ambiguous method resolution.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous method resolution.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after نقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_UNARY" {
        give fluent_diagnostic_fields("malformed unary", "malformed unary.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed unary.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_UNARY_MISMATCH" {
        give fluent_diagnostic_fields("unary عدم تطابق", "unary عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unary عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BORROW" {
        give fluent_diagnostic_fields("malformed استعارة", "malformed استعارة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed استعارة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_MEMBER" {
        give fluent_diagnostic_fields("malformed عضو", "malformed عضو.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed عضو.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_INDEX" {
        give fluent_diagnostic_fields("malformed فهرس", "malformed فهرس.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed فهرس.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_BINARY" {
        give fluent_diagnostic_fields("malformed ثنائي", "malformed ثنائي.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed ثنائي.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_CAST" {
        give fluent_diagnostic_fields("malformed تحويل", "malformed تحويل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed تحويل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_MALFORMED_IF" {
        give fluent_diagnostic_fields("malformed if", "malformed if.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "malformed if.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INVALID_HIR" {
        give fluent_diagnostic_fields("hir غير صالح", "hir غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "hir غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPECK_E_INTERNAL" {
        give fluent_diagnostic_fields("داخلي", "داخلي.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "داخلي.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0001" {
        give fluent_diagnostic_fields("type0001", "type0001.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0001.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0002" {
        give fluent_diagnostic_fields("type0002", "type0002.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0002.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0003" {
        give fluent_diagnostic_fields("type0003", "type0003.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0003.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0004" {
        give fluent_diagnostic_fields("type0004", "type0004.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0004.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0005" {
        give fluent_diagnostic_fields("type0005", "type0005.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0005.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0006" {
        give fluent_diagnostic_fields("type0006", "type0006.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0006.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0007" {
        give fluent_diagnostic_fields("type0007", "type0007.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0007.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0008" {
        give fluent_diagnostic_fields("type0008", "type0008.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0008.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0009" {
        give fluent_diagnostic_fields("type0009", "type0009.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0009.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE0010" {
        give fluent_diagnostic_fields("type0010", "type0010.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type0010.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE9999" {
        give fluent_diagnostic_fields("type9999", "type9999.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "type9999.", "make the expression type match the type contract named by the type checker");
    }

    if code == "BORROWCK_E_MOVE_AFTER_MOVE" {
        give fluent_diagnostic_fields("نقل after نقل", "نقل after نقل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نقل after نقل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("قيمة مستخدم after نقل", "قيمة مستخدم after نقل.", "A value is used after ownership has moved away from its binding.", "A value is used after ownership has moved away from its binding.", "Show where the value is created, where it is moved, and where it is reused after the move.", "use the value before the move, borrow it instead of moving it, or create a new value", "let value: string = \"vitte\"", "قيمة مستخدم after نقل.", "use the value before the move, borrow it instead of moving it, or create a new value");
    }

    if code == "BORROWCK_E_PARTIAL_MOVE" {
        give fluent_diagnostic_fields("قيمة partially منقول", "قيمة partially منقول.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Part of a compound value was moved, then the original value was used as if it were still complete.", "Show the original construction, the field or variant payload that moved, and the later whole-value use.", "avoid using the whole value after moving one part, or rebuild the missing part before use", "form User { name: string, age: int }", "قيمة partially منقول.", "avoid using the whole value after moving one part, or rebuild the missing part before use");
    }

    if code == "BORROWCK_E_BORROW_OF_MOVED_VALUE" {
        give fluent_diagnostic_fields("استعارة of منقول قيمة", "استعارة of منقول قيمة.", "A borrow starts after ownership has already moved away from the original binding.", "A borrow starts after ownership has already moved away from the original binding.", "Show the value creation, the move, and the later borrow attempt in that order.", "create the borrow before the move, or move a different value", "let value: string = \"vitte\"", "استعارة of منقول قيمة.", "create the borrow before the move, or move a different value");
    }

    if code == "BORROWCK_E_MUTABLE_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("قابل للتغيير استعارة تعارض", "قابل للتغيير استعارة تعارض.", "A value is already mutably borrowed when another borrow of the same value starts.", "A value is already mutably borrowed when another borrow of the same value starts.", "Report the value creation first, then the first mutable borrow, then the later conflicting borrow, then where the first borrow ends.", "end the first mutable borrow before starting the next borrow", "let value: int = 1", "قابل للتغيير استعارة تعارض.", "end the first mutable borrow before starting the next borrow");
    }

    if code == "BORROWCK_E_MUTABLE_SHARED_CONFLICT" {
        give fluent_diagnostic_fields("قابل للتغيير and مشترك borrows overlap", "قابل للتغيير and مشترك borrows overlap.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "قابل للتغيير and مشترك borrows overlap.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_SHARED_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("مشترك استعارة تعارض", "مشترك استعارة تعارض.", "A mutable borrow overlaps with a shared borrow of the same value.", "A mutable borrow overlaps with a shared borrow of the same value.", "Show the value creation, the shared borrow, the mutable borrow, and the point where each borrow stops being used.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope", "let value: int = 1", "مشترك استعارة تعارض.", "move the mutation after the shared borrow's last use, or shorten the shared borrow scope");
    }

    if code == "BORROWCK_E_WRITE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("كتابة اثناء مستعار", "كتابة اثناء مستعار.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "كتابة اثناء مستعار.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_MOVE_WHILE_BORROWED" {
        give fluent_diagnostic_fields("نقل اثناء مستعار", "نقل اثناء مستعار.", "Ownership moves out of a value while an active borrow can still use it.", "Ownership moves out of a value while an active borrow can still use it.", "Show the value creation, the borrow start, the move, the later borrow use, and where the borrow ends.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership", "let value: string = \"vitte\"", "نقل اثناء مستعار.", "move the value only after the borrow's last use, or pass a borrow instead of moving ownership");
    }

    if code == "BORROWCK_E_DROP_WHILE_BORROWED" {
        give fluent_diagnostic_fields("اسقاط اثناء مستعار", "اسقاط اثناء مستعار.", "A value is destroyed while an active borrow still points to it.", "A value is destroyed while an active borrow still points to it.", "Show the value creation, the borrow start, the destruction point, and the last borrow use.", "destroy the value only after the borrow is no longer used", "let value: string = \"vitte\"", "اسقاط اثناء مستعار.", "destroy the value only after the borrow is no longer used");
    }

    if code == "BORROWCK_E_ASSIGN_WHILE_BORROWED" {
        give fluent_diagnostic_fields("assign اثناء مستعار", "assign اثناء مستعار.", "A `set` mutates a value while an active borrow can still observe the old value.", "A `set` mutates a value while an active borrow can still observe the old value.", "Show the borrow start, the mutation, the later borrow use, and where the borrow ends.", "move the `set` after the borrow's last use, or shorten the borrow", "let value: int = 1", "assign اثناء مستعار.", "move the `set` after the borrow's last use, or shorten the borrow");
    }

    if code == "BORROWCK_E_RETURN_REF_TO_LOCAL" {
        give fluent_diagnostic_fields("ارجاع ref to محلي", "ارجاع ref to محلي.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "ارجاع ref to محلي.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_RETURN_BORROW_OF_LOCAL" {
        give fluent_diagnostic_fields("ارجاع استعارة of محلي", "ارجاع استعارة of محلي.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "A procedure gives back a reference to a local value that ends when the procedure exits.", "Show the local value creation, the reference creation, and the `give` that would let the reference escape.", "give an owned value, or take the referenced value from a parameter that outlives the procedure", "proc name() -> string { give \"vitte\" }", "ارجاع استعارة of محلي.", "give an owned value, or take the referenced value from a parameter that outlives the procedure");
    }

    if code == "BORROWCK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "dangling مرجع.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_REFERENCE_OUTLIVES_VALUE" {
        give fluent_diagnostic_fields("مرجع outlives the قيمة it points to", "مرجع outlives the قيمة it points to.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "مرجع outlives the قيمة it points to.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A reference can remain usable after the value it points to is no longer alive.", "A reference can remain usable after the value it points to is no longer alive.", "Show where the value is created, where the reference is created, and where the value stops being alive.", "keep the referenced value alive longer, or give an owned value instead of a reference", "proc name() -> string { give \"vitte\" }", "مدة حياة زائد short.", "keep the referenced value alive longer, or give an owned value instead of a reference");
    }

    if code == "BORROWCK_E_IMMUTABLE_ASSIGN" {
        give fluent_diagnostic_fields("immutable assign", "immutable assign.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "immutable assign.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNINITIALIZED_USE" {
        give fluent_diagnostic_fields("غير مهيأ use", "غير مهيأ use.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "غير مهيأ use.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MOVE_AFTER_BORROW" {
        give fluent_diagnostic_fields("نقل after استعارة", "نقل after استعارة.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نقل after استعارة.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_MUTABLE_ALIAS" {
        give fluent_diagnostic_fields("قابل للتغيير alias", "قابل للتغيير alias.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "قابل للتغيير alias.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_INTERNAL" {
        give fluent_diagnostic_fields("داخلي", "داخلي.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "داخلي.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "BORROWCK_E_UNKNOWN" {
        give fluent_diagnostic_fields("مجهول", "مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_EVAL_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("قسمة by صفر in ثابت evaluation", "قسمة by صفر in ثابت evaluation.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "change the const divisor to a non-zero value or guard the expression before const evaluation", "const size: int = 4", "قسمة by صفر in ثابت evaluation.", "change the const divisor to a non-zero value or guard the expression before const evaluation");
    }

    if code == "CONST_EVAL_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "اسم مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_NON_CONST_CALL" {
        give fluent_diagnostic_fields("non const استدعاء", "non const استدعاء.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non const استدعاء.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_MUTATION_IN_CONST" {
        give fluent_diagnostic_fields("mutation in const", "mutation in const.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutation in const.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNSUPPORTED_EXPR" {
        give fluent_diagnostic_fields("expr غير مدعوم", "expr غير مدعوم.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "replace the const expression with literals, supported arithmetic, or another compile-time value", "const size: int = 4", "expr غير مدعوم.", "replace the const expression with literals, supported arithmetic, or another compile-time value");
    }

    if code == "CONST_EVAL_E_OVERFLOW" {
        give fluent_diagnostic_fields("فيض", "فيض.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "use a wider const type or reduce the arithmetic result before overflow", "const size: int = 4", "فيض.", "use a wider const type or reduce the arithmetic result before overflow");
    }

    if code == "CONST_EVAL_E_STATIC_ASSERT_FAILED" {
        give fluent_diagnostic_fields("static تحقق فشل", "static تحقق فشل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "make the asserted const condition true or remove the invalid compile-time assumption", "const size: int = 4", "static تحقق فشل.", "make the asserted const condition true or remove the invalid compile-time assumption");
    }

    if code == "CONST_EVAL_E_CYCLE" {
        give fluent_diagnostic_fields("دورة", "دورة.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "دورة.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_PARSE" {
        give fluent_diagnostic_fields("parse", "parse.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "parse.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_EVAL_E_UNKNOWN" {
        give fluent_diagnostic_fields("مجهول", "مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MOD_E_MODULE_NOT_FOUND" {
        give fluent_diagnostic_fields("وحدة مفقود", "وحدة مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "وحدة مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة detected", "استيراد دورة detected.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "استيراد دورة detected.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_SYMBOL_NOT_EXPORTED" {
        give fluent_diagnostic_fields("رمز is غير مصدر بواسطة وحدة", "رمز is غير مصدر بواسطة وحدة.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "رمز is غير مصدر بواسطة وحدة.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "استيراد مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_PACKAGE_MISSING" {
        give fluent_diagnostic_fields("package مفقود", "package مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "package مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_STDLIB_MISSING" {
        give fluent_diagnostic_fields("stdlib مفقود", "stdlib مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "stdlib مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MOD_E_AMBIGUOUS_MODULE" {
        give fluent_diagnostic_fields("ambiguous وحدة مسار", "ambiguous وحدة مسار.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "vitte check path/to/file.vit", "ambiguous وحدة مسار.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "HIR_E_INVALID_EXPR" {
        give fluent_diagnostic_fields("expr غير صالح", "expr غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "expr غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STMT" {
        give fluent_diagnostic_fields("stmt غير صالح", "stmt غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "stmt غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_TYPE" {
        give fluent_diagnostic_fields("نوع غير صالح", "نوع غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_SYMBOL" {
        give fluent_diagnostic_fields("رمز مفقود", "رمز مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "رمز مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CONTROL_FLOW" {
        give fluent_diagnostic_fields("control تدفق غير صالح", "control تدفق غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "control تدفق غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LOWERING_FAILED" {
        give fluent_diagnostic_fields("lowering فشل", "lowering فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "lowering فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("كتلة غير صالح", "كتلة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "كتلة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_TERMINATOR" {
        give fluent_diagnostic_fields("منهي غير صالح", "منهي غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "منهي غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_BLOCK" {
        give fluent_diagnostic_fields("غير قابل للوصول كتلة", "غير قابل للوصول كتلة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "غير قابل للوصول كتلة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERAND" {
        give fluent_diagnostic_fields("معامل غير صالح", "معامل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "معامل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PLACE" {
        give fluent_diagnostic_fields("place غير صالح", "place غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "place غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DATAFLOW_CONFLICT" {
        give fluent_diagnostic_fields("dataflow تعارض", "dataflow تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dataflow تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_VERIFICATION_FAILED" {
        give fluent_diagnostic_fields("verification فشل", "verification فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verification فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODULE" {
        give fluent_diagnostic_fields("وحدة غير صالح", "وحدة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وحدة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_FUNCTION" {
        give fluent_diagnostic_fields("procedure غير صالح", "procedure غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "procedure غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BLOCK" {
        give fluent_diagnostic_fields("كتلة غير صالح", "كتلة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "كتلة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INSTRUCTION" {
        give fluent_diagnostic_fields("تعليمة غير صالح", "تعليمة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعليمة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TYPE_MISMATCH" {
        give fluent_diagnostic_fields("نوع عدم تطابق", "نوع عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_VERIFY_FAILED" {
        give fluent_diagnostic_fields("verify فشل", "verify فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "verify فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "هدف غير مدعوم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNSUPPORTED_FEATURE" {
        give fluent_diagnostic_fields("ميزة غير مدعوم", "ميزة غير مدعوم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ميزة غير مدعوم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CODEGEN_FAILED" {
        give fluent_diagnostic_fields("توليد الكود فشل", "توليد الكود فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "توليد الكود فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_OBJECT_WRITE_FAILED" {
        give fluent_diagnostic_fields("كائن كتابة فشل", "كائن كتابة فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "كائن كتابة فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSEMBLER_FAILED" {
        give fluent_diagnostic_fields("assembler فشل", "assembler فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "assembler فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NATIVE_TOOL_MISSING" {
        give fluent_diagnostic_fields("native اداة مفقود", "native اداة مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "native اداة مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNDEFINED_SYMBOL" {
        give fluent_diagnostic_fields("غير معرف رمز", "غير معرف رمز.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "غير معرف رمز.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_SYMBOL" {
        give fluent_diagnostic_fields("رمز مكرر", "رمز مكرر.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "رمز مكرر.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIBRARY_NOT_FOUND" {
        give fluent_diagnostic_fields("library مفقود", "library مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "library مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_OBJECT_NOT_FOUND" {
        give fluent_diagnostic_fields("كائن مفقود", "كائن مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "كائن مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_FORMAT" {
        give fluent_diagnostic_fields("تنسيق غير مدعوم", "تنسيق غير مدعوم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تنسيق غير مدعوم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_SYSTEM_LINKER_FAILED" {
        give fluent_diagnostic_fields("system linker فشل", "system linker فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "system linker فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_PANIC" {
        give fluent_diagnostic_fields("هلع", "هلع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "هلع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSERT_FAILED" {
        give fluent_diagnostic_fields("تحقق فشل", "تحقق فشل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تحقق فشل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_STACK_OVERFLOW" {
        give fluent_diagnostic_fields("مكدس فيض", "مكدس فيض.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "مكدس فيض.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_OUT_OF_MEMORY" {
        give fluent_diagnostic_fields("out of memory", "out of memory.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "out of memory.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("قسمة by صفر", "قسمة by صفر.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "قسمة by صفر.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BOUNDS_CHECK" {
        give fluent_diagnostic_fields("bounds check", "bounds check.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "bounds check.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NULL_DEREF" {
        give fluent_diagnostic_fields("فارغ deref", "فارغ deref.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "فارغ deref.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("وسيط غير صالح", "وسيط غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "وسيط غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_INPUT" {
        give fluent_diagnostic_fields("ادخال مفقود", "ادخال مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ادخال مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_COMMAND" {
        give fluent_diagnostic_fields("compiler command مفقود", "compiler command مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler command مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OPTION_VALUE" {
        give fluent_diagnostic_fields("compiler option قيمة مفقود", "compiler option قيمة مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler option قيمة مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_SOURCE_UNREADABLE" {
        give fluent_diagnostic_fields("or unreadable ادخال مصدر مفقود", "or unreadable ادخال مصدر مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "or unreadable ادخال مصدر مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CONFIGURATION" {
        give fluent_diagnostic_fields("compiler configuration غير صالح", "compiler configuration غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler configuration غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_TARGET" {
        give fluent_diagnostic_fields("compiler هدف مفقود", "compiler هدف مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler هدف مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_DIR" {
        give fluent_diagnostic_fields("compiler اخراج directory مفقود", "compiler اخراج directory مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler اخراج directory مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_OUTPUT_PATH" {
        give fluent_diagnostic_fields("compiler اخراج مسار مفقود", "compiler اخراج مسار مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler اخراج مسار مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LINK_TARGET" {
        give fluent_diagnostic_fields("compiler link هدف غير صالح", "compiler link هدف غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "compiler link هدف غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INPUT_NOT_FOUND" {
        give fluent_diagnostic_fields("ادخال مفقود", "ادخال مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ادخال مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_OUTPUT_WRITE_FAILED" {
        give fluent_diagnostic_fields("اخراج كتابة فشل", "اخراج كتابة فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "اخراج كتابة فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_READ_FAILED" {
        give fluent_diagnostic_fields("cache read فشل", "cache read فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache read فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CACHE_WRITE_FAILED" {
        give fluent_diagnostic_fields("cache كتابة فشل", "cache كتابة فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "cache كتابة فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PROFILE_NOT_FOUND" {
        give fluent_diagnostic_fields("ملف تعريف مفقود", "ملف تعريف مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ملف تعريف مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TARGET_NOT_FOUND" {
        give fluent_diagnostic_fields("هدف مفقود", "هدف مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "هدف مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "BOOTSTRAP_E_STAGE_FAILURE" {
        give fluent_diagnostic_fields("stage failure", "stage failure.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "stage failure.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SEED_MISSING" {
        give fluent_diagnostic_fields("seed مفقود", "seed مفقود.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "seed مفقود.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_COMPILER_MISSING" {
        give fluent_diagnostic_fields("compiler مفقود", "compiler مفقود.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "compiler مفقود.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_SELF_CHECK_FAILED" {
        give fluent_diagnostic_fields("self check فشل", "self check فشل.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "self check فشل.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "BOOTSTRAP_E_ARTIFACT_INVALID" {
        give fluent_diagnostic_fields("artifact غير صالح", "artifact غير صالح.", "The bootstrap phase found code that violates this diagnostic rule.", "The bootstrap phase found code that violates this diagnostic rule.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "artifact غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_BANNER" {
        give fluent_diagnostic_fields("bootstrap banner is غير صالح", "bootstrap banner is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap banner is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap ثابت signature is غير صالح", "bootstrap ثابت signature is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap ثابت signature is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_CONST_TYPE" {
        give fluent_diagnostic_fields("bootstrap ثابت has wrong نوع", "bootstrap ثابت has wrong نوع.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap ثابت has wrong نوع.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_DUP_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure مكرر", "bootstrap procedure مكرر.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure مكرر.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_EXPORT" {
        give fluent_diagnostic_fields("bootstrap export is غير صالح", "bootstrap export is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap export is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_BODY" {
        give fluent_diagnostic_fields("bootstrap main body is غير صالح", "bootstrap main body is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main body is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_MAIN_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap main signature is غير صالح", "bootstrap main signature is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap main signature is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_BODY" {
        give fluent_diagnostic_fields("bootstrap procedure body is غير صالح", "bootstrap procedure body is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_PROC_SIGNATURE" {
        give fluent_diagnostic_fields("bootstrap procedure signature is غير صالح", "bootstrap procedure signature is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure signature is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_SPACE" {
        give fluent_diagnostic_fields("bootstrap space تصريح is غير صالح", "bootstrap space تصريح is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap space تصريح is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_TOP_LEVEL" {
        give fluent_diagnostic_fields("bootstrap top-level تصريح is غير صالح", "bootstrap top-level تصريح is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap top-level تصريح is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNCLOSED_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure body is not closed", "bootstrap procedure body is not closed.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap procedure body is not closed.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_CONST" {
        give fluent_diagnostic_fields("bootstrap ثابت مجهول", "bootstrap ثابت مجهول.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap ثابت مجهول.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_UNKNOWN_PROC" {
        give fluent_diagnostic_fields("bootstrap procedure غير مدعوم", "bootstrap procedure غير مدعوم.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "vitte check path/to/file.vit", "bootstrap procedure غير مدعوم.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "E_BOOTSTRAP_VERSION" {
        give fluent_diagnostic_fields("bootstrap version is غير صالح", "bootstrap version is غير صالح.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "The bootstrap compiler rejected a trust-root, stage artifact, or seed-root invariant.", "Inspect the bootstrap artifact and the stage named by the diagnostic code.", "repair the seed-rooted bootstrap artifact before using it as compiler input", "make bootstrap-seed-root-test", "bootstrap version is غير صالح.", "repair the seed-rooted bootstrap artifact before using it as compiler input");
    }

    if code == "INSTALL_E_VITTE_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vitte help command فشل", "installed vitte help command فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vitte help command فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_VITTEC_HELP_FAILED" {
        give fluent_diagnostic_fields("installed vittec help command فشل", "installed vittec help command فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed vittec help command فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("installed package استيراد test فشل", "installed package استيراد test فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed package استيراد test فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("installed shell completion is مفقود", "installed shell completion is مفقود.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "installed shell completion is مفقود.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_CC_MISSING" {
        give fluent_diagnostic_fields("macOS package build requires cc", "macOS package build requires cc.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires cc.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_LIPO_MISSING" {
        give fluent_diagnostic_fields("universal macOS package build requires lipo", "universal macOS package build requires lipo.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "universal macOS package build requires lipo.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_MACOS_REQUIRED" {
        give fluent_diagnostic_fields("macOS package build requires macOS", "macOS package build requires macOS.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "macOS package build requires macOS.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_PACKAGE_SOURCE_MISSING" {
        give fluent_diagnostic_fields("package مصدر ملف is مفقود", "package مصدر ملف is مفقود.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "package مصدر ملف is مفقود.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_HELP_FAILED" {
        give fluent_diagnostic_fields("postinstall vitte help check فشل", "postinstall vitte help check فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall vitte help check فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_IMPORT_TEST_FAILED" {
        give fluent_diagnostic_fields("postinstall package استيراد check فشل", "postinstall package استيراد check فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall package استيراد check فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "INSTALL_E_POSTINSTALL_COMPLETION_MISSING" {
        give fluent_diagnostic_fields("postinstall shell completion check فشل", "postinstall shell completion check فشل.", "The general phase found code that violates this diagnostic rule.", "The general phase found code that violates this diagnostic rule.", "Fix the first span reported for this diagnostic, then run the command again.", "repair the highlighted compiler contract before checking later diagnostics", "vitte check path/to/file.vit", "postinstall shell completion check فشل.", "repair the highlighted compiler contract before checking later diagnostics");
    }

    if code == "LIMIT_FILE_SIZE_MAX" {
        give fluent_diagnostic_fields("ملف size max", "ملف size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ملف size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_TOKEN_SIZE_MAX" {
        give fluent_diagnostic_fields("رمز لغوي size max", "رمز لغوي size max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "رمز لغوي size max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_AST_DEPTH_MAX" {
        give fluent_diagnostic_fields("ast depth max", "ast depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ast depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_EXPR_DEPTH_MAX" {
        give fluent_diagnostic_fields("expr depth max", "expr depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "expr depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_IMPORT_DEPTH_MAX" {
        give fluent_diagnostic_fields("استيراد depth max", "استيراد depth max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استيراد depth max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MODULE_COUNT_MAX" {
        give fluent_diagnostic_fields("وحدة count max", "وحدة count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "وحدة count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_DIAGNOSTICS_MAX" {
        give fluent_diagnostic_fields("زائد many تشخيصات emitted", "زائد many تشخيصات emitted.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "زائد many تشخيصات emitted.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_SYMBOL_COUNT_MAX" {
        give fluent_diagnostic_fields("رمز count max", "رمز count max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "رمز count max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_PARSER_RECURSION_MAX" {
        give fluent_diagnostic_fields("محلل recursion max", "محلل recursion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "محلل recursion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_MACRO_EXPANSION_MAX" {
        give fluent_diagnostic_fields("macro expansion max", "macro expansion max.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion max.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "MACRO_E_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("expansion فشل", "expansion فشل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "expansion فشل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RECURSION_LIMIT" {
        give fluent_diagnostic_fields("recursion limit", "recursion limit.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "recursion limit.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ARGUMENT" {
        give fluent_diagnostic_fields("وسيط غير صالح", "وسيط غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "وسيط غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MACRO" {
        give fluent_diagnostic_fields("macro مجهول", "macro مجهول.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro مجهول.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSTABLE_FEATURE" {
        give fluent_diagnostic_fields("unstable ميزة", "unstable ميزة.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unstable ميزة.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "SYNTAX_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "معرف متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تعبير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نوع متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نمط متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "كتلة متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "فاصل متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "رمز لغوي غير متوقع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "unbalanced فاصل.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "attribute غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تصريح غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عبارة غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تعبير غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نمط غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "قيمة حرفية غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عامل غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "modifier غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "body مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ارجاع مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "اسم مكرر.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "اسم مجهول.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نوع مجهول.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "وحدة مجهول.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عضو مجهول.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "ambiguous اسم.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "private رمز.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "استيراد مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "استيراد دورة.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "export تعارض.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "arity عدم تطابق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "وسيط عدم تطابق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تعيين عدم تطابق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "branch عدم تطابق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "استدعاء هدف is not callable.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "تحويل غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "فهرس غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "deref غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "استعارة غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "نقل غير صالح.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after نقل.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use after اسقاط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "use before init.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "double اسقاط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "استعارة تعارض.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "mutability تعارض.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "مدة حياة زائد short.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "dangling مرجع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "non exhaustive match.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "غير قابل للوصول نمط.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const required.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const فيض.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const قسمة by صفر.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "const دورة.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro recursion.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "macro expansion فشل.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait not implemented.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "trait ambiguous.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عام وسيط مفقود.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "عام bound فشل.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "هدف غير مدعوم.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "abi عدم تطابق.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "link فشل.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "SYNTAX_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The parser or lexer could not form the next valid source construct.", "The parser or lexer could not form the next valid source construct.", "Look at the highlighted token and complete or remove the construct around it.", "complete the syntax shape named by the parser label at the highlighted token", "proc main() -> int { give 0 }", "runtime هلع.", "complete the syntax shape named by the parser label at the highlighted token");
    }

    if code == "NAME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "معرف متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعبير متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نوع متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نمط متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "كتلة متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "فاصل متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "رمز لغوي غير متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced فاصل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تصريح غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عبارة غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعبير غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نمط غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "قيمة حرفية غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عامل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ارجاع مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "اسم مكرر.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "اسم مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نوع مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "وحدة مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عضو مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous اسم.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private رمز.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استيراد مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استيراد دورة.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "وسيط عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعيين عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استدعاء هدف is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تحويل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "فهرس غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استعارة غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نقل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after نقل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after اسقاط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double اسقاط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استعارة تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "مدة حياة زائد short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling مرجع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "غير قابل للوصول نمط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const فيض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const قسمة by صفر.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const دورة.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عام وسيط مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عام bound فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "هدف غير مدعوم.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "NAME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime هلع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "معرف متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعبير متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نوع متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نمط متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "كتلة متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "فاصل متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "رمز لغوي غير متوقع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "unbalanced فاصل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "attribute غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تصريح غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عبارة غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعبير غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نمط غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "قيمة حرفية غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عامل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "modifier غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "body مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ارجاع مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "اسم مكرر.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "اسم مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نوع مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "وحدة مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عضو مجهول.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "ambiguous اسم.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "private رمز.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استيراد مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استيراد دورة.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "export تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "arity عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "وسيط عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تعيين عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "branch عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استدعاء هدف is not callable.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "تحويل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "فهرس غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "deref غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استعارة غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "نقل غير صالح.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after نقل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use after اسقاط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "use before init.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "double اسقاط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "استعارة تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "mutability تعارض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "مدة حياة زائد short.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "dangling مرجع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "non exhaustive match.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "غير قابل للوصول نمط.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const required.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const فيض.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const قسمة by صفر.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "const دورة.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro recursion.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "macro expansion فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait not implemented.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "trait ambiguous.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عام وسيط مفقود.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "عام bound فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "هدف غير مدعوم.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "abi عدم تطابق.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "link فشل.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "MODULE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "A symbol, module path, binding, visibility rule, or import contract did not resolve.", "Check the spelling and the nearest import or declaration.", "resolve the exact symbol contract reported by semantic analysis", "use vitte/core", "runtime هلع.", "resolve the exact symbol contract reported by semantic analysis");
    }

    if code == "TYPE_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "معرف متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "كتلة متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فاصل متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "رمز لغوي غير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced فاصل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تصريح غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عبارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "قيمة حرفية غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عامل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ارجاع مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وحدة مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عضو مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous اسم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private رمز.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وسيط عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعيين عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استدعاء هدف is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تحويل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فهرس غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نقل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after نقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "مدة حياة زائد short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling مرجع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "غير قابل للوصول نمط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const فيض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const قسمة by صفر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام وسيط مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام bound فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "هدف غير مدعوم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TYPE_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime هلع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "معرف متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "كتلة متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فاصل متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "رمز لغوي غير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced فاصل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تصريح غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عبارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "قيمة حرفية غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عامل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ارجاع مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وحدة مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عضو مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous اسم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private رمز.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وسيط عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعيين عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استدعاء هدف is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تحويل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فهرس غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نقل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after نقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "مدة حياة زائد short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling مرجع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "غير قابل للوصول نمط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const فيض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const قسمة by صفر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام وسيط مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام bound فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "هدف غير مدعوم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "GENERIC_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime هلع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "معرف متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "كتلة متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فاصل متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "رمز لغوي غير متوقع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "unbalanced فاصل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "attribute غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تصريح غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عبارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعبير غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نمط غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "قيمة حرفية غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عامل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "modifier غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "body مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ارجاع مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مكرر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "اسم مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نوع مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وحدة مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عضو مجهول.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "ambiguous اسم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "private رمز.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استيراد دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "export تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "arity عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "وسيط عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تعيين عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "branch عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استدعاء هدف is not callable.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "تحويل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "فهرس غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "deref غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "نقل غير صالح.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after نقل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use after اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "use before init.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "double اسقاط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "استعارة تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "mutability تعارض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "مدة حياة زائد short.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "dangling مرجع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "non exhaustive match.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "غير قابل للوصول نمط.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const required.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const فيض.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const قسمة by صفر.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "const دورة.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro recursion.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "macro expansion فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait not implemented.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "trait ambiguous.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام وسيط مفقود.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "عام bound فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "هدف غير مدعوم.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "abi عدم تطابق.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "link فشل.", "make the expression type match the type contract named by the type checker");
    }

    if code == "TRAIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The inferred type does not satisfy the type required at this location.", "The inferred type does not satisfy the type required at this location.", "Compare the expected and found types in the diagnostic labels.", "make the expression type match the type contract named by the type checker", "let count: int = 1", "runtime هلع.", "make the expression type match the type contract named by the type checker");
    }

    if code == "OWNERSHIP_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "معرف متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعبير متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نوع متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نمط متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "كتلة متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "فاصل متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "رمز لغوي غير متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced فاصل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تصريح غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عبارة غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعبير غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نمط غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "قيمة حرفية غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عامل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ارجاع مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "اسم مكرر.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "اسم مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نوع مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "وحدة مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عضو مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous اسم.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private رمز.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استيراد مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استيراد دورة.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "وسيط عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعيين عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استدعاء هدف is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تحويل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "فهرس غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استعارة غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نقل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after نقل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استعارة تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "مدة حياة زائد short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling مرجع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "غير قابل للوصول نمط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const فيض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const قسمة by صفر.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const دورة.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عام وسيط مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عام bound فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "هدف غير مدعوم.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "OWNERSHIP_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime هلع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "معرف متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعبير متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نوع متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نمط متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "كتلة متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "فاصل متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "رمز لغوي غير متوقع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "unbalanced فاصل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "attribute غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تصريح غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عبارة غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعبير غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نمط غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "قيمة حرفية غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عامل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "modifier غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "body مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ارجاع مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "اسم مكرر.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "اسم مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نوع مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "وحدة مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عضو مجهول.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "ambiguous اسم.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "private رمز.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استيراد مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استيراد دورة.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "export تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "arity عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "وسيط عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تعيين عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "branch عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استدعاء هدف is not callable.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "تحويل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "فهرس غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "deref غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استعارة غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "نقل غير صالح.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after نقل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use after اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "use before init.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "double اسقاط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "استعارة تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "mutability تعارض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "مدة حياة زائد short.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "dangling مرجع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "non exhaustive match.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "غير قابل للوصول نمط.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const required.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const فيض.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const قسمة by صفر.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "const دورة.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro recursion.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "macro expansion فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait not implemented.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "trait ambiguous.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عام وسيط مفقود.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "عام bound فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "هدف غير مدعوم.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "abi عدم تطابق.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "link فشل.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "LIFETIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "A value was moved, borrowed, assigned, or dropped in an invalid order.", "Find the earlier move or borrow mentioned by the diagnostic.", "repair the ownership transition named by the borrow checker before the highlighted use", "let view = &value", "runtime هلع.", "repair the ownership transition named by the borrow checker before the highlighted use");
    }

    if code == "CONST_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "معرف متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "تعبير متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "نوع متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "نمط متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "كتلة متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "فاصل متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "رمز لغوي غير متوقع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "unbalanced فاصل.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "attribute غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "تصريح غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "عبارة غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "تعبير غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "نمط غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "قيمة حرفية غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "عامل غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "modifier غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "body مفقود.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ارجاع مفقود.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "اسم مكرر.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "اسم مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "نوع مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "وحدة مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "عضو مجهول.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "ambiguous اسم.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "private رمز.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "استيراد مفقود.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "استيراد دورة.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "export تعارض.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "arity عدم تطابق.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "وسيط عدم تطابق.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "تعيين عدم تطابق.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "branch عدم تطابق.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "استدعاء هدف is not callable.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "تحويل غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "فهرس غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "deref غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "استعارة غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "نقل غير صالح.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after نقل.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use after اسقاط.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "use before init.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "double اسقاط.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "استعارة تعارض.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "mutability تعارض.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "مدة حياة زائد short.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "dangling مرجع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "non exhaustive match.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "غير قابل للوصول نمط.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const required.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const فيض.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const قسمة by صفر.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "const دورة.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro مفقود.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro recursion.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "macro expansion فشل.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait not implemented.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "trait ambiguous.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "عام وسيط مفقود.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "عام bound فشل.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "هدف غير مدعوم.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "abi عدم تطابق.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "link فشل.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "CONST_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "A compile-time expression used an operation that cannot be evaluated safely.", "A compile-time expression used an operation that cannot be evaluated safely.", "Reduce the constant expression at the reported span.", "rewrite the highlighted constant expression so const evaluation can prove it safely", "const size: int = 4", "runtime هلع.", "rewrite the highlighted constant expression so const evaluation can prove it safely");
    }

    if code == "MACRO_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "معرف متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "تعبير متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "نوع متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "نمط متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "كتلة متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "فاصل متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "رمز لغوي غير متوقع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "unbalanced فاصل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "attribute غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "تصريح غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "عبارة غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "تعبير غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "نمط غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "قيمة حرفية غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "عامل غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "modifier غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "body مفقود.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ارجاع مفقود.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "اسم مكرر.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "اسم مجهول.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "نوع مجهول.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "وحدة مجهول.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "عضو مجهول.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "ambiguous اسم.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "private رمز.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "استيراد مفقود.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "استيراد دورة.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "export تعارض.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "arity عدم تطابق.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "وسيط عدم تطابق.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "تعيين عدم تطابق.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "branch عدم تطابق.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "A macro invocation, argument, recursion limit, or expansion contract could not produce valid user-facing code.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "my_macro(value)", "استدعاء هدف is not callable.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "تحويل غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "فهرس غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "deref غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "استعارة غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "نقل غير صالح.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after نقل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use after اسقاط.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "use before init.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "double اسقاط.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "استعارة تعارض.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "mutability تعارض.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "مدة حياة زائد short.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "dangling مرجع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "non exhaustive match.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "غير قابل للوصول نمط.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const required.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const فيض.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const قسمة by صفر.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "const دورة.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro مفقود.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro recursion.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "macro expansion فشل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait not implemented.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "trait ambiguous.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "عام وسيط مفقود.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "عام bound فشل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "هدف غير مدعوم.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "abi عدم تطابق.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "link فشل.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "MACRO_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The macro expansion phase found code that violates this diagnostic rule.", "The macro expansion phase found code that violates this diagnostic rule.", "Inspect the macro invocation and the expansion note attached to the primary diagnostic.", "change the macro arguments or stop the recursive expansion before lowering resumes", "vitte check path/to/file.vit", "runtime هلع.", "change the macro arguments or stop the recursive expansion before lowering resumes");
    }

    if code == "HIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "معرف متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "كتلة متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فاصل متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "رمز لغوي غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced فاصل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تصريح غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عبارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "قيمة حرفية غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عامل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ارجاع مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مكرر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وحدة مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عضو مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous اسم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private رمز.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وسيط عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعيين عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "استدعاء هدف is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تحويل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فهرس غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نقل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after نقل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "مدة حياة زائد short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling مرجع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "غير قابل للوصول نمط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const فيض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const قسمة by صفر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام وسيط مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام bound فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "هدف غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "HIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime هلع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "معرف متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "كتلة متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فاصل متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "رمز لغوي غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced فاصل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تصريح غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عبارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "قيمة حرفية غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عامل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ارجاع مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مكرر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وحدة مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عضو مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous اسم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private رمز.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وسيط عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعيين عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "استدعاء هدف is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تحويل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فهرس غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نقل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after نقل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "مدة حياة زائد short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling مرجع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "غير قابل للوصول نمط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const فيض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const قسمة by صفر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام وسيط مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام bound فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "هدف غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "MIR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime هلع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "معرف متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "كتلة متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فاصل متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "رمز لغوي غير متوقع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "unbalanced فاصل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "attribute غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تصريح غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عبارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعبير غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نمط غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "قيمة حرفية غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عامل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "modifier غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "body مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ارجاع مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مكرر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "اسم مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نوع مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وحدة مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عضو مجهول.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "ambiguous اسم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "private رمز.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استيراد دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "export تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "arity عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "وسيط عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تعيين عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "branch عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "A compiler representation failed the structural invariant required before the next pipeline phase.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "proc main() -> int { give 0 }", "استدعاء هدف is not callable.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "تحويل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "فهرس غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "deref غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "نقل غير صالح.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after نقل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use after اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "use before init.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "double اسقاط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "استعارة تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "mutability تعارض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "مدة حياة زائد short.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "dangling مرجع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "non exhaustive match.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "غير قابل للوصول نمط.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const required.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const فيض.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const قسمة by صفر.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "const دورة.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro recursion.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "macro expansion فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait not implemented.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "trait ambiguous.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام وسيط مفقود.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "عام bound فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "هدف غير مدعوم.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "abi عدم تطابق.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "link فشل.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "IR_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The compiler pipeline phase found code that violates this diagnostic rule.", "The compiler pipeline phase found code that violates this diagnostic rule.", "Inspect the highlighted AST, HIR, MIR, or IR node and preserve its required fields during lowering.", "repair the malformed intermediate representation before continuing to the next phase", "vitte check path/to/file.vit", "runtime هلع.", "repair the malformed intermediate representation before continuing to the next phase");
    }

    if code == "BACKEND_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "معرف متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعبير متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نوع متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نمط متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "كتلة متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "فاصل متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "رمز لغوي غير متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced فاصل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تصريح غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عبارة غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعبير غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نمط غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "قيمة حرفية غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عامل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ارجاع مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "اسم مكرر.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "اسم مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نوع مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "وحدة مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عضو مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous اسم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private رمز.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استيراد مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استيراد دورة.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "وسيط عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعيين عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استدعاء هدف is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تحويل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "فهرس غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استعارة غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نقل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after نقل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after اسقاط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double اسقاط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استعارة تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "مدة حياة زائد short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling مرجع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "غير قابل للوصول نمط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const فيض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const قسمة by صفر.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const دورة.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عام وسيط مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عام bound فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "BACKEND_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime هلع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "معرف متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعبير متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نوع متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نمط متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "كتلة متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "فاصل متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "رمز لغوي غير متوقع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "unbalanced فاصل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "attribute غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تصريح غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عبارة غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعبير غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نمط غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "قيمة حرفية غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عامل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "modifier غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "body مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ارجاع مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "اسم مكرر.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "اسم مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نوع مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "وحدة مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عضو مجهول.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "ambiguous اسم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "private رمز.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استيراد مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استيراد دورة.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "export تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "arity عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "وسيط عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تعيين عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "branch عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استدعاء هدف is not callable.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "تحويل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "فهرس غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "deref غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استعارة غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "نقل غير صالح.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after نقل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use after اسقاط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "use before init.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "double اسقاط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "استعارة تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "mutability تعارض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "مدة حياة زائد short.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "dangling مرجع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "non exhaustive match.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "غير قابل للوصول نمط.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const required.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const فيض.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const قسمة by صفر.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "const دورة.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro recursion.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "macro expansion فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait not implemented.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "trait ambiguous.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عام وسيط مفقود.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "عام bound فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "هدف غير مدعوم.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "abi عدم تطابق.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "link فشل.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "LINK_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The selected target, linker, ABI, object file, or native toolchain failed.", "The selected target, linker, ABI, object file, or native toolchain failed.", "Check the target triple and the first backend or linker note.", "install the missing native tool, change target, or fix undefined symbols", "vitte build app.vit -o app", "runtime هلع.", "install the missing native tool, change target, or fix undefined symbols");
    }

    if code == "RUNTIME_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "معرف متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تعبير متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "نوع متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "نمط متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "كتلة متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "فاصل متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "رمز لغوي غير متوقع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "unbalanced فاصل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "attribute غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تصريح غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "عبارة غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تعبير غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "نمط غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "قيمة حرفية غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "عامل غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "modifier غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "body مفقود.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ارجاع مفقود.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "اسم مكرر.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "اسم مجهول.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "نوع مجهول.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "وحدة مجهول.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "عضو مجهول.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "ambiguous اسم.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "private رمز.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "استيراد مفقود.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "استيراد دورة.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "export تعارض.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "arity عدم تطابق.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "وسيط عدم تطابق.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تعيين عدم تطابق.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "branch عدم تطابق.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Generated program execution reached a runtime safety check or trap reported by the Vitte runtime.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "if index < len(items) { give items[index] }", "استدعاء هدف is not callable.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "تحويل غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "فهرس غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "deref غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "استعارة غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "نقل غير صالح.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after نقل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use after اسقاط.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "use before init.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "double اسقاط.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "استعارة تعارض.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "mutability تعارض.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "مدة حياة زائد short.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "dangling مرجع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "non exhaustive match.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "غير قابل للوصول نمط.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const required.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const فيض.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const قسمة by صفر.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "const دورة.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro مفقود.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro recursion.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "macro expansion فشل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait not implemented.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "trait ambiguous.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "عام وسيط مفقود.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "عام bound فشل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "هدف غير مدعوم.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "abi عدم تطابق.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "link فشل.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "RUNTIME_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The runtime phase found code that violates this diagnostic rule.", "The runtime phase found code that violates this diagnostic rule.", "Inspect the runtime span and the operation named by the diagnostic code.", "change the program state that reaches the runtime trap or add an explicit check before it", "vitte check path/to/file.vit", "runtime هلع.", "change the program state that reaches the runtime trap or add an explicit check before it");
    }

    if code == "DRIVER_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "معرف متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "تعبير متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "نوع متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "نمط متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "كتلة متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "فاصل متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "رمز لغوي غير متوقع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "unbalanced فاصل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "attribute غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "تصريح غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "عبارة غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "تعبير غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "نمط غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "قيمة حرفية غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "عامل غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "modifier غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "body مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ارجاع مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "اسم مكرر.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "اسم مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "نوع مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "وحدة مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "عضو مجهول.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "ambiguous اسم.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "private رمز.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "استيراد مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "استيراد دورة.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "export تعارض.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "arity عدم تطابق.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "وسيط عدم تطابق.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "تعيين عدم تطابق.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "branch عدم تطابق.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "استدعاء هدف is not callable.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "تحويل غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "فهرس غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "deref غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "استعارة غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "نقل غير صالح.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after نقل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use after اسقاط.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "use before init.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "double اسقاط.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "استعارة تعارض.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "mutability تعارض.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "مدة حياة زائد short.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "dangling مرجع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "non exhaustive match.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "غير قابل للوصول نمط.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const required.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const فيض.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const قسمة by صفر.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "const دورة.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro recursion.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "macro expansion فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait not implemented.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "trait ambiguous.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "عام وسيط مفقود.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "عام bound فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "هدف غير مدعوم.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "abi عدم تطابق.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "link فشل.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "DRIVER_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The driver could not use the provided input, option, cache, output, or profile.", "The driver could not use the provided input, option, cache, output, or profile.", "Re-run the command with --help and verify paths and option values.", "provide an existing input, writable output path, and supported target/profile", "vitte check src/main.vit --lang en", "runtime هلع.", "provide an existing input, writable output path, and supported target/profile");
    }

    if code == "LIMIT_E_EXPECTED_IDENTIFIER" {
        give fluent_diagnostic_fields("معرف متوقع", "معرف متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "معرف متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير متوقع", "تعبير متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "تعبير متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_TYPE" {
        give fluent_diagnostic_fields("نوع متوقع", "نوع متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "نوع متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_PATTERN" {
        give fluent_diagnostic_fields("نمط متوقع", "نمط متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "نمط متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_BLOCK" {
        give fluent_diagnostic_fields("كتلة متوقع", "كتلة متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "كتلة متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPECTED_DELIMITER" {
        give fluent_diagnostic_fields("فاصل متوقع", "فاصل متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "فاصل متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNEXPECTED_TOKEN" {
        give fluent_diagnostic_fields("رمز لغوي غير متوقع", "رمز لغوي غير متوقع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "رمز لغوي غير متوقع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNBALANCED_DELIMITER" {
        give fluent_diagnostic_fields("unbalanced فاصل", "unbalanced فاصل.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "unbalanced فاصل.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_ATTRIBUTE" {
        give fluent_diagnostic_fields("attribute غير صالح", "attribute غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "attribute غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DECLARATION" {
        give fluent_diagnostic_fields("تصريح غير صالح", "تصريح غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "تصريح غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_STATEMENT" {
        give fluent_diagnostic_fields("عبارة غير صالح", "عبارة غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "عبارة غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_EXPRESSION" {
        give fluent_diagnostic_fields("تعبير غير صالح", "تعبير غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "تعبير غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_PATTERN" {
        give fluent_diagnostic_fields("نمط غير صالح", "نمط غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "نمط غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_LITERAL" {
        give fluent_diagnostic_fields("قيمة حرفية غير صالح", "قيمة حرفية غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "قيمة حرفية غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_OPERATOR" {
        give fluent_diagnostic_fields("عامل غير صالح", "عامل غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "عامل غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MODIFIER" {
        give fluent_diagnostic_fields("modifier غير صالح", "modifier غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "modifier غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_BODY" {
        give fluent_diagnostic_fields("body مفقود", "body مفقود.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "body مفقود.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MISSING_RETURN" {
        give fluent_diagnostic_fields("give مفقود", "ارجاع مفقود.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ارجاع مفقود.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DUPLICATE_NAME" {
        give fluent_diagnostic_fields("اسم مكرر", "اسم مكرر.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "اسم مكرر.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_NAME" {
        give fluent_diagnostic_fields("اسم مجهول", "اسم مجهول.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "اسم مجهول.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_TYPE" {
        give fluent_diagnostic_fields("نوع مجهول", "نوع مجهول.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "نوع مجهول.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MODULE" {
        give fluent_diagnostic_fields("وحدة مجهول", "وحدة مجهول.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "وحدة مجهول.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNKNOWN_MEMBER" {
        give fluent_diagnostic_fields("عضو مجهول", "عضو مجهول.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "عضو مجهول.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_AMBIGUOUS_NAME" {
        give fluent_diagnostic_fields("ambiguous اسم", "ambiguous اسم.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "ambiguous اسم.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_PRIVATE_SYMBOL" {
        give fluent_diagnostic_fields("private رمز", "private رمز.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "private رمز.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_NOT_FOUND" {
        give fluent_diagnostic_fields("استيراد مفقود", "استيراد مفقود.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استيراد مفقود.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_IMPORT_CYCLE" {
        give fluent_diagnostic_fields("استيراد دورة", "استيراد دورة.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استيراد دورة.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_EXPORT_CONFLICT" {
        give fluent_diagnostic_fields("export تعارض", "export تعارض.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "export تعارض.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARITY_MISMATCH" {
        give fluent_diagnostic_fields("arity عدم تطابق", "arity عدم تطابق.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "arity عدم تطابق.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ARGUMENT_MISMATCH" {
        give fluent_diagnostic_fields("وسيط عدم تطابق", "وسيط عدم تطابق.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "وسيط عدم تطابق.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ASSIGNMENT_MISMATCH" {
        give fluent_diagnostic_fields("تعيين عدم تطابق", "تعيين عدم تطابق.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "تعيين عدم تطابق.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BRANCH_MISMATCH" {
        give fluent_diagnostic_fields("branch عدم تطابق", "branch عدم تطابق.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "branch عدم تطابق.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CALL" {
        give fluent_diagnostic_fields("استدعاء هدف is not callable", "استدعاء هدف is not callable.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استدعاء هدف is not callable.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_CAST" {
        give fluent_diagnostic_fields("تحويل غير صالح", "تحويل غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "تحويل غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_INDEX" {
        give fluent_diagnostic_fields("فهرس غير صالح", "فهرس غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "فهرس غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_DEREF" {
        give fluent_diagnostic_fields("deref غير صالح", "deref غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "deref غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_BORROW" {
        give fluent_diagnostic_fields("استعارة غير صالح", "استعارة غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استعارة غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_INVALID_MOVE" {
        give fluent_diagnostic_fields("نقل غير صالح", "نقل غير صالح.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "نقل غير صالح.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_MOVE" {
        give fluent_diagnostic_fields("use after نقل", "use after نقل.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after نقل.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_AFTER_DROP" {
        give fluent_diagnostic_fields("use after اسقاط", "use after اسقاط.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use after اسقاط.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_USE_BEFORE_INIT" {
        give fluent_diagnostic_fields("use before init", "use before init.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "use before init.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DOUBLE_DROP" {
        give fluent_diagnostic_fields("double اسقاط", "double اسقاط.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "double اسقاط.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_BORROW_CONFLICT" {
        give fluent_diagnostic_fields("استعارة تعارض", "استعارة تعارض.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "استعارة تعارض.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MUTABILITY_CONFLICT" {
        give fluent_diagnostic_fields("mutability تعارض", "mutability تعارض.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "mutability تعارض.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LIFETIME_TOO_SHORT" {
        give fluent_diagnostic_fields("مدة حياة زائد short", "مدة حياة زائد short.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "مدة حياة زائد short.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_DANGLING_REFERENCE" {
        give fluent_diagnostic_fields("dangling مرجع", "dangling مرجع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "dangling مرجع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_NON_EXHAUSTIVE_MATCH" {
        give fluent_diagnostic_fields("non exhaustive match", "non exhaustive match.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "non exhaustive match.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNREACHABLE_PATTERN" {
        give fluent_diagnostic_fields("غير قابل للوصول نمط", "غير قابل للوصول نمط.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "غير قابل للوصول نمط.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_REQUIRED" {
        give fluent_diagnostic_fields("const required", "const required.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const required.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_OVERFLOW" {
        give fluent_diagnostic_fields("const فيض", "const فيض.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const فيض.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_DIVISION_BY_ZERO" {
        give fluent_diagnostic_fields("const قسمة by صفر", "const قسمة by صفر.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const قسمة by صفر.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_CONST_CYCLE" {
        give fluent_diagnostic_fields("const دورة", "const دورة.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "const دورة.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_NOT_FOUND" {
        give fluent_diagnostic_fields("macro مفقود", "macro مفقود.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro مفقود.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_RECURSION" {
        give fluent_diagnostic_fields("macro recursion", "macro recursion.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro recursion.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_MACRO_EXPANSION_FAILED" {
        give fluent_diagnostic_fields("macro expansion فشل", "macro expansion فشل.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "macro expansion فشل.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_NOT_IMPLEMENTED" {
        give fluent_diagnostic_fields("trait not implemented", "trait not implemented.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait not implemented.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_TRAIT_AMBIGUOUS" {
        give fluent_diagnostic_fields("trait ambiguous", "trait ambiguous.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "trait ambiguous.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_ARGUMENT_MISSING" {
        give fluent_diagnostic_fields("عام وسيط مفقود", "عام وسيط مفقود.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "عام وسيط مفقود.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_GENERIC_BOUND_FAILED" {
        give fluent_diagnostic_fields("عام bound فشل", "عام bound فشل.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "عام bound فشل.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_UNSUPPORTED_TARGET" {
        give fluent_diagnostic_fields("هدف غير مدعوم", "هدف غير مدعوم.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "هدف غير مدعوم.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_ABI_MISMATCH" {
        give fluent_diagnostic_fields("abi عدم تطابق", "abi عدم تطابق.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "abi عدم تطابق.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_LINK_FAILED" {
        give fluent_diagnostic_fields("link فشل", "link فشل.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "link فشل.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    if code == "LIMIT_E_RUNTIME_PANIC" {
        give fluent_diagnostic_fields("runtime هلع", "runtime هلع.", "The input exceeded a configured compiler safety limit.", "The input exceeded a configured compiler safety limit.", "Split the file, expression, import graph, token, or macro expansion named by the code.", "reduce the input size or raise the limit only in a trusted build profile", "vitte check src/main.vit", "runtime هلع.", "reduce the input size or raise the limit only in a trusted build profile");
    }

    give fluent_diagnostic_fields_empty();
}
