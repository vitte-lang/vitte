space vitte/compiler/abi/pipeline

use src/vitte/compiler/abi/contracts as contracts
use src/vitte/compiler/abi/metrics as metrics

form PipelineResult {
  success: bool,
  notes: [string],
}

proc run_pipeline() -> PipelineResult {
  let ok: bool = contracts.contracts_ok()
  let m = metrics.baseline_metrics()
  give PipelineResult {
    success: ok && m.pass_total == m.checks_total,
    notes: ["module=abi", "checks=" + (m.checks_total as string)],
  }
}
