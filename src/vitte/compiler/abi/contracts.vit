space vitte/compiler/abi/contracts

use src/vitte/compiler/abi/manifest as manifest

proc contracts_ok() -> bool {
  let info = manifest.manifest()
  give info.module_name == "abi" && info.owner == "vitte"
}
