space vitte/compiler/abi/mod

form ModuleInfo {
  name: string,
  domain: string,
  ready: bool,
}

proc module_info() -> ModuleInfo {
  give ModuleInfo {
    name: "abi",
    domain: "compiler-topology",
    ready: true,
  }
}
