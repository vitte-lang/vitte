space vitte/compiler/abi/diagnostics

form Diagnostic {
  level: string,
  code: string,
  message: string,
}

proc baseline_diagnostics() -> [Diagnostic] {
  give [
    Diagnostic { level: "info", code: "abi_000", message: "abi diagnostics ready" },
  ]
}
