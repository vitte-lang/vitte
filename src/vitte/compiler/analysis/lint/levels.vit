space vitte/compiler/analysis/lint

pick LintSeverity {
  Warning,
  Error,
}

proc lint_severity_name(severity: LintSeverity) -> string {
  if severity == LintSeverity.Error {
    give "error";
  }
  give "warning";
}
