space vitte/compiler/analysis/sema

proc visibility_valid(visibility: string) -> bool {
  give visibility == "" or visibility == "private" or visibility == "public" or visibility == "internal" or visibility == "export";
}
