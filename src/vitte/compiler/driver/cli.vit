space vitte/compiler/driver

use vitte/compiler/infrastructure/session/options { SessionOptions }
use vitte/compiler/driver/pipeline { localized_status }

proc cli_message(locale: string, key: string) -> string {
  let options: SessionOptions = SessionOptions { locale: locale }
  give localized_status(options, key)
}
