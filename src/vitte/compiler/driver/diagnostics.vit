space vitte/compiler/driver/diagnostics

export *

pick DriverDiagnosticLevel {
    Note,
    Warning,
    Error,
}

form DriverDiagnostic {
    level: DriverDiagnosticLevel
    stage: string
    code: string
    message: string
    valid: bool
}
