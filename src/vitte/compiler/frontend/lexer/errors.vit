space vitte/compiler/frontend/lexer/errors

form LexerDiagnostic {
  code: string
  message: string
  fatal: bool
}

proc lex_error(code: string, message: string) -> LexerDiagnostic {
  give LexerDiagnostic { code: code, message: message, fatal: true };
}
