space vitte/compiler/frontend/macros/errors

form MacroDiagnostic {
  code: string
  message: string
  fatal: bool
}

proc macro_error(code: string, message: string) -> MacroDiagnostic {
  give MacroDiagnostic { code: code, message: message, fatal: true };
}
