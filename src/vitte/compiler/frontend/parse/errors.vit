space vitte/compiler/frontend/parse/errors

form ParseDiagnostic {
  code: string
  message: string
  fatal: bool
}

proc parse_error(code: string, message: string) -> ParseDiagnostic {
  give ParseDiagnostic { code: code, message: message, fatal: true };
}
