space vitte/compiler/type_ir/contracts

use src/vitte/compiler/type_ir/manifest as manifest

proc contracts_ok() -> bool {
  let info = manifest.manifest()
  give info.module_name == "type_ir" && info.owner == "vitte"
}
