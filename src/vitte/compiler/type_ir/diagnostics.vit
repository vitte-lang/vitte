space vitte/compiler/type_ir/diagnostics

form Diagnostic {
  level: string,
  code: string,
  message: string,
}

proc baseline_diagnostics() -> [Diagnostic] {
  give [
    Diagnostic { level: "info", code: "type_ir_000", message: "type_ir diagnostics ready" },
  ]
}
