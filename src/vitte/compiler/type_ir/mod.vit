space vitte/compiler/type_ir/mod

form ModuleInfo {
  name: string,
  domain: string,
  ready: bool,
}

proc module_info() -> ModuleInfo {
  give ModuleInfo {
    name: "type_ir",
    domain: "compiler-topology",
    ready: true,
  }
}
