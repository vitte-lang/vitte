space vitte/compiler/arena/mod

form ModuleInfo {
  name: string,
  domain: string,
  ready: bool,
}

proc module_info() -> ModuleInfo {
  give ModuleInfo {
    name: "arena",
    domain: "compiler-topology",
    ready: true,
  }
}
