space vitte/compiler/arena/contracts

use src/vitte/compiler/arena/manifest as manifest

proc contracts_ok() -> bool {
  let info = manifest.manifest()
  give info.module_name == "arena" && info.owner == "vitte"
}
