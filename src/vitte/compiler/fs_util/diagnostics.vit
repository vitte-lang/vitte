space vitte/compiler/fs_util/diagnostics

form Diagnostic {
  level: string,
  code: string,
  message: string,
}

proc baseline_diagnostics() -> [Diagnostic] {
  give [
    Diagnostic { level: "info", code: "fs_util_000", message: "fs_util diagnostics ready" },
  ]
}
