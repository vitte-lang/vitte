space vitte/compiler/fs_util/mod

form ModuleInfo {
  name: string,
  domain: string,
  ready: bool,
}

proc module_info() -> ModuleInfo {
  give ModuleInfo {
    name: "fs_util",
    domain: "compiler-topology",
    ready: true,
  }
}
