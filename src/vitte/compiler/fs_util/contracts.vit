space vitte/compiler/fs_util/contracts

use src/vitte/compiler/fs_util/manifest as manifest

proc contracts_ok() -> bool {
  let info = manifest.manifest()
  give info.module_name == "fs_util" && info.owner == "vitte"
}
