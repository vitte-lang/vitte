space vitte/compiler/backend/ir

use vitte/compiler/backend/ir/ir.{ IrUnit, unit_module_count, verify_unit }

proc verify_and_count_modules(unit: IrUnit) -> u64 {
  let verified: IrUnit = verify_unit(unit);
  if not verified.valid {
    give 0;
  }
  give unit_module_count(verified);
}
