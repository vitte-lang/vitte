space vitte/compiler/backend/adapters

// Canonical ownership boundary for non-production backend families.
use vitte/compiler/backends/c_emit
use vitte/compiler/backends/llvm_emit
use vitte/compiler/backends/vitte_emit
use vitte/compiler/backends/wasm

proc backend_adapter_registry_contract() -> string {
  give "canonical-backend-owns-adapter-reachability";
}

export *
