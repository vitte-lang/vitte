space vitte/compiler/backend/codegen

proc emit_x86_64_text(lines: [string]) -> string {
  let output: string = "";
  let i: u64 = 0;
  while i < len(lines) {
    if i > 0 {
      set output = output + "\n";
    }
    set output = output + lines[i];
    set i = i + 1;
  }
  give output;
}
