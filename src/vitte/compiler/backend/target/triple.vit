space vitte/compiler/backend/target/triple

form TargetTriple {
  arch: string
  vendor: string
  os: string
  abi: string
}

proc triple_x86_64_linux() -> TargetTriple {
  give TargetTriple {
    arch: "x86_64",
    vendor: "vitte",
    os: "linux",
    abi: "gnu"
  };
}
