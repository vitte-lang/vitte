module sema.pipeline.sema_stage;

use sema.core.sema_id::SemaPassId;

/// Coarse-grained stages of semantic analysis.
pub enum SemaStage {
    Names,
    Types,
    Traits,
    Borrow,
    ConstEval,
    Lints,
}

impl SemaStage {
    pub fn default_passes(self) -> Array<SemaPassId> {
        // Placeholder; you can wire this to a real registry later.
        Array::new()
    }
}
