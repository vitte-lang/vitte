module sema.integration.middle_bridge;

use sema.integration.sema_providers::SemaProviders;

/// Placeholder for functions that connect sema with `middle`.
pub fn ensure_middle_ready<P: SemaProviders>(_providers: &P) {
    // To be implemented.
}
