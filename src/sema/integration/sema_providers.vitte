module sema.integration.sema_providers;

/// Trait exposing compiler services required by semantic analysis.
///
/// You can extend this trait as you plug real middle/incremental/lsp components.
pub trait SemaProviders {
    fn has_middle(&self) -> bool;
    fn has_incremental(&self) -> bool;
    fn has_lsp(&self) -> bool;
}
