module sema.integration.lsp_bridge;

use sema.integration.sema_providers::SemaProviders;

/// Placeholder for functions that emit sema diagnostics towards LSP.
pub fn publish_sema_diagnostics<P: SemaProviders>(_providers: &P) {
    // To be implemented.
}
