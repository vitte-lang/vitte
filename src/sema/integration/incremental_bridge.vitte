module sema.integration.incremental_bridge;

use sema.integration.sema_providers::SemaProviders;

/// Placeholder for functions that connect sema with the incremental engine.
pub fn touch_incremental_nodes<P: SemaProviders>(_providers: &P) {
    // To be implemented.
}
