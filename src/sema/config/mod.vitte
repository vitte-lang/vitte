module sema.config;

/// Flags that control which groups of passes are enabled.
pub struct SemaFeatureFlags {
    pub enable_lints: bool,
}

impl SemaFeatureFlags {
    pub fn defaults() -> SemaFeatureFlags {
        SemaFeatureFlags { enable_lints: true }
    }
}
