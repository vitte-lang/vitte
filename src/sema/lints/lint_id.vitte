module sema.lints.lint_id;

/// Simple lint identifier.
pub struct LintId(u32);

impl LintId {
    pub fn new(raw: u32) -> LintId {
        LintId(raw)
    }

    pub fn raw(self) -> u32 {
        let LintId(x) = self;
        x
    }
}
