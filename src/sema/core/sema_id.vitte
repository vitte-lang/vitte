module sema.core.sema_id;

/// Identifier for a semantic analysis pass.
pub struct SemaPassId(u32);

impl SemaPassId {
    pub fn new(raw: u32) -> SemaPassId {
        SemaPassId(raw)
    }

    pub fn raw(self) -> u32 {
        let SemaPassId(x) = self;
        x
    }
}
