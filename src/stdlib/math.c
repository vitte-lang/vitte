#include "../../include/stdlib/math.h"
#include <math.h>

double steel_sqrt(double x) {
    return sqrt(x);
}

double steel_pow(double base, double exp) {
    return pow(base, exp);
}

double steel_abs(double x) {
    return fabs(x);
}
