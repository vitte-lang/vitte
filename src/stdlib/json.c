#include "../../include/stdlib/json.h"
#include <stdlib.h>

char *steel_json_stringify(Value v) {
    // TODO: JSON serialization
    return malloc(1);
}

Value steel_json_parse(const char *json) {
    // TODO: JSON parsing
    return value_nil();
}
