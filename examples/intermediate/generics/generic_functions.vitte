// examples/intermediate/generics/generic_functions.vitte
fn swap<T>(a: T, b: T) -> (T, T) {
    return (b, a);
}

fn main() {
    let (x, y) = swap(10, 20);
    println("Swapped: {}, {}", x, y);
    
    let (s1, s2) = swap("hello", "world");
    println("Swapped strings: {}, {}", s1, s2);
}
