proc accumulate(limit: int) -> int {
  let total: int = 0
  let i: int = 0
  loop {
    if i >= limit { break }
    total = total + i
    i = i + 1
  }
  return total
}

entry main at core/app {
  let baseline: int = accumulate(10000)
  return baseline
}
