proc accumulate(limit: int) -> int {
  let total: int = 0
  let i: int = 0
  while true {
    if i >= limit { break }
    total = total + i
    i = i + 1
  }
  give total
}

entry main at core/app {
  let baseline: int = accumulate(10000)
  give baseline
}
