proc add(a: int, b: int) -> int {
  give a + b
}

proc non_negative(value: int) -> int {
  if value < 0 {
    give 0
  } else {
    give value
  }
}

entry main at core/app {
  let result: int = add(2, 3)
  while true {
    break
  }
  give non_negative(result)
}
