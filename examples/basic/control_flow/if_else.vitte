// examples/basic/control_flow/if_else.vitte
fn main() {
    let x = 10;
    
    if x > 0 {
        println("x is positive");
    } else if x < 0 {
        println("x is negative");
    } else {
        println("x is zero");
    }
}
