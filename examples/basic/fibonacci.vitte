// examples/basic/fibonacci.vitte - Calcul suite de Fibonacci

fn fibonacci(n: i32) -> i32 {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() {
    let n = 10;
    println("Fibonacci({}) = {}", n, fibonacci(n));
}
