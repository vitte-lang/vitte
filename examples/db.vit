use std/core/types.int

proc sum(n: int) -> int {
  let i: int = 0
  let acc: int = 0
  loop {
    if i >= n { break }
    acc = acc + i
    i = i + 1
  }
  give acc
}

entry main at core/app {
  return sum(100)
}
