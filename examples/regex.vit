use std/regex

entry main at core/app {
  let re = compile("[a-z]+")
  when re is Result.Ok {
    let _ = is_match(re.value, "abc123")
    let _ = find(re.value, "abc123")
    let _ = replace(re.value, "abc123", "X")
    let _ = split(re.value, "abc123")
  }
  return 0
}
