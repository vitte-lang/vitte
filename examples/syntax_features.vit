proc choose(flag: bool, when_true: int, when_false: int) -> int {
  if flag {
    give when_true
  } else {
    give when_false
  }
}

entry main at core/app {
  let value: int = choose(true, 7, 3)
  give value
}
