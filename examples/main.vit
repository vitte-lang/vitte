use std/core/types.int

proc checksum(seed: int, n: int) -> int {
  let i: int = 0
  let acc: int = seed
  loop {
    if i >= n { break }
    acc = ((acc * 33) + i + 17) % 1000003
    i = i + 1
  }
  give acc
}

entry main at core/app {
  let x = checksum(7, 5000)
  return x
}
