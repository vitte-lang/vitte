//! uuid — v4 (random)
import math.rand as rnd;

export fn v4() -> String {
  let mut r = rnd.seed(__intrinsics::now_unix_ms() as u32);
  // fabrique un UUID v4 simple
  let a = r.next(); let b = r.next(); let c = r.next(); let d = r.next();
  __intrinsics::uuid_format(a,b,c,d)
}
