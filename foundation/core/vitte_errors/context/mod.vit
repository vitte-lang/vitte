# ============================================================
# vitte_errors :: context
# ------------------------------------------------------------
# Façade d’agrégation des contextes d’erreurs.
# Centralise l’accès aux types et helpers de contexte.
# ============================================================

space vitte_errors/context

# ------------------------------------------------------------
# Dépendances
# ------------------------------------------------------------

pull vitte_error_codes/context

# ------------------------------------------------------------
# Re-exports ciblés
# ------------------------------------------------------------

share
  # Types principaux
  context/ErrorContext,
  context/ErrorPhase,
  context/ErrorSpan,
  context/ErrorNote,

  # Constructeurs et helpers
  context/new_context,
  context/with_span,
  context/add_note,
  context/format,
  context/short
.end
