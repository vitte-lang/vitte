# ============================================================
# vitte_errors :: diagnostic :: builder
# ------------------------------------------------------------
# Façade des builders de diagnostics.
# Centralise les variantes (core, with_note, with_span, with_fixit).
# ============================================================

space vitte_errors/diagnostic/builder

# ------------------------------------------------------------
# Sous-modules
# ------------------------------------------------------------

pull vitte_errors/diagnostic/builder/core
pull vitte_errors/diagnostic/builder/with_note
pull vitte_errors/diagnostic/builder/with_span
pull vitte_errors/diagnostic/builder/with_fixit

# ------------------------------------------------------------
# Re-exports ciblés
# ------------------------------------------------------------

share
  # -------- Core --------
  core/DiagnosticBuilder,
  core/new,
  core/at,
  core/note,
  core/notes,
  core/build,
  core/simple,

  # -------- With note --------
  with_note/DiagnosticBuilderWithNote,
  with_note/new,
  with_note/from_builder,
  with_note/note,
  with_note/notes,
  with_note/because,
  with_note/help,
  with_note/build,
  with_note/simple,

  # -------- With span --------
  with_span/DiagnosticBuilderWithSpan,
  with_span/new,
  with_span/from_builder,
  with_span/at,
  with_span/override_span,
  with_span/clear_span,
  with_span/note,
  with_span/notes,
  with_span/build,
  with_span/simple,

  # -------- With fix-it --------
  with_fixit/FixIt,
  with_fixit/DiagnosticBuilderWithFixIt,
  with_fixit/new,
  with_fixit/from_builder,
  with_fixit/at,
  with_fixit/note,
  with_fixit/fix,
  with_fixit/build,
  with_fixit/fixes
.end
