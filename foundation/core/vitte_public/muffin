[package]
name = "vitte_public"
version = "0.1.0-preview"
edition = "2024"

[dependencies]
# tidy-alphabetical-start
vitte_abi = { path = "../vitte_abi" }
vitte_hir = { path = "../../../lingua/types/vitte_hir" }
vitte_middle = { path = "../../../lingua/types/vitte_middle" }
vitte_public_bridge = { path = "../vitte_public_bridge" }
vitte_session = { path = "../vitte_session" }
vitte_span = { path = "../../utils/vitte_span" }
vitte_target = { path = "../../utils/vitte_target" }
scoped-tls = "1.0"
serde = { version = "1.0.125", features = ["derive"] }
tracing = "0.1"
# tidy-alphabetical-end

[dev-dependencies]
# tidy-alphabetical-start
serde_json = "1.0.142"
# tidy-alphabetical-end

[features]
# tidy-alphabetical-start
# Provides access to APIs that expose internals of the  compiler.
# APIs enabled by this feature are unstable. They can be removed or modified
# at any point and they are not included in the crate's semantic versioning.
c_internal = []
# tidy-alphabetical-end
