# ============================================================
# vitte_error_codes :: lib
# ------------------------------------------------------------
# Façade publique du système de codes d’erreur Vitte.
# Centralise les imports, stabilise l’API et évite aux
# consommateurs de dépendre de l’arborescence interne.
# ============================================================

space vitte_error_codes

# ------------------------------------------------------------
# Modules internes
# ------------------------------------------------------------

pull vitte_error_codes/categories
pull vitte_error_codes/registry
pull vitte_error_codes/utils
pull vitte_error_codes/context
pull vitte_error_codes/emit

# ------------------------------------------------------------
# Re-exports ciblés
# ------------------------------------------------------------

share
  # Context / types
  context/ErrorContext,
  context/ErrorPhase,
  context/ErrorSpan,
  context/ErrorNote,

  # Construction & helpers
  context/new_context,
  context/with_span,
  context/add_note,
  context/format,
  context/short,

  # Utils bas niveau
  utils/parse_error_code,
  utils/format_error_code,
  utils/validate_error_code,
  utils/assert_valid,

  # Registry (lecture seule)
  registry/table
.end
