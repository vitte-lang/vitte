# ============================================================
# vitte_error_messages :: render
# ------------------------------------------------------------
# Façade des moteurs de rendu des messages d’erreur.
# Centralise les backends (plain, json, lsp).
# ============================================================

space vitte_error_messages/render

# ------------------------------------------------------------
# Sous-modules
# ------------------------------------------------------------

pull vitte_error_messages/render/plain
pull vitte_error_messages/render/json
pull vitte_error_messages/render/lsp

# ------------------------------------------------------------
# Re-exports ciblés
# ------------------------------------------------------------

share
  # Plain text
  plain/render,
  plain/render_all,
  plain/short,

  # JSON
  json/render,
  json/short,

  # LSP
  lsp/Diagnostic,
  lsp/render,
  lsp/render_all
.end
