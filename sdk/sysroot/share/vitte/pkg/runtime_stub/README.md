# runtime_stub

Implémentation minimale de `libvitte_runtime` (C) pour tester l'ABI du SDK.
Produit:
- libvitte_runtime.a (static)
- libvitte_runtime.so/.dylib (optionnel)

Le vrai runtime peut être remplacé par ton runtime/VM.
