pkgname=vitte
pkgver=0.1.0
pkgrel=1
pkgdesc="Vitte language toolchain"
arch=('x86_64' 'aarch64')
url="https://example.org/vitte"
license=('MIT')
source=("vitte-linux-x86_64.tar.gz")
sha256sums=('SKIP')
package() {
  install -Dm755 vitte "$pkgdir/usr/bin/vitte"
  install -Dm755 vitte-cli "$pkgdir/usr/bin/vitte-cli"
}
