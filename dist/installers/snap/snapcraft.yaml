name: vitte
base: core24
version: 0.1.0
summary: Vitte language toolchain
description: Vitte compiler, VM and CLI
grade: stable
confinement: classic
apps:
  vitte:
    command: vitte
  vitte-cli:
    command: vitte-cli
parts:
  vitte:
    plugin: dump
    source: ../archives/vitte-linux-x86_64.tar.gz
