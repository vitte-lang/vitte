[package]
# Remplacer 'template' par le nom réel du module
name = "vitte_template"
version = "0.0.0"
edition = "2024"

# Documentation optionnelle du package
description = "Template module for Vitte language development"
repository = "https://github.com/YOUR_REPO/vitte"
license = "MIT OR Apache-2.0"

[dependencies]
# tidy-alphabetical-start

# Exemple: Dépendances internes au même domaine
# vitte_sibling_module = { path = "../vitte_sibling_module" }

# Exemple: Dépendances d'autres domaines
# vitte_foundation_utils = { path = "../../utils/vitte_foundation_utils" }

# Crates externes communs
# serde = { version = "1.0", features = ["derive"], optional = true }
# tracing = "0.1"

# tidy-alphabetical-end

[dev-dependencies]
# Crates pour les tests uniquement

[features]
# Définir les features optionnelles du module
# default = []
# advanced = []

[lib]
# Par défaut, lib.vit est le point d'entrée
# Vous pouvez ajouter d'autres configurations au besoin
