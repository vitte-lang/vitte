[package]
name = "vitte_builtin_macros"
version = "0.0.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
# tidy-alphabetical-start
vitte_ast = { path = "../vitte_ast" }
vitte_ast_pretty = { path = "../vitte_ast_pretty" }
vitte_attr_parsing = { path = "../vitte_attr_parsing" }
vitte_data_structures = { path = "../../../foundation/utils/vitte_data_structures" }
vitte_errors = { path = "../../../foundation/core/vitte_errors" }
vitte_expand = { path = "../vitte_expand" }
vitte_feature = { path = "../../../foundation/core/vitte_feature" }
vitte_fluent_macro = { path = "../../../foundation/utils/vitte_fluent_macro" }
vitte_hir = { path = "../../types/vitte_hir" }
vitte_index = { path = "../../../foundation/utils/vitte_index" }
vitte_lexer = { path = "../vitte_lexer" }
vitte_lint_defs = { path = "../../../foundation/core/vitte_lint_defs" }
vitte_macros = { path = "../../../foundation/utils/vitte_macros" }
vitte_parse = { path = "../vitte_parse" }
vitte_parse_format = { path = "../vitte_parse_format" }
# We must use the proc_macro version that we will compile proc-macros against,
# not the one from our own sysroot.
vitte_proc_macro = { path = "../../foundation/utils/vitte_proc_macro" }
vitte_session = { path = "../../../foundation/core/vitte_session" }
vitte_span = { path = "../../../foundation/utils/vitte_span" }
vitte_target = { path = "../../../foundation/utils/vitte_target" }
smallvec = { version = "1.8.1", features = ["union", "may_dangle"] }
thin-vec = "0.2.12"
tracing = "0.1"
# tidy-alphabetical-end

[features]
# tidy-alphabetical-start
llvm_enzyme = []
# tidy-alphabetical-end
