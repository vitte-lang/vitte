# =========================================================
# Vitte 1.0 — AST / Attribute / Module
# =========================================================

space vitte/ast/attribute

share all

<<< doc
Module racine pour la gestion des attributs au niveau AST.

Ce module ne contient aucune logique.
Il agrège simplement les sous-modules liés aux attributs :

- représentation syntaxique
- métadonnées
- erreurs
- structures de support

Rôle :
- point d’entrée unique pour `pull vitte/ast/attribute`
- stabilité d’API pour le reste du compilateur
>>>

# ---------------------------------------------------------
# Sous-modules
# ---------------------------------------------------------

pull vitte/ast/attribute/attribute as attribute
pull vitte/ast/attribute/meta as meta

# ---------------------------------------------------------
# Ré-export explicite (API publique)
# ---------------------------------------------------------

share
    Attribute,
    AttributeList,
    AttributeTarget,
    BuiltinAttribute,
    AttributeError,
    AttributeTable,
    ResolvedAttribute,

    AttributeMeta,
    AttributeMetaTable,
    AttributeLevel,
    AttributeArity