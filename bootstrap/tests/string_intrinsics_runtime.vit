space bootstrap/string_intrinsics_runtime;

proc main() -> int {
    let value: string = trim("  fn(int) -> bool  ");
    if value == "fn(int) -> bool" and starts_with(value, "fn(") and ends_with(value, "bool") {
        give 0;
    }
    give 1;
}
