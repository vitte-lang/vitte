space bootstrap/tests/grammar_alignment/ref_param_type_marker_ok

proc read_value(value: ref int) -> int {
    give value
}

proc write_value(value: ref mut int) -> int {
    give value
}

proc pick_value(value: int) -> int {
    pick value {
        _ {
            give 0
        }
    }
    give 0
}

proc call_ref() -> int {
    let value: int = 1
    give read_value(ref value)
}

proc set_typed() -> int {
    set value: int = 1
    give value
}

proc main() -> int {
    give 0
}
