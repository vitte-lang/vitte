procedure main()
   write(image(x := []) | "failed")
   write(image(every push(x,1 to 1000)) | "failed")
   write(image(x) | "failed")
   write(image(every push(x,1 to 10000)) | "failed")
   write(image(x) | "failed")
end
