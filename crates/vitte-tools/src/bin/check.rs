//! Temporary stub for the `vitte-check` CLI.

fn main() {
    eprintln!(
        "`vitte-check` is not implemented yet. Enable the `check-cli` feature once available."
    );
    std::process::exit(1);
}
