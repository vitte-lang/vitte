//! Temporary stub for the `vitte-graph` CLI.

fn main() {
    eprintln!(
        "`vitte-graph` is not implemented yet. Graph tooling will arrive in a future release."
    );
    std::process::exit(1);
}
