//! Temporary stub for the `vitte-parse` CLI.
//! The full CLI is not implemented yet; this keeps cargo tooling happy.

fn main() {
    eprintln!("`vitte-parse` CLI is not implemented yet. Track progress on the Vitte roadmap.");
    std::process::exit(1);
}
