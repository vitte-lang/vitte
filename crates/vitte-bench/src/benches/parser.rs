//! Parser benchmark stubs.

use criterion::Criterion;

/// Registers parser benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: plug real parser benchmarks when the parser is ready.
}
