//! IR passes benchmark stubs.

use criterion::Criterion;

/// Registers IR pass benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: integrate real IR pipeline benchmarks.
}
