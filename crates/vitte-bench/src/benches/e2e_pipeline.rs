//! End-to-end pipeline benchmark stubs.

use criterion::Criterion;

/// Registers end-to-end pipeline benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: wire the real pipeline benchmarks once the compiler is integrated.
}
