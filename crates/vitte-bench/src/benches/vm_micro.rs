//! VM micro benchmark stubs.

use criterion::Criterion;

/// Registers VM micro benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: benchmark VM micro-operations here.
}
