//! Lexer benchmark stubs.

use criterion::Criterion;

/// Registers lexer benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: plug real lexer benchmarks when the lexer is available.
}
