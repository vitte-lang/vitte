//! VM hotpath benchmark stubs.

use criterion::Criterion;

/// Registers VM hotpath benchmarks with Criterion (stub implementation).
pub fn criterion_benchmark(_c: &mut Criterion) {
    // TODO: benchmark VM hot loops when available.
}
