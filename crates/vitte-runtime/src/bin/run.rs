//! Temporary stub for the `vitte-runtime-run` binary.

fn main() {
    eprintln!("`vitte-runtime-run` is not available yet. Enable the appropriate feature when implemented.");
    std::process::exit(1);
}
