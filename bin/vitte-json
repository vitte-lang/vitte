#!/usr/bin/env sh
# Wrapper to provide JSON diagnostics surface until native binary is rebuilt
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
PY="$(command -v python3 || echo python3)"
exec "$PY" "$SCRIPT_DIR/tools/emit_diagnostics_json.py" "$@"
