fn sieve(n) {
  let a = [true; 100000];
  let p = 2;
  while p*p <= n {
    if a[p] {
      let i = p*p;
      while i <= n { a[i] = false; i = i + p; }
    }
    p = p + 1;
  }
  let cnt = 0;
  for i in 2..n { if a[i] { cnt = cnt + 1; } }
  return cnt;
}
fn main() { print(sieve(20000)); }
