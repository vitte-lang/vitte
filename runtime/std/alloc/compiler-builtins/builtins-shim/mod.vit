# ============================================================
# mod.vit — compiler-builtins shim (public root)
# ============================================================
# Rôle :
# - Point d’entrée PUBLIC du compiler-builtins shim
# - Exposé par std/alloc
# - Surface stable pour le runtime, le compilateur et les backends
#
# Ce module :
# - ne contient AUCUNE implémentation
# - ne contient AUCUNE logique
# - orchestre uniquement les sous-modules
# ============================================================

space std/alloc/compiler_builtins


# ------------------------------------------------------------
# Shim compiler-builtins
# ------------------------------------------------------------
# Le shim fournit :
# - config cible / backend
# - implémentations fallback
# - surface ABI + symboles linker
# ------------------------------------------------------------

pull std/alloc/compiler_builtins/builtins_shim


# ------------------------------------------------------------
# Visibilité
# ------------------------------------------------------------
# Tout ce qui est exposé par builtins_shim
# est considéré comme faisant partie de l’ABI std
# ------------------------------------------------------------

share all