space doc

<<<
doc.vit
role: Root boundary for vitteos orchestration.
allowed-dependencies: boot/api, arch/api, mm/api, sched/api, fs/api, drivers/api, syscall/api.
invariants: profile in {dev,ci,prod}; deterministic root health states.
errors: invalid-config:profile and explicit failed status paths.
contract: public entry points are api_default_version, api_default_config, api_bootstrap, api_health.
>>>

proc doc_summary() -> string {
  give "vitteos root contract: stable API + deterministic bootstrap/health"
}
