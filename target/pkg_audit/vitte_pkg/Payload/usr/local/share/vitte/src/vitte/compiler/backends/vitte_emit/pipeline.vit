space vitte/compiler/backends/vitte_emit

proc canonical_pipeline_entry() -> string {
  give "vitte/compiler/backend.compile_to_valid_ir";
}

proc pipeline() -> string {
  give "pipeline adapter -> " + canonical_pipeline_entry();
}
