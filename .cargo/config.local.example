# Copiez en config.local.toml et personnalisez.
# Exemple: forcer l'utilisation de lld, définir des proxys, ou un mirror local.
[env]
# RUSTFLAGS = "-C target-cpu=native -C link-arg=-fuse-ld=lld"
# HTTP_PROXY = "http://127.0.0.1:3128"
# HTTPS_PROXY = "http://127.0.0.1:3128"

[source.crates-io]
# replace-with = "mirror"
# [source.mirror]
# registry = "sparse+https://mon-mirror.local/index"
