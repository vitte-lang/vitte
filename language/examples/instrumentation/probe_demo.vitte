service telemetry:
    probe io_latency(sample):
        trace "io-latency" (sample.device, sample.value)
        emit "telemetry.log" sample

    meta register("telemetry.service", version="1.0")

pragma optimize(level=2)
