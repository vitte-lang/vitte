module examples.basic:
    import runtime.fs as fs
    export only (hello, metrics)

let version = "0.1"

scenario hello(name):
    return "Bonjour " + name

table metrics(data):
    column "Key"
    column "Value"
    rows data
