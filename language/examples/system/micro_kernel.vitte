kernel micro:
    boot start:
        map memory:
            layout:
                reg control = 0xFF00
                reg status = 0xFF01

        interrupt timer:
            unsafe:
                asm {
                    inc reg:status
                }
