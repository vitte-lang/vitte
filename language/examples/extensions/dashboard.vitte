software dashboard:
    ui control_panel:
        table status(nodes):
            column "Node"
            column "State"
            rows nodes

        plot load(points):
            axis x "time"
            axis y "load"
            data points
