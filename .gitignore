# ============================================================================
# .gitignore – vitte-core
#
# Objectifs :
#   - Ignorer uniquement les artefacts générés et fichiers temporaires.
#   - Garder sous contrôle de version : sources, manifests Muffin, configs,
#     scripts, docs, et fichiers de configuration d’outillage utiles (.vscode).
#   - Rester générique mais clairement centré sur le workspace Vitte.
# ============================================================================

# ---------------------------------------------------------------------------
# Artefacts de build / caches – Vitte
# ---------------------------------------------------------------------------

# Répertoire global des artefacts Vitte (compilations, bootstrap, logs, etc.)
/target/
/target/**

# Distribution / packaging éventuels (au cas où)
/dist/
/dist/**

# Fichiers de sortie temporaires, dumps, reports ad hoc
*.log
*.tmp
*.cache
*.bak

# ---------------------------------------------------------------------------
# Outils front-end / IDE / systèmes
# ---------------------------------------------------------------------------

# macOS
.DS_Store
.AppleDouble
.LSOverride

# icônes de volume externes
Icon?

# Thumbs Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini

# VS Code – on garde .vscode/ dans le dépôt, donc pas de règle globale ici.
# Si tu veux ignorer des fichiers spécifiques, ajoute-les plus bas.

# JetBrains / autres IDEs (par précaution)
.idea/
.vscode-test/

# ---------------------------------------------------------------------------
# Node / JS tooling (si jamais tu ajoutes du tooling autour)
# ---------------------------------------------------------------------------

node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml
package-lock.json

# ---------------------------------------------------------------------------
# Environnements / secrets locaux
# ---------------------------------------------------------------------------

.env
.env.*
*.local

# ---------------------------------------------------------------------------
# Fichiers temporaires éditeur
# ---------------------------------------------------------------------------

*~
*.swp
*.swo
*.orig
*.rej

# ---------------------------------------------------------------------------
# Patterns spécifiques aux répertoires de travail Vitte
# (à étendre à mesure que la toolchain produit de nouveaux artefacts)
# ---------------------------------------------------------------------------

# Bootstrap – artefacts générés (rapports, dumps, logs)
/bootstrap/**/logs/
/bootstrap/**/report.txt
/bootstrap/**/reports/
/bootstrap/**/out/
/bootstrap/**/tmp/

# Tests – sorties générées lors de l’exécution
/tests/**/out/
/tests/**/logs/
/tests/**/tmp/

# Dossiers auxiliaires locaux
.local/
.cache/
