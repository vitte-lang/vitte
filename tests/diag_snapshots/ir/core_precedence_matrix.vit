proc main() -> int {
  let a = 1
  let b = 2
  let c = 3
  let d = 4
  let arr = [a, b, c]

  let p_or_and = a < b or b < c and c < d
  let p_eq_rel = a + b == c * d
  let p_add_mul = a + b * c - d / a % b
  let p_unary_postfix = -arr[0]

  give p_add_mul
}
