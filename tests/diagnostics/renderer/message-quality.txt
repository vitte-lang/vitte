error[PAR0035] parser: top-level declaration expected
  --> hello.vit:1:1
  |
1 | pro main() -> int {
  | ^^^ expected `proc`, `const`, `form`, `pick`, or another declaration keyword
  = note: the parser is at module scope, not inside a declaration body
  = help: start declarations with a valid top-level keyword
  = suggestion[machine-applicable]: replace `pro` with `proc`
