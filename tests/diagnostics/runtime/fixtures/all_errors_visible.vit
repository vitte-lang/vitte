space tests.diagnostics.runtime.fixtures.all_errors_visible

proc bad_call(left: int, right: int) -> int {
  give left + right
}

proc main() -> int {
  let first: int = "one"
  let second: bool = 42
  give missing_name
  give bad_call(1)
  set immutable_value = 3
}
