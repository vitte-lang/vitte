assertions:
  - code identifies an import cycle instead of a missing module
  - title is present
  - primary span points at the import edge that closes the cycle
  - notes include the import chain
  - suggestions do not propose an unrelated module
  - no parser diagnostic is emitted for syntactically valid files
  - order is stable across both files
  - resolver recovery reports independent imports when possible
