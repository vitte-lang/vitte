space diagnostics/catalog/borrowck

form Box {
    value: string,
}

proc consume(item: Box) -> int {
    give 1;
}

proc main() -> int {
    let item = Box { value: "vitte" };
    let moved = consume(item);
    give consume(item);
}
