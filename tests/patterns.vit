form Some
  field a as int
  field b as int
.end

entry main at core/app {
  make value as Some = Some(1, 2)
  select value
    when Some(x, y) { return x }
    when Some(x, x) { return x }
  otherwise { return 0 }
}
