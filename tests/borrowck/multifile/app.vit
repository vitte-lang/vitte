space tests/borrowck/multifile/app

use tests/borrowck/multifile/box.{ Box, make_box }
use tests/borrowck/multifile/take.{ borrow_box, take_box }

proc main() -> int {
  let value = make_box();
  let borrowed = borrow_box(value);
  let moved = take_box(value);
  give value.value;
}
