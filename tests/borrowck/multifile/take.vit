space tests/borrowck/multifile/take

use tests/borrowck/multifile/box.{ Box }

export take_box
export borrow_box

proc take_box(value: Box) -> int {
  give value.value;
}

proc borrow_box(borrow value: Box) -> int {
  give value.value;
}
