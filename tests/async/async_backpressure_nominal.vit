use vitte/async as async_pkg

entry main at core/app {
  let s0 = async_pkg.scheduler_new(2)
  let s1 = async_pkg.scheduler_enqueue(s0, 10)
  if !async_pkg.scheduler_backpressure(s1) {
    return 1
  }

  if !async_pkg.load_test_backpressure(1000, 4) {
    return 2
  }

  return 0
}
