space vitte/tests/composite_types

pick Option[T] {
  case Some(value: T),
  case None
}

form Composite[T] {
  maybe: ?T,
  pair: (T, T),
  unioned: T | int,
  wrapped: dyn Option[T],
  impld: impl Option[T]
}

proc demo(value: Option[int]) -> int {
  give 0
}
