space tests/truth_triangle/stdlib_runtime_deep/strings_only

use src/vitte/stdlib/strings as strings

proc main() -> int {
  give strings.str_length("alpha") + strings.str_length("beta") + strings.str_length("gamma")
}
