space tests/truth_triangle/stdlib_runtime_deep/math_matrix_arithmetic

use src/vitte/stdlib/math/arithmetic as arithmetic
use src/vitte/stdlib/math/matrix as matrix
use src/vitte/stdlib/strings as strings

proc main() -> int {
  give arithmetic.add_i64(4, 6) + strings.str_length(matrix.matrix_version())
}
