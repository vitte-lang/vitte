space tests/truth_triangle/stdlib_runtime_deep/arithmetic_chain

use src/vitte/stdlib/math/arithmetic as arithmetic

proc main() -> int {
  give arithmetic.add_i64(1, 2) + arithmetic.add_i64(3, 4) + arithmetic.add_i64(5, 6)
}
