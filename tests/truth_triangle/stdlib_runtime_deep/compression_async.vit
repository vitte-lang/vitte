space tests/truth_triangle/stdlib_runtime_deep/compression_async

use src/vitte/stdlib/async/async as async
use src/vitte/stdlib/compression as compression
use src/vitte/stdlib/strings as strings

proc main() -> int {
  give strings.str_length(async.async_version()) + strings.str_length(compression.compression_version())
}
