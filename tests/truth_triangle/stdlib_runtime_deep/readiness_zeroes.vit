space tests/truth_triangle/stdlib_runtime_deep/readiness_zeroes

use src/vitte/stdlib/io/stream as stream
use src/vitte/stdlib/memory as memory
use src/vitte/stdlib/os as os
use src/vitte/stdlib/runtime as runtime

proc main() -> int {
  give stream.stream_ready() + memory.memory_ready() + os.os_ready() + runtime.vitte_c_abi_version()
}
