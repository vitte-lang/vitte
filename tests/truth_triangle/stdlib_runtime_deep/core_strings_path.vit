space tests/truth_triangle/stdlib_runtime_deep/core_strings_path

use src/vitte/stdlib/io as io
use src/vitte/stdlib/strings as strings

proc main() -> int {
  give strings.str_length("core") + strings.str_length(io.path_join("std", "lib"))
}
