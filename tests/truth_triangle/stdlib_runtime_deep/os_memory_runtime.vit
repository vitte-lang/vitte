space tests/truth_triangle/stdlib_runtime_deep/os_memory_runtime

use src/vitte/stdlib/memory as memory
use src/vitte/stdlib/os as os
use src/vitte/stdlib/runtime as runtime

proc main() -> int {
  give os.os_ready() + memory.memory_ready() + runtime.vitte_c_abi_version()
}
