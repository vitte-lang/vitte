space tests/truth_triangle/stdlib_runtime_deep/async_json

use src/vitte/stdlib/async as async
use src/vitte/stdlib/json as json
use src/vitte/stdlib/strings as strings

proc main() -> int {
  give strings.str_length(async.async_version()) + json.json_module_count()
}
