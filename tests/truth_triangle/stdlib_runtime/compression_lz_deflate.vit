space tests/truth_triangle/stdlib_runtime/compression_lz_deflate

use src/vitte/stdlib/compression as compression
use src/vitte/stdlib/strings as strings

// stdlib-runtime: real compression metadata call scenario
proc main() -> int {
  give strings.str_length(compression.compression_version())
}
