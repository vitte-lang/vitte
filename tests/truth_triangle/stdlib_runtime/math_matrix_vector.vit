space tests/truth_triangle/stdlib_runtime/math_matrix_vector

use src/vitte/stdlib/math/matrix as matrix
use src/vitte/stdlib/strings as strings

// stdlib-runtime: real matrix metadata call scenario
proc main() -> int {
  give strings.str_length(matrix.matrix_version())
}
