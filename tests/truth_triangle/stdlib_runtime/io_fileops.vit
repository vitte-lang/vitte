space tests/truth_triangle/stdlib_runtime/io_fileops

use src/vitte/stdlib/io/fileops as fileops
use src/vitte/stdlib/strings as strings

// stdlib-runtime: real fileops metadata call scenario
proc main() -> int {
  give strings.str_length(fileops.fileops_version())
}
