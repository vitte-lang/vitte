space tests/truth_triangle/stdlib_runtime/path_join_normalize

use src/vitte/stdlib/io as io
use src/vitte/stdlib/strings as strings

// stdlib-runtime: real path join scenario
proc main() -> int {
  give strings.str_length(io.path_join("src", "vitte"))
}
