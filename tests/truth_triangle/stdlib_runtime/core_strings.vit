space tests/truth_triangle/stdlib_runtime/core_strings

use src/vitte/stdlib/strings as strings

// stdlib-runtime: real strings call scenario
proc main() -> int {
  give strings.str_length("vitte")
}
