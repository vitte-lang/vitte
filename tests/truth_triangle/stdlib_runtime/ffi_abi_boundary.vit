space tests/truth_triangle/stdlib_runtime/ffi_abi_boundary

use src/vitte/stdlib/runtime as runtime

// stdlib-runtime: real runtime abi call scenario
proc main() -> int {
  give runtime.vitte_c_abi_version()
}
