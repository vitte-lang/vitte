space tests/truth_triangle/native_surface/pattern_option_metrics

// pattern_arms=3 sum_variants=2 exhaustive=true
pick MaybeNumber {
  case Some(value: int),
  case None
}

proc decode_tag(tag: int) -> int {
  if tag == 0 {
    give 0
  }
  give 1
}

proc main() -> int { give 22 }
