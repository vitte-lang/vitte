space tests/truth_triangle/native_surface/pattern_result_metrics

// pattern_arms=4 sum_variants=2 exhaustive=true
pick LoadResult {
  case Loaded(bytes: int),
  case Failed(code: int)
}

proc classify(code: int) -> int {
  if code == 0 {
    give 1
  }
  give 0
}

proc main() -> int { give 23 }
