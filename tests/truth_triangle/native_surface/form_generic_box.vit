space tests/truth_triangle/native_surface/form_generic_box

form Box[T] {
  value: T,
}

proc unwrap_first<T>(value: T, fallback: T) -> T {
  let _ = fallback
  give value
}

proc main() -> int { give 12 }
