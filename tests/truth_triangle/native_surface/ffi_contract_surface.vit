space tests/truth_triangle/native_surface/ffi_contract_surface

// ffi_abi=c symbols=1 checked_boundary=true
form CHandle {
  raw: int,
}

proc ffi_ready() -> int {
  give 4
}

proc main() -> int { give 4 }
