space tests/truth_triangle/native_surface/stdlib_path_surface

use src/vitte/stdlib/strings as strings
use src/vitte/stdlib/io as io

proc main() -> int {
  give strings.str_length(io.path_join("native", "surface"))
}
