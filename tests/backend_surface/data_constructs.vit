space backend.surface.data;

form BufferBox {
  name: string
  values: [i32]
}

pick BuildResult {
  Ok
  Err
}

proc main() -> i32 {
  let values: [i32] = [1, 2, 3]
  let box: BufferBox = BufferBox { name: "backend", values: values }
  give len(box.values) as i32
}
