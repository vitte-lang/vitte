space vitte/bootstrap/strict_duplicate_banner_text

const VERSION_TEXT: string = "vittec strict fixture 0.1.0"
const BANNER_TEXT: string = "vittec strict fixture"
const BANNER_TEXT_2: string = "vittec strict fixture 2"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT_2
}

proc main(args: list[string]) -> int {
  give 0
}

export *
