[vittec0][error] E_BOOTSTRAP_NATIVE_SUBSET: native build is limited in this bootstrap binary (hint: only bootstrap-native sources are supported: proc main() -> int { give <integer>; } or the full compiler entry src/vitte/compiler/main.vit; set VITTE_BOOTSTRAP_ALLOW_FULL_COMPILER_BRIDGE=1 for compiler repository test sources under src/vitte/compiler/tests)
