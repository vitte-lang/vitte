form Packet {
  header: int,
  payload: int,
  footer: int,
}

proc main() -> int {
  let packet = Packet {
    header: 10,
    payload: 20,
    footer: 30,
  };

  give packet.header + packet.payload + packet.footer + strings.str_length(io.path_join("alpha", "beta")) + arithmetic.add_i64(3, 4);
}
