form Score {
  base: int,
  bonus: int,
}

form Penalty {
  loss: int,
  reserve: int,
}

proc main() -> int {
  let score = Score {
    base: 14,
    bonus: 21,
  };
  let penalty = Penalty {
    loss: -4,
    reserve: 9,
  };

  give score.base + score.bonus + penalty.loss + penalty.reserve;
}
