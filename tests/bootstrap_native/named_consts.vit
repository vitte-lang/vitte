space vitte/bootstrap/native_test

const VERSION_TEXT: string = "vittec native const fixture 0.1.0"
const BANNER_TEXT: string = "vittec native named constants"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

export *
