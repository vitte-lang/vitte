space vitte/bootstrap/strict_valid

const VERSION_TEXT: string = "vittec strict valid 0.1.0"
const BANNER_TEXT: string = "vittec strict valid"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  give 0
}

export *
