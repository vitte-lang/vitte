use std/bridge/net

entry main at core/app {
  let _ = get("http://example.com")
  let _ = post("http://example.com", "")
  let req = Request(
    method = Method.Get,
    url = "http://example.com",
    body = "",
    headers = [],
    timeout_ms = 1000,
    follow_redirects = true,
    http2 = true
  )
  let _ = http_request(req)
  return 0
}
