use std/metrics

entry main at core/app {
  let c = counter("requests")
  let _ = inc(&c, 1)
  let g = gauge("load")
  let _ = set(&g, 0.5)
  let t = timer("latency")
  let _ = record_ms(&t, 10)
  return 0
}
