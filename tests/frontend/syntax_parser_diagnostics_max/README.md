# Syntax Parser Diagnostics Max

Fixtures for `make syntax-parser-diagnostics-max`.

The valid fixture targets the repository frontend syntax validator rather than
the bootstrap-only parser surface. It keeps the complete syntax contract
executable while the native parser is still converging on the full language.
