space tests/typeck/multifile/app
use tests/typeck/multifile/types as types;
use tests/typeck/multifile/math as math;

proc main() -> int {
  let point = types.make_point();
  give math.sum(point.x, 1);
}
