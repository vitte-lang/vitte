# Type System Advanced Full Gate

Fixtures for `make type-system-advanced-gate` and its subgroup gates.

The valid fixture is intentionally limited to currently executable type-system
surface.  The gate links the larger 81-item matrix to the compiler, borrowck,
MIR, ABI, diagnostics, fuzz, and documentation evidence that currently exists.
