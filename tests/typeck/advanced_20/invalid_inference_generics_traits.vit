space tests/typeck/advanced_20/invalid

form Point {
  x: int
}

trait Show {
  proc show(self: Self) -> string;
}

proc make[T]() -> T {
  give 0;
}

proc keep[T: Show](value: T) -> T {
  give value;
}

proc main() -> int {
  let missing = make();
  let contradicted: int = "text";
  let bounded = keep(7);
  give contradicted;
}
