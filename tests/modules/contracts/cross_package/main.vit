use vitte/abi as abi

proc main() -> i32 {
  if not abi.ready() { give 1 }

  let t = abi.type_info("int", 32)
  let sig = abi.signature(abi.default_calling_convention(), t, [t])
  if not abi.validate_signature(sig) { give 2 }

  give 0
}
