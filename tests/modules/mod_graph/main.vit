space vitte/tests/modules/mod_graph

const VERSION_TEXT: string = "vittec mod graph 0.1.0"
const BANNER_TEXT: string = "vittec mod graph"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  give 0
}

export *
