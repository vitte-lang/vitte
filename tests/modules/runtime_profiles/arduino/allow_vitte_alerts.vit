use vitte/alerts as alerts

proc main() -> i32 {
  let r = alerts.rule_new("ard", 5, 1)
  let e = alerts.AlertEvent("k", "src", "m", 6, 1, ["tenant:t1"], alerts.trace_id("tr"))
  if alerts.rule_eval(r, e).name.len == 0 { return 1 }
  return 0
}
