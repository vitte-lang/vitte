use vitte/alloc as alloc

entry main at profile/arduino_alloc {
  let rt = alloc.stable_runtime_new(alloc.AllocatorKind.Bump, 128)
  let a = alloc.stable_alloc(rt, alloc.bytes(16), alloc.alignment(4), alloc.pool_id("arduino"))
  if a.ok { return 0 }
  return 1
}
