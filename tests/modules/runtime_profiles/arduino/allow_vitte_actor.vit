use vitte/actor as actor

proc main() -> i32 {
  let a0 = actor.spawn(1, "arduino")
  let a1 = actor.send(a0, actor.make_message("evt", "p", "trace-ard"))
  let a2 = actor.step(a1)
  if actor.mailbox_len(a2) < 0 { return 1 }
  return 0
}
