use vitte/actor as actor

proc main() -> i32 {
  let a0 = actor.spawn(1, "core")
  let a1 = actor.send(a0, actor.make_message("evt", "p", "trace-core"))
  let a2 = actor.step(a1)
  if !actor.ready() { return 1 }
  if actor.processed_count(a2) < 0 { return 2 }
  return 0
}
