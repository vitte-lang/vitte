use vitte/alloc as alloc

entry main at profile/core_alloc {
  let rt = alloc.stable_runtime_new(alloc.AllocatorKind.Global, 256)
  let a = alloc.stable_alloc(rt, alloc.bytes(32), alloc.alignment(8), alloc.pool_id("core"))
  if a.ok { return 0 }
  return 1
}
