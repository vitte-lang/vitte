use vitte/alerts as alerts

proc main() -> i32 {
  let r = alerts.rule_new("core", 10, 1)
  let e = alerts.AlertEvent("k", "src", "m", 11, 1, ["tenant:t1"], alerts.trace_id("tr"))
  let out = alerts.notify(r, e)
  if out.len == 0 { return 1 }
  return 0
}
