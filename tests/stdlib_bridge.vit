space tests/stdlib_bridge

use std/bridge/print
use std/bridge/read
use std/bridge/path
use std/bridge/env
use std/bridge/bytes
use std/bridge/alloc
use std/bridge/process
use std/bridge/time

proc main() -> i32 {
    let _ = cwd()
    let _ = now()
    give 0
}
