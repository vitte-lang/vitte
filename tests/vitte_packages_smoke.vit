space tests/vitte_packages_smoke

use vitte/core as vitte_core_pkg
use vitte/alloc as vitte_alloc_pkg
use vitte/collections as vitte_collections_pkg
use vitte/math as vitte_math_pkg

use vitte/async as vitte_async_pkg
use vitte/net as vitte_net_pkg
use vitte/runtime as vitte_runtime_pkg

use vitte/fs as vitte_fs_pkg
use vitte/process as vitte_process_pkg
use vitte/abi as vitte_abi_pkg
use vitte/ffi as vitte_ffi_pkg

use vitte/ast as vitte_ast_pkg
use vitte/hir as vitte_hir_pkg
use vitte/mir as vitte_mir_pkg
use vitte/monomorphize as vitte_monomorphize_pkg
use vitte/borrow as vitte_borrow_pkg
use vitte/typeck as vitte_typeck_pkg
use vitte/const_eval as vitte_const_eval_pkg
use vitte/codegen as vitte_codegen_pkg
use vitte/codegen/llvm as vitte_codegen_llvm_pkg
use vitte/codegen/cranelift as vitte_codegen_cranelift_pkg
use vitte/codegen/gcc as vitte_codegen_gcc_pkg

use vitte/std as vitte_std_pkg
use vitte/io as vitte_io_pkg

use vitte/test as vitte_test_pkg
use vitte/bench as vitte_bench_pkg
use vitte/lint as vitte_lint_pkg
use vitte/cli as vitte_cli_pkg
use vitte/log as vitte_log_pkg
use vitte/trace as vitte_trace_pkg

use vitte/serialize as vitte_serialize_pkg
use vitte/graph as vitte_graph_pkg
use vitte/indexmap as vitte_indexmap_pkg
use vitte/lru as vitte_lru_pkg
use vitte/rope as vitte_rope_pkg
use vitte/slab as vitte_slab_pkg
use vitte/audio as vitte_audio_pkg
use vitte/gui as vitte_gui_pkg

use vitte/array as vitte_array_pkg
use vitte/stats as vitte_stats_pkg
use vitte/plot as vitte_plot_pkg
use vitte/ml as vitte_ml_pkg

use vitte/eval as vitte_eval_pkg
use vitte/plugin as vitte_plugin_pkg
use vitte/shell as vitte_shell_pkg

use vitte/ws as vitte_ws_pkg
use vitte/http as vitte_http_pkg
use vitte/template as vitte_template_pkg
use vitte/openapi as vitte_openapi_pkg

use vitte/sql as vitte_sql_pkg
use vitte/db as vitte_db_pkg
use vitte/orm as vitte_orm_pkg
use vitte/migrate as vitte_migrate_pkg
use vitte/cache as vitte_cache_pkg

use vitte/crypto as vitte_crypto_pkg
use vitte/hash as vitte_hash_pkg
use vitte/jwt as vitte_jwt_pkg
use vitte/tls as vitte_tls_pkg
use vitte/random_secure as vitte_random_secure_pkg
use vitte/auth as vitte_auth_pkg

use vitte/channel as vitte_channel_pkg
use vitte/actor as vitte_actor_pkg
use vitte/queue as vitte_queue_pkg
use vitte/stream as vitte_stream_pkg
use vitte/codec as vitte_codec_pkg
use vitte/kv as vitte_kv_pkg
use vitte/lsp as vitte_lsp_pkg
use vitte/observability as vitte_observability_pkg
use vitte/retry as vitte_retry_pkg
use vitte/circuit_breaker as vitte_circuit_breaker_pkg
use vitte/rate_limit as vitte_rate_limit_pkg
use vitte/idempotency as vitte_idempotency_pkg
use vitte/secrets as vitte_secrets_pkg
use vitte/audit as vitte_audit_pkg
use vitte/health as vitte_health_pkg
use vitte/feature_flags as vitte_feature_flags_pkg
use vitte/contracts as vitte_contracts_pkg
use vitte/jobs as vitte_jobs_pkg
use vitte/policy as vitte_policy_pkg
use vitte/ecs as vitte_ecs_pkg
use vitte/timeout as vitte_timeout_pkg
use vitte/bulkhead as vitte_bulkhead_pkg
use vitte/outbox as vitte_outbox_pkg
use vitte/saga as vitte_saga_pkg
use vitte/schema_registry as vitte_schema_registry_pkg
use vitte/tenant as vitte_tenant_pkg
use vitte/pagination as vitte_pagination_pkg
use vitte/search as vitte_search_pkg
use vitte/alerts as vitte_alerts_pkg
use vitte/slo as vitte_slo_pkg
use vitte/fixtures as vitte_fixtures_pkg
use vitte/mock_http as vitte_mock_http_pkg

use vitte/pack as vitte_pack_pkg
use vitte/registry as vitte_registry_pkg
use vitte/doc as vitte_doc_pkg

use vitte/image as vitte_image_pkg
use vitte/video as vitte_video_pkg

use vitte/macros as vitte_macros_pkg
use vitte/reflect as vitte_reflect_pkg

use vitte/config_derive as vitte_config_derive_pkg
use vitte/env as vitte_env_pkg
use vitte/config as vitte_config_pkg
use vitte/scheduler as vitte_scheduler_pkg

use vitte/http_client as vitte_http_client_pkg
use vitte/requests as vitte_requests_pkg
use vitte/jsonpath as vitte_jsonpath_pkg
use vitte/yaml as vitte_yaml_pkg
use vitte/pickle as vitte_pickle_pkg
use vitte/datetime as vitte_datetime_pkg
use vitte/pathlib as vitte_pathlib_pkg
use vitte/subprocess as vitte_subprocess_pkg

entry main at tests/packages {
  return 0
}
