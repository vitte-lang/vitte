space tests/type_system/call_result_cast_negative

// metrics: generic_instantiations=0 trait_impls=0 sum_variants=0 pattern_arms=0 inference_constraints=3
proc item_count() -> u64 {
  give 1 as u64
}

proc main() -> int {
  let count_text: string = item_count()
  give 0
}
