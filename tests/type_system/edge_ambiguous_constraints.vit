space tests/type_system/edge_ambiguous_constraints

// metrics: generic_instantiations=1 trait_impls=1 sum_variants=2 pattern_arms=3 inference_constraints=7
proc choose(flag: bool, a: i32, b: i32) -> i32 {
  if flag {
    give a
  }
  give b
}
