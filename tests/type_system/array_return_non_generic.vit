space tests/type_system/array_return_non_generic

proc values() -> [u64] {
  give [1 as u64]
}

proc first(items: [u64]) -> u64 {
  give items[0]
}

proc main() -> u64 {
  let items: [u64] = values()
  give first(items)
}
