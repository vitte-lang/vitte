space tests/type_system/call_result_projection_positive

form Token {
  text: string
}

proc token() -> Token {
  give Token { text: "ok" }
}

proc main() -> int {
  let text: string = token().text
  if text == "ok" {
    give 0
  }
  give 1
}
