space vitte/bootstrap/general_source

// A bootstrap namespace does not imply the restricted seed source format.
form BuildState {
  complete: bool
}

proc build_complete(state: BuildState) -> bool {
  give state.complete
}
