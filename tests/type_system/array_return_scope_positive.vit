space tests/type_system/array_return_scope_positive

// metrics: generic_instantiations=0 trait_impls=0 sum_variants=0 pattern_arms=0 inference_constraints=10
form State {
  valid: bool
}

proc accepts_string(value: string) -> bool {
  give value != ""
}

proc collect() -> [string] {
  let current: string = "ok"
  if accepts_string(current) {
    give [current]
  }
  give []
}

proc state() -> State {
  let current: State = State { valid: true }
  give current
}

proc main() -> int {
  let values = collect()
  if len(values) > 0 {
    give 0
  }
  give 1
}
