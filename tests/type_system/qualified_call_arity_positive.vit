space tests/type_system/qualified_call_arity_positive

// metrics: generic_instantiations=0 trait_impls=0 sum_variants=0 pattern_arms=0 inference_constraints=2
use src/vitte/stdlib/strings as strings

proc str_length() -> int {
  give 0
}

proc main() -> int {
  give strings.str_length("vitte")
}
