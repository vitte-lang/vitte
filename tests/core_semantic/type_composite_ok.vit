space vitte/tests/type_composite_ok

pick Option[T] {
  case Some(value: T),
  case None
}

form Composite[T] {
  maybe: Option[T],
  pair: (T, T),
  unioned: T | int,
  wrapped: dyn Option[T],
  impld: impl Option[T]
}

proc build(value: Option[int]) -> int {
  give 0
}
