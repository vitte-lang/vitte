module app.types

# ============================================================================
# Types nominaux utilisés par le mini projet Vitte MVP.
# ============================================================================

struct Point
    x: f64
    y: f64
.end

enum Direction
    North
    East
    South
    West
.end

enum Result
    Ok(value: i32)
    Error(code: i32)
.end

type Distance = f64
