mod sample.parse

use sample.math/utils as math

fn demo(x: i32) -> i32
    let mut acc: i32 = x

    for item in data
        acc += item
    .end

    while acc > 0
        acc = acc - 1
    .end

    if acc > 0
        acc -= 1
    elif acc == 0
        acc = 1
    else
        acc = 2
    .end

    match acc
        0 => return 1
        _ => {
            return 0
        }
    .end

    return acc
.end
