module sample.parse

fn demo(x: i32) -> i32
    let mut acc: i32 = x
    while acc > 0
        acc = acc - 1
    .end

    if acc == 0
        return 1
    else
        return 0
    .end
.end

fn match_demo(v: Result)
    match v
        Ok => return 1
        Error => return 0
    .end
.end
