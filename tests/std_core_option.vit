use std/core/option.Option

proc plus_one(x: int) -> int {
  give x + 1
}

entry main at core/app {
  let a: Option[int] = Option.Some(41)
  let b = Option.map(a, plus_one)
  when b is Option.Some {
    return b.value
  }
  return 0
}
