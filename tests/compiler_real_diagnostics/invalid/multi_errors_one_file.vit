space tests/compiler_real_diagnostics/multi_errors_one_file

proc add(left: int, right: int) -> int {
  give left + right
}

proc main() -> int {
  let count: i32 = 1
  set count = "one"
  let missing: int = unknown_value
  give add(count)
}
