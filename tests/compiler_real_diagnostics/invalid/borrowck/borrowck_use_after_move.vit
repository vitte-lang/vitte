space tests.compiler_real_diagnostics.invalid.borrowck_use_after_move

proc consume(value: string) -> int {
  give len(value)
}

proc main() -> int {
  let name: string = "vitte"
  let moved: int = consume(name)
  give len(name) + moved
}
