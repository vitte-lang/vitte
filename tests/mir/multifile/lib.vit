space tests/mir/multifile/lib

export add
export identity
export Box

form Box {
  value: int
}

proc add(left: int, right: int) -> int {
  give left + right;
}

proc identity[T](value: T) -> T {
  give value;
}
