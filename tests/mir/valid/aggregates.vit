space tests/mir/valid/aggregates

form Point {
  x: int
  y: int
}

pick Option {
  None
  Some(int)
}

proc main() -> int {
  let point = Point { x: 1, y: 2 };
  let pair = (point.x, point.y);
  let values = [1, 2, 3];
  let opt = Option.Some(values[0]);
  give pair.0;
}
