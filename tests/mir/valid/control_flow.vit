space tests/mir/valid/control_flow

proc main() -> int {
  let value = 3;
  if value {
    set value = value + 1;
  } else {
    set value = value + 2;
  }
  while value {
    set value = value - 1;
  }
  give value;
}
