use vitte/std as std_pkg

entry main at core/app {
  let lj = std_pkg.light_config()
  if lj.timeout_ms > 500 { give 1 }
  let flags = std_pkg.startup_flags_light()
  if flags.len == 0 { give 2 }
  let dj = std_pkg.doctor_json()
  if dj.len == 0 { give 3 }
  give 0
}
