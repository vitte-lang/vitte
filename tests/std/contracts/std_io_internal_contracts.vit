use vitte/std/io as std_io_pkg

entry main at core/app {
  let cfg = std_io_pkg.default_config()
  let vr = std_io_pkg.validate_config(cfg)
  when vr is std_io_pkg.StdIoResult.Err { give 1 }
  let rr = std_io_pkg.read_text("tmp/a.txt")
  when rr is std_io_pkg.StdIoResult.Err { give 2 }
  give 0
}
