use vitte/std/io as std_io_pkg

entry main at core/app {
  let i: int = 0
  loop {
    if i >= 512 { break }
    let p = std_io_pkg.path_join("tmp", "m.txt")
    let _ = std_io_pkg.atomic_write(p, "line")
    i = i + 1
  }
  give 0
}
