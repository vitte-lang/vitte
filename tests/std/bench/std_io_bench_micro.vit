<<<
std_io_bench_micro.vit
micro bench placeholder for std/io
>>>

use vitte/std/io as std_io_pkg

entry main at core/app {
  let i: int = 0
  loop {
    if i >= 128 { break }
    let _ = std_io_pkg.path_join("tmp", "f")
    i = i + 1
  }
  give 0
}
