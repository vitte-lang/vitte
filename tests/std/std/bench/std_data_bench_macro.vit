use vitte/std/data as std_data_pkg

entry main at core/app {
  let i: int = 0
  let out = "{}"
  loop {
    if i >= 256 { break }
    let r = std_data_pkg.deep_merge(out, "{\"k\":1}")
    when r is std_data_pkg.StdDataResult.Err { give 1 }
    out = r.value
    i = i + 1
  }
  give 0
}
