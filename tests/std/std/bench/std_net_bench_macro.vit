use vitte/std/net as std_net_pkg

entry main at core/app {
  let hs: [std_net_pkg.StdNetHeader] = [std_net_pkg.auth_bearer("token")]
  let i: int = 0
  loop {
    if i >= 256 { break }
    let _ = std_net_pkg.post("api.vitte.dev", hs, "{}", 800)
    i = i + 1
  }
  give 0
}
