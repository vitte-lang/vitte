use vitte/std/async as std_async_pkg

entry main at core/app {
  let i: int = 0
  loop {
    if i >= 256 { break }
    let _ = std_async_pkg.retry_async("echo", 1, 10, std_async_pkg.cancel_token("t", ""))
    i = i + 1
  }
  give 0
}
