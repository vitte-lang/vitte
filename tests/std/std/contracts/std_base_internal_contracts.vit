use vitte/std/base as std_base_pkg

entry main at core/app {
  let cfg = std_base_pkg.normalize_config(0, -1, true)
  let v = std_base_pkg.validate_or_err(cfg)
  when v is std_base_pkg.StdResult.Err { give 1 }
  if std_base_pkg.ready() == false { give 2 }
  give 0
}
