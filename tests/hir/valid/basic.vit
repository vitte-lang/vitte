space tests/hir/valid/basic

form Point {
  x: int,
  y: int,
}

proc add(left: int, right: int) -> int {
  let total: int = left + right;
  give total;
}

proc main() -> int {
  let point = Point { x: 1, y: 2 };
  give add(point.x, point.y);
}
