use vitte/std as std_pkg

entry main at core/app {
  let idx: std_pkg.StdIndex = std_pkg.std_index()
  let idx2: std_pkg.StdIndex = std_pkg.register(idx, "path", "core", "vitte/std/base")
  let resolved: string = std_pkg.resolve_path(idx2, "path")
  if resolved == "" {
    give 1
  }
  give 0
}
