//! wav — lecture/écriture
export fn decode(b: Bytes) -> Any { __intrinsics::wav_decode(b) }
export fn encode(samples: Any, sample_rate: i32, channels: i32) -> Bytes { __intrinsics::wav_encode(samples, sample_rate, channels) }
