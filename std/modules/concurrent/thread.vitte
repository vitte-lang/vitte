//! thread — spawn/join
struct JoinHandle<T> { __raw: __intrinsics::Thread }

export fn spawn<T>(f: fn()->T) -> JoinHandle<T> { __intrinsics::thread_spawn(f) }
export fn join<T>(h: JoinHandle<T>) -> T { __intrinsics::thread_join(h.__raw) }
