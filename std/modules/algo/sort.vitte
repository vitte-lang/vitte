//! sort — tri (intrinsics optimisés)
export fn quicksort<T>(mut v: Vec<T>) { __intrinsics::sort_quick(v) }
export fn mergesort<T>(mut v: Vec<T>) { __intrinsics::sort_merge(v) }
export fn heapsort<T>(mut v: Vec<T>) { __intrinsics::sort_heap(v) }
