//! search — recherches
export fn binary_search<T>(v: Vec<T>, key: T) -> Option<i32> { __intrinsics::binary_search(v, key) }
export fn lower_bound<T>(v: Vec<T>, key: T) -> i32 { __intrinsics::lower_bound(v, key) }
