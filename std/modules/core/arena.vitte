//! arena — allocateur arène (intrinsic)
struct Arena { __raw: __intrinsics::Arena }
export fn new() -> Arena { Arena{ __raw: __intrinsics::arena_new() } }
export fn alloc<T>(mut a: Arena, v: T) -> &T { __intrinsics::arena_alloc(a.__raw, v) }
