//! vec — helpers
export fn push<T>(mut v: Vec<T>, x: T) { __intrinsics::vec_push(v, x) }
export fn pop<T>(mut v: Vec<T>) -> Option<T> { __intrinsics::vec_pop(v) }
export fn clear<T>(mut v: Vec<T>) { __intrinsics::vec_clear(v) }
