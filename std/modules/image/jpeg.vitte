//! jpeg — lecture/écriture
export fn decode(b: Bytes) -> Any { __intrinsics::jpeg_decode(b) }
export fn encode(pixels: Any, w: i32, h: i32, quality: i32) -> Bytes { __intrinsics::jpeg_encode(pixels, w, h, quality) }
