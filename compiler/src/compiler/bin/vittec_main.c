/*
  vittec_main.c — placeholder driver for the Vitte compiler CLI.
  The actual frontend/backends are not wired yet; this stub just
  prints a banner so the binary links and can be smoke-tested.
*/

#include <stdio.h>

int main(int argc, char** argv) {
  (void)argc;
  (void)argv;
  printf("vittec (stub) — compiler frontend not implemented yet\n");
  return 0;
}
