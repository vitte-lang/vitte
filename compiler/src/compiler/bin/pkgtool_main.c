/*
  pkgtool_main.c — placeholder package manager CLI.
  Currently prints a stub message so the binary can be built and invoked.
*/

#include <stdio.h>

int main(int argc, char** argv) {
  (void)argc;
  (void)argv;
  printf("vittepkg (stub) — package tooling not implemented yet\n");
  return 0;
}
