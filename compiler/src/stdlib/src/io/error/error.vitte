mod io.error.error

use core.error.error.*

const E_IO: i64 = 1000
const E_EOF: i64 = 1001

fn eof(msg: str) -> Error
  ret error_new(E_EOF, msg)
.end

fn io_err(msg: str) -> Error
  ret error_new(E_IO, msg)
.end
