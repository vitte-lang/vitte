mod io.net.tcp

use core.error.error.*

type TcpStream
  handle: i64
.end

fn connect(addr: str) -> (bool, TcpStream, Error)
  let s: TcpStream
  set s.handle = -1
  ret (false, s, core.error.error.error_new(2000, "tcp not wired"))
.end
