mod algo.compat.slice

fn is_sorted_non_decreasing(xs: [i64]) -> bool
  let i: i64 = 1
  loop
    if i >= xs.len
      ret true
    .end
    if xs[i-1] > xs[i]
      ret false
    .end
    set i = i + 1
  .end
.end
