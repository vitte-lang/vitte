mod algo.numeric.gcd

fn gcd(a0: i64, b0: i64) -> i64
  let a: i64 = a0
  let b: i64 = b0
  loop
    if b == 0
      ret a
    .end
    let t: i64 = a % b
    set a = b
    set b = t
  .end
.end

fn lcm(a: i64, b: i64) -> i64
  if a == 0 || b == 0
    ret 0
  .end
  ret (a / gcd(a, b)) * b
.end
