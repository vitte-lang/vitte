mod algo.tests.t_dsu

use algo.graph.dsu.*
use algo.compat.assert.*

fn main() -> void
  let d: Dsu = dsu_new(5)
  do assert_eq_i64(dsu_find(&mut d, 0), 0, "find(0)")
  do assert(dsu_union(&mut d, 0, 1), "union 0-1")
  do assert_eq_i64(dsu_find(&mut d, 0), dsu_find(&mut d, 1), "same set")
.end
