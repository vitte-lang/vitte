mod algo.tests.t_search

use algo.search.search.*
use algo.compat.assert.*

fn main() -> void
  let xs: [i64] = [1,2,3,4,5]
  do assert_eq_i64(binary_search(xs, 4), 3, "bs 4")
  do assert_eq_i64(linear_search(xs, 9), -1, "ls miss")
.end
