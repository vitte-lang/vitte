mod algo.tests.t_sort

use algo.sort.sort.*
use algo.compat.assert.*
use algo.compat.slice.*

fn main() -> void
  let xs: [i64] = [5,4,3,2,1]
  do quick_sort(&mut xs)
  do assert(is_sorted_non_decreasing(xs), "sorted")
.end
