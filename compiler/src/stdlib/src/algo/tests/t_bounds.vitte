mod algo.tests.t_bounds

use algo.search.bounds.*
use algo.compat.assert.*

fn main() -> void
  let xs: [i64] = [1,2,2,2,3]
  do assert_eq_i64(lower_bound(xs, 2), 1, "lb")
  do assert_eq_i64(upper_bound(xs, 2), 4, "ub")
.end
