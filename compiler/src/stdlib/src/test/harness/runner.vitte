mod test.harness.runner

use test.harness.types.*

type Runner
  # configuration (stubs)
  verbose: bool
.end

fn runner_new() -> Runner
  let r: Runner
  set r.verbose = false
  ret r
.end

fn run_suite(r: &Runner, suite: &TestSuite) -> [TestResult]
  # placeholder: runner doesn't have reflection; returns empty result set
  ret []
.end
