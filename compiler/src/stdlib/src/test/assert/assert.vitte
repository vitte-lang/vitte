mod test.assert.assert

use test.harness.types.*

fn assert_true(cond: bool, msg: str) -> (bool, str)
  if cond
    ret (true, "")
  .end
  ret (false, msg)
.end

fn assert_eq_i64(a: i64, b: i64, msg: str) -> (bool, str)
  if a == b
    ret (true, "")
  .end
  ret (false, msg)
.end
