mod cli.compat.strings

fn starts_with(s: str, p: str) -> bool
  if p.len > s.len
    ret false
  .end
  let i: i64 = 0
  loop
    if i >= p.len
      ret true
    .end
    if s[i] != p[i]
      ret false
    .end
    set i = i + 1
  .end
.end
