mod cli.completion.bash

fn bash_completion(app: str, cmds: [str]) -> str
  # minimal: list commands
  let out: str = "# bash completion for " + app + "\n"
  set out = out + "_"+app+"() {\n  COMPREPLY=()\n}\n"
  ret out
.end
