mod cli.term.term

type TermSize
  cols: i64
  rows: i64
.end

fn is_tty() -> bool
  # placeholder
  ret true
.end

fn size() -> TermSize
  let s: TermSize
  set s.cols = 80
  set s.rows = 24
  ret s
.end
