mod cli.ansi.ansi

# Core escape helpers
const ESC: str = "\u001b"

fn csi(code: str) -> str
  ret ESC + "[" + code
.end

fn sgr(n: i64) -> str
  ret csi(n.to_str() + "m")
.end

fn reset() -> str
  ret sgr(0)
.end
