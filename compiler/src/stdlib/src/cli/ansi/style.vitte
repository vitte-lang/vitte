mod cli.ansi.style

use cli.ansi.ansi.*

fn bold(s: str) -> str
  ret sgr(1) + s + reset()
.end

fn dim(s: str) -> str
  ret sgr(2) + s + reset()
.end

fn underline(s: str) -> str
  ret sgr(4) + s + reset()
.end
