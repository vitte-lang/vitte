mod cli.ansi.cursor

use cli.ansi.ansi.*

fn clear_line() -> str
  ret csi("2K")
.end

fn move_up(n: i64) -> str
  ret csi(n.to_str() + "A")
.end

fn move_down(n: i64) -> str
  ret csi(n.to_str() + "B")
.end
