mod net.addr.socket_addr

use net.addr.ip.*

type SocketAddr
  ip: IpAddr
  port: u16
.end

fn sock(ip: IpAddr, port: u16) -> SocketAddr
  let s: SocketAddr
  set s.ip = ip
  set s.port = port
  ret s
.end
