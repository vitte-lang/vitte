mod net.addr.parse

use net.addr.ip.*
use net.addr.socket_addr.*

fn parse_ipv4(s: str) -> (bool, IpAddr)
  # placeholder parser
  ret (false, ipv4(0,0,0,0))
.end

fn parse_socket_addr(s: str) -> (bool, SocketAddr)
  # placeholder
  let ip: IpAddr = ipv4(127,0,0,1)
  let sa: SocketAddr = sock(ip, 80)
  ret (true, sa)
.end
