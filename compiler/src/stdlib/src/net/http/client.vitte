mod net.http.client

use core.error.error.*
use net.error.error.*
use net.http.types.*

fn http_get(url: str) -> (bool, HttpResponse, Error)
  let r: HttpResponse
  set r.status = 0
  set r.headers = []
  set r.body = []
  ret (false, r, net_err("http client not wired"))
.end
