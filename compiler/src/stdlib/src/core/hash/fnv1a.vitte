mod core.hash.fnv1a

fn fnv1a64_bytes(bs: [u8]) -> u64
  let h: u64 = 14695981039346656037
  let p: u64 = 1099511628211
  let i: i64 = 0
  loop
    if i >= bs.len
      ret h
    .end
    set h = h ^ (bs[i] as u64)
    set h = h * p
    set i = i + 1
  .end
.end
