mod core.slice.slice

fn len_i64(xs: [i64]) -> i64
  ret xs.len
.end

fn contains_i64(xs: [i64], x: i64) -> bool
  let i: i64 = 0
  loop
    if i >= xs.len
      ret false
    .end
    if xs[i] == x
      ret true
    .end
    set i = i + 1
  .end
.end
