mod core.tests.t_option_result

use core.option.option.*
use core.result.result.*
use core.error.error.*

fn main() -> void
  let o = some_i64(7)
  if !is_some(&o)
    say "expected some"
  .end

  let r = ok_i64(9)
  if !is_ok(&r)
    say "expected ok"
  .end
.end
