mod core.num.num

fn clamp_i64(x: i64, lo: i64, hi: i64) -> i64
  if x < lo
    ret lo
  .end
  if x > hi
    ret hi
  .end
  ret x
.end

fn abs_i64(x: i64) -> i64
  if x < 0
    ret -x
  .end
  ret x
.end
