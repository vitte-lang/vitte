
# main.vit (Vitte)
#
# Steel entrypoint.
#
# Rust version was:
#   fn main() { rustc_driver::main() }
#
# In the Vitte toolchain, we model this as a thin wrapper that delegates to the
# driver module.

use steel.driver

fn main()
  driver::main()
.end