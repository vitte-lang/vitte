proc lower_expr(ctx, out, e)
    select e.kind
        when Literal
            emit_literal(out, e)
        when Binary
            emit_binary(out, e)
        otherwise
            error UnsupportedExpr(e.span)
    end
.end
