proc lower_expr(ctx, builder, e)
    select e.kind
        when Literal
            emit_literal(builder, e)
        when Binary
            emit_binary(builder, e)
        otherwise
            error UnsupportedExpr(e.span)
    end
.end
