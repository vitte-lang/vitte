proc codegen_function(ctx: CodegenContext, f: ir::Function)
    let sig = lower_signature(ctx, f)
    let clif_fn = ctx.builder.create_function(sig)

    lower_body(ctx, clif_fn, f.body)

    ctx.builder.finalize_function(clif_fn)
.end
