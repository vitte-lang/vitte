# -----------------------------------------------------------------------------
# Vitte bench data generators - runtime dependencies
# -----------------------------------------------------------------------------
# These generators are designed to run with the Python standard library only.
#
# If you use an external venv, you can keep this file empty.
#
# Optional dev tooling is declared in pyproject.toml under:
#   [project.optional-dependencies] dev
# -----------------------------------------------------------------------------

# (no runtime dependencies)
