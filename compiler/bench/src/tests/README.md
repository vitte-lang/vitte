# tests

Golden tests and schema validation for `vitte-bench` outputs.

- `schema/bench_result.schema.json` : JSON schema
- `golden/sample.json`             : minimal output
- `golden/baseline.json`           : baseline fixture for regression checks
