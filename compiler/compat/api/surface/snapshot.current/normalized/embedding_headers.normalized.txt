

# embedding_headers.normalized.txt
#
# Normalized "embedding" header surface snapshot.
#
# Goal:
# - Track the public C/C++ header entrypoints that are intended for embedders
#   (FFI users, host integrations, SDK consumers).
# - Keep this file stable and diff-friendly.
# - Populate/refresh this file from your snapshot generator.
#
# Format:
# - One header per line.
# - Paths are repo-relative.
# - Lines starting with '#' are comments.
# - Blank lines are ignored.
#
# Notes:
# - Prefer listing canonical umbrella headers (one per subsystem).
# - Avoid listing private/internal headers; those belong to implementation.

# -----------------------------------------------------------------------------
# Compiler embedding API (FFI)
# -----------------------------------------------------------------------------

compiler/compat/api/include/vitte/compiler.h
compiler/compat/api/include/vitte/compiler_version.h

# -----------------------------------------------------------------------------
# Runtime embedding API (FFI)
# -----------------------------------------------------------------------------

compiler/compat/api/include/vitte/runtime.h
compiler/compat/api/include/vitte/runtime_version.h

# -----------------------------------------------------------------------------
# Common / shared ABI types (if split)
# -----------------------------------------------------------------------------

compiler/compat/api/include/vitte/common.h
compiler/compat/api/include/vitte/error.h

# -----------------------------------------------------------------------------
# Optional: SDK convenience umbrella
# -----------------------------------------------------------------------------

compiler/compat/api/include/vitte/vitte.h

# -----------------------------------------------------------------------------
# Templates (keep commented; for guidance only)
# -----------------------------------------------------------------------------

# compiler/compat/api/include/vitte/<subsystem>.h
# compiler/compat/api/include/vitte/<subsystem>_version.h