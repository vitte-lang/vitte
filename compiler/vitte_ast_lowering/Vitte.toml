# Vitte.toml — vitte_ast_lowering
#
# Manifest du crate `vitte_ast_lowering`.
# Lowering: vitte_ast_ir -> vitte_ast_lowering.asm

[manifest]
format = "vitte"
version = 1

[package]
name = "vitte_ast_lowering"
version = "0.1.0"
description = "Vitte compiler lowering crate (AST_IR -> ASM)."
license = "MIT"
readme = "README.md"
authors = ["Vincent"]

[layout]
src_dir = "src"
include = [
  "Vitte.toml",
  "README.md",
  "messages.ftl",
  "src/**/*.vit",
]
exclude = [
  "target/**",
  ".git/**",
]

[target.lib]
# Entrypoint du crate
path = "src/lib.vit"
module = "vitte_ast_lowering"
# Arbre des sous-modules (pour builds/outils)
module_tree = "src/mod.vit"

[profile.dev]
opt_level = 0
debug = true

[profile.release]
opt_level = 2
debug = false
lto = true

[dependencies]
vitte_ast = { path = "../vitte_ast" }
vitte_ast_ir = { path = "../vitte_ast_ir" }

[dev-dependencies]

[features]
# Features volontairement minimalistes.
# Ajoute ici des hooks de debug, traces, etc.
default = []

# Exemple:
# trace_lowering = []
# verify_all = []

[diagnostics]
# Fichier Fluent (si ton pipeline diag l'utilise)
fluent = "messages.ftl"
