# ============================================================
# vittec — driver/mod.vit
# Compiler Driver Aggregator — ULTRA MAX
# ============================================================

space vittec/driver

# ----------------------------
# Submodules
# ----------------------------

pull vittec/driver/check
pull vittec/driver/build
pull vittec/driver/compile

# ----------------------------
# Public API
# ----------------------------

share all

# ----------------------------
# Driver dispatch helpers
# ----------------------------

proc run_check(ctx: CliContext) -> int
    return check(ctx)
.end

proc run_build(ctx: CliContext) -> int
    return build(ctx)
.end

proc run_compile(ctx: CliContext) -> int
    return compile(ctx)
.end

# ----------------------------
# End of module
# ----------------------------
