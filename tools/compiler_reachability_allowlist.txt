# Strict temporary allowlist for non-yet-wired compiler modules.
# Format (required): module|reason
# Example:
# vitte/compiler/backend/experimental/foo|waiting for backend pipeline migration
vitte/compiler|top-level umbrella facade is exported for convenience but not imported by the real compiler entry
vitte/compiler/infrastructure|aggregate facade is kept for namespace ergonomics while the real entry imports concrete infrastructure modules

# Legacy IR fixtures retained for bootstrap compatibility, never production imports.
vitte/compiler/ir/ast|bootstrap compatibility fixture; canonical AST lives under frontend/ast
vitte/compiler/ir/hir_to_mir_lowering|bootstrap compatibility fixture; canonical lowering lives under middle/lower
vitte/compiler/ir/mir_extended|bootstrap compatibility fixture; canonical MIR lives under middle/mir
vitte/compiler/ir/mir_optimizations|bootstrap compatibility fixture; canonical passes live under optimizations/mir
vitte/compiler/ir/pipeline|bootstrap compatibility fixture; canonical compiler pipeline uses frontend, analysis, middle, and backend
