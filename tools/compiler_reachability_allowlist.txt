# Strict temporary allowlist for non-yet-wired compiler modules.
# Format (required): module|reason
# Example:
# vitte/compiler/backend/experimental/foo|waiting for backend pipeline migration
vitte/compiler|top-level umbrella facade is exported for convenience but not imported by the real compiler entry
vitte/compiler/infrastructure|aggregate facade is kept for namespace ergonomics while the real entry imports concrete infrastructure modules
