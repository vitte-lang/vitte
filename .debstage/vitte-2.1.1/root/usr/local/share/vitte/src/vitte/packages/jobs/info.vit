<<<
info.vit
package vitte/jobs
>>>

space vitte/jobs

proc package_name() -> string {
    give "jobs"
}

proc package_tag() -> string {
    give "vitte/jobs"
}

proc package_ready() -> bool {
    give true
}
