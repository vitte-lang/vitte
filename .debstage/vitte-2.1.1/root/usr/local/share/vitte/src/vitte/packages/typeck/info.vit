<<<
info.vit
package vitte/typeck
>>>

space vitte/typeck

proc package_name() -> string {
    give "typeck"
}

proc package_tag() -> string {
    give "vitte/typeck"
}

proc package_ready() -> bool {
    give true
}
