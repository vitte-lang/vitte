<<<
info.vit
package vitte/migrate
>>>

space vitte/migrate

proc package_name() -> string {
    give "migrate"
}

proc package_tag() -> string {
    give "vitte/migrate"
}

proc package_ready() -> bool {
    give true
}
