<<<
info.vit
package vitte/timeout
>>>

space vitte/timeout

proc package_name() -> string {
    give "timeout"
}

proc package_tag() -> string {
    give "vitte/timeout"
}

proc package_ready() -> bool {
    give true
}
