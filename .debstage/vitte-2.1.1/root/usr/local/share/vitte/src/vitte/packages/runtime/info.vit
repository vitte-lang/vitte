<<<
info.vit
package vitte/runtime
>>>

space vitte/runtime

proc package_name() -> string {
    give "runtime"
}

proc package_tag() -> string {
    give "vitte/runtime"
}

proc package_ready() -> bool {
    give true
}
