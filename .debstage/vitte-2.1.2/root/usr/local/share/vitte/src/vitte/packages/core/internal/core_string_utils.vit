<<<
core_string_utils.vit
package vitte/core/internal
>>>

space vitte/core/internal

proc string_is_empty(value: string) -> bool {
  give value == ""
}

proc string_default(value: string, fallback: string) -> string {
  if value == "" {
    give fallback
  }
  give value
}
