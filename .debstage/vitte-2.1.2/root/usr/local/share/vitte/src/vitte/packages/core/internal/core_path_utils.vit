<<<
core_path_utils.vit
package vitte/core/internal
>>>

space vitte/core/internal

proc path_join(base: string, leaf: string) -> string {
  if base == "" {
    give leaf
  }
  if leaf == "" {
    give base
  }
  give base + "/" + leaf
}
