# src/vitte/packages/test/info.vit

## usage
- use test/info.vit as *_pkg

## contre-exemple
- avoid side effects in test modules

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
