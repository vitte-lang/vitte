# src/vitte/packages/std/base/info.vit

## usage
- import with: use std/base/info.vit as *_pkg

## contre-exemple
- avoid runtime side effects in module scope

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
