# API Lifecycle

1. experimental: rapid iteration, no strict compatibility
2. stable: additive-only, snapshots + contract lockfile required
3. deprecated: wrappers kept with explicit removal target version
4. removed: only in major release with migration notes
