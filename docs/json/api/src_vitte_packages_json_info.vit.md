# src/vitte/packages/json/info.vit

## usage
- use json/info.vit as *_pkg

## contre-exemple
- avoid oversized/untrusted payload without limits

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
