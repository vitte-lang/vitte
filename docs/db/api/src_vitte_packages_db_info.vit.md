# src/vitte/packages/db/info.vit

## usage
- import with: use db/info.vit as *_pkg

## contre-exemple
- avoid unprepared query paths

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
