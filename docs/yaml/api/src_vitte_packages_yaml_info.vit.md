# src/vitte/packages/yaml/info.vit

## usage
- use yaml/info.vit as *_pkg

## contre-exemple
- avoid unsafe tags without allowlist

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
