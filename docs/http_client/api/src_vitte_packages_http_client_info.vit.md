# src/vitte/packages/http_client/info.vit

## usage
- use http_client/info.vit as *_pkg

## contre-exemple
- avoid insecure redirects/tls

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
