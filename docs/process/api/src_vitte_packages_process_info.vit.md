# src/vitte/packages/process/info.vit

## usage
- use process/info.vit as *_pkg

## contre-exemple
- avoid unsafe shell/argv/env

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
