# let-make-set-role

Rule id: `let-make-set-role`

Canonical roles:

- `let`: declaration (default)
- `make`: mutable declaration intent
- `set`: reassignment only

Keep these roles explicit to reduce ambiguity when reading state transitions.
