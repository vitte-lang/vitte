# brace-blocks

Rule id: `brace-blocks`

Use brace blocks `{ ... }` as the single canonical block syntax in new code.
Avoid historical terminators (`end`, `.end`) outside legacy compatibility contexts.
