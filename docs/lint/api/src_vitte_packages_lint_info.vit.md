# src/vitte/packages/lint/info.vit

## usage
- use lint/info.vit as *_pkg

## contre-exemple
- avoid non-deterministic lint output

## symbols
proc package_name() -> string {
proc package_tag() -> string {
proc package_ready() -> bool {
