# Backend

- Single declared functional backend: C backend.
- Outputs:
  - C source (`--emit-c`)
  - Object (`--emit-obj`)
  - Linked executable (default build)
- External failures are captured and turned into diagnostics.
