# Vitte Stdlib Changelog

This changelog is generated from the stdlib development stream.

## Current

- Added core contracts for primitives, option/result, conversion, memory,
  drop/scope, iterators, ranges, numbers, floats, math, strings, ASCII, and
  Unicode.
- Added allocation contracts for vectors, strings, boxes, ref-counting, and
  collections.
- Added standard library contracts for runtime, platform, data formats, and
  utility modules.
- Added generated API and LSP indexes.
