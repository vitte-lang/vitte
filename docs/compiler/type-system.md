# Type System (Current Seed Subset)

Checked rules currently enforced:
- explicit `int/string/bool/void`-style checks
- return type consistency for `give`
- invalid operator pair rejection
- unsafe cast rejection pattern
- boolean condition checks in `if`
- non-void requires return
