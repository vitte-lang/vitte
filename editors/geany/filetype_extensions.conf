# Geany extension mapping for Vitte.
# Install to: ~/.config/geany/filetype_extensions.conf
#
# This maps *.vit, *.vitte, and *.vitl to the vitte filetype automatically.

[Extensions]
vitte=*.vit;*.vitte;*.vitl;
