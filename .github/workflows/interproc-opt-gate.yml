name: interproc-opt-gate

on:
  push:
  pull_request:

jobs:
  interproc-opt-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run interprocedural optimization gate
        run: make interproc-opt-gate
