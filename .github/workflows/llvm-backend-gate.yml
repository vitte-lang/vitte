name: llvm-backend-gate

on:
  push:
  pull_request:

jobs:
  llvm-backend-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run LLVM backend gate
        run: make llvm-backend-gate
