name: optimization-phase2-gate

on:
  push:
  pull_request:

jobs:
  optimization-phase2-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run phase-2 optimization evidence gate
        run: make optimization-phase2-gate
