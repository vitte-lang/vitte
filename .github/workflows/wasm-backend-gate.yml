name: wasm-backend-gate

on:
  push:
  pull_request:

jobs:
  wasm-backend-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run WASM backend gate
        run: make wasm-backend-gate
