name: vitte-emit-gate

on:
  push:
  pull_request:

jobs:
  vitte-emit-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run Vitte emit gate
        run: make vitte-emit-gate
