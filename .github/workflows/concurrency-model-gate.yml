name: concurrency-model-gate

on:
  push:
  pull_request:

jobs:
  concurrency-model-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run concurrency model gate
        run: make concurrency-model-gate
