name: mir-opt-gate

on:
  push:
  pull_request:

jobs:
  mir-opt-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run MIR optimization gate
        run: make mir-opt-gate
