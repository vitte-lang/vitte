name: memory-model-gate

on:
  push:
  pull_request:

jobs:
  memory-model-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run memory model gate
        run: make memory-model-gate
