name: compiler-gate

on:
  push:
  pull_request:

jobs:
  compiler-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run compiler gate
        run: make compiler-gate
