name: backend-gate

on:
  push:
  pull_request:

jobs:
  backend-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run unified backend gate
        run: make backend-gate
