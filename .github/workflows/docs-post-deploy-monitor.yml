name: docs-post-deploy-monitor

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: tools/docs/post_deploy_css_monitor.sh
