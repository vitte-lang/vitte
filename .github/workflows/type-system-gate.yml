name: type-system-gate

on:
  push:
  pull_request:

jobs:
  type-system-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run type system gate
        run: make type-system-gate
