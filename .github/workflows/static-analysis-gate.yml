name: static-analysis-gate

on:
  push:
  pull_request:

jobs:
  static-analysis-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static analysis gate
        run: make static-analysis-gate
