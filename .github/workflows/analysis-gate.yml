name: analysis-gate

on:
  push:
  pull_request:

jobs:
  analysis-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run unified analysis gate
        run: make analysis-gate
