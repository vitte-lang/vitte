# plugins/regex/runtime/mod.vitte
# Runtime matcher
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.runtime

pub mod limits
pub mod input
pub mod unicode
pub mod matcher
pub mod rope_stub

.end
