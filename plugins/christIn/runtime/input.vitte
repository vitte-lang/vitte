# plugins/regex/runtime/input.vitte
# Input adapters
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.runtime.input

pub struct Cursor
  pos: u32
.end

pub fn cursor_new() -> Cursor
  ret Cursor(pos: 0)
.end

.end
