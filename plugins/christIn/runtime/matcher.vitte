# plugins/regex/runtime/matcher.vitte
# Matcher
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.runtime.matcher

pub fn run(_prog: usize, _hay: str, _limits: plugins.regex.runtime.limits.Limits) -> i32
  ret 0
.end

.end
