# plugins/regex/runtime/limits.vitte
# Execution limits
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.runtime.limits

pub struct Limits
  max_steps: usize
  max_matches: usize
.end

pub fn defaults() -> Limits
  ret Limits(max_steps: 10_000_000, max_matches: 1_000_000)
.end

.end
