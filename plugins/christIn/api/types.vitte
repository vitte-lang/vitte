# plugins/regex/api/types.vitte
# Core types
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.api.types

pub struct Error
  code: i32
  message: str
.end

pub fn error_new(code: i32, message: str) -> Error
  ret Error(code: code, message: message)
.end

pub struct Span
  lo: u32
  hi: u32
.end

pub struct Match
  span: Span
.end

pub struct Captures
  ptr: *u32
  len: usize
.end

pub struct Regex
  prog: usize
  flags: plugins.regex.api.flags.Flags
.end

.end
