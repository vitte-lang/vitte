# plugins/regex/api/diagnostics.vitte
# Errors + spans
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.api.diagnostics

pub const E_PARSE: i32 = 1
pub const E_UNSUPPORTED: i32 = 2
pub const E_LIMIT: i32 = 3

.end
