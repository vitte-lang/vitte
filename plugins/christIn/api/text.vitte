# plugins/regex/api/text.vitte
# UTF-8 text helpers
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.api.text

pub fn compile(pattern: str) -> plugins.regex.api.types.Regex
  ret plugins.regex.api.pattern.compile(pattern, plugins.regex.api.flags.defaults())
.end

.end
