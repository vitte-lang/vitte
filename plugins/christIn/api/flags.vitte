# plugins/regex/api/flags.vitte
# Flags/options
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.api.flags

pub struct Flags
  ignore_case: bool
  multi_line: bool
  dot_matches_newline: bool
  unicode: bool
  greedy: bool
.end

pub fn defaults() -> Flags
  ret Flags(
    ignore_case: false,
    multi_line: false,
    dot_matches_newline: false,
    unicode: true,
    greedy: true,
  )
.end

.end
