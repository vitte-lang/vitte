# plugins/regex/api/bytes.vitte
# Byte-oriented regex
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.api.bytes

pub struct ByteSpan
  lo: usize
  hi: usize
.end

.end
