# plugins/regex/mod.vitte
# Plugin entry + registration
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex

pub mod api
pub mod engine
pub mod runtime
pub mod data

pub struct PluginDescriptor
  id: str
  version: str
.end

pub fn vitte_plugin_entry() -> PluginDescriptor
  ret PluginDescriptor(id: "org.vitte.plugin.regex", version: "0.1.0")
.end

.end
