# plugins/regex/engine/optim/mod.vitte
# Optimizations
# Skeleton for Vitte plugin: regex
# Blocks use `.end` only.

mod plugins.regex.engine.optim

pub mod literal_prefix
pub mod charclass_simplify
pub mod nfa_prune
pub mod cache

.end
