# plugins/crypto/mod.vitte
# Plugin entry + registration + feature routing

mod plugins.crypto

pub mod api
pub mod primitives
pub mod backends
pub mod host
pub mod vectors
pub mod tooling

pub struct PluginDescriptor
  id: str
  version: str
.end

pub fn vitte_plugin_entry() -> PluginDescriptor
  ret PluginDescriptor(id: "org.vitte.plugin.crypto", version: "0.1.0")
.end

.end
