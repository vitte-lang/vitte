# plugins/beryl-crypto/backends/hw/mod.vitte
# HW backends aggregator — MAX (Vitte 2025)
# Blocks use `.end` only.

module plugins.crypto.backends.hw

import plugins.crypto.backends.hw.aesni as aesni
import plugins.crypto.backends.hw.armv8_crypto as armv8_crypto
import plugins.crypto.backends.hw.rng_os as rng_os

export aesni, armv8_crypto, rng_os

.end
