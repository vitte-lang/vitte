# plugins/beryl-crypto/api/mac/mod.vitte
# MAC API module aggregator — MAX (Vitte 2025)
# Blocks use `.end` only.

module plugins.crypto.api.mac

import plugins.crypto.api.mac.hmac as hmac
import plugins.crypto.api.mac.poly1305 as poly1305
import plugins.crypto.api.mac.kmac as kmac

export hmac, poly1305, kmac

.end
