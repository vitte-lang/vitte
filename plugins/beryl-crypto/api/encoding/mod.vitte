# plugins/beryl/api/encoding/mod.vitte
# Encoding API umbrella module — MAX++ (2025)
#
# Conventions:
#   - Module index only (no logic).
#   - Uses `.end` block terminator.
#   - Re-exports submodules for a stable import surface.

mod plugins.crypto.api.encoding

# ---------------------------------------------------------------------------
# Primitive encodings
# ---------------------------------------------------------------------------

pub mod hex
pub mod base64

# ---------------------------------------------------------------------------
# Text armors / containers
# ---------------------------------------------------------------------------

pub mod pem

# ---------------------------------------------------------------------------
# ASN.1 / binary structured encodings
# ---------------------------------------------------------------------------

pub mod der_stub

# ---------------------------------------------------------------------------
# Future:
#   - json, cbor, msgpack
#   - protobuf, asn1-ber, asn1-per
#   - bincode, leb128, varint
# ---------------------------------------------------------------------------

.end
