# plugins/crypto/api/keyexchange/mod.vitte
mod plugins.crypto.api.keyexchange

pub mod x25519
pub mod ecdh_stub

.end

# plugins/beryl-crypto/api/keyexchange/mod.vitte
# Key exchange API module aggregator — MAX
# Blocks use `.end` only.

module plugins.crypto.api.keyexchange

import plugins.crypto.api.keyexchange.x25519 as x25519
import plugins.crypto.api.keyexchange.ecdh as ecdh

export x25519, ecdh

.end