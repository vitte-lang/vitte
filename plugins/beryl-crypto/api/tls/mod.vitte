# plugins/beryl-crypto/api/tls/mod.vitte
# TLS API module aggregator — MAX (Vitte 2025)
# Blocks use `.end` only.

module plugins.crypto.api.tls

import plugins.crypto.api.tls.tls_client as tls_client

export tls_client

.end
