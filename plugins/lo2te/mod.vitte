# plugins/fs_vfs/mod.vitte
# Plugin entry + registration

mod plugins.fs_vfs

pub mod api
pub mod host
pub mod core
pub mod backends
pub mod tooling

pub struct PluginDescriptor
  id: str
  version: str
.end

pub fn vitte_plugin_entry() -> PluginDescriptor
  ret PluginDescriptor(id: "org.vitte.plugin.fs_vfs", version: "0.1.0")
.end

.end
