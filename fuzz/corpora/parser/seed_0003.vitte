# seed_0003.vitte — expressions + pattern-ish constructs
module fuzz.parser.seed_0003

let maybe = match 3 {
  0 => none,
  1 => some(1),
  _ => some(3),
}

fn tupled(a: i8, b: i8) -> (i8, i8) {
  return (a - b, b - a)
}
