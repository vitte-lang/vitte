# seed_0002.vitte — recovery edges (missing end, dangling else)
module fuzz.parser.seed_0002

fn broken(x: i64) -> i64 {
  if x == 0 {
    return 1
  else {
    return x * 2
  }
# missing closing brace on purpose
