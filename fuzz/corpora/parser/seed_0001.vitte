# seed_0001.vitte — minimal module + call chain
module fuzz.parser.seed_0001

fn add(a: i32, b: i32) -> i32 {
  return a + b
}

fn main() -> i32 {
  let total = add(4, 5)
  if total > 0 {
    return total
  }
  return 0
}
