# seed_0002.txt — operators + nested comments
module fuzz.lexer.seed_0002

// arithmetic + punctuation
let answer = (1 + 2) * 3 - 4 / 2 % 5
let tuple = (foo, bar?.baz, qux[3..5])

/* block comment /* nested */ still open */
let str = "unicode→αβγ" // UTF-8 path
