# seed_0003.txt — strings, escapes, weird whitespace
module fuzz.lexer.seed_0003
let raw = "line1\nline2\t\x41"
let chars = ['a', '\\', '\n', '\u{1F984}']
# indent with tabs
	fn helper(x: i64) -> i64
	  if x >= 0 {
	    return x << 2
	  }
	  return -1
.end
