# seed_0001.txt — minimal lexer smoke
# - exercises: keywords, identifiers, integers, strings, comments, indentation + `.end`

module fuzz.lexer.seed_0001

# comment line
let x : i32 = 0
let s : str = "hello\tworld\n"

fn main() -> i32
  set x = x + 1
  say s
  ret x
.end
