space toolchain/stage0/stage0_target

const STAGE0_STAGE0_TARGET_SCHEMA: string = "vitte.toolchain.stage0.stage0_target.v1"
const STAGE0_STAGE0_TARGET_STAGE: string = "stage0"
const STAGE0_STAGE0_TARGET_ROLE: string = "signed trust root and bootstrap input"
const STAGE0_STAGE0_TARGET_PREDECESSOR: string = "none"
const STAGE0_STAGE0_TARGET_POLICY: string = "signature-required, offline, single-platform-artifact"
const STAGE0_STAGE0_TARGET_CAPABILITY: string = "stage0_target"
const STAGE0_STAGE0_TARGET_VERSION: int = 1

proc stage0_stage0_target_contract_ready() -> bool {
    give STAGE0_STAGE0_TARGET_SCHEMA == "vitte.toolchain.stage0.stage0_target.v1" and STAGE0_STAGE0_TARGET_STAGE == "stage0" and STAGE0_STAGE0_TARGET_VERSION == 1;
}

export *
