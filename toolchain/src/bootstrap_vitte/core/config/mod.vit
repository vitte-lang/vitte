space vitte/bootstrap/toolchain/bootstrap_vitte/core/config/mod

form Config {
  host: string,
  target: string,
  jobs: int
}

proc default_config() -> Config {
  give Config { host: "native", target: "native", jobs: 4 }
}
