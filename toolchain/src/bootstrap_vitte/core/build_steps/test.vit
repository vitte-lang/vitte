space vitte/bootstrap/bootstrap_vitte/build_steps/test

const VERSION_TEXT: string = "bootstrap_vitte test 0.1.0"
const BANNER_TEXT: string = "bootstrap_vitte test shim"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  give 0
}

export *
