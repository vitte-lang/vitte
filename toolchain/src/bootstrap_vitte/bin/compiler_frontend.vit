space vitte/bootstrap/bootstrap_vitte/bin/compiler_frontend

const VERSION_TEXT: string = "bootstrap_vitte compiler_frontend 0.1.0"
const BANNER_TEXT: string = "bootstrap_vitte compiler_frontend shim"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  give 0
}

export *
