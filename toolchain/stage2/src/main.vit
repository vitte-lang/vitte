space vitte/bootstrap/stage2

const VERSION_TEXT: string = "vittec stage2-vitte 0.1.0"
const BANNER_TEXT: string = "vittec stage2 native bootstrap"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  give 0
}

export *
