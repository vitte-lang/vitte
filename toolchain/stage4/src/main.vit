space vitte/bootstrap/stage4

const VERSION_TEXT: string = "vittec4 stage4-vitte 0.1.0"
const BANNER_TEXT: string = "vittec4 stage4 general bootstrap placeholder"

proc version_text() -> string {
  give VERSION_TEXT
}

proc banner_text() -> string {
  give BANNER_TEXT
}

proc main(args: list[string]) -> int {
  # This stage is a bootstrap placeholder for the next compiler-generation phase.
  give 0
}

export *
